# クリーンルーム通し実行 監査ログ (2026-07-29 / Day 11〜20)

- 実行者: 独立オラクル（教材テキストのみを読む。リポジトリ `src/` は一切参照していない）
- 開始状態: 同じ実行者が Day 01〜10 まで組んだアプリ（`audit/clean-run-2026-07-29.md`）
- 展開先: リポジトリ外の作業ディレクトリ
- 環境: macOS / Node v25.9.0 / npm 11.12.1 / Docker 29.0.2

原文は `audit/clean-run-raw/2026-07-29-day11-20.md` に置いてある。

## 件数

手戻り 4 / 混乱 22（計26件）。BLOCKER は無い。

**この26件は、まだ全件を現物で確かめていない。** 下の表で確かめた分だけを「成立」と書く。
Day 01〜10 の回で、指摘を疑わずに数え方だけを直して4回やり直した失敗があったため、
今回は最初から確認状況を分けて記録する。

## 前回より踏み込めたこと

Day 01〜10 の回は「画面が 200 を返した」までの記録が多かった。今回は操作の結果まで見ている。

- Day 16: カードに「合計作業時間: 12h 0m」が出る → 0分で送信すると「1分以上入力してください」
  → 1時間30分を入れると 13h 30m に増える、まで実際に操作して観測した。
- Day 12: 重複追加は「このユーザーは既にプロジェクトのメンバーです」、唯一の OWNER 削除は
  「プロジェクト唯一のオーナーは削除できません」で弾かれることを実 API で確認した。
- 各日の「完成コード全体」と実ファイルの照合も行った（Day 11 は完全一致、
  Day 16 は `task-card.tsx` が props の並び順のみ差分、ほかは抜粋のみで全文ではない）。

## 確認状況

| 指摘 | 確認状況 |
|---|---|
| `day11:347` の確認ポイント「型エラーが出ていない」と `day11:969` の「型エラーの出ることがあります」が矛盾する | **成立**。同じファイルの2箇所を読んで確認した |
| `day12` の「（続き）」ブロックの貼り先が複数ある | **成立**。137行が「（続き）は末尾の `});` の1行上へ」と定めているのに、253行の「（続き）」ブロックの中身は `const projectMemberSchema = z.object({` で、その位置はルーターオブジェクトの内側にあたる。規則どおり貼ると構文エラーになる |
| `day12:604` の `handleProjectClick` が二重宣言になる | **成立**。`day09:506` が `const handleProjectClick` を定義している。`day12:604` は「あわせて `handleProjectClick` も追加します」と書き、直後の注意書きは `handleDetailClose`（Day 11 の仮定義）にしか触れていない。書かれたとおりに追加すると同名の `const` が2つになる |
| `day12:1193` が「Step 2 の `console.log` 版を削除」と書くが該当コードが無い | **成立**。1193行は「Step 2 で追加した仮実装を削除」、1234行の確認ポイントは「Step 2 の仮実装（`console.log` の版）が削除されている」と書く。しかし Step 2（710行）自身が「Day 11 Step 9 で追加済み」「ここではコードの追加は不要」と述べており、仮定義の中身も空実装で `console.log` は無い。探しても見つからないコードを消せと言われる |
| `day12:1373` の「候補は1人だけ」が実際は3件 | **条件付きで成立**。1373行は「追加の候補に出るのは『新人 太郎』1人だけです」と初期データを前提に書いている。Day 06 は `/register` を開いてエラーが出ないことまでを確認ポイントにしており、実際にアカウントを登録させる手順は無い。したがって教材どおり進めた読者は1人のままになる可能性がある。実行者は自分で登録したため3件になった。**教材の誤りとは言えないが、登録を試した読者は必ずずれる。** 「自分で登録したぶんは候補に増えます」の1行があると迷わない |
| `day13:826/1009` の手順どおりだと `react` の import が3本になり、完成コードの1本と一致しない | **成立**。Step 1（441行）で `import { Suspense, useState } from 'react';`、Step 6（826行）で `import { useCallback, useMemo } from 'react';`、Step 7（1009行）で `import { useEffect } from 'react';` を足させる。完成コード（1484行）は `import { Suspense, useCallback, useEffect, useMemo, useState } from 'react';` の1本。「既存の react の import に足す」とは書かれていない |
| `day13:802` の仮ハンドラーが未使用引数を放置している | **成立（実害は小さい）**。`day09` の同じ場面では `void projectId;` を1行入れて未使用引数を避けているのに、`day13` は空関数のまま。書き方が Day 間で揃っていない。今の設定では警告が出ないので、読者が止まることはない |
| `day15` の完成コードに、`day14` が「追加しない」と明記した API 呼び出しが入っている | **成立**。`day14:1321` の表は `api.search.getProjectMembers` について「一覧の担当者フィルターで使う。フィルターは Day 20 で作る」と書き、直後に「`page.tsx` で呼ぶのは今日ではありません」と述べている。しかし `day15:1891` の完成コードには `const { data: users } = api.search.getProjectMembers.useQuery();` が入っている。Day 15 の手順にも追加指示は無く、`users` はどこからも参照されていない。読者は「どこかで写し忘れた」と誤解する |
| `day17:692` の「ここまでのインポート一覧（中間確認）」に、同じ Day で足したはずの import が載っていない | **成立**。140行が `import { useCallback, useMemo, useState } from 'react';`、148行が `@/lib/constant/roles` を足しているのに、692行の表は `react` 欄を「`useMemo`, `useState`」のままにし、`roles` の行が無い。表のとおりに整理すると、445行の `useCallback` が未定義になる |
| `day18:480` の貼り先が「return 内」としか書かれていない | 未確認 |
| `day20:1573` の `closeTaskDialog` が `router` を使うのに、その宣言がどこにも無い | **成立（手戻り）**。1573行のブロックは貼り先が `src/app/task/page.tsx` で、1586行で `router.replace(...)` を呼ぶ。しかし `useRouter` の import（650行）と `const router = useRouter();`（741行）は、どちらも貼り先が `src/app/search/page.tsx` である。Day 20 のどこにも「`task/page.tsx` に `useRouter` を足す」という指示が無い。貼った瞬間に `Cannot find name 'router'` で止まる |
| `day20:514` の `loading.tsx` を作る作業が、どの Step にも属していない | 未確認 |
| 残り14件 | 未確認 |

`day12` の1件は手戻りに分類されている。規則どおり貼った読者は、構文エラーを読んでから
貼り直すことになる。同じ「（続き）」の規則で、`day11` の `setArchiveStatus` には
位置の指定が別に書いてあり、`day12` の `projectMemberSchema` には無い。
規則と例外の書き分けが揃っていない。

「未確認」は、指摘が成立するかをまだ現物で当たっていないという意味である。
成立するとも、しないとも言えない。

## いちばん重い指摘 — Day 11 の終わりで `npm run build` が落ちる

`day11:347` の確認ポイントは「`npm run dev` で型エラーが出ていない」と書いている。
一方 `day11:969-971` は「この時点ではエディタに `getById` が無いという型エラーの
出ることがあります。Day 12 Step 0 で `getById` を足すと消えます」と書いている。
同じファイルの中で、片方は型エラーが無いことを確認させ、もう片方は出ると予告している。

実行者の観測では、Day 11 完了時点で型エラーが5件残り `npm run build` が
`Failed to compile.` で落ちた。予告されているのは `getById` の1件だけで、
そこから派生した4件（`project-detail-view.tsx` の implicit any）は予告されていない。

`npm run dev` は型検査をしないので画面は動く。Day 04 で「公開前に必ず `npm run build`」と
習慣づけた読者が、ここで build して落ちると原因を追えない。

## 次の通し実行で足す条件

Day 01〜10 の記録から引き継ぐ。

- **目印の行も含めて貼る。** 省略すると、目印の修正が効いているかを測れない。
- 各日の確認内容を1項目ずつ突き合わせ、見た項目と見ていない項目を分けて記録する。

今回の実行は2つ目を満たしている。1つ目は Day 11〜20 でも未実施である。
