# クリーンルーム通し実行 監査ログ (2026-07-29 / Day 21〜30)

- 実行者: 独立オラクル（教材テキストのみを読む。リポジトリ `src/` は一切参照していない）
- 開始状態: 同じ実行者が Day 01〜20 まで組んだアプリ
- 展開先: リポジトリ外の作業ディレクトリ

原文は `audit/clean-run-raw/2026-07-29-day21-30.md` に置いてある。

## 件数

手戻り 5 / 混乱 24（計29件）。BLOCKER は無い。うち4件は検証環境の都合で、教材の欠陥ではない（day21 の dev サーバー残留、day22、day26、day30 の Vercel デプロイ未実施）。

**29件はまだ全件を現物で確かめていない。** Day 01〜10 の回で、指摘を疑わずに数え方だけを
直して4回やり直した失敗があったため、確認できたものだけを「成立」と書く。

## 目印の修正が効いていることを、初めて実測できた

Day 01〜10 と Day 11〜20 の実行者は、目印の行を省略して写経していた。
そのため「画面に出なかった」という結果は、修正が効いた証拠にならなかった。

**この回は条件を変えた。目印の行も含めて、印字どおりに貼らせた。**

| 測ったこと | 結果 |
|---|---|
| 貼った目印の行数 | 204行 |
| 目印が原因の構文エラー | **0件** |
| 実ブラウザで見たページ数 | 14ページ |
| ページの `innerText` / `innerHTML` に現れた `filepath` | **0件** |

ページごとの内訳は原文の「表示崩れ」節にある。たとえば Day 29 は
`user.ts` に11個、`[id]/page.tsx` に2個、`user-detail-client.tsx` に18個、
`edit/page.tsx` に2個、`edit/user-edit-client.tsx` に21個を貼って、
`/user/[id]` と `/user/[id]/edit` の両方で0件だった。

これで `#341` の目印の修正が効いていることを、**貼った状態で**確認できた。

**`#346` はこの回では確かめられていない。** `#346` が直したのは Day 01 の説明文3箇所で、
この回は Day 01〜20 を組み終えた状態から始めているため、その3箇所を読み直していない。
これまでの回で挙げていた「配信 HTML の0件」は、貼っていない状態の0件だったので
証拠にならなかった。今回のものは条件が違う。

## 確認状況

| 指摘 | 確認状況 |
|---|---|
| 目印を貼っても画面に文字が出ない | **成立（実測）**。204行を貼って14ページで0件、構文エラーも0件 |
| `day21:605-606` の日本語の文が JSX 内で途中改行され、画面に半角スペースが入る | **成立**。`<p>` の子として「プロジェクトの進捗とタスクの」「状況を確認できます。」の2行に分かれている。JSX は改行とインデントを半角スペース1個に畳むので、画面には「〜タスクの 状況を〜」と文の途中に空白が入る。教材の折り返しの幅が、そのまま日本語の文の欠陥になっている |
| 残り28件 | 未確認 |

### 同じ型を教材全体で数えた — 5箇所

`check_jsx_marker.py` の構造判定を流用し、JSX の子要素の位置にある行のうち、
ASCII の記号を1つも含まない日本語だけの行が2行続き、1行目が句読点で終わらないものを数えた。

| 場所 | 分かれ方 |
|---|---|
| `day12_メンバー追加.md:944` | このプロジェクトに ⏎ 新しいメンバーを追加します。 |
| `day21_統計カードを表示.md:605` | プロジェクトの進捗とタスクの ⏎ 状況を確認できます。 |
| `day21_統計カードを表示.md:1523` | プロジェクトの進捗とタスクの ⏎ 状況を確認できます。 |
| `day22_グラフを表示.md:766` | プロジェクトの進捗とタスクの ⏎ 状況を確認できます。 |
| `day24_ユーザー一覧（管理者用）.md:485` | この機能は管理者のみ ⏎ 利用できます |

どれも画面に出る文で、貼ると文の途中に半角スペースが1つ入る。

数え方は2回やり直した。1回目（日本語だけの行が2行続く形）は115件で、
オブジェクトの値や mermaid の記述まで拾っていた。2回目（コードらしさの判定を足した形）は
73件で、`//` のコメントや zod の連鎖が残っていた。3回目に「ASCII の記号を1つも含まない」で
絞って5件になった。**画面に出る文字だけを数えるには、コードの記号が1つも無いことを
条件にするのがいちばん確実だった。**

「未確認」は、指摘が成立するかをまだ現物で当たっていないという意味である。

## 次にやること

- 29件を1件ずつ現物で確かめ、成立・不成立を分ける。Day 01〜10 では8件中3件が
  成立しなかったので、そのまま件数として扱わない。
- Day 11〜20 の未確認15件も同じ。

## この回で取り下げた記録

codex の指摘を受けて現物を読み直したところ、次の5件は教材に説明が実在しており、
記録側の誤りだった。取り下げる。

| 取り下げた記録 | 実在する説明 |
|---|---|
| Day 29 `getById` の権限の説明が無い | `day29:165` と `day29:1903` が「管理者専用ではない」理由を書いている |
| Day 25 アバターの空文字の扱いが説明されていない | `day25:1626` が `.or(z.literal(''))` の理由を書いている |
| Day 25 パスワード復旧に外部知識が要る | `day25:1448` が `npx prisma studio` の手順を書いている |
| Day 27 が確認だけの日だと知らされない | `day27:115` ほか各 Step の冒頭が「コードを追加せず」と書いている |
| 404 の確認に CUID 形式の値が要る | サーバー側は `findUnique` の後に `notFound()` を呼ぶので形式は問わない |

**スクリーンショットは残していない。** 本文が `d29-detail.png` などを根拠として挙げているが、
実物はリポジトリに入れていないので、第三者は追試できない。
