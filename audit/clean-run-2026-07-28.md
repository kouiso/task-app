# クリーンルーム通し実行 監査ログ (2026-07-28)

- 実行者: 独立オラクル（教材テキストのみを読む。リポジトリ `src/` は一切参照していない）
- 教材: `scripts/build-zip.sh` で生成した `task-app-curriculum-v1.1.zip`（185ファイル / 6.7MB）
- 展開先: `/tmp/cleanroom-run-b/task-app`（リポジトリ外）
- 環境: macOS 26.1 / Node v22.23.1 / npm 10.9.8 / Docker 29.0.2 / Docker Compose v5.1.2

このログは **1日分ずつ追記** する。観測していないことは書かない。

## 環境の前提（自分で作った状態ではないもの）

- ポート 25532 / 25533 は既存の `task-app-db-1` / `task-app-test-db-1`（本リポジトリの compose 由来、前回実行の残骸）が使用中。同一プロジェクト名のため再利用された。停止・削除はしていない。
- ポート 3000 は無関係プロジェクト（`mypappy-api` の ts-node、PID 83596）が使用中。指示どおり一切触っていない。

---

## Day 01: 開発環境を整えて、初めてのアプリを動かそう

### 実行したこと・観測結果

**前提確認（day01 L120-156）**

```
node -v  -> v22.23.1     （期待 v22.x 以上：合致）
npm -v   -> 10.9.8       （期待 10.x 以上：合致）
docker info >/dev/null 2>&1 && echo "docker ok"  -> docker ok
```

**Step 1（L158-238）**: ZIP 展開直後のルートで `ls -a` を実行。
観測: `doc/ material/ scripts/ .env.example .mise.toml .node-version README.md`。
教材 L217-221 が挙げる `README.md` / `material` / `scripts` / `scripts/scaffold-from-scratch.sh` / `.env.example` はすべて存在。**合致**。

**Step 2（L240-338）**: `chmod +x scripts/scaffold-from-scratch.sh && bash scripts/scaffold-from-scratch.sh`
終了コード 0。最終行に教材 L318-319 と同じ

```
初期セットアップは完了しました。
カリキュラムの Day 01 の続きを進めてください。
```

が出力された。成功判定リスト（L328-338）の11ファイルを実測で全件確認。

```
OK package.json / tsconfig.json / biome.json / .env.example / .env
OK docker-compose.yml / prisma/schema.prisma
OK src/app/layout.tsx / src/app/page.tsx / src/app/globals.css
OK src/component/ui/button.tsx
```

**合致（11/11）**。

**Step 3（L367-411）**: `npm run dev`

観測した出力:

```
⚠ Port 3000 is in use by process 83596, using available port 3001 instead.
  ▲ Next.js 15.5.21
  - Local:        http://localhost:3001
  ✓ Ready in 1301ms
```

`curl http://localhost:3001/` → HTTP 200。本文に `Get started by editing` + `src/app/page.tsx` を確認。
教材 L408-411 の「Next.js のロゴと案内文が見えれば OK」は**内容としては合致**。ただしポート番号は 3000 ではなかった（下記 D01-1）。

**Step 4-1（L449-753）**: `globals.css` を教材の CSS ブロック 9 個を順に連結して全置換。
連結結果 206 行 / 波括弧 `{` 11 個・`}` 11 個で均衡。dev サーバーはエラーなく再コンパイルした。
配信された `/_next/static/css/app/layout.css`（409,360 バイト）内に `--primary: 253 77% 60%`（:root）と `--primary: 254 86% 68%`（.dark）、および `--color-primary: hsl(var(--primary))` を実測で確認。**token は実際に効いている**。

**Step 4-2（L755-979）**: `page.tsx` を tsx ブロック 7 個を順に連結して全置換（154 行）。
**Step 4-3（L981-1015）**: `src/app/dashboard/page.tsx` を新規作成（20 行）。

**Step 5（L1165-1207）**: チェックポイント 8 項目を実測。

```
curl http://localhost:3001/           -> HTTP 200
  OK  Getting Started / My Task App
  OK  Day 01 Ready
  OK  自分専用のタスク管理アプリが、今日ここから動き出す。
  OK  bg-primary（主役色クラス）
  OK  ダッシュボードへ入る
  OK  今日のゴール / 今日のひとこと / 明日につながる入口
curl http://localhost:3001/dashboard  -> HTTP 200
  OK  Hello Task-App
```

**チェックポイント 8/8 合致**。dev サーバーのログにエラー行なし。

**参考（機械的ゲートは既に緑）**: 教材全体の画像参照 128 件・相対 Markdown リンク 229 件をリンク切れ検査したところ **欠損 0**。したがって以下の発見は「リンクは生きているが中身が違う」種類のもので、リンクチェッカでは検出できない。

### 発見

| ID | 箇所 | 初心者が踏むこと | 深刻度 |
|----|------|------------------|--------|
| D01-1（修正済み 2026-07-28） | `day01_...md:380`（および L404 / L1183 / L1015 など `localhost:3000` 全箇所） | ポート3000が他プロセスに埋まっている場合、教材は「`Port 3000 is in use` と出たら、別のターミナルの開発サーバーを `control + C` で止めれば起動できます」と書く。実測した Next.js 15.5.21 はエラーを出さず **自動で 3001 に退避して起動する**（観測: `⚠ Port 3000 is in use by process 83596, using available port 3001 instead.`）。教材が想定した文言は出ない。結果 (a) 初心者は存在しない「別ターミナルの dev サーバー」を探し、最悪 **無関係のプロセスを止めにいく**、(b) 以降 Day 30 まで一貫して案内される `http://localhost:3000` を開くと別アプリか接続不可になる。「3001 と表示されたらそちらを開く」の一行が無い。 | 中 |
| D01-2（修正済み 2026-07-28） | `day01_...md:15`（および L1199-1201 の再掲） | Day 01 の**冒頭に置かれた完成イメージ** `screenshots/day01/first-render.png` が、実際には **トップページではなくダッシュボード画面**（`DASHBOARD` / `Hello Task-App` の1枚カードだけ）を写している。学習者が Step 4 で作るのは、ヘッダ・大カード・小カード3枚・右カラム3枚から成る密度の高いトップページで、**この画像とは似ても似つかない**。さらにこの画像の本文は「Day 01 で立ち上げた最初のダッシュボード**や**。…育て**ていこう**。」と関西弁の旧文面で、教材 L1005-1006 が書かせる「Day 01 で用意した最初のダッシュボード**です**。…していき**ます**。」と一致しない（正しい画像は同ディレクトリの `dashboard-hello.png` に別途存在し、そちらは本文一致）。有償教材の1日目・最初に目に入る画像が、作るものと違う画面かつ古い文面。Step 5 でも「雰囲気比較として眺めて」と再提示されるので、**正しく完成させた学習者ほど「自分は間違えた」と誤認する**。リンクは生きているためリンクチェッカでは検出不能。 | 中〜高 |
| D01-3 | `day01_...md:910, 916, 967` | JSX のテキストノードにバッククォートが書かれている（例: `` 空のディレクトリから `task-app` を始める ``）。JSX はバッククォートを文字としてそのまま描画するため、**画面にバッククォート記号が出る**（実測: レンダリング後 HTML に確認）。Markdown のコード記法のつもりが画面へ漏れている。動作影響なし・見た目のみ。 | 低 |

---

## Day 02: ダッシュボードに自分だけのメッセージを追加しよう

### 実行したこと・観測結果

**前日からの状態確認（L105-137）**: 教材が「Day 01 直後の状態」として提示する `dashboard/page.tsx` のコードが、Day 01 で自分が書いたファイルと**完全一致**。継続性は保たれている。

**Step 1（L139-281）**: tsx ブロック 4 個を連結して `dashboard/page.tsx` を全置換（74 行）。`curl /dashboard` → HTTP 200。
ブラウザ確認 3 項目（L276-279）を実測。

```
OK  見出しが Hello Task-App から変化（Personal Message / こんにちは、{ownerName}さん。）
OK  こんにちは、+ Taro + さん。（RSC ペイロード上は3分割で出力される）
OK  右側に補助カード2枚（今日の狙い / 今の変化）
```

**3/3 合致**。

**Step 2（L283-485）**: tsx ブロック 6 個を連結して全置換（124 行）。
実行時刻 22 時 → 見出しに `こんばんは` を確認。
教材 L486 が案内する検証（`const currentHour = new Date().getHours();` を `= 20;` に一時変更）も実際に実行 → `こんばんは` に変化することを確認し、元に戻した。**教材に書かれた実験手順がそのとおり動く**。
ブラウザ確認 4 項目（L479-482）: あいさつ・名前・集中テーマ・`Owner`/`Focus` の2枚とも確認。ターミナルにエラーなし。**4/4 合致**。

**Step 3-4（L487-753）**: tsx ブロック 7 個を連結して全置換（159 行）。
チェックポイント 5 項目（L742-746）を実測。

```
OK  あいさつが現在時刻に一致（22時 → こんばんは）
OK  Taro が画面に出る
OK  Focus: バッジが bg-primary（実 class: "... bg-primary ... text-primary-foreground ..."）
OK  下段に Owner / Today / Next の3カード
OK  右側補助カード（今日のワンフレーズ / ここで増えた価値 / 次につながる視点）
```

**5/5 合致**。dev ログにエラー・警告なし。

**Day 02 は機能面では詰まりどころなし。**

### 発見

| ID | 箇所 | 初心者が踏むこと | 深刻度 |
|----|------|------------------|--------|
| D02-1 | `day02_...md:29` → `screenshots/day02/dashboard-message.png` | 完成イメージのスクショと、教材が書かせるコードの出力が**一致しない**。スクショのヘッダは `G0 FOUNDATION` だが、教材コード（L68 相当）は `Task App`。オーナー名もスクショは `taskapp demo` だが教材コードは `Taro`（名前だけは L281 で「サンプル」と断りがある。`G0 FOUNDATION` には断りが無い）。D01-2 と合わせると「スクショが教材コードと別ビルドから撮られている」という**系統的な問題**に見える。 | 低〜中 |
| D02-2 | `day02_...md:242`（Step 1「今の変化」カード） | D01-3 と同じバッククォート漏れが再発。`` `Hello Task-App` から、 `` が画面にバッククォート付きで描画される（実測でレンダリング後 HTML に確認）。**全 30 日を機械的に走査した結果、この問題は Day 01（3箇所: L910/916/967）と Day 02（1箇所: L242）の計 4 行のみで、以降の日には無い**。教材全体に蔓延してはいないが、**購入者が最初に触る 2 日間だけに集中している**ぶん、第一印象への影響は大きい。 | 低 |

---

## Day 03: GitHub に保存する

### スコープの申告（重要）

Day 03 は Step 4 以降が **GitHub アカウント作成・空リポジトリ作成・`gh auth login`・`git remote add`・`git push`** という外部サービスへの書き込みを伴う。監査目的で実在アカウント配下にリポジトリを作るのは指示範囲外の外部操作なので、**Step 4〜6・8〜9 は実行していない**。
実行したのは **Step 0・1・2・3・7（add とローカル commit まで）** と、Step 5 の読み取り確認のみ。push 系の記述の正しさは**未検証**である。

### 実行したこと・観測結果

**Step 0-1（L62-185）**: 5 コマンドを実行。

```
pwd                            -> /tmp/cleanroom-run-b/task-app
git status -sb                 -> "## main" + 未保存 24 行
git branch --show-current      -> main
git log --oneline --decorate -3-> 5d99dec (HEAD -> main) Initial commit from Create Next App
git remote -v                  -> （空）
```

教材 L163-176 の期待「`## main` / コミット1本 / remote 空 / 未保存が10行以上」に**全項目合致**（実測 24 行）。

**Step 2（L186-265）**: README.md を教材の 49 行へ全置換。

**Step 3（L267-309）**: `.gitignore` L33-34 に `# env files (can opt-in for committing if needed)` / `.env*` を確認。
`git check-ignore -v .env.example` → `.gitignore:34:.env*  .env.example`（exit 0）。
教材の主張「`.env.example` も既定では除外される」を**実測で確認**。

**Step 5（L348-355、読み取りのみ）**: `gh --version` → `gh version 2.89.0`。

**Step 7（L447-577）**: 教材の 8 行の `git add` 列を順に実行 → 全行成功（`fatal: pathspec` は発生せず）。
`git add -f .env.example` が必要という説明も、`-f` 無しでは無視される挙動と整合。
その後 `git commit -m "feat: save initial dashboard project to GitHub"` を実行（`user.name`/`user.email` はコマンドローカル指定で、グローバル設定は変更していない）。
コミット後 `git log --oneline -3`:

```
208e4ca (HEAD -> main) feat: save initial dashboard project to GitHub
5d99dec Initial commit from Create Next App
```

**成功。ただし残る未追跡ファイルが教材の記述と違った（下記 D03-1）。**

### 発見

| ID | 箇所 | 初心者が踏むこと | 深刻度 |
|----|------|------------------|--------|
| D03-1（修正済み 2026-07-28） | `day03_...md:536-538` および `L575-577` | 教材は add 後・commit 後ともに「いちばん下に `?? material/` と `?? scripts/` が残ります」「`git status -sb` に残るのは `?? material/` と `?? scripts/` の**2行だけ**になります」と断言する。**実測は5行**: `?? .mise.toml` / `?? .node-version` / `?? doc/` / `?? material/` / `?? scripts/`。`.mise.toml`・`.node-version`・`doc/` はいずれも**配布 ZIP に同梱されている**（README L49 が `.mise.toml` を、`doc/SUPPORTED_ENVIRONMENTS.md` を README L35 が明示的に案内している）のに、Step 7 の add リストにも期待出力にも登場しない。教材の言うとおりの画面にならないので、初心者は「余分な3つは何だ、add し忘れたのか」で確実に止まる。 | 中 |
| D03-2 | `day03_...md:491-513`（add リスト）+ Day 04 への影響 | 上記の未追跡ファイルのうち **`.node-version`（中身 `22`）が GitHub に送られない**。Day 04 は「今日 GitHub に保存した内容がそのまま公開の土台になります」（L811）と述べて Vercel へデプロイするが、Vercel は `.node-version` をリポジトリから読んで Node バージョンを決める。教材が Node 22 前提（README L28）であるのに、その固定ファイルだけがリポジトリに入らない。Vercel 側の既定 Node で動くかどうかは**未検証**（Step 8-9 を実行していないため）だが、意図と成果物がずれているのは確か。 | 中（Day 04 未検証） |
| D03-3 | `day01_...md:345-355` vs 実ファイル | Day 01 が「`.env.example` の中身はこんな感じです」として見せる 8 行が、**実ファイルの 3 分の 1 程度に縮めた別物**。実ファイルには `_DEVELOPER_EMAIL` / `_DEVELOPER_FIRSTNAME` / `_DEVELOPER_LASTNAME`（コメント: 「seedコマンドで管理者ユーザーを作成する際に使用」、いずれも空）、`_DOCKER_COMPOSE_HOST_PORT_BACKEND=3000`、`_DOCKER_COMPOSE_HOST_PORT_BACKEND_DEBUG`、`_DOCKER_COMPOSE_HOST_PORT_SCHEMASPY`、本番URL 変数などが含まれる。Day 03 Step 3（L295-301）は `sed -n '1,120p' .env.example` を**実際に開かせる**ので、教材が一度も説明していない変数群を初心者がそこで目にする。特に `_DEVELOPER_EMAIL` が空で「管理者ユーザー作成に使う」と書いてあると、「埋めないと管理者が作れないのでは」と手が止まる。**実害は無いことを確認済み**（`scripts/_seed/seed.ts:30` が `|| 'admin@example.com'` でフォールバックし、DB 実測で `admin@example.com / 管理者 / ADMIN` が作られている）が、教材はそのフォールバックに一言も触れていない。 | 低〜中 |

---

## Day 04: ネットに公開

### スコープの申告

Step 3 以降が **Vercel アカウント作成・GitHub Import・本番デプロイ** という外部サービスへの登録・課金対象になりうる操作なので、**Step 3〜終わりは実行していない**。
実行したのは **Step 1（ローカル側の確認）・Step 2（`npm run build`）** のみ。デプロイ結果に関する記述は**未検証**。

### 実行したこと・観測結果

**Step 2（L163-233）**: `npm run build` → **終了コード 0**。観測した出力:

```
> prisma generate && next build
✔ Generated Prisma Client (v6.19.3) ...
   ▲ Next.js 15.5.21
   - Environments: .env
   Creating an optimized production build ...
 ✓ Compiled successfully in 4.3s
   Linting and checking validity of types ...
   Collecting page data ...
 ✓ Generating static pages (6/6)
   Finalizing page optimization ...
   Collecting build traces ...
Route (app)          Size  First Load JS
┌ ○ /              3.46 kB   106 kB
├ ○ /_not-found      995 B   103 kB
├ ƒ /api/trpc/[trpc] 127 B   103 kB
└ ○ /dashboard       127 B   103 kB
```

教材 L186-206 が予告する順序（Prisma 行 → `▲ Next.js` → `Compiled successfully` → 型チェック → `Generating static pages` → `Finalizing` → `Collecting build traces`）と**行単位で一致**。ページ数が `(5/5)` ではなく `(6/6)` だった点も、教材が L189 で「その時点のページ数によって変わります」と先に断っているため**問題なし**。

**Day 04 の検証できた範囲では詰まりどころなし。** Day 01〜03 で書いたコードが本番ビルドを通ることを確認できた。

### 発見

なし（実行した Step 1-2 の範囲では、教材の記述と実測が一致）。
ただし D03-2（`.node-version` が GitHub に送られない）の影響は、この日の Step 4-6 を実行していないため**未解決のまま残る**。

---

## Day 05: ログイン画面の UI を作ろう

### 実行したこと・観測結果

Day 05 は **Day 01-04 と教材の構造が変わる**。Day 01-02 は「このブロックを順に貼れば1ファイルが完成する」形だったが、Day 05 は Step 1〜9 にまたがる **20 個以上の断片**（「import 文に追加」「LoginForm 内の先頭に追加」「Button を以下に書き換え」「Step 4-5 の `<form>` は全部削除」）で構成され、**完成した 1 ファイルの全文がどこにも載っていない**。
実際に断片を全部読み、指示どおりマージして `src/app/login/page.tsx`（187 行）と `src/lib/redirect.ts`（17 行）を作成した。

**依存の実在確認**（教材が import しているもの）:

```
OK  src/component/ui/{input,label,button,card,alert}.tsx  すべて scaffold 済み
OK  react-hot-toast / @hookform/resolvers / react-hook-form / zod / lucide-react
OK  src/server/api/root.ts に auth: authRouter が登録済み（api.auth.login が実在）
```

**型チェック**: `npx tsc --noEmit` → **No errors found**（終了コード 0）。

**画面（L860 のチェックポイント）**: `curl http://localhost:3001/login` → HTTP 200。

```
OK  ログイン（CardTitle）
OK  アカウントにログインしてください（CardDescription）
OK  メールアドレス / パスワード
OK  アカウントをお持ちでない方は … こちら（href="/register"）
OK  /register は 404（教材 L862 が「この時点では 404 表示が正常」と明記。合致）
```

**API の実挙動**（Step 7-8 が依存する `api.auth.login` を直接叩いて確認）:

```
誤ったパスワード -> HTTP 401 / message="メールアドレスまたはパスワードが正しくありません" / Set-Cookie なし
正しいパスワード -> HTTP 200 / user={name:"管理者", role:"ADMIN"} / Set-Cookie あり
```

Step 7 の `onSuccess` が使う `data.user.name` が実在することも確認（→ トースト文言 `おかえりなさい、管理者さん` が成立する）。

**Day 05 は機能面では最後まで通った。**

### 発見

| ID | 箇所 | 初心者が踏むこと | 深刻度 |
|----|------|------------------|--------|
| D05-1 | `day05_...md` 全体（特に L862 の注記） | **完成形の全文がどこにも無い**。Step 1〜9 の 20 以上の断片を、初心者が自力で正しい位置へマージしないと完成しない。しかも教材自身が L862 で「販売用 ZIP に完成版の `src/` は入っていません。教材内のコードと確認ポイントが正本です」と認めている。つまり**マージを間違えた学習者には突き合わせる正解が存在しない**。Day 01-02 は全文ブロック方式だったので、購入者はここで初めて「答え合わせできない」状態に落ちる。断片には `// LoginForm コンポーネント内の先頭に追加` のように**場所を日本語で指示するだけ**のものが多く、`useState` と `Suspense` が別々の `import ... from 'react'` 行になるなど、正解が一意に決まらない箇所もある。今日の日は自分（機械的にマージ）でも通せたが、**初心者が最も脱落しやすい構造**。 | 高 |
| D05-2 | `day05_...md:439-441`（Step 6 の途中） | Step 6 は「この時点で保存すると構文エラーが出る。壊れているのではなく続きが未入力なだけ」と、**意図的に画面が壊れた状態で数分進ませる**。設計としては説明済みだが、Day 01-04 で「保存するたびに画面が更新される」体験をしてきた初心者が、初めて赤い画面に置かれる。加えて Step 6 冒頭（L487）で「**Step 4 と Step 5 で書いた `<form>` から `</form>` までは、先にすべて削除してください**」と、直前の 2 ステップ（表 L79-81 の見積で計 10 分）の成果を捨てさせる。捨てる前提なら Step 4-5 を書かせる意味の説明が要るが、無い。 | 中 |
| D05-3 | `day05_...md:795-797`（Step 8 の確認ポイント） | 確認ポイントが「間違ったパスワードでエラーメッセージが出る」「同じメールアドレスで10回続けて失敗すると15分ログインできなくなるので、試すのは2〜3回にとどめる」と書くが、**Day 05 のどこにもログインできるアカウント（メール／パスワード）が書かれていない**。初期データの資格情報 `admin@example.com` / `password123` が初出するのは **Day 06 L714 と Day 07 L1356**。したがって Day 05 の時点では「失敗する」ことしか試せず、Step 7 で書いた `onSuccess`（トースト＋`/dashboard` へ遷移）は**その日のうちに一度も確認できない**。さらに「15分ロックされる」と警告だけして、**その日のうちには解除方法を書いていない**（ロック自体は実在。`src/lib/rate-limit.ts:4` の `WINDOW_MS = 15 * 60 * 1000` を実測確認）。解除方法「15分待つか、別のメールアドレスで試す」が載るのは **Day 07 L1514 のトラブルシューティング表**で、2日後である。 | 中〜高 |

---

## Day 06: ユーザー登録画面を作ろう

### 実行したこと・観測結果

Day 06 は Step 1〜10 の断片方式だが、**Day 05 と違って末尾に「完成コード全体」節（L865〜L1139）がある**。「途中で迷った場合は、以下のコードをそのまま `src/app/register/page.tsx` にコピーしてください」と明記されているので、そのとおり 11 ブロックを連結して `src/app/register/page.tsx`（178 行）を作成した。

**型チェック**: `npx tsc --noEmit` → **No errors found**。

**画面**: `curl http://localhost:3001/register` → HTTP 200。`新規登録` / `メールアドレス` / `パスワード` / `確認` / `すでにアカウントをお持ちの方は` / `href="/login"` を確認。
Day 05 で置いた `/login` からの `こちら` リンクが、この日に**実際につながった**（Day 05 L862 の予告どおり）。

**登録 API の実挙動**（画面が呼ぶ `api.auth.register` を直接実行）:

```
POST auth.register {name:"検証太郎", email:"cleanroom...@example.com", password:"Passw0rd!23"}
-> HTTP 200  user={id:"cms47fcbn...", name:"検証太郎", role:"USER"}
```

**実際に DB へユーザーが作られた**。Day 06 の中心機能は動く。

**Day 06 は詰まりどころなし。**

### 発見

なし（Day 06 単体としては、教材の記述と実測が一致）。
ただしこの日は次項の**構造的な問題を浮き彫りにする基準点**になっている。

---

## 全体を通しての構造的な発見（1日単位では見えないもの）

### S-1: 「完成コード全体」がある日は 30 日中 **1 日だけ**

全 30 ファイルを走査した結果:

```
完成コード全体 節を持つ日: day06 のみ（1/30）
持たない日: day01〜05, day07〜day30（29/30）
```

Day 01・02 は「ブロックを上から順に貼れば1ファイルになる」全文方式なので実質困らない。
問題は **Day 05 と Day 07 以降**で、断片指示（「〜に追加」「〜を書き換え」「〜は削除」）だけで組み立てさせながら、**突き合わせる完成形が存在しない**。Day 06 が完成形を用意できている以上、これは技術的制約ではなく**単に他の日に無いだけ**である。

各日のコードブロック構成（実測）:

| day | ブロック数 | 対象ファイル数 | 断片指示ラベル数 |
|-----|-----------|---------------|-----------------|
| day05 | 24 | 3 | 5 |
| **day07** | **42** | **8** | **34** |
| day12 | 46 | 3 | 20 |
| day14 | 52 | 7 | 21 |
| day20 | 71 | 7 | 21 |
| day25 | 70 | 7 | 15 |
| day29 | 69 | 6 | 16 |

**最も危険なのは Day 07**（42 ブロック / 8 ファイル / 断片ラベル 34 = 断片率 81%）。しかも Day 07 は認証バックエンド `auth.ts` を**作り直す**日で、ここでマージを誤ると Day 08 以降のログインが全部壊れる。教材自身も L883 で「`auth.ts` の**末尾にある `});` の1行上**へ貼ります。ファイルの一番下に足すとルーターの外に出てしまい、英語のエラーで止まります」と、失敗しやすさを認識した注意書きを入れている。それでも完成形は無い。

### S-2: 「保存すると壊れる」状態を意図的に通過させる設計が複数日にまたがる

- Day 05 L439-441: 「この時点で保存すると…構文エラーが出る。壊れているのではなく続きが未入力なだけ」
- Day 06 L213: 「このブロックは括弧を開いたまま終わっているので、保存すると赤い波線が出ます。それで正常です」
- Day 06 L944: 「このブロックはまだ括弧が開いたままなので、次の続きと合わせて1つの式になります」

いずれも説明はされている。ただし Day 01〜04 では一度も起きない体験で、**「赤いのは正常」と「赤いのは自分のミス」を初心者が区別する手段は文章の記憶しかない**。S-1（完成形が無い）と重なると、赤が消えないときに自力で復帰できない。

### S-3: スクリーンショットが教材コードと別ビルドから撮られている

D01-2（`first-render.png` がトップページではなくダッシュボード、かつ旧・関西弁文面）と D02-1（ヘッダが `G0 FOUNDATION` vs コードは `Task App`）は、いずれも**リンク切れではない**ため、リンクチェッカ（画像 128 件・相対リンク 229 件、欠損 0 を実測）では検出できない。画像の**中身**と教材コードの一致は、現状どの機械的ゲートも見ていない。

---

## 実行の到達点

| Day | 実行範囲 | 結果 |
|-----|---------|------|
| Day 01 | Step 1〜5 全部 | 完走。チェックポイント 8/8 合致。発見 3 件 |
| Day 02 | Step 1〜4 全部 | 完走。チェックポイント 3/3・4/4・5/5 合致。発見 2 件 |
| Day 03 | Step 0-3・5(読取)・7 | **部分**。Step 4/6/8/9（GitHub 作成・push）は外部書き込みのため未実行。発見 3 件 |
| Day 04 | Step 1-2 | **部分**。Step 3 以降（Vercel 登録・デプロイ）は外部操作のため未実行。発見 0 件 |
| Day 05 | Step 1〜9 全部 | 完走。tsc 0 エラー、`/login` 200、auth.login の成功・失敗両経路を実測。発見 3 件 |
| Day 06 | 完成コード全体を採用 | 完走。tsc 0 エラー、`/register` 200、auth.register で実ユーザー作成を確認。発見 0 件 |

**Day 07 以降は未実行。** 停止理由は時間ではなく、S-1 で示した Day 07 の構造（42 ブロック / 8 ファイル / 断片率 81%、完成形なし）を正しく検証するには 1 日ぶんの独立した通し実行が要るため、中途半端に触って「通った/通らない」を書くより未検証と明記するほうが正確だと判断した。

## 技術的には問題ないが、読んでいて引っかかったところ

- **所要時間の見積り**: Day 05 は「合計 47 分」と書く（L86）。ただし直後に「この時間はコードを読んで理解する目安です。写経して打ち込む時間、詰まって調べる時間は別に見てください」と断りがある。断りは誠実だが、**目次に出るのは 47 分のほうだけ**なので、購入前後の期待値としては 187 行を 9 ステップで組み上げる作業が 47 分に見える。
- **Day 05 Step 4-5 の使い捨て**: Step 4（5分）と Step 5（5分）で書いた `<form>` を Step 6 の冒頭で全削除させる。見積り 47 分のうち 10 分が意図的な破棄で、なぜ一度書かせるのかの説明が無い。
- **`localhost:3000` の全編ハードコード**: D01-1 のとおりポートが動くと全日に効く。教材側に「表示されたポートを使う」という一般的な断りが1箇所も無い。
- **`api.auth.login` を Day 05 で「借りて使う」正直さ**: L703 で「今日ログインが通ったとしても、自分で組んだ仕組みの正しさを証明したことにはなりません」と明言している。ここは教材として誠実で、良い書き方だと感じた。




