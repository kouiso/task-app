# 全day横断の整合性チェック

対象: `material/30days-curriculum/` の36ファイル（day01〜day30、appendix 4本、目次、ロードマップ）
実施日は 2026-07-27、対象コミットは 78aa586 です。

day単位の検査では見つからない不整合を洗い出す。1ファイルずつは正しいのに、通して読むと
食い違っている類が対象になる。

## 結果

| # | 見る対象 | 手段 | 結果 |
|---|---|---|---|
| 1 | 用語の表記ゆれ | `scripts/curriculum-qa/check_terms.py` | ゆれなし（36ファイル） |
| 2 | day間の相互参照 | `scripts/curriculum-qa/check_crossref.py` | 参照先がすべて実在（36ファイル） |
| 3 | コード直後の理由説明 | `scripts/curriculum-qa/check_why.py` | 878ブロックすべて基準を満たす |
| 4 | 画像の実体 | 相対パスの実在確認 | 131件すべて実在 |
| 5 | 相対リンクの実体 | 同上 | リンク切れゼロ |
| 6 | `_meta/procedure-day-map.json` | 参照ファイル名の実在確認 | 不一致なし |
| 7 | Step番号の連続性 | 見出しの番号を抽出して連番判定 | 欠番・重複なし |

### 7 について

初回の走査で day22 に Step 4 の重複が出たが、これは誤検出だった。実体は
`#### Step 4-5 の完成構造` という見出しで、Step 4 と Step 5 の成果をまとめて示す節です。
Step 見出し自体は 1 から 9 まで連続している。

多くのファイルが Step 0 から始まるのも欠番ではない。Step 0 は準備作業を指す取り決めで、
教材全体で一貫している。

## 今回のスコープ外として記録するもの

次の2件は壊れているわけではないが、通して読むと気になる点として残す。直すかどうかは
別の判断になるため、この監査では変更していない。

1. **ロードマップへの導線が2箇所しかなかった（対応済み）**
   `00-1_学びのロードマップ.md` へのリンクは `00_カリキュラム目次.md` と day01 にしかなかった。

2. **各dayの末尾にリンク形式の導線がなかった（対応済み）**
   すべてのdayが「明日のプレビュー」または「次回予告」で締めくくられていたが、次のファイルへの
   リンクが無く、読者は次に開くファイルを自分で探すことになっていた。

   30日分の末尾に「次に読むもの」の節を足した。前の日、次の日、学びのロードマップ、目次への
   4本のリンクを置いている。追加後に相対リンクの解決を確認し、切れているものは1件も無い。

3. **どこからも参照されていない画像が16枚あった（削除済み）**
   `screenshots/` にある71枚のうち16枚が、36ファイルのどこからも参照されていない。
   販売ZIPには入るため、読まれないまま容量だけを占める。

   このうち4枚は、写っている画面が説明と違うことが分かって参照を外したものである。
   撮り直しが要る。

   | ファイル | 写っているもの |
   |---|---|
   | `project-detail-members.png` | 「プロジェクトが見つかりません。」の失敗画面 |
   | `project-add-member.png` | 同上（中身が同一） |
   | `change-password.png` | 編集前のプロフィール画面。入力欄が無い |
   | `profile-edit.png` | 同上 |

   残る12枚は、今回の作業より前から参照されていない。内訳は `day01-env-pasted.png` が1枚、
   `day02-` で始まるものが4枚、`day03-` が2枚、`day04-` が3枚、`day08-` が2枚である。
   本文の改稿で参照が消えたと見られる。

   16枚とも削除した。有償の配布物に、誰も見ない画像を2.3MB分入れておく理由が無いためである。
   削除後にZIPを作り直し、8.5MB から 6.4MB に減ること、参照切れが1件も出ないことを確認した。
   復活が必要になった場合は git の履歴から取り出せる。

## 追記（2026-07-27・24周目まで完了時点）

### PR #285 の指摘 #16「ルーティング」の注釈（未確認だった1件）

現物で確認した。day01 の用語表に注釈が入っている。

> ルーティング（URL と表示する画面を結び付ける仕組み）やビルドが最初から入っている

PR #285 の16指摘は、これで全件が解消済みとなる。

### 24周目で全dayへ入れた指し先の明示

「続き」「上のコード」「さきほど」だけで済ませていた箇所を、ファイル名と Step 番号で
書き直した。1日分を後から開いた読者、目次から飛んできた読者が、書き込み先を判別できない
状態を潰すためです。

| 種別 | 件数 |
|---|---|
| `// filepath: 続き` を実ファイル名または読み比べ用の断りに置換 | 68 |
| day02 の無注記コードブロックへ書き込み先を追加 | 25 |
| 節の1行目にある指し先の無い「上の書き方」を修正 | 14 |
| 中身のない区切り線の重複を削除 | 12 |
| 「さきほど」等の指示語を名前と Step 番号へ | 11 |
| 「上のコード」が2つ前を指していた箇所 | 3 |

### 読み上げソフトへの対応（教材と完成版の両方）

`AvatarImage` に `alt`、フォームの入力欄に `aria-invalid` と `aria-describedby`、
エラー文言に `id` を入れた。教材だけ直すと完成版とズレるため、`src/` と
`scripts/_app-components/` にも同じ変更を入れている。

`src/component/ui/form.tsx` の `FormControl` は元から正しく設定していたが、この部品を
使っているファイルが完成版で1つだけ、教材では0だったため、読者が書くフォームには
効いていなかった。

## 追記（2026-07-27・27周目の初心者通し読み）

### 何を当てたか

Day 01 から Day 30 までを、完全な初心者になりきって順に読み通した。観点は文章の質ではなく
「そこで手が止まって先へ進めないか」に絞ってある。40件が出た。26周かけて言葉・事実・
指し先・整合性を直したあとで、これまでで最大の件数になった。

### 見つかった型

| 型 | 例 | 件数 |
|---|---|---|
| 書いていない前提 | `git config user.name` が30日のどこにも無い。GitHub アカウント作成手順が無い | 4 |
| 存在しないものを使わせる | `const utils = api.useUtils()` を「上で定義済み」と書くが、書かせていない | 6 |
| 二重定義でコンパイルが止まる | Day 13 の仮ハンドラーを Day 15 が削除指示なしに再宣言 | 3 |
| コードが閉じていない | day18 のコメント一覧は `.map` が閉じられず、day29 はタグの途中で終わる | 2 |
| 矛盾した指示 | 「赤いエラーが正常」の24行後に「エラーが出ていないこと」を確認させる | 5 |
| 貼る位置が決められない | 「（続き）」を末尾に足すとルーターの外に出る | 2 |
| その他 | 外部サービス丸投げ、動かないコマンド、復旧手段なしの全置換 | 18 |

37件を修正し、1件は指摘のほうが誤りだったため却下した（読者の tsconfig に
`noUnusedLocals` は入らない。scaffold の `configure_tsconfig` は `exclude` しか足さない）。
残り2件は既に対処済みだった。

### 自分の修正が壊した箇所

一括置換で `（同じファイルの続き）` が最大6回積み上がった（day01 の5箇所、day14 の2箇所）。
原因は、書き換え済みのバッファから値を拾い直したこと。検査 `check_anchor.py` に
「同じ注記が2回以上ぶら下がった値」を止める規則を足して塞いだ。

day30 の Vercel コマンドを直す際、`npx dotenv` を使う形に書き換えたが、読者の環境に
入るのは `dotenv`（ライブラリ本体）で実行ファイルを持たない。追加の道具を要らない
`set -a` と `.` の形へ再修正し、値が渡ることを実際に実行して確認した。

day25 のパスワード復旧手順も、書きかけの段階で誤りに気付いて訂正した。
`npm run db:seed` は既存ユーザーを書き換えない（`scripts/_seed/seed.ts:58` の `update: {}`）。

### 追加した検査

| 検査 | 止めるもの |
|---|---|
| `check_anchor.py` | 書き込み先が指示語だけ／注記の重複／完成版に存在しないパス |
| `check_procedure_order.py` | その日までに書いていない tRPC の手続きの呼び出し |

どちらも、修正前の版に当てると実際に落ちることを確認してから CI へ配線した。

### 販売ZIPの実物確認

`bash scripts/build-zip.sh` で生成し、中身を確認した。6.7MB / 185ファイル / 30日分すべて収録。
`project` `task` `search` `comment` `report` `user` の6ルーターは除外され、`auth.ts` だけが
入る。外部レビュー指摘④は解消された状態を保っている。

## 追記（2026-07-27・28周目）

27周目の修正15コミットを対象に、敵対的な観点で再点検した。7件が出て、**7件とも27周目の
修正そのものの誤り**だった。直した側を直さないと、直した分だけ壊れることになる。

| 内容 | どう誤っていたか |
|---|---|
| day18 の閉じタグ | `</div>` を3つ書かせたが、読者が開いた `<div>` は1つだけ。完成版にある2つの箱を開く指示が無かった |
| day18・day29 の「エラーが出なくなります」 | 外側の箱が開いたままなので偽。書いた本人が数えていなかった |
| day14 の導入文と表 | 3行後の「session は書かない」と正反対のことを言っていた |
| day07 の「消える」範囲 | seed が消すのは固定IDの2プロジェクトだけ。自分で作ったものは消えない |
| day12 のゴール文 | 見出しを5手続きに直したのに、ゴール文が4つのまま |
| day08 の本文 | 見出しを「読む」に直したのに、本文が「作ります」のまま |

閉じタグは減らさず、開く指示を足す形で直した。完成版と同じ構造にするためです。

### CI が動かない手順を守らせていた

`check_scaffold_curriculum_alignment.py` の deployment contract に問題がありました。
固定していたのは次のコマンドです。

```
npx vercel env run -e production -- npx prisma db push
```

`vercel env` に `run` はありません。あるのは ls / add / rm / pull です。
動かない手順が契約として守られていたことになります。
取り出してから読み込む形へ変え、契約もそれに合わせました。

契約の書き換えは、実態が正しいことを確かめてから行います。実行して値が渡ることを確認しました。
`set -a` → `. ./.env.production.local` → `set +a` で `DATABASE_URL` と `JWT_SECRET` が
後続コマンドから見える。

### 閉包の主張は実質ゼロになった

「エラーが出なくなります」「すべて閉じる」に当たる文を全34ファイルから抽出したところ、
残ったのは day02 のコード内文字列3件と、day27 の「どれがどれを閉じるかを数えておいてください」
だけになった。逆に「この時点では構文エラーが残ります」と正しく断っている文は20箇所ある。

## 追記（2026-07-27・29周目）

全30日について、写経対象のコードブロックをファイル別に連結し、括弧とタグの開閉を数えた。
13件が出ました。うち3件は「開いたまま終わる構造」で、いずれも確認項目が
「保存してエラーが出ていない」だった。読者には満たせない条件です。

| 日 | 閉じていないもの | 読者に起きること |
|---|---|---|
| day29 | `<form>`（`</form>` が30日のどこにも無い） | ビルドが失敗し、エラーは無関係な `</CardContent>` を指す |
| day25 | `ProfilePage` 関数の `}` | ファイル末尾で「`}` が足りない」と出る |
| day10 | `ProjectDialog` 関数の `}` | 次の Step の `npm run dev` が動かない |

残る10件は「読み比べ用のコードが実ファイル名を名乗っていた」型です。Before / After の
節では、続きのブロックだけが読み比べ用と書かれ、先頭だけ実ファイル名になっていた。
読者は30日ずっと `// filepath:` に従って写しているので、そこに本物のパスがあれば、
改善前のコードを本物のファイルへ貼ります。しかもどれも閉じていない断片です。

### 手で直した件数は全数ではなかった

この型を `check_anchor.py` の規則にしたところ、報告された9件のほかに12件が出ました。
day01・day09・day13・day20・day21・day22 の「参考」「比較用の一部」「アンチパターン」で、
注記は付いていますが、`// filepath:` は本物のパスのままでした。

人が挙げた件数を全数として扱いません。同じ型を機械にかけると、たいてい残りが出ます。

### 22日は問題なしと確認された

day03〜09・12〜15・17〜22・24・26〜28・30 は、再構成した全ファイルで、括弧とタグがゼロに
収まりました。「まだエラーが残る」という記述もすべて正確でした。

## 追記（2026-07-27・30周目と31周目）

### 30周目 — 日をまたいだファイルの積み上がり

9ファイルを411ブロックから日順に再構成した。積み上がり自体は健全でした。重複宣言・丸ごと
上書き・未定義参照はほぼゼロで、`@/` の46パスもすべて解決します。出たのは別の層でした。

もっとも重い1件は、8箇所が「このリポジトリの `src/...` と見比べて確認してください」と
書いていた点です。`scripts/build-zip.sh:26` に「完成アプリの package.json / src /
prisma は入れない」とあるとおり、販売ZIPに `src/` は無い。買った人は30日ためた自分の
ファイルを照合できません。著者は day20:502 で正しく書いていたので、同じ教材の中で
言うことが割れていました。

| 内容 | 直したこと |
|---|---|
| 8箇所の照合指示 | 「販売用 ZIP に完成版の `src/` は入っていません」を添えた |
| day11「完成版と揃えます」 | その完成版は Day 12・27 まで書いた後の姿。揃えると翌日の手順が壊れる |
| day28 の `Trash2` | 「import 済みなら追加しない」という条件付きだが、`page.tsx` には無い。読者は飛ばして落ちる |
| day17 の `onTimeLogSuccess` | マイタスク画面だけ受け取っておらず、時間を記録しても表示が変わらない |

1件は指摘のほうが誤りだった。「検索条件の URL 保存が未教授」という指摘だが、day20 は
`searchParams.get` と `router.push` の両方を教えている。モジュールの不在と機能の不在は別です。

### 31周目 — 30周目の修正への敵対レビュー

11件が出ました。**11件とも30周目の修正そのものの誤り**です。5回続けて同じ結果になりました。

もっとも重い1件を挙げます。`onTimeLogSuccess` を通して「その場で反映される」ようにした
70行下へ、「記録した直後は数字がすぐ変わりません。開き直すと反映されます」が残っていました。
同じ日の中で正反対のことを言っていました。

自分の足した注記が本編と食い違う例もありました。day13 へ「優先度での絞り込みは本編では
扱いません」と書いたのに、同じ日の Step 0 で読者自身が `if (input?.priority)` を書きます。
扱わないのは画面側のセレクトだけでした。確かめずに書いています。

### 追加した検査

`check_step_ref.py` — 本文の指す Step 番号がその日に実在するかを見ます。ただし実測した
2件（day29 の Step 1、day17 の Step 2）は、どちらも参照先の Step 自体が在って中身だけ
違いました。この検査では捕まりません。捕まえるには、参照している識別子がその Step の
コードブロックに在るかまで見る必要があります。そこまではやっていません。

## 追記（2026-07-27・所要時間の実測）

手順表の合計時間が実態と合っていませんでした。写経するコードの文字数を数え、毎分120文字
（記号混じりのコードを写す初心者としては速い方）で割ったところ、**26日のうち23日で、
打鍵だけで手順表の合計を超えます**。

| 日 | 手順表 | 打鍵だけで必要 | 倍率 |
|---|---|---|---|
| day29 ユーザー詳細・編集 | 76分 | 216分 | 2.8倍 |
| day20 タスク検索 | 70分 | 194分 | 2.8倍 |
| day25 プロフィール編集 | 73分 | 177分 | 2.4倍 |
| day16 ステータス変更 | 26分 | 58分 | 2.2倍 |

読む時間と詰まる時間は1秒も含んでいません。毎分60文字なら day29 は7時間になります。

### 数字を作り替えなかった理由

正しい数字を持っていないためです。打鍵の推定は出せますが、読む時間と詰まる時間は
測っていません。根拠の無い数字へ置き換えるのは、いまの数字と同じくらい不誠実です。

代わりに、いまの数字が何を表すかを26日すべてへ書き足しました。

> この時間はコードを読んで理解する目安です。写経して打ち込む時間、詰まって調べる時間は
> 別に見てください。

目次の「1日あたり1〜1.5時間」も「2〜3時間を見ておくと安心です」へ戻しました。26周目に
手順表の合計だけを見て下げたのが誤りで、写経の量を見ていませんでした。測ってから戻して
います。

適正値は実際の初心者が1日分を通して計るまで確定しません。

## 追記（2026-07-27・観点ごとの収束の違い）

35周を通して分かったのは、観点によって収束の仕方が違うことです。

### 自分の修正への敵対レビューは収束しません

7回まわした結果です。

| 周回 | 26A | 28 | 31 | 32 | 33 | 34 | 35 |
|---|---|---|---|---|---|---|---|
| 件数 | 8 | 7 | 11 | 4 | 12 | 9 | 8 |

減っていません。直すたびに新しい文が入り、その新しい文へ誤りが混ざるためです。
この観点だけを続けても終わりません。ただし、見つかるものは実在の欠陥なので、
やめてよいという意味ではありません。

### 新しい観点は層を掘り当てます

| 周回 | 観点 | 件数 |
|---|---|---|
| 27 | 初心者になりきった通し読み | 40 |
| 29 | 全30日のタグと括弧の収支 | 13 |
| 30 | 日をまたいだファイルの積み上がり | 6 |

観点を変えるたびに、それまで一度も見ていなかった層が出ます。

### 繰り返した自分の失敗

同じ型を3回繰り返しました。

1. 一括置換で、書き換え済みのバッファを拾い直す（`（同じファイルの続き）` が6回重なった）
2. 同じ文言が複数ある文書で最初の1件だけ書き換える（4箇所のうち狙いと違う所を直した）
3. 1箇所直して同型の残りを数えない（6箇所のうち1箇所だけ直した）

どれも「置換の前に件数を数える」で防げます。2件を記憶へ残しました。

### 先送りには受け皿が要ります

「画面での確認は Step 10 の最後で行います」と書いたのに、Step 10 にその項目を作って
いませんでした。読者は一度も確認しないまま終わります。day29 で指摘され、day25 でも
同じことをしていました。さらに day25 では、先送りの注記を受け皿である動作確認の節の
中へ入れてしまい、「画面を見ている最中に画面での確認は後で」と書いていました。
