# findings day11-20

## day11 行153-155

- 何をした: Step 0-1 で `update` 手続きを貼る場所を探した。
- 期待: 貼る位置が1つに決まる。
- 実際: 行153 は「`getAll` の下に追加します」、行155 は「ここから先の『（続き）』のブロックは、`project.ts` の末尾にある `});` の1行上へ貼ります」と書いてある。Day 10 で `create` を `getAll` の下に足しているので、この2つは別の場所を指す。さらに 0-3 の `setArchiveStatus` だけは「`projectUpdateSchema` の下、`export const projectRouter` の上」とルーター外を指定しており、「（続き）」ブロックの貼り先が3種類ある。初心者はどれに従うか判断できない。
- 深刻度: 混乱
- 回避に必要だった外部知識: 「トップレベルの関数定義はルーターの外、手続きはルーターの中」というスコープの理解。完成コード全体（行1325〜）と照合して末尾追加を採用した。

## day11 行347/593/921/957

- 何をした: 各 Step の確認ポイント「`npm run dev` で型エラーが出ていない」を確認するため `npx tsc --noEmit` と `npm run build` を実行した。
- 期待: 型エラー 0。
- 実際: Day 11 完了時点で型エラーが 5件 残る。`src/component/project/project-detail-view.tsx` の `RouterOutputs['project']['getById']` が未定義で、そこから派生して implicit any が4件（144行/167行/237行/246行）。`npm run build` は `Failed to compile.` で落ちる。行969-971 は「`getById` が無いという型エラーの出ることがあります」と1件だけ予告しているが、確認ポイントの「型エラーが出ていない」と真っ向から矛盾し、派生の4件は予告されていない。`npm run dev` は型検査をしないので画面は動く。
- 深刻度: 混乱
- 回避に必要だった外部知識: 「Next.js の dev サーバーは型検査をせず、`npm run build` だけが落ちる」という知識。Day 12 まで build が通らないと理解して先へ進んだ。

### day11 結果

- Step 0〜10 を完走。`project.update` / `delete` / `archive` / `unarchive` を実 API で呼び、更新・アーカイブ・解除・削除がすべて 200 で成功することを確認した。
- `/project` と `/project?projectId=...` の配信 HTML はどちらも 200、`filepath:` の出現 0 件。
- 完成コード全体（行1316-2219）と実ファイルを空白・コメントを除いて比較 → 完全一致。

---

## day12 行137/243-261

- 何をした: Step 0-3 で「（続き）」ブロックの `const projectMemberSchema = z.object({...})` を貼る場所を探した。
- 期待: トップレベル（ルーターの外）に貼ると分かる。
- 実際: 行137 に「ここから先の『（続き）』のブロックは、`project.ts` の末尾にある `});` の1行上へ貼ります」と全体規則が書いてあり、`projectMemberSchema` にはそれを打ち消す位置指定が無い。規則通りに貼るとルーターオブジェクトの中に `const` 宣言が入り、`,` 区切りのオブジェクト内で構文エラーになる。同じ罠は Day 11 の `setArchiveStatus` にもあったが、あちらには位置指定があった。
- 深刻度: 手戻り
- 回避に必要だった外部知識: 「オブジェクトリテラルの中に `const` は書けない」という JS 構文の知識。完成コード全体を見て `projectUpdateSchema` の下（トップレベル）に置いた。

## day12 行131 / day11 行234

- 何をした: `getById` を「`getAll` の下に追加します」の指示どおり置こうとした。
- 期待: getAll の直下。
- 実際: Day 11 で `update` / `delete` / `archive` / `unarchive` を末尾に足しているため、`getAll` の直下に入れると完成コード全体（行1404〜）の並び（getAll → create → update → delete → archive → unarchive → getById → …）と一致しない。行137 の「末尾の `});` の1行上へ」とも矛盾する。Day 11 と同じ矛盾が Day 12 でも繰り返されている。
- 深刻度: 混乱
- 回避に必要だった外部知識: 完成コード全体との照合。末尾追加を採用した。

## day12 行604-627

- 何をした: Step 1 の「あわせて `handleProjectClick` も追加します」に従って `handleArchive` の下に `handleProjectClick` を書いた。
- 期待: 追加すればよい。
- 実際: Day 09 で置いた受け皿 `const handleProjectClick = (id: string) => { void id; };` がまだ残っているため、そのまま「追加」すると同名 `const` の二重宣言でコンパイルエラーになる。同じ Step 内で `handleDetailClose` と `projectDetail` については「Day 11 の仮定義を削除してから」と赤字で3回も注意しているのに、`handleProjectClick` だけ削除指示が無い。Day 11 行935 には「`handleProjectClick` は Day 09 で置いた受け皿がそのまま残っているので、Day 12 の Step 1 ではその受け皿を書き換えます」と書いてあり、Day 12 本文の「追加します」と食い違う。
- 深刻度: 手戻り
- 回避に必要だった外部知識: Day 11 側の注記を覚えているか、二重宣言エラーを読んで受け皿を消す判断。受け皿を削除して対応した。

## day12 行1193/1234

- 何をした: Step 6 の「Step 2 で追加した仮実装を削除」「Step 2 の仮実装（`console.log` の版）が削除されている」に従って該当コードを探した。
- 期待: Step 2 に `console.log` 版の `handleRemoveMember` がある。
- 実際: `handleRemoveMember` の仮定義を置いたのは **Day 11 Step 9** で、中身は空実装（`console.log` は無い）。Step 2（行710）自身も「Day 11 Step 9 で追加済み」と書いており、Step 6 の記述だけが古い版のまま残っている。探しても見つからないコードを消せと言われる。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し（Day 11 の仮定義を消せばよいと推測して対応）。

## day12 行1373

- 何をした: Step 8 の「追加の候補に出るのは「新人 太郎」1人だけです」を確認した。
- 期待: `getAvailableUsers` が1件返る。
- 実際: 3件返った（新人 太郎 のほか、Day 06 のユーザー登録演習で自分が作ったアカウントが候補に並ぶ）。教材どおりに Day 06 を実施していれば必ずずれる記述。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

### day12 結果

- Step 0〜8 を完走。`getById` / `getAvailableUsers` / `addMember` / `removeMember` / `updateMemberRole` を実 API で検証。重複追加は「このユーザーは既にプロジェクトのメンバーです」、唯一の OWNER 削除は「プロジェクト唯一のオーナーは削除できません」で正しく弾かれた。
- Day 11 から残っていた型エラー5件が解消し、`npx tsc --noEmit` は 0 件。
- 完成コード全体（行1395-2899）と実ファイルは空白・コメントを除いて完全一致。

---

## day13 行802-812

- 何をした: Step 6 の仮ハンドラー `const handleTaskClick = (taskId: string) => {};` をそのまま写した。
- 期待: 何も起きない受け皿ができる。
- 実際: 引数 `taskId` を使わない空関数が3つ並ぶ。Day 09 の同じ場面では `void projectId;` を1行入れて未使用引数を回避していたのに、ここでは何も書かない形になっており、書き方が Day 間で揃っていない（今回の tsconfig / biome 設定では警告は出なかったので実害は無かった）。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

## day13 行826-827 と 行1474以降（完成コード）

- 何をした: Step 6 の「import群に追加」で `import { useCallback, useMemo } from 'react';`、Step 7 で `import { useEffect } from 'react';` を指示どおり追加した。
- 期待: 完成コードと同じ import になる。
- 実際: Step 1 の `import { Suspense, useState } from 'react';` と合わせて `'react'` からの import 文が3本並ぶ。完成コード（行1474〜）は `import { Suspense, useCallback, useEffect, useMemo, useState } from 'react';` の1本にまとまっており、手順どおり写すと完成コードと一致しない。「既存の react import に足す」とは書かれていない。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し（動作・lint とも問題なし）。手順どおり3本のまま残した。

### day13 結果

- Step 0〜8 を完走。`task.getAll` / `task.getById` を新規作成し `root.ts` に登録、サイドバーに「タスク」を追加、`/task` にフィルター2つ＋カード3枚が表示されることをブラウザで確認した。カードをクリックすると詳細ダイアログが開き本文が表示されることも確認。
- `npx tsc --noEmit` 0件、`biome lint src/app/task/page.tsx` 0件。

---

## day14 行1416-1423

- 何をした: Step 8 の「return の中、ページ見出し `<h1>` の直後に追加」に従って `<Button onClick={handleCreate}>新規タスク</Button>` を貼った。
- 期待: 教材のスクリーンショット（`screenshots/task-list-after-create.png`）と同じく、見出し「タスク」の右端に小さなボタンが並ぶ。
- 実際: Day 13 で作った `<div className="flex flex-col gap-6">` の直下に素の `<Button>` が入るため、**ボタンが画面幅いっぱいに伸びた帯**になる（実測: 幅約1140px の紫の帯）。Day 09 のプロジェクト一覧では見出しとボタンを `flex items-center justify-between` の行で囲っていたが、タスク一覧にはその囲いが無く、囲えという指示も無い。Day 14 の「完成コード全体」（行2810〜）も同じ構造なので、完成コードを見ても直せない。
- 深刻度: 混乱
- 回避に必要だった外部知識: Tailwind の flex コンテナ内で子要素が伸びる挙動の理解。教材どおりの見た目にするなら見出しとボタンを囲む div が要る。今回は教材どおり（帯のまま）で進めた。

## day14 行1315-1341

- 何をした: Step 8 の「セッションは Day 13 で取得済みなので追加しません」「`session` は Day 13 の Step 6 で追加済み」を確認した。
- 期待: Day 13 で `session` を宣言している。
- 実際: 宣言はしているが、Day 13 の時点では `session` は `myRoleByProject` の中でしか使っておらず、Day 14 の `handleSubmit` が `session?.user?.id` を参照して初めて意味を持つ。記述自体は正しいので実害は無い。（誤りではないため参考として記載）
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

### day14 結果

- Step 0〜9 を完走。`task.create` を追加、`search.ts` を新規作成し `root.ts` に登録、`task-dialog.tsx` を全面的に書き直し、`/task` に配線。
- ブラウザで「新規タスク」→ 空タイトル送信で「タイトルは必須です」表示 → タイトル入力して「作成」→ ダイアログが閉じ一覧に新タスクが出ることを確認。
- `npx tsc --noEmit` 0件、`biome lint`（4ファイル）0件。
- 完成コード全体（行1670-3252）とトークン単位で比較 → task.ts / search.ts / root.ts / task-dialog.tsx は完全一致。`src/app/task/page.tsx` のみ Day 13 由来の react import 3本 vs 完成コードの1本の差（前掲 day13 の findings と同じ原因）。

---

## day15 行1767以降（完成コード）vs day14 行1317-1324

- 何をした: Day 15 の「完成コード全体」の `src/app/task/page.tsx` を、手順どおりに作った実ファイルと突き合わせた。
- 期待: 一致する。
- 実際: 完成コードには `const { data: users } = api.search.getProjectMembers.useQuery();` の1行がある。Day 14 の Step 8 には「ここでは追加しないAPI」という表があり、`api.search.getProjectMembers` は「一覧の担当者フィルターで使う。フィルターは Day 20 で作る」と**明示的に追加しないと書かれている**。Day 15 の手順にも追加指示は無い。しかも完成コードの中で `users` はどこからも参照されておらず、そのまま貼ると未使用変数になる。読者が「どこかで写し忘れた」と誤解する。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。手順側（追加しない）を採用した。

## day15 行799 / day13 行802-812

- 何をした: Step 7 の「Day 13 で置いた仮の `handleDelete` を置き換えます」に従った。
- 期待: 素直に置き換えられる。
- 実際: Day 13 の仮実装は `const handleDelete = (taskId: string) => {};` で、位置は `handleTaskClick` / `handleEdit` と並んでいる。Day 15 の Step 6 で定義する `deleteTargetId` state と `deleteMutation` は「useState 群 / mutation 群」に置くよう指示されており、`handleDelete` の位置との前後関係が本文からは決まらない。置き場所が決まらないので、読者は当てずっぽうで置くことになる（今回は state と mutation を先に置いた）。なお `handleDelete` は利用者の操作で呼ばれるコールバックなので、後ろで宣言した値を参照しても実行時エラーにはならない。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。置き場所が本文から決まらないだけで、どちらに置いても動く。

### day15 結果

- Step 0〜11 を完走。`task.update`（楽観ロック付き）と `task.delete` を追加し、画面側で編集・削除を配線。
- ブラウザで確認: 編集ボタン → 見出し「タスク編集」・ボタン「更新」でダイアログが開き既存値が入る → タイトル変更 → 一覧に反映。削除ボタン → 「本当に削除しますか」ダイアログ → キャンセルで残る → 削除で一覧から消える。
- `npx tsc --noEmit` 0件、`biome lint` 0件。
- 完成コード照合: `src/server/api/routers/task.ts` はトークン単位で完全一致。`src/app/task/page.tsx` は上記の `users` 1行と、Day 13 由来の react import 分割のみ差分。

---

## day16 行1767以降（完成コード `src/app/task/page.tsx`）

- 何をした: Step 3 の最後で `<TaskCard>` に props を2つ足したあと、完成コードと照合した。
- 期待: 完成コードが Day 13〜15 で作った変数名を使っている。
- 実際: 完成コードの `<TaskCard>` は `canEdit={taskCanEdit}` / `canDelete={taskCanDelete}` になっているが、`taskCanEdit` / `taskCanDelete` という変数は Day 13〜16 のどこにも定義が無い（Day 13 で定義したのは `canEditProject` / `canDeleteProject`）。完成コードをそのまま貼ると未定義変数でコンパイルエラーになる。
- 深刻度: 手戻り
- 回避に必要だった外部知識: Day 13 の変数名を覚えていること。手順側（`canEditProject` / `canDeleteProject`）を採用した。

## day16 行762-781

- 何をした: Step 3「カード内に合計作業時間の表示と『時間記録』ボタンを置きます」に従って JSX ブロックを貼る場所を探した。
- 期待: 貼る場所が示されている。
- 実際: `task-card.tsx` のどこに置くかの記述が無い。`return ( <> <Card> {/* ...カードの中身... */} </Card> ...` という省略形しか無いため、既存の `CardHeader` / `CardContent` のどの階層に入れるか読者が決めるしかない。完成コード（行1416〜）を見れば分かるが、手順だけでは決まらない。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し（完成コードと同じ位置＝担当者・期限の行の直下に置いた）。

## day16 行704-711 / 行1416以降

- 何をした: `TaskCardProps` に `timeSpentMinutes` と `onTimeLogSuccess` を「// ...既存のprops... の後」に追加した。
- 期待: 完成コードと同じ並びになる。
- 実際: 完成コードでは `timeSpentMinutes` が `onEdit` の**前**に入っている。手順のコメント（`// ...既存のprops...` を先に書いてから2つ）に従うと末尾に付き、並びがずれる。動作に影響は無い。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

### day16 結果

- Step 0〜4 を完走。`task.addTime` を追加、`time-log-dialog.tsx` を配布版を消して自分で書き直し、`task-card.tsx` に合計作業時間・時間記録ボタン・ダイアログを追加、`page.tsx` から `timeSpentMinutes` / `onTimeLogSuccess` を接続。
- ブラウザで確認: カードに「合計作業時間: 12h 0m」と「時間記録」ボタンが出る → 0時間0分で送信すると「1分以上入力してください」→ 1時間30分を入れて「時間を追加」→ ダイアログが閉じ合計が 13h 30m に増える。
- `npx tsc --noEmit` 0件、`biome lint` 0件。
- 完成コード照合: `time-log-dialog.tsx` はトークン単位で完全一致。`task-card.tsx` は props の並び順のみ差分。`task.ts` / `page.tsx` の完成コードは抜粋のみ（全文ではない）。

---

## day17 行687-702

- 何をした: 「ここまでのインポート一覧（中間確認）」の表と、Step 1〜6 で実際に書いた import を突き合わせた。
- 期待: 表のとおりに揃っていれば正しい。
- 実際: 表に **`useCallback`（Step 2 で追加）と `@/lib/constant/roles` の3つ（`hasPermission` / `isProjectMemberRole` / `ProjectMemberRole`、Step 2 で追加）が載っていない**。表は `react` 欄を「`useMemo`, `useState`（Step 1）」のままにしており、Step 2 の「置き換えて」を反映していない。表を信じて import を整理すると Step 5 で未定義エラーになる。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し（Step 2 の本文が正しいと判断した）。

## day17 行815-822

- 何をした: Step 8 の仮ハンドラー `const handleEdit = (_taskId: string) => {};` を写した。
- 期待: 他の Day と同じ書き方。
- 実際: Day 13 の同じ場面では `(taskId: string) => {}`（アンダースコア無し）、Day 11 の受け皿では `void projectId;` と、未使用引数の書き方が Day ごとに3種類ある。どれが正しい流儀なのか読者には分からない。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

## day17 行599-606 / day16 行845-847

- 何をした: `handleTimeLogSuccess` を写した。
- 期待: Day 16 と同じコード。
- 実際: Day 16 は `void utils.task.getAll.invalidate();`、Day 17 は `void` 無しの `utils.task.getAll.invalidate();`。Day 16 の本文は「この教材では、この1か所と Day 10 の読み比べ用のコードにだけ出てきます」と明記しているので意図的だが、同名・同目的の関数が Day 跨ぎで書き分けられており、写経していると迷う。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

### day17 結果

- Step 1〜12 を完走。`src/app/my-task/page.tsx` を新規作成。
- ブラウザで確認: 「マイタスク」見出し、ステータス Tabs 5つ、プロジェクト Select、「期限切れ (1)」グループにカード1枚（教材の記述どおり）。「完了」タブに切り替えると「あなたに割り当てられたタスクはありません」が出る。編集ボタンで「タスク編集」ダイアログ、削除ボタンで「本当に削除しますか」ダイアログが開く。合計作業時間 13h 30m も表示（Day 16 の記録が反映）。
- `npx tsc --noEmit` 0件、`biome lint` 0件。
- 完成コード全体（行1342-1956）とトークン単位で比較 → **完全一致**（宣言順のみ差）。

---

## day18 行480-489 / 行646-653

- 何をした: Step 3 の「TaskDetailDialog の return 内: コメントヘッダー」以降のブロックを `task-detail-dialog.tsx` に貼る場所を探した。
- 期待: 既存の JSX のどこに入れるかが示されている。
- 実際: 「return 内」としか書かれておらず、配布ファイルの `{taskDetail && ( ... )}` ブロックの中なのか外なのかが決まらない。フォームは `taskDetail.projectId` を、一覧は `taskDetail.comments` を参照するので中に置かないと型エラーになるが、その説明は無い。また、コメント欄の前に `<Separator />` を入れるかどうかも本文には無く、完成コード（行1210〜）を見ないと分からない。
- 深刻度: 混乱
- 回避に必要だった外部知識: `taskDetail` が `undefined` のときに `.projectId` を読めない、という TypeScript の絞り込みの理解。

## day18 行391 / 行448

- 何をした: 「配布ファイルの 27 行目あたりにすでに書いてあります」「配布ファイルの 3 行目と 4 行目にすでに書いてあります」を確認した。
- 期待: 行番号どおり。
- 実際: `taskDetail` の `useQuery` は **28行目**（「27行目あたり」と幅を持たせてあるので実害なし）。`Avatar` は3行目、`Badge` は4行目で一致。行番号での案内は Day 01〜10 の作業でファイルが変わっていれば簡単にずれるので、脆い書き方ではある。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

### day18 結果

- Step 0〜7 を完走。`comment.ts` を新規作成（`getByTaskId` / `create`）、`root.ts` に登録、`task-detail-dialog.tsx` にコメント一覧・件数 Badge・投稿フォームを追加、`/task` から `canEditProject` を渡す。
- ブラウザで確認: タスク詳細に「コメント 2」の Badge、アバター＋名前＋`2026/07/29 00:44` 形式の日時＋本文が新しい順に並ぶ。テキストエリアに入力して「コメント投稿」→ 一覧の先頭に追加され、テキストエリアが空に戻る。
- `npx tsc --noEmit` 0件。
- 完成コード照合: `comment.ts` / `root.ts` はトークン単位で完全一致。`task-detail-dialog.tsx` は宣言順と className の改行のみ差分。

---

## day19 行400-421（Step 3）vs 行1031以降（完成コード）

- 何をした: Step 3 の「本人チェックで編集・削除ボタンを表示」ブロックを、Day 18 で作った「名前と日時を包む箱」（`flex items-center justify-between`）の中に貼った。
- 期待: 教材のスクリーンショットどおり、日時とアイコン2つが右端に並ぶ。
- 実際: 完成コード（行1031〜）では日時 `<span>` とボタンの `<div>` を **`<div className="flex items-center gap-2">` でもう一段包んでいる**が、Step 3 の本文にはその包む div の指示が無い。手順どおりに貼ると `justify-between` の子が「名前 / 日時 / ボタン」の3つになり、日時が中央へ飛ぶ（実測: 名前が左端、日時が中央、アイコンが右端）。完成コードとレイアウトが食い違う。
- 深刻度: 混乱
- 回避に必要だった外部知識: flex の `justify-between` が子要素の数で配置を変えるという知識。今回は手順どおり（3つ並び）のまま進めた。

## day19 行186 / 行652-663

- 何をした: `update` / `delete` を `commentRouter` へ、`handleClose` を `task-detail-dialog.tsx` へ追加した。
- 期待: 追加位置が一意に決まる。
- 実際: `handleClose` は「その前に、ダイアログを閉じる処理を1か所にまとめます」としか書かれておらず、コンポーネント内のどこ（mutation 群の後か、return の直前か）かは示されない。`commentForm` / `editCommentForm` / 3つの state をすべて参照するため、それらより後ろに置く必要があるが、その制約の説明は無い。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。置き場所が本文から決まらないだけで、どちらに置いても動く。

### day19 結果

- Step 1〜7 を完走。`comment.ts` に `commentUpdateSchema` / `findCommentAndAssertOwnership` / `update` / `delete` を追加、`task-detail-dialog.tsx` に編集・削除ボタン、編集モード、削除確認ダイアログ、`handleClose` を追加。
- ブラウザで確認: 自分のコメント（管理者）にだけペンとゴミ箱が出て、他人（田中太郎）のコメントには出ない。編集ボタン → テキストエリア＋「キャンセル/更新」→ 更新すると本文が差し替わる。削除ボタン → 「コメントを削除しますか？」→ 削除でコメントが消える。
- `npx tsc --noEmit` 0件。
- 完成コード照合: `comment.ts` はトークン単位で完全一致。`task-detail-dialog.tsx` は上記の包む div 1個ぶんの差と className の改行差。

---

## day20 行1573-1596

- 何をした: Step 8 の `closeTaskDialog` を `src/app/task/page.tsx` に追加した。
- 期待: そのまま貼れば動く。
- 実際: 中で `router.replace(...)` を呼んでいるが、`src/app/task/page.tsx` には `router` が存在しない。Day 13 で入れた import は `import { useSearchParams } from 'next/navigation';` だけで、`useRouter` も `const router = useRouter();` も無い。Day 20 のどこにも「`useRouter` を足す」という指示が無いので、貼った瞬間に `Cannot find name 'router'` で止まる。Day 20 の完成コード（行3497〜）も抜粋なので import 部分が載っておらず、そこを見ても解決しない。
- 深刻度: 手戻り
- 回避に必要だった外部知識: Next.js App Router で URL を書き換えるには `useRouter()` が要る、という知識。`import { useRouter, useSearchParams } from 'next/navigation';` と `const router = useRouter();` を自分で追加した。

## day20 行514-530

- 何をした: 実装ステップ一覧（行88-101）に沿って Step 0 → Step 1 と進めた。
- 期待: 全作業がステップ番号のどれかに属する。
- 実際: `src/app/search/loading.tsx` を作る作業が **Step 0 の区切り線（`---`）の後、Step 1 の見出しの前** に、番号も見出しも無いまま挟まっている。しかも冒頭が「完成版は同じ4行を dashboard・my-task・project・report・task の各フォルダにも置いています。」という前置き抜きの一文で始まるため、何の話か分からない。ステップ表にも `loading.tsx` は載っていないので、表を頼りに進むと作り忘れる。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し（本文を最後まで読んで気づいた）。

## day20 行1602 / 行1690

- 何をした: Step 8 の「Step 2 の `{/* Step 8-9: 検索結果 */}` を以下に置き換えます」と、その後の「上の `<TaskCard key={task.id} ... />` を以下に置き換えてください」に従った。
- 期待: 一度書いたものをすぐ書き直す必要がない。
- 実際: 同じ Step の中で `<TaskCard>` を一度書かせてから、8行下でその全体を置き換えさせる。Day 13 の Step 6 でも同じ構成（書いてから置き換え）だったので、少なくとも2回繰り返されるパターン。写経の手が二度手間になる。
- 深刻度: 混乱
- 回避に必要だった外部知識: 無し。

### day20 結果

- Step 0〜10 を完走。`search.ts` に `search` / `quickSearch` / `getUserProjects` を追加、`src/app/search/loading.tsx` と `src/app/search/page.tsx` を新規作成、サイドバーに「検索」を追加、`/task` に編集リンク（`?taskId=...&edit=true`）対応を追加。
- ブラウザで確認: `/search` を開くと「検索条件を入力してください」。キーワード「設計」で検索 → URL が `/search?keyword=設計` になり「検索結果:1件（タスク:1件）」とカード1枚。存在しない語では「検索結果が見つかりません」。「クリア」で URL が `/search` に戻り初期メッセージへ。検索結果の編集ボタン → `/task?taskId=...&edit=true` へ遷移し、詳細ダイアログは開かず編集ダイアログだけが開く。閉じると URL から `taskId` と `edit` が消えて `/task` になる。
- `npx tsc --noEmit` 0件、`biome lint` 0件。
- 完成コード照合: `search.ts` / `loading.tsx` はトークン単位で完全一致。`search/page.tsx` は className の改行のみ差分。`app-layout.tsx` と `task/page.tsx` の完成コードは抜粋のみ（全文ではない）。

---

## 表示崩れ

**結論: day11〜day20 では、教材の目印（`filepath:` を含む行）が動作中のアプリに文字として出る箇所は 0 件。**

検証方法:
1. day11〜day20 の全コードブロックを機械的に走査し、「ブロック先頭が `// filepath:` で、その直後の実コード行が `<` または `{` で始まる（＝JSX の中に素の JS コメントを置いている疑い）」ものを抽出した。
2. 開発サーバー（`PORT=3001 npm run dev`）を起動し、Playwright で実際にログインして `/`, `/dashboard`, `/project`, `/project?projectId=...`, `/task`, `/task?taskId=...&edit=true`, `/my-task`, `/search`, `/search?keyword=...` を開き、各ページの `document.body` の可視テキストに `filepath:` が含まれるかを数えた。ダイアログ（タスク作成・タスク編集・タスク詳細＋コメント・時間記録・各削除確認）も開いた状態で数えた。
3. 完成した `src/` 全体を `grep -rn filepath` した。

結果:
- 1 の走査で検出されたのは day20 の2箇所（行626・行3485）のみ。どちらも `menuItems` 配列の **オブジェクトリテラル**であり、JSX の子要素ではない。`// filepath:` は正しい JS コメントとして扱われ、画面には出ない（実際に `/search` を開いて 0 件を確認済み）。day11〜day19 は 0 箇所。
- 実際に開いた全ページ・全ダイアログの可視テキスト中の `filepath:` 出現数: **すべて 0**。
- 完成した `src/` 配下の `filepath` 文字列: **0 件**。

つまり day11〜day20 に「目印が画面に出る」不具合は無い。JSX 内の目印は一貫して `{/* filepath: ... */}` 形式で書かれている。

---

## 完成コードの照合

各 Day の「完成コード全体」を機械抽出し、手順どおりに作った実ファイルと **トークン単位（空白・改行・コメント・末尾カンマを無視）** で比較した。

| Day | ファイル | 判定 |
|-----|---------|------|
| 11 | `src/server/api/routers/project.ts` | ✅ 完全一致 |
| 11 | `src/app/project/page.tsx` | ⚠️ 宣言順のみ差（内容は同一）＋ Day 09 の見出し行の className が完成コードだけ違う（後述） |
| 12 | `src/server/api/routers/project.ts` | ⚠️ **手続きの並び順が手順と完全に食い違う**（後述） |
| 12 | `src/app/project/page.tsx` | ⚠️ 宣言順の差＋ `<Label htmlFor="show-archived">` の className 差（後述） |
| 13 | `src/server/api/routers/task.ts` / `root.ts` | ✅ 完全一致 |
| 13 | `src/app/task/page.tsx` | ⚠️ react の import が手順では3本・完成コードでは1本 |
| 13 | `src/component/layout/app-layout.tsx` | 抜粋のみ（全文ではない） |
| 14 | `task.ts` / `search.ts` / `root.ts` / `task-dialog.tsx` | ✅ 完全一致 |
| 14 | `src/app/task/page.tsx` | ⚠️ Day 13 由来の react import 差のみ |
| 15 | `src/server/api/routers/task.ts` | ✅ 完全一致 |
| 15 | `src/app/task/page.tsx` | ❌ **完成コードにだけ `const { data: users } = api.search.getProjectMembers.useQuery();` がある**（Day 14 が「追加しない」と明記した API。しかも完成コード内で未使用） |
| 16 | `src/component/task/time-log-dialog.tsx` | ✅ 完全一致 |
| 16 | `src/component/task/task-card.tsx` | ⚠️ props の宣言順のみ差 |
| 16 | `src/app/task/page.tsx`（抜粋） | ❌ **`canEdit={taskCanEdit}` / `canDelete={taskCanDelete}` という未定義変数**（正しくは `canEditProject` / `canDeleteProject`） |
| 17 | `src/app/my-task/page.tsx` | ✅ 完全一致 |
| 18 | `comment.ts` / `root.ts` | ✅ 完全一致 |
| 18 | `src/component/task/task-detail-dialog.tsx` | ⚠️ 宣言順と className の改行のみ差 |
| 19 | `src/server/api/routers/comment.ts` | ✅ 完全一致 |
| 19 | `src/component/task/task-detail-dialog.tsx` | ❌ **完成コードには日時とボタンを包む `<div className="flex items-center gap-2">` があるが、Step 3 の手順には無い**（レイアウトが変わる） |
| 20 | `search.ts` / `loading.tsx` | ✅ 完全一致 |
| 20 | `src/app/search/page.tsx` | ⚠️ className の改行のみ差 |
| 20 | `app-layout.tsx` / `task/page.tsx` | 抜粋のみ（全文ではない） |

### 深刻な不一致（読者が完成コードで復旧できない箇所）

1. **day12 `project.ts` の手続き順**
   手順の指示（行137「末尾の `});` の1行上へ貼る」／行131「`getAll` の下に追加」）でどう解釈しても、完成コードの並び
   `getAll → getById → getAvailableUsers → create → update → delete → addMember → removeMember → updateMemberRole → archive → unarchive`
   にはならない。手順どおりだと `getAll → create → update → delete → archive → unarchive → getById → getAvailableUsers → addMember → removeMember → updateMemberRole` になる。
   トークンの中身は完全に同じなので**動作は変わらない**が、「場所を見失ったら完成コードで復旧する」使い方をすると、以降の Day で差分を見比べられなくなる。

2. **day15 `task/page.tsx` の `users`**
   Day 14 Step 8 が「ここでは追加しないAPI」と表で明示した `api.search.getProjectMembers` を、Day 15 の完成コードだけが呼んでいる。しかも未使用変数。完成コードを正としてコピーすると、手順と食い違う上に lint 警告の種になる。

3. **day16 `task/page.tsx` 抜粋の `taskCanEdit` / `taskCanDelete`**
   Day 13〜16 のどこにも定義が無い変数名。この抜粋をそのままコピーするとコンパイルエラーになる。

4. **day19 `task-detail-dialog.tsx` の包む div**
   手順には無い `<div className="flex items-center gap-2">` が完成コードにだけある。手順どおりに書くと日時が中央へ飛び、教材のスクリーンショットと見た目が変わる。

5. **day11/day12 `project/page.tsx` のヘッダー className**
   完成コードのヘッダーは `flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between` / `shrink-0 whitespace-nowrap text-3xl...` / `flex shrink-0 items-center gap-4` / Label に `whitespace-nowrap` が付いているが、Day 09〜12 のどの手順にもこれらを書く指示が無い。

---

## 全体の注記

- Day 11〜20 のすべてで `npx tsc --noEmit` を実行した。エラーが残ったのは Day 11 完了時点の5件のみで、Day 12 完了時に 0 件になった（教材が予告しているのは `getById` の1件だけ）。
- Day 20 完了時点で `npm run build` が成功する（10ルートを出力）。Day 11 完了時点では `Failed to compile.` で落ちる。
- 動作確認はすべて実際の開発サーバー（`http://localhost:3001`）へ Playwright でログインして実施した。DB とコンテナは day01-10 のものをそのまま再利用し、リセットしていない。
- 検証のために作成したデータ（タスク「写経テスト day14 タスク」、コメント2件）は検証中に削除済み、または残置（コメント「写経テスト day18 のコメント」は day19 の削除検証で消去）。
