# クリーンルーム通し実行 監査ログ (2026-07-29 / Day 01〜10)

- 実行者: 独立オラクル（教材テキストのみを読む。リポジトリ `src/` は一切参照していない）
- 展開先: `/Users/kouiso/cleanroom-d/task-app`（リポジトリ外）
- 環境: macOS / Node v25.9.0 / npm 11.12.1 / Docker 29.0.2 / DB は `task-app-db-1`（localhost:25532）
- 対象: `#341` の修正を取り込んだあとの教材

`audit/clean-run-2026-07-28.md` は Day 01〜06 までだった。今回はそこから Day 10 まで伸ばし、
かつ `#341` で直した表示崩れが本当に消えているかを実測で確かめている。

## 件数

BLOCKER 0 / 手戻り 4 / 混乱 8。

## 表示崩れ（`#341` の効果の実測）

構築した実アプリの配信 HTML を取得し、`filepath:` の出現数を数えた。

| 見た場所 | `filepath:` の出現数 |
|---|---|
| `/` `/login` `/register` `/dashboard` `/project` | **0** |
| 完成した `src/` 配下の全ファイル | **0** |

Day 02〜10 の写経ブロックには、JSX の子要素の位置に残る `//` 形式の目印が1つも無い。
Day 01 にだけ3件残っていた（行990・1110・1164）。これは `#346` で直した。

Day 01 の行862・926 も目印が `//` のままだが、こちらは**開始タグの途中**であり、
その位置では `//` が正しいコメントになる。実測（transpile）で
`{/* */}` を置くと `'...' expected.` で構文エラーになることを確認しており、直さない。

## 通し実行が見つけた欠陥のうち、機械で数え直したもの

### 配布物が既に置いているファイルを「作る」と書いている — 17箇所

通し実行は Day 10 で1件見つけた。`src/component/project/project-dialog.tsx` は
Day 01 の scaffold が 168行の実装ごと配置しているのに、Day 10 Step 1 は
「ProjectDialogの骨格を作る」と書き、既にあることに触れていない。読者はファイルを
開いた瞬間に「もう自分が書いたのか」「消していいのか」で止まる。

同じ型を corpus 全体で数えた。貼り先が配布物の置くファイルと一致し、その直前14行に
「作る／新規作成／骨格を作る」の語がある箇所は **17件** ある。

| ファイル | 貼り先 |
|---|---|
| `day07_ログイン体験を改善しよう.md:141` | `src/lib/session.ts` |
| `day07_ログイン体験を改善しよう.md:612` | `src/server/api/routers/_helpers/select.ts` |
| `day07_ログイン体験を改善しよう.md:675` | `src/server/api/routers/auth.ts` |
| `day09_プロジェクト一覧画面.md:133` | `src/server/api/routers/project.ts` |
| `day10_プロジェクト新規作成.md:210` | `src/component/project/project-dialog.tsx` |
| `day13_タスク一覧画面.md:152` | `src/server/api/routers/task.ts` |
| `day14_タスク新規作成.md:115` | `src/server/api/routers/task.ts` |
| `day14_タスク新規作成.md:319` | `src/server/api/routers/search.ts` |
| `day14_タスク新規作成.md:455` | `src/component/task/task-dialog.tsx` |
| `day16_ステータス変更・時間記録.md:254` | `src/component/task/time-log-dialog.tsx` |
| `day18_コメント投稿.md:116` | `src/server/api/routers/comment.ts` |
| `day18_コメント投稿.md:453` | `src/component/task/task-detail-dialog.tsx` |
| `day21_統計カードを表示.md:116` | `src/server/api/routers/report.ts` |
| `day24_ユーザー一覧（管理者用）.md:126` | `src/server/api/routers/user.ts` |
| `day25_プロフィール編集.md:119` | `src/server/api/routers/user.ts` |
| `day27_プロジェクト詳細・アーカイブを実装しよう.md:300` | `src/component/project/project-detail-view.tsx` |
| `day28_タスク一括操作を実装しよう.md:159` | `src/server/api/routers/task.ts` |

配布物の一覧は `check_scaffold_curriculum_alignment.py` の `SCAFFOLD_COPY_MAP` から
組み立てた（65ファイル）。

**直すのは本文の言い回しなので、検査を先に入れると CI が17件で赤くなる。**
本文を直してから検査を足す順にする。

### 教材のコードがそのままでは `npm run lint` を通らない

Day 05 の「完成コード全体」を組み立てて `npx biome check` を走らせると、
4ファイルで整形の差分が出る（`src/app/page.tsx`・`src/app/dashboard/page.tsx`・
`src/app/login/page.tsx`・`src/lib/redirect.ts`）。教材は `<h1>` を3行に分けて書くが、
Biome は1行を要求する。

Day 01〜04 では lint に一度も触れないため、初心者は「自分が写経を間違えた」と誤解する。
Day 05 行353 に `npm run fix` の話はあるが、import の並べ替えの文脈でしか書かれておらず、
「教材のコードは整形前の形なので lint はそのままでは落ちる」とはどこにも書いていない。

### 通し実行の指摘のうち、確かめたら成立しなかったもの

- Day 06 行140「Step 10 までに全部出番が来ます」が実在しない Step を指している、という指摘。
  **成立しない。** `day06` には `### Step 10` が実在する（Step は1〜10で10個）。

## 1日ごとの結果

| Day | 実行範囲 | 結果 |
|-----|---------|------|
| Day 01 | Step 1〜5 全部 | 完走。チェックポイント合致。発見4件 |
| Day 02 | 全部 | 完走。`/dashboard` 200。発見0件 |
| Day 03 | ローカル手順のみ | 完走。GitHub への push は外部書き込みのため未実行 |
| Day 04 | `npm run build` まで | 完走（9/9 static pages）。Vercel デプロイは未実行 |
| Day 05 | 全部 | 完走。`/login` 200。発見3件 |
| Day 06 | 全部 | 完走。`/register` 200。発見1件 |
| Day 07 | Step 0〜6 全部 | 完走。実ログイン成功（`admin@example.com`）。発見1件 |
| Day 08 | Step 1〜4 全部 | 完走。型エラー0、`/dashboard` 200。発見0件 |
| Day 09 | 全部 | 完走。`project.getAll` 200。発見2件 |
| Day 10 | 全部 | 完走。実際にプロジェクトが作成されることを確認。発見1件 |

Day 03 の GitHub push と Day 04 の Vercel デプロイは、外部サービスへの書き込みのため
実行していない。教材の欠陥ではなく検証環境の制約である。

## 元の記録

`/Users/kouiso/cleanroom-d/findings.md`（実行者が1日ごとに追記したもの）
