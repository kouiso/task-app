# クリーンルーム通し実行 監査ログ (2026-07-29 / Day 01〜10)

- 実行者: 独立オラクル（教材テキストのみを読む。リポジトリ `src/` は一切参照していない）
- 展開先: リポジトリ外の作業ディレクトリ
- 環境: macOS / Node v25.9.0 / npm 11.12.1 / Docker 29.0.2 / DB は Docker の PostgreSQL
- 対象: `#341` の修正を取り込んだあとの教材

`audit/clean-run-2026-07-28.md` は Day 01〜06 までだった。今回はそこから Day 10 まで伸ばした。

原文は `audit/clean-run-raw/2026-07-29-day01-10.md` に置いてある。**この要約のほうが新しい。**
原文には、あとで成立しないと分かった記述（Day 06 の Step 数）がそのまま残っている。
食い違ったら、この要約の判断を採る。原文は「実行者がその場で何を見たか」の記録として読む。

## 件数

手戻り 4 / 混乱 6。BLOCKER は無い。

原文の見出しは12件あるが、そのうち2件は成立しないと分かったので混乱から外した
（Day 06 の Step 数、Day 10 の「配布済みなのに作ると書いている」）。
原文の集計（手戻り4・混乱8）はその判定より前の数である。

## 目印の表示崩れについて — **この通し実行では検証できていない**

当初この節に「配信 HTML の `filepath:` 出現数が 0 だったので `#341` の修正が効いている」と
書いた。**これは誤りである。**

原文の28行目に、実行者自身がこう書いている。

> 目印を書かなければ回避できるので、今回は目印行をすべて省略して写経した。

目印の行を貼っていないのだから、画面に出ないのは当然である。0 という数は
「実行者が目印を貼らなかった」ことを示すだけで、修正が効いたことの証拠にならない。

`#341` の修正が効いていることの根拠は、次の2つで別に取ってある。

1. **変換して描画した実測**。同じコードを `//` の書き方と `{/* */}` の書き方で
   transpile し、出力に `filepath:` が何回残るかを数えた。`//` は1回、`{/* */}` は0回。
2. **corpus の走査**。JSX の子要素の位置に残る `//` 形式の目印が0件であることを
   `check_jsx_marker.py` で確認した。走査したのは `#346` を取り込んだ木である。
   `#346` を取り込む前の木で同じ走査をすると、Day 01 の3件（行990・1110・1164）を
   検出して落ちる。0という数は、取り込んだ木で測ったものだと読むこと。

目印を貼った状態で通した実行は、まだ無い。次の通し実行で「目印の行も含めて貼る」条件を
1回入れて測る必要がある。

## 通し実行が見つけた欠陥のうち、機械で数え直したもの

### 配布物が既に置いているファイルを「作る」と書いている — **0箇所（指摘は成立しない）**

通し実行は Day 10 でこれを挙げた。「Day 10 の本文には『すでにあるので上書きする』という
記述が一切なく、初心者はファイルを開いた瞬間に止まる」というものである。

**成立しない。** `day10_プロジェクト新規作成.md:64` のファイル構成図に
「`project-dialog.tsx` ← 配布済み。今日 Step 1〜6 で中身を書き直す」と書いてあり、
続く70行目でも「`project-dialog.tsx` は配布済みですが、今日は Step 1 から Step 6 で
中身を自分の手で書き直します」と本文で述べている。どちらも Step 1（201行）より前にある。

`day14` も452行で「`src/component/task/task-dialog.tsx` は Day 01 の scaffold が
配布済みです」と書いている。

読者がそこを読み飛ばす可能性は残るが、それは「記述が無い」とは別の問題である。
この型の欠陥は0件とする。

#### 数え直した経緯（17 → 5 → 1 → 0）

1回目は配布物の一覧をディレクトリの中身から組み立てて65ファイルとし、17件を挙げた。
しかし `scaffold-from-scratch.sh:474` は `project.ts` `task.ts` `search.ts` `comment.ts`
`report.ts` `user.ts` を**意図的にコピーしない**（「auth.ts 以外のルーターは、各導入 Day で
受講生が1から書く」）。ディレクトリに在ることと配布されることは別である。
この取り違えで、配布されていない9件を「配布済み」と数えていた。

2回目は配布規則から61ファイルを組み立てて5件にした。しかしこの5件にも、本文が既存に
触れている箇所が残っていた。

- `day07` の `auth.ts`: 同じ Day の `session.ts` の節で「中身をすべて削除してから作り直します」
  と既存を前提にしている。
- `day18` の `task-detail-dialog.tsx`: 同じ Day の冒頭で「今日は `comment.ts` が
  配布されていません」と、配布の有無を読者へ明示している。
- `day27` の `project-detail-view.tsx`: 「Day 12 で作成済みのファイルを削除したり…しないで
  ください」と明記している。

語の一致だけで数えると、正しく書いてある箇所まで欠陥に数えてしまう。3回目は本文を
1件ずつ読んで1件（Day 10）に絞った。

4回目に Day 10 の全文を読んで0件になった。64行と70行に「配布済み」と書いてある。
1〜3回目は、そのファイルを扱う Step の周辺しか読んでいなかった。

**4回とも、元になった通し実行の指摘を疑わずに数え方だけを直していた。**
指摘そのものが成立するかを、該当 Day の全文を読んで確かめるのが先だった。
この型は0件なので、直す本文も足す検査も無い。

### 教材のコードがそのままでは `npm run lint` を通らない

Day 05 の「完成コード全体」を組み立てて `npx biome check` を走らせると、
4ファイルで整形の差分が出る（`src/app/page.tsx`・`src/app/dashboard/page.tsx`・
`src/app/login/page.tsx`・`src/lib/redirect.ts`）。教材は `<h1>` を3行に分けて書くが、
Biome は1行を要求する。

Day 05 行353 に `npm run fix` の話はあるが、import の並べ替えの文脈でしか書かれておらず、
「教材のコードは整形前の形なので lint はそのままでは落ちる」とはどこにも書いていない。
そのため初心者は「自分が写経を間違えた」と誤解する。

（当初この節に「Day 01〜04 では lint に一度も触れていない」と書いたが誤りである。
`day04_ネットに公開.md:179` が `npm run lint` に触れており、「`build` が見てくれるのは
型まで」「使っていない変数を見つけるのは `npm run lint` の役目」と説明している。
欠けているのは lint の存在ではなく、教材のコードが整形前の形だという断りである。）

## 指摘ごとの確認状況

通し実行の指摘をそのまま採らず、1件ずつ現物で確かめた結果を残す。
「配布済みなのに作ると書いている」を4回数え直した末に0件（＝指摘自体が誤り）と
分かったため、他の指摘も同じやり方で当たり直した。

| 指摘 | 確認状況 |
|---|---|
| 教材のコードが `npm run lint` を通らない | **成立**。教材の `<h1>` の書き方に Biome の formatter を当てると1行化を要求して落ちる |
| `day09:1379` の表と完成コードが矛盾する | **成立**。表は「`PageLoadingSpinner` を `AppLayout` で囲まない」と書くが、同じ Day の完成コード（1230行）は囲んでいる |
| 配布済みなのに「作る」と書いている | **成立しない**。`day10:64` と `day10:70` に「配布済み」と書いてある |
| `day06` の「Step 10」が実在しない Step を指す | **成立しない**。`### Step 10` が実在する |
| `day05` の Step 4・5 が Step 6 で捨てられる | 未確認。原文が引く「先にすべて削除してください」は本文に見つからなかった。引用の照合から要りなおす |
| `day07` の middleware がトップページも保護する | **成立**。matcher が `/((?!_next/static\|_next/image\|favicon.ico).*)` で `/` も対象になる。教材に副作用の説明が無い |
| `day09` の `dialogOpen` で lint が落ちる | 未確認 |
| `day01:176` の unzip に「展開済みならスキップ」の導線が無い | 未確認 |
| `day05:590-608` の Step 7〜8 の途中状態は `npm run build` が通らない | 未確認。原文はこれを Day 05 の3件のうち1件として数えている。この要約の本文では触れていなかったので、表へ足した |

「未確認」は、指摘が成立するかをまだ現物で当たっていないという意味である。
成立するとも、しないとも言えない。

## 確かめたら成立しなかったもの

- Day 06 行140「Step 10 までに全部出番が来ます」が実在しない Step を指している、という指摘。
  **成立しない。** `day06` には `### Step 10` が実在する（Step は1〜10で10個）。
  原文にはこの指摘が混乱1件として残っているが、この要約では外している。

## 1日ごとの結果

「完走」と書けるのは、その日の全 Step を実行し、教材が挙げる確認内容を実際に見た日だけとする。

| Day | 実行範囲 | 実際に見たもの |
|-----|---------|------|
| Day 01 | Step 1〜5 全部 | チェックポイント8項目の合致。発見4件 |
| Day 02 | 全部 | `/dashboard` が 200 |
| Day 03 | ローカル手順のみ | 選択 add とコミット。GitHub への push は外部書き込みのため未実行 |
| Day 04 | Step 2 まで | `npm run build` が `Generating static pages (6/6)` で完走。Vercel は未実行 |
| Day 05 | 全部 | `/login` が 200。発見3件 |
| Day 06 | 全部 | `/register` が 200。パスワード4条件が配布済み `auth.ts` と一致。発見1件 |
| Day 07 | Step 0〜6 | 型エラー0、実ログイン成功。Step 7（JWT・Cookie 属性・削除後の遷移・再ログイン）は未実行。発見1件 |
| Day 08 | Step 1〜4 | 型エラー0、`/dashboard` が 200。Step 5 は未実行 |
| Day 09 | 全部 | `project.getAll` が 200、`/project` が 200。発見2件 |
| Day 10 | 全部 | `project.create` でプロジェクトが作成され一覧に反映。型エラー0、lint 0、build 成功。発見1件 |

Day 05・06・09 の行は、画面が 200 を返したことと個別の API 応答までを見た記録である。
教材が挙げる確認内容を1項目ずつ突き合わせた記録は、Day 01 以外には無い。

Day 03 の GitHub push と Day 04 の Vercel デプロイは、外部サービスへの書き込みのため
実行していない。教材の欠陥ではなく検証環境の制約である。

## 次の通し実行で足す条件

- **目印の行も含めて貼る。** 省略すると、目印の修正が効いているかを測れない。
- 各日の確認内容を1項目ずつ突き合わせ、見た項目と見ていない項目を分けて記録する。
