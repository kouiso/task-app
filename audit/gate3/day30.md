# Gate3 初心者シミュレーション — Day 30 (2026-07-28)

- 対象: material/30days-curriculum/day30_完成版を公開！.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 初心者役が挙げた件数: 2 件 / うち反証を生き延びた件数: 0 件
- 外部レビューが挙げた件数: 1 件 / うち反証を生き延びた件数: 1 件（下の「1.」）
- 生き延びた件数（合計）: 1 件 / 反証で消えた件数: 2 件

## 生き延びた詰まり

### 1. 行524 最後の動作確認のコマンドが macOS 専用で、他の環境の読者には無い

**逐語引用**

```text
# デプロイURLをブラウザで開く（macOS）
open https://your-app-name.vercel.app
```

**何ができないか**

Windows (WSL2) と Ubuntu で進めてきた読者は、この行を実行すると
`open: command not found` が返る。Step 5 に載っているコマンドはこれ1つなので、
30日の最後の確認地点で手が止まる。

**教材のどこが足りないか**

Day 01 の行63で「Windows の場合は WSL2 の Ubuntu を使う前提」と宣言し、
行69-71・79-88・101-109 では起動方法も Docker の入れ方も OS ごとに分けて書いている。
つまりこの教材は macOS 以外を支えると読者に約束している。
ところが Day 30 の Step 5 には macOS の分岐しか無い。
`xdg-open`、WSL の `explorer.exe`、あるいは「URL をブラウザのアドレス欄に貼る」という
最も単純な代替のどれも書かれていない。

**反証側が確かめた根拠**

- 行524-525を現物で確認した。コメントに「（macOS）」と明記されており、macOS 専用であることは
  教材自身が認めている。
- day30 全体を `xdg-open` / `explorer.exe` / `WSL` / `Ubuntu` で検索したが、1件も無い。
- day01:63 に「このカリキュラムは macOS を基準に説明します。Windows の場合は……WSL2 の Ubuntu を
  使う前提です。……Ubuntu 22.04 を直接使っている場合も、Linux 用と書かれた手順を選びます。」が実在する。
  他の日は OS ごとの分岐を実際に書いているので、Day 30 だけが約束を満たしていない。
- 初心者役が挙げた2件はどちらも `your-app-name` という仮の URL の紛らわしさを問うており、
  コマンドそのものが動かない環境を試していない。

## 反証で消えた件

2 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行296 1つ目のブロック（278〜289行）は `],`『}』『];』『},』まで閉じきっているので、「続けて足す」と言われても…

**逐語引用**

````text
続けて、通信と権限に関する3つを足します。

```typescript
// filepath: next.config.ts（headers の配列に追加）
          {
            key: 'Strict-Transport-Security',
````

**初心者役が挙げた詰まり**

1つ目のブロック（278〜289行）は `],`『}』『];』『},』まで閉じきっているので、「続けて足す」と言われても続きに貼ると閉じかっこの外側になり、`npm run build` が構文エラーで止まる。

**初心者役が挙げた不足**

最初のブロックが完結した形で提示されているのに、2つ目のブロックの挿入位置が「headers の配列に追加」としか書かれていない。`async headers()` と内側の `headers: [` の両方が『headers』と呼べるため、どちらの配列かも読み取れない。完成コード（906〜919行）を見ないと正しい入れ子が分からない。

**反証側が示した、成立しない根拠**

【引用の実在確認】material/30days-curriculum/day30_完成版を公開！.md の294行が「続けて、通信と権限に関する3つを足します。」、296-297行が「```typescript / // filepath: next.config.ts（headers の配列に追加）」、299行が「key: 'Strict-Transport-Security',」。指摘の引用は実在する（指摘は296行としているが、実際の文言行は294行、コードブロック開始が296行）。1つ目のブロックは275-290行で、286-289行が「], / }, / ]; / },」と閉じきっているのも事実。

【同一ファイル内で埋まっている】この「どの階層に貼るか」問題は、同じ day30 の「完成コード全体」節（870行〜）で名指しで手当てされている。

- 872行 逐語: 「だから完成状態を並べる対象は Step 2.5 の `next.config.ts` に絞られます。ヘッダーの入れ子が深く、どの階層へ貼るか迷いやすい場所なので、以下のコードで手元と見比べてください。」— 迷う箇所であることを教材が明示し、照合用の完成形を提示している。
- 880行 逐語: 「このファイルは丸ごと置き換えません。もとから書かれている設定はそのまま残し、`const nextConfig` の中へ以下の `headers` を足した状態が完成形です。」
- 887-897行に開始部分（`async headers()` → `return [` → `{` → `source` → `headers: [` → X-Frame-Options 等）、906-919行に続き（Strict-Transport-Security 以下 → `],` `},` `];` `},`）が、インデント込みで連続した1つの完成形として並べてある。906行のインデントが10スペースで、892行の `{ key: 'X-Frame-Options'...}` と同じ深さであることが、内側 `headers:` 配列の要素であることを示している。
- 899行 逐語: 「入れ子が3段になっている理由は…いちばん外の配列が組の一覧、その中の `source` が対象のURL、内側の `headers` が配る中身です。」— 「どちらの headers か」を明示的に切り分けている。
- 922行 逐語: 「閉じかっこが `]`、`}`、`]`、`}` の順に4段ぶん並ぶので、貼り付けたあとは `const nextConfig` の閉じかっこが1つ余っていないかを確かめてください。ここが合っていないと、`npm run build` は設定を読む前に構文エラーで止まります。」— 指摘が言う「build が構文エラーで止まる」失敗そのものを予告し、確認手順まで書いてある。

【本文だけでも判別可能】297行の「headers の配列に追加」が指す先は、282行の `headers: [` （配列）であって 278行の `async headers()` （関数）ではない。加えて 298-307行の要素は `{ key: ..., value: ... }` という形で、282-286行の内側配列にすでに並んでいる3件と完全に同じ形をしている。関数側の配列（279行 `return [`）の要素は `{ source, headers }` 形であり、形が一致しない。

以上より、必要な入れ子情報は同一ファイル内（870-922行）で、しかも「迷いやすい」と名指しした上で提示されている。読者がそこで止まり続けるとは言えない。

### 消えた件 2. 行525 自分のデプロイ URL が何なのか分からず、このコマンドをそのまま打って開けない。

**逐語引用**

```text
# filepath: ターミナル
# デプロイURLをブラウザで開く（macOS）
open https://your-app-name.vercel.app
```

**初心者役が挙げた詰まり**

自分のデプロイ URL が何なのか分からず、このコマンドをそのまま打って開けない。

**初心者役が挙げた不足**

Step 4 の確認ポイントに「デプロイ URL が発行された」とあるだけで、Vercel のどこ（Deployments の該当デプロイ、プロジェクト上部の Domains など）でその URL を読み取るかを示していない。

**反証側が示した、成立しない根拠**

1) 引用は現物にある。day30_完成版を公開！.md L522-526: 「```bash / # filepath: ターミナル / # デプロイURLをブラウザで開く（macOS） / open https://your-app-name.vercel.app / ```」。L530-531「**確認ポイント**: - ブラウザでデプロイ URL が開けた」。

2) しかし「自分のデプロイ URL が何か分からない」は前の日で埋まっている。day04_ネットに公開.md L505-514（Step 7「発行された URL を開いて、最初の公開を体験する」）:

- L505-506「デプロイ完了後、 / Vercel が Production URL を表示してくれます。」
- L509-511「```text / https://task-app-kouiso.vercel.app / ```」
- L512-513「実際の URL は人によって違います。 / でもこの形式に近いものが出たら、それが今日の成果物です。」
- L514「`.vercel.app` の手前にある部分は、Step 5 で決めた Project Name から作られます。」
さらに day04 L516-518「### まずはブラウザで開く / 表示された URL をクリックして、 / 自分のアプリを開きましょう。」、L636-638 では SNS 共有文に自分の URL を貼る手順まであり、読者は Day 04 の時点で自分の `*.vercel.app` URL を実際に開き、他人へ送っている。

3) day30 の同一ファイル内でも直前に読み取り場所が示されている。day30 L459-461「確認メモ: / Vercel ダッシュボードの「Deployments」タブで / 最新デプロイが `Ready` になっていればOKです。」— Deployments タブを見る導線が Step 4 の確認ポイント（L457「- デプロイ URL が発行された」）の直後に書かれている。

4) `your-app-name` がプレースホルダであることは day04 L512-514（「実際の URL は人によって違います」「Project Name から作られます」）で既に説明済み。よって読者がここで止まるとは言えない。
