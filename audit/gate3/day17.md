# Gate3 初心者シミュレーション — Day 17 (2026-07-28)

- 対象: material/30days-curriculum/day17_自分のタスクページ.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 3 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

3 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行693 「ファイル先頭が以下の状態になっていることを確認してください」に従って自分のファイルと表を突き合わせる。自分の1行目は 

**逐語引用**

```
| インポート元 | インポート内容 | 追加Step |
|-------------|---------------|---------|
| `react` | `useMemo`, `useState` | Step 1 |
```

**初心者役が挙げた詰まり**

「ファイル先頭が以下の状態になっていることを確認してください」に従って自分のファイルと表を突き合わせる。自分の1行目は Step 2 で置き換えた `import { useCallback, useMemo, useState } from 'react';` なので、表と食い違う。表に合わせて `useCallback` を消すと、Step 5 の `canEditProject` / `canDeleteProject` と Step 6 の `handleTimeLogSuccess` が未定義エラーになる。

**初心者役が挙げた不足**

Step 2 でインポートを `useCallback` 込みに置き換えさせているのに、中間確認の表がその置き換えを反映していない。表の Step 1 行が Step 1 時点のままになっている。

**反証側が示した、成立しない根拠**

【引用の実在】確認した。material/30days-curriculum/day17_自分のタスクページ.md:691-693 に
```
| インポート元 | インポート内容 | 追加Step |
|-------------|---------------|---------|
| `react` | `useMemo`, `useState` | Step 1 |
```
が実在する。指摘どおり、この表の Step 1 行は Step 2 の置き換えを反映していない（さらに `@/lib/constant/roles` 行そのものが表に無い）。

【同ファイル内で埋まっているか — 埋まっている】
1) L140（Step 2）: `import { useCallback, useMemo, useState } from 'react';` を含むブロックを「Step 1 のインポート部分を以下に**置き換えて**ください」（L134）と指示。
2) その直後 L152 に、なぜ表に無い名前がファイルに残るのかの説明が置かれている（逐語）:
「`useCallback` は Step 5 で権限判定の関数を作るときに使います。残る `hasPermission` と `isProjectMemberRole` は、Step 5 で「このプロジェクトでの自分の役割」を調べるための道具です。**まだ出番のない名前も並びますが、インポートを何度も書き足すより、先にそろえておくと差分を追いやすくなります。**」
→ 表に載っていない3つ（`useCallback` / `hasPermission` / `isProjectMemberRole`）は、いずれも「Step 5 で使うため先出しした」と本文が明示済み。読者が「なぜ余分があるのか」を調べる先は、まさに直前に自分が置き換えた Step 2 のこの段落である。

【指摘が想定する破綻手順は教材が指示していない】
中間確認の作業指示は L704-706 の確認ポイントで、逐語は
「- 上記のインポートがすべて揃っている
- `npm run dev` でインポートエラーが出ていない」
であり、**存在確認（すべて揃っている）であって「表に無いものを消せ」ではない**。教材のどこにも削除指示は無い。指摘が言う未定義エラー（L445 `const canEditProject = useCallback(` / L453 `const canDeleteProject = useCallback(` / L603 `useCallback(() => {`）は、読者が教材にない削除操作を自分で行った場合にのみ発生する。

【結論】表の記載が Step 2 の置き換えを反映しておらず不正確なのは事実だが、余分なインポートの理由は同ファイル L152 で説明済み、確認ポイント（L705）は存在確認のみで削除を促さないため、読者がそこで止まる経路は成立しない。

### 消えた件 2. 行818 この2行をどこへ書くのかが書かれていない。ファイルのトップレベルに置くと、Step 9 で「Step 8 の const

**逐語引用**

```
Step 9・10 でハンドラーを本実装しますが、先にJSXを書くために仮の関数を用意します。

const handleEdit = (_taskId: string) => {};
const handleDelete = (_taskId: string) => {};
```

**初心者役が挙げた詰まり**

この2行をどこへ書くのかが書かれていない。ファイルのトップレベルに置くと、Step 9 で「Step 8 の const handleEdit を削除して置き換えて」と言われたときに、置き換え先の本実装（`tasks?.find` を使う）がコンポーネント内の値を参照できず動かない。

**初心者役が挙げた不足**

他のブロックでは「`MyTasksPage` 内」「`canDeleteProject` の下」と置き場所を書いているのに、この仮実装だけ位置の指定が無い。

**反証側が示した、成立しない根拠**

1) 引用は現物に存在する。material/30days-curriculum/day17_自分のタスクページ.md:813「Step 9・10 でハンドラーを本実装しますが、先にJSXを書くために仮の関数を用意します。」/ 818「const handleEdit = (_taskId: string) => {};」/ 819「const handleDelete = (_taskId: string) => {};」。確かにこの仮実装ブロックには「MyTasksPage 内」等の位置指定が無い。

2) しかし指摘が主張する実害（「置き換え先の本実装が tasks?.find などコンポーネント内の値を参照できず動かない」）は、同じファイルの後段で潰されている。本実装の置き場所は仮実装の位置に依存せず、Step 9・10 が独立に明示している。
- 932行「`MyTasksPage` 内にstate・mutation・ハンドラーを追加します。」（Step 9 の実装ブロック群の直前）
- 936行「// 編集ダイアログの状態管理（early return より前のhook定義ブロックに追加）」
- 957行「Step 8 の `const handleEdit = (_taskId: string) => {}` を**削除して**、以下で**置き換えて**ください。」→ 962行以降の本実装は 932行の「MyTasksPage 内」指定の配下にある。
- 998行「`MyTasksPage` 内にstate・mutation・ハンドラーを追加します。」（Step 10）
- 1002行「// 削除ダイアログの状態管理（early return より前に追加）」
- 1024行「Step 8 の `const handleDelete = (_taskId: string) => {}` を**削除して**、以下で**置き換えて**ください。」
つまり読者が仮実装をトップレベルへ置いたとしても、本実装は「MyTasksPage 内 / early return より前」に書くと明記されているため、`tasks?.find` や `setEditingTask` を参照できない配置にはならない。

3) 仮実装そのものは、トップレベルに置いても壊れない。JSX（832〜880行の `onEdit={handleEdit}` / `onDelete={handleDelete}`）はコンポーネント内にあり、同一モジュールのトップレベル const を参照できる。824-825行の確認ポイント「`npm run dev` でTypeScript エラーが出ていない」も満たされる。したがって Step 8 の時点で読者が止まる事象は起きない。

4) 位置指定の一貫性が他ブロックより弱いのは事実（154行「`MyTasksPage` の `return` の**前に**」、596行「**Step 5 で書いた `canDeleteProject` の下**へ追加してください」、728行「`MyTasksPage` 内の `useQuery` の**下に**」）。ただしこれは記述の粒度の揺れであって、読者が詰まる欠落ではない。判定基準に従い survives: false。

### 消えた件 3. 行1082 Step 8 までに TaskGroupSection や0件メッセージを足した結果、JSX の中に `</div>` 

**逐語引用**

```
JSXの `</div>`（メインコンテンツの閉じタグ）の**下に** `TaskDialog` を配置します。
```

**初心者役が挙げた詰まり**

Step 8 までに TaskGroupSection や0件メッセージを足した結果、JSX の中に `</div>` が何本も並んでいる。「メインコンテンツの閉じタグ」がどれを指すのかが本文中で一度も定義されていないので、貼る行が決まらない。`</AppLayout>` の外に置くとページに出ない。

**初心者役が挙げた不足**

Step 1 で作った外枠 `<div className="flex flex-col gap-6">` を「メインコンテンツ」と呼ぶ、という対応づけがどこにも書かれていない。

**反証側が示した、成立しない根拠**

対象: /Users/kouiso/ghq/kouiso/task-app-worktrees/gate3/material/30days-curriculum/day17_自分のタスクページ.md

1) 引用の実在は確認した。1082行「JSXの `</div>`（メインコンテンツの閉じタグ）の**下に** `TaskDialog` を配置します。」。「メインコンテンツ」の語はファイル中この1回のみ（grep結果1件）で、指摘どおり語の定義そのものは無い。Step 1（109行 `<div className="flex flex-col gap-6">`、113行 `</div>`、114行 `</AppLayout>`）でこの外枠を「メインコンテンツ」と呼ぶ対応づけは書かれていない。

2) しかし同一ファイルの後半「## 完成コード全体」（1342行）が、この貼り位置を逐語で解消している。
- 1342行:「12個の Step で同じファイルへ書き足し続けたので、途中でどこへ貼ったか分からなくなった場合は、以下のコードと見比べてください。上から順に並べてあり、これが Day 17 終了時点の全文です。」→ 貼り位置が分からない読者を明示的にこの節へ誘導している。
- 1911〜1917行「**1件も無いときの案内**」の完成版コード末尾が
```
        )}
      </div>
```
（1916行 `      </div>` = Step 1 の外枠の閉じタグ、インデント6）で終わり、
- 1925〜1946行「**2つのダイアログ**」が同じインデント6で `<TaskDialog ... />` `<DeleteConfirmDialog ... />` と続き、1945行 `    </AppLayout>` / 1946行 `  );` で閉じる。つまり「どの `</div>` の下か」「`</AppLayout>` の内側か外側か」が完成コードのインデント付き全文で確定している。
- さらに1953行に散文でも明記:「2つとも、タスクを並べる `</div>` の外側に置いてあります。カードの並びの中に入れると、グリッドの1マスとして扱われて位置が崩れるためです。」

3) 指摘が懸念した「`</AppLayout>` の外に置くとページに出ない」も、1945行の `</AppLayout>` がダイアログの**後**に来ている完成コードで潰れている。

よって、語の定義は確かに無いが、同一ファイル内の完成コード全体（1342/1911-1946/1953行）で貼る行が一意に決まるため、読者はそこで止まらない。
