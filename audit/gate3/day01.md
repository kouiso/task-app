# Gate3 初心者シミュレーション — Day 01 (2026-07-28)

- 対象: material/30days-curriculum/day01_開発環境を整えて、初めてのアプリを動かそう.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 3 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

3 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行251 Step 1 の指示どおり ZIP を展開し、`ls` に `scripts` と `material` が並ぶ場所（＝…

**逐語引用**

```text
3. PostgreSQL を使えるか確認
4. 空ディレクトリなら `create-next-app` を実行
5. このカリキュラムで使う依存パッケージを追加
```

**初心者役が挙げた詰まり**

Step 1 の指示どおり ZIP を展開し、`ls` に `scripts` と `material` が並ぶ場所（＝空ではないフォルダ）で `bash scripts/scaffold-from-scratch.sh` を実行しようとする。だがスクリプトの説明が「空ディレクトリなら create-next-app を実行」なので、いま自分がいる場所では土台が作られないのではと迷い、実行してよいのか判断できずに止まる。

**初心者役が挙げた不足**

Step 1 の「期待する結果」は `scripts` と `material` が見えている状態を要求している（208行）のに、Step 2 のスクリプト説明はその逆の「空ディレクトリ」を条件として書いている。どちらが正しい前提なのか、教材のどこにも書かれていない。

**反証側が示した、成立しない根拠**

現物を確認した結果、引用は実在するが、指摘の「実行してよいのか判断できずに止まる」は同じファイルの直後で埋まっている。

1. 引用の実在（251行、逐語）: 「4. 空ディレクトリなら `create-next-app` を実行」。これは 247行「このスクリプトは、次の順番で仕事してくれます。」に続く 249-259行の9項目リストの4番目で、スクリプトが内部でやることの要約であり、読者への実行条件の提示ではない。

2. 実行場所は指定済み（265-269行、逐語）:
「**ターミナル（`~/workspace/task-app`）**

```bash
chmod +x scripts/scaffold-from-scratch.sh
bash scripts/scaffold-from-scratch.sh
```」
この cwd ラベル `~/workspace/task-app` は、Step 1 の期待する結果（206行「- `pwd` の結果が `/Users/あなたのユーザー名/workspace/task-app` のような形になっている」、208行「- `scripts` と `material` が見える配布物ルートにいる」）と同一の場所を名指ししている。つまり教材は「scripts と material が並んでいるその場所で、この2行を打て」と明示している。読者が「どこで実行するか」を判断する必要はない。

3. その場所で土台が作られると明言（273行、逐語）: 「この2行を実行すると、いまいるフォルダの中に `package.json` や `src` などが一気に作られ、データベース用のコンテナも起動します。」——「いまいるフォルダ」= 265行のラベルどおり配布物ルート。指摘が言う「いま自分がいる場所では土台が作られないのでは」という迷いは、この1文が直接否定している。

4. 期待される出力でも同じ場所が示される（281-285行、逐語）:
「**ターミナル出力（`~/workspace/task-app`）**
```text
教材用の初期土台を /Users/you/workspace/task-app に作成します。

Creating a new Next.js app in /Users/you/workspace/task-app.」
成功時のログとして、まさにその配布物ルートに作られる様子が示されている。

5. 実装側も整合（scripts/scaffold-from-scratch.sh 129-154行）: `ensure_empty_or_existing_next_app()` があり、135行「# create-next-app は空ディレクトリを要求するため、」の通り、非空ディレクトリでも別ディレクトリで生成してから配置する回避を実装している。既存 Next.js 土台がある場合は131行「既存の Next.js 土台があるため create-next-app はスキップします。」でスキップ。配布物ルート（非空）での実行は想定どおり動く。

したがって、251行の要約表現は厳密さを欠くものの、実行場所・実行コマンド・実行後に何が起きるか・成功時の出力が同一セクション内（265・273・281-285行）で連続して示されており、読者が実行を躊躇して止まる状態にはならない。
```

### 消えた件 2. 行271 スクリプトが途中で止まったとき、再実行してよいかを判断しようとする。ここでは「Day 10 以降」の再実行が危険と読める…

**逐語引用**

```text
このスクリプトを走らせてよいのは、この Day 01 の一度だけです。Day 10 以降にもう一度実行すると、`src/component/` に自分で書いたファイルが配布版で上書きされます。
```

**初心者役が挙げた詰まり**

スクリプトが途中で止まったとき、再実行してよいかを判断しようとする。ここでは「Day 10 以降」の再実行が危険と読めるが、322行では「Day 02 以降に自分で書いたコードは上書きされる」と書かれている。Day 03〜09 の途中で失敗した場合、再実行してよいのか分からず止まる。

**初心者役が挙げた不足**

同一 Day 内で、再実行が危険になる境目が「Day 10 以降」と「Day 02 以降」の2通りに書かれていて食い違っている。

**反証側が示した、成立しない根拠**

確認したファイル: material/30days-curriculum/day01_開発環境を整えて、初めてのアプリを動かそう.md

【271行（引用は実在を確認）】逐語:
「このスクリプトを走らせてよいのは、この Day 01 の一度だけです。Day 10 以降にもう一度実行すると、`src/component/` に自分で書いたファイルが配布版で上書きされます。初期データのプロジェクトと、その中に自分で作ったタスクやコメントも消えます。うまくいかないときは、先に付録のトラブルシューティングを読んでください。」

【322行（同一ファイル内・同一 Day 01 内、271行の51行後）】逐語:
「このログで見るべきなのは、最後の `初期セットアップは完了しました。` の行です。ここが出ていれば、データベースの準備まで含めて最後まで走りきっています。逆に `Docker で PostgreSQL を起動しています...` から先へ進まないときは、Docker 側で止まっています。Docker Desktop が起動しているか確かめてから、同じスクリプトをもう一度実行してください。Day 01 の時点なら、2回実行しても土台は壊れません。ただし Day 02 以降に自分で書いたコードは上書きされるので、このスクリプトは Day 01 でだけ使ってください。」

判定理由:

1. 指摘が「読者が止まる」と主張する場面は「スクリプトが途中で止まったときに再実行してよいか」である。この問いは322行が明示的に答えている。逐語「同じスクリプトをもう一度実行してください。Day 01 の時点なら、2回実行しても土台は壊れません。」— 途中失敗時の再実行は明文で許可されており、読者は止まらない。

2. 指摘が言う「Day 03〜09 の途中で失敗した場合」は、このスクリプトの実行場面として教材上存在しない。267-268行（逐語: 「chmod +x scripts/scaffold-from-scratch.sh」「bash scripts/scaffold-from-scratch.sh」）の通り、実行は Day 01 の1箇所のみで、271行・322行の双方が「Day 01 でだけ使う」という同一の運用規則を述べている。271行「走らせてよいのは、この Day 01 の一度だけです」／322行「このスクリプトは Day 01 でだけ使ってください」。読者が従うべき指示は両行で一致しており、行動が分岐しない。

3. 「Day 10 以降」（271行）と「Day 02 以降」（322行）の差は、いずれも「Day 01 以外では実行するな」という同じ結論を支える補足説明であり、読者の次の行動（=Day 01 のみで実行、途中失敗時は再実行可）を変えない。行動が変わらない以上、読者はここで止まらない。

以上より、指摘の「詰まり」は同一ファイル322行で埋まっている。

### 消えた件 3. 行263 `chmod +x` が拒否された、あるいは打ち忘れたときに、先へ進んでよいのか判断できない。

**逐語引用**

```text
そこで最初に `chmod +x` というコマンドで「このファイルは実行してよい」という印（実行権限）を付けます。
```

**初心者役が挙げた詰まり**

`chmod +x` が拒否された、あるいは打ち忘れたときに、先へ進んでよいのか判断できない。

**初心者役が挙げた不足**

273行では「ここでは `bash` にファイルを渡しているので、実行権限が付いていなくても動きます」と書かれており、263行で必須のように示した `chmod +x` が実は不要だと同じ節の中で否定されている。どちらに従えばよいかが示されていない。

**反証側が示した、成立しない根拠**

現物確認（material/30days-curriculum/day01_開発環境を整えて、初めてのアプリを動かそう.md）:

263行（引用は実在）:「スクリプト（`.sh` ファイル）は、そのままでは「実行してよいファイル」として扱われないことがあります。そこで最初に `chmod +x` というコマンドで「このファイルは実行してよい」という印（実行権限）を付けます。そのうえで `bash` コマンドにファイルを渡して実行します。`bash` は、スクリプトに書かれた命令を上から順番に処理してくれるプログラムです。」

267-268行（コマンド）:

```text
chmod +x scripts/scaffold-from-scratch.sh
bash scripts/scaffold-from-scratch.sh
```

273行（同じ節の直後、`Permission denied` の対処として明示）:「`Permission denied` と出たときは、ファイルを直接指定して実行していないか確かめてください。`./scripts/scaffold-from-scratch.sh` のようにファイル名だけで動かす書き方は、実行権限が付いていないと止まります。ここでは `bash` にファイルを渡しているので、実行権限が付いていなくても動きます。それでも出るときは、書き込みできない場所にいる可能性があるので、`pwd` で `~/workspace/task-app` にいるか確認してください。」

判定理由:

1. 263行は `chmod +x` を「必須」とは書いておらず、「扱われないことがあります」→ 印を付ける、という手順の説明である。否定関係ではない。
2. 指摘が問題視する「chmod が拒否された／打ち忘れた場合に先へ進んでよいか」は、273行がまさにその場面（`Permission denied` が出たとき）を名指しで扱い、「`./` 直接実行なら止まる／`bash` にファイルを渡す本教材の書き方なら実行権限なしでも動く」と切り分けたうえで、それでも出る場合の次の確認手順（`pwd` で作業ディレクトリ確認）まで示している。
3. よって読者が「先へ進んでよいか判断できない」状態にはならず、同一節（263行の10行後）で埋まっている。
