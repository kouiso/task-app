# Gate3 初心者シミュレーション — Day 22 (2026-07-28)

- 対象: material/30days-curriculum/day22_グラフを表示.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 3 件 / 反証を生き延びた件数: 1 件

## 生き延びた詰まり

### 1. 行392 Step 5 の確認用に、完成画面の画像を再掲している

**逐語引用**

```
スクリーンショット: ステータス別の円グラフが色分けされて表示されることを確認してください。

![左のステータス別円グラフ。右の優先度別は次の Step で追加する](./screenshots/report.png)
```

**何ができないか**

Step 5 まで写経した画面と見比べようとするが、画像には円グラフが2枚写っている。自分の画面には1枚しか無いので、
写経を間違えたのかと疑って先へ進めなくなる。

**教材のどこが足りないか**

画像の代替テキストは「右の優先度別は次の Step で追加する」と断っているが、貼られている画像は Step 9 の
完成画面と同じ `report.png` で、Step 5 時点の状態を写した画像ではない。可視の本文キャプションには
断りが無い。

**反証側が確かめた根拠**

- 行390から392を現物で確認した。
- `report.png` の参照は行15、行392、行520、行593の4箇所すべて同一ファイルで、Step 5 専用の画像は無い。
- 画像を実際に開き、2枚の円グラフが横並びのグリッドで描画された画面だと確認した。
  Step 8 で作るグリッド配置まで先取りしている。
- 緩和の記述は行392の代替テキスト内だけで、通常の表示では読者に見えない。しかもその断りは
  右の円グラフにしか触れておらず、グリッド配置の差は説明していない。
- Day 01 のスクリーンショットの見本の節にも「途中の画像は完成画面を流用する」という全体の約束は無い。
- 配布物は画面画像の食い違いを埋める性質のものではない。

## 反証で消えた件

2 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行536 2枚のカードを囲むグリッド用 div を「まず開始タグと閉じタグだけ追加」しようとするが、その2行をファイルのどこへ書け

**逐語引用**

````
**実装**: Step 4-5 と Step 7 で作った
2つの `<Card>` を、1つのグリッド用 `<div>` で囲みます。
まず、その外枠になる開始タグと閉じタグだけを追加します。

```tsx
// filepath: src/app/report/page.tsx
// グリッドの外枠（この2行の間に Card を移す）
<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
</div>
```
````

**初心者役が挙げた詰まり**

2枚のカードを囲むグリッド用 div を「まず開始タグと閉じタグだけ追加」しようとするが、その2行をファイルのどこへ書けばよいかが分からず、手が止まる。

**初心者役が挙げた不足**

Step 3〜7 では毎回「Day 21 の統計カードの `</div>` の下」「Step 4-5 の直後」のように貼り付け位置が示されていたのに、Step 8 の外枠 div だけ位置指定が無い。「Card を移す」とは書いてあるが、移す先の div 自体の置き場所（統計カードのグリッドの後ろ、プロジェクト統計テーブルの前）が本文中に一度も出てこない。

**反証側が示した、成立しない根拠**

現物: /Users/kouiso/ghq/kouiso/task-app-worktrees/gate3/material/30days-curriculum/day22_グラフを表示.md

1) 引用の実在確認（該当箇所）
- 533-538行:
```tsx
// filepath: src/app/report/page.tsx
// グリッドの外枠（この2行の間に Card を移す）
<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
</div>
```
指摘の引用は実在する（529-531行「**実装**: Step 4-5 と Step 7 で作った / 2つの `<Card>` を、1つのグリッド用 `<div>` で囲みます。/ まず、その外枠になる開始タグと閉じタグだけを追加します。」）。

2) 置き場所は同じ Step 8 内で確定している
- 540-542行: 「次に、Step 4-5 で書いたステータス円グラフを移します。/ その `<Card>` の開始行から `</Card>` の行までを / まるごと切り取り、上の開始タグと閉じタグの間に貼り付けます。」
- そのステータス Card の現在位置は Step 4 の303-305行で明示済み: 「> 以下のJSXは `return` 文の中、/ > Day 21 の統計カード `</div>` の下に / > 追加します。」
- Step 7 の454-456行: 「**実装**: Step 4-5 のステータスグラフの / 直後に、以下の Card を追加します。」
つまり移動対象の2枚が「統計カードの `</div>` の下」に連続して存在することは既に本文で確定しており、その2枚を囲む外枠を書く位置は同じ場所以外にない。

3) Step 8 の確認ポイントで位置が明記されている
- 556-558行: 「**確認ポイント**: / - PCでは横並び、モバイルでは縦並び / - Day 21 の統計カードの下に配置されている」

4) 同ファイルの完成コード節で、グリッド div の前後関係が逐語で示されている
- 826-828行（統計カードのグリッドの閉じ）:
```
            </CardContent>
          </Card>
        </div>
```
- 831行: 「時間の2枚は…末尾の `</div>` は、前のブロックで開いたグリッドを閉じるタグになります。ここを書き忘れると、このあとのグラフ2枚までグリッドの中へ入り、4列のうちの1マスに押し込まれた細いグラフが並びます。」
- 838-840行（統計カードのグリッド直後にグラフ用グリッドが来ることを明示）:
```
// 完成版: ステータス円グラフの枠
        <div className="grid grid-cols-1
          md:grid-cols-2 gap-6">
          <Card>
```
- 942行: 「最後の `</div>` でグラフ用のグリッドを閉じます。…閉じ忘れると、次のプロジェクト統計テーブルまでグリッドの中へ入り、グラフの隣に半分の幅の表が置かれます。」
→ 指摘が「本文中に一度も出てこない」とする「統計カードのグリッドの後ろ、プロジェクト統計テーブルの前」は、831行・838-840行・942行で逐語的に示されている。

結論: 指摘の言う不足は同一ファイル内（Step 4 の位置指定、Step 8 の確認ポイント、完成コード節の前後関係説明）で埋まっており、成立しない。

### 消えた件 2. 行305 Step 4 の Card を貼ろうとして、Day 21 のファイルを開き「統計カードの `</div>`」を探すが、統

**逐語引用**

```
> 以下のJSXは `return` 文の中、
> Day 21 の統計カード `</div>` の下に
> 追加します。
```

**初心者役が挙げた詰まり**

Step 4 の Card を貼ろうとして、Day 21 のファイルを開き「統計カードの `</div>`」を探すが、統計カードの周りには4枚のカードそれぞれの `</div>` とグリッドの `</div>` が並んでおり、どれの下なのか特定できない。

**初心者役が挙げた不足**

指し示している `</div>` が「4枚のカードを囲むグリッドの閉じタグ」であることが本文に書かれていない。Day 22 の完成コード（831行目付近）を読めば分かるが、Step 4 の時点では読者はまだそこまで到達していない。

**反証側が示した、成立しない根拠**

【引用の実在確認】day22_グラフを表示.md 305-307行に「> 以下のJSXは `return` 文の中、/ > Day 21 の統計カード `</div>` の下に / > 追加します。」が実在。Step 4「ステータス円グラフのCard枠を作る（5分）」(300行) の直下。

【指摘の前提が事実と違う】指摘は「4枚のカードそれぞれの `</div>` とグリッドの `</div>` が並んでおり特定できない」と言うが、統計カードは `</div>` を1つも持たない。day21_統計カードを表示.md の Step 7 実装ブロック(759-831行)を通読すると、4枚のカードはすべて `<Card><CardContent>...</CardContent></Card>` で構成され、`</div>` は 831行の1個だけ。同ファイル完成コード(1538-1588行)でも同様で、1588行の `        </div>` が唯一。つまり「統計カードの `</div>`」に該当する候補は1つしか存在せず、特定不能にならない。

【前の日で説明済み】day21 834行「最後の `</div>` は、このステップの先頭で開いたグリッドを閉じるタグです。閉じ忘れると開始タグと終了タグの対応が取れなくなり…」— 指摘が「本文に書かれていない」と言う「グリッドの閉じタグである」という説明は、前日 Day 21 に逐語で書かれている。
さらに day21 887-888行「次に、Step 7 のカードグリッドの `</div>` の / 直後にテーブルの JSX を追加します。」— 読者は Day 21 の時点で既に同じ `</div>` を目印にコードを追加する操作を1回経験済み。

【同ファイル内でも埋まっている】day22 830行「末尾の `</div>` は、前のブロックで開いたグリッドを閉じるタグになります。ここを書き忘れると、このあとのグラフ2枚までグリッドの中へ入り…」

以上より、読者が Step 4 で `</div>` を特定できずに止まる状況は成立しない。
