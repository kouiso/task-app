# Gate3 初心者シミュレーション — Day 02 (2026-07-28)

- 対象: material/30days-curriculum/day02_ダッシュボードに自分だけのメッセージを追加しよう.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 3 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 行275 の件は、最初の実行では判定が返らなかった

3 件のうち行275の件は、最初の実行では反証する側が通信の切断で途中終了し、判定が返らなかった。
判定の付かないものを「生き延びなかった」として数えていたため、いったんこの日の 0 件から外した。
その後、同じ手順で反証をやり直し、`survives: false` の判定と根拠を得ている（下の消えた件に収録）。
いまの 0 件は、3 件すべてに判定が付いた 0 である。

## 反証で消えた件

3 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行275 Step 1 の写経を終えて「ブラウザ確認」に来たが、どの URL を開けばよいかがこの節に書かれていない。直前に開いた…

**逐語引用**

```text
### ブラウザ確認

- 見出しが `Hello Task-App` から変わっている
- `こんにちは、Taroさん。` が主役として見える
```

**初心者役が挙げた詰まり**

Step 1 の写経を終えて「ブラウザ確認」に来たが、どの URL を開けばよいかがこの節に書かれていない。直前に開いた `http://localhost:3000`（91行）のままだと Day 01 のトップページが出るだけで、見出しは `Hello Task-App` ですらないため、成功したのか失敗したのかを判定できない。

**初心者役が挙げた不足**

編集したのは `src/app/dashboard/page.tsx` なのに、確認先が `/dashboard` であることが Step 1・Step 2 のどちらの確認節にも書かれていない。URL が明示されるのは Step 4（738行）の `http://localhost:3000/dashboard` が初出。

**反証側が示した、成立しない根拠**

引用は day02 の 275-278 行に実在する。しかし「どの URL か書かれていない」という主張は同一ファイルの Step 1 より前で埋まっている。91-92行「先に `http://localhost:3000` を開いて、`ダッシュボードへ入る` ボタンから `/dashboard` に移動できることも見ておきましょう。」で、読者は Step 1 に入る前に `/dashboard` へ移動済みの状態を作らされている。さらに 105行の見出し「### Day 01 直後の `src/app/dashboard/page.tsx`」と 113-125行のコードで、`Hello Task-App` が `/dashboard` の見出しであることが明示されている。day01 側でも 1019行「`http://localhost:3000/dashboard` を開いて、`Hello Task-App` の見出しが出るか確かめましょう。」で同じ対応が確立済み。

### 消えた件 2. 行743 281行の案内どおり `dashboardOwner.name` を自分の名前に書き換えた読者は、最終チェックポイントの…

**逐語引用**

```text
- メイン見出しのあいさつが、いま開発サーバーで見ている時刻に合っている
- `Taro` の名前が画面に出る
- `Focus:` のバッジが `bg-primary`（メインカラー）で表示されている
```

**初心者役が挙げた詰まり**

281行の案内どおり `dashboardOwner.name` を自分の名前に書き換えた読者は、最終チェックポイントの「`Taro` の名前が画面に出る」を満たせない。書き換えを戻すべきなのか、このチェック項目を無視してよいのかが分からず、Day 02 を完了と判断できない。

**初心者役が挙げた不足**

281行で「値を自分の名前に書き換えると、あいさつもその名前で表示されます」と書き換えを勧めておきながら、Day 02 の完了条件では `Taro` の表示を要求している。書き換えた場合の合格条件が示されていない。

**反証側が示した、成立しない根拠**

確認は material/30days-curriculum/day02_ダッシュボードに自分だけのメッセージを追加しよう.md の現物。

1) 引用は実在する。743行: 「- `Taro` の名前が画面に出る」。281行: 「> `Taro` はサンプルの名前です。`ownerName`（このあとの Step では `dashboardOwner.name`）の値を自分の名前に書き換えると、あいさつもその名前で表示されます。」

2) ただし同じファイル内で合格条件は既に埋まっている。

- 281行が `Taro` を明示的に「サンプルの名前です」と定義している。読者は 743行の `Taro` を「サンプル名（＝自分が入れた名前）」として読む土台をすでに与えられている。
- 743行のチェックポイント直前、738行が合格条件そのものを本文で述べている: 「ブラウザで `http://localhost:3000/dashboard` を開き、Day 01 の `Hello Task-App` ではなく自分の名前を含む見出しが出ていれば、今日書いたものは画面まで届いています。」——書き換えた読者の合格条件は「自分の名前を含む見出しが出る」と、チェックポイントの5行前に明記されている。
- 中間チェックポイントの481行も「- 見出しの中に自分の名前と集中テーマが入っている」と、書き換え後を前提にした表現になっている。

3) よって、書き換えた読者が「戻すべきか／無視してよいか分からず Day 02 を完了と判断できない」という詰まりは成立しない。738行が完了判定を与えており、281行が `Taro` をサンプルと定義している。743行の `Taro` は文言の緩さであって、読者を停止させる欠落ではない。

（参考：162行 `const ownerName = 'Taro';`、307/521/788/1011行の `'Taro',` はいずれもサンプル値としての初期コード。day01 には `Taro` の出現なし＝前日からの持ち越し前提もない。）

### 消えた件 3. 行33 Step 1 を写経したあと、自分の画面がこの「途中状態」の画像と合っているかを確かめたいが、画像は冒頭にまとめて置かれ…

**逐語引用**

```text
![Day 02 作業前のダッシュボード](./screenshots/day01/dashboard-hello.png)

![Day 02 メッセージカードを追加した途中状態](./screenshots/day02/step2-greeting-card.png)
```

**初心者役が挙げた詰まり**

Step 1 を写経したあと、自分の画面がこの「途中状態」の画像と合っているかを確かめたいが、画像は冒頭にまとめて置かれているだけで、Step 1 と Step 2 のどちらの完了時点の絵なのかが本文と対応づけられていない。

**初心者役が挙げた不足**

3枚の画像が Day 02 冒頭に連続で並び、それぞれがどの Step の直後の状態かを本文が示していない。Step 1・Step 2 の「ブラウザ確認」節からも参照されていない。

**反証側が示した、成立しない根拠**

【現物確認】day02 の 28〜33行目は指摘のとおり画像が冒頭に連続配置されている。逐語:
28: 「【スクリーンショット】Day 02 完成時のダッシュボード」
29: 「![Day 02 完成時のダッシュボード](./screenshots/day02/dashboard-message.png)」
31: 「![Day 02 作業前のダッシュボード](./screenshots/day01/dashboard-hello.png)」
33: 「![Day 02 メッセージカードを追加した途中状態](./screenshots/day02/step2-greeting-card.png)」
grep の結果、このファイル内で `screenshots` を含む行は29/31/33の3行のみで、Step 節からの再参照は無い。ここまでは指摘の事実関係は正しい。

【しかし「読者が止まる」は成立しない】画像が担う「自分の画面が合っているかの確認」機能は、同じファイルの各 Step の「ブラウザ確認」節が文章のチェックリストとして埋めている。

- Step 1（139行「## Step 1: 自分だけのメッセージを、まず1枚のカードにする」）に対応する275行「### ブラウザ確認」直下の逐語:
  「- 見出しが `Hello Task-App` から変わっている」
  「- `こんにちは、Taroさん。` が主役として見える」
  「- 右側に小さな補助カードが2枚並ぶ」
  さらに281行「> `Taro` はサンプルの名前です。`ownerName`（このあとの Step では `dashboardOwner.name`）の値を自分の名前に書き換えると、あいさつもその名前で表示されます。」と、画面文言のブレ要因まで明示している。
- Step 2 に対応する477行「### ブラウザ確認」直下の逐語:
  「**確認ポイント**:」
  「- 見出しが `おはよう` `こんにちは` `こんばんは` のどれかで始まっている」
  「- 見出しの中に自分の名前と集中テーマが入っている」
  「- カードの下段に `Owner` と `Focus` の2枚が並んでいる」
  「- ターミナルにもブラウザの開発者ツールにもエラーが出ていない」
- 仕上げの Step 4（「## Step 4: 保存して、ブラウザで「自分の画面」に変わったか確認する」）にも「### チェックポイント」として「- メイン見出しのあいさつが、いま開発サーバーで見ている時刻に合っている」「- `Taro` の名前が画面に出る」「- `Focus:` のバッジが `bg-primary`（メインカラー）で表示されている」「- 下段に `Owner` `Today` `Next` の3カードがある」がある。

また31行の画像は alt が「Day 02 作業前のダッシュボード」で、53行「## 前提（Day 01 完了していること）」の「- ダッシュボードに `Hello Task-App` と出る最初の画面がある」と対応が取れており、day01 の15行「![Day 01 の最後に作るダッシュボード画面](./screenshots/day01/dashboard-hello.png)」と同一ファイルなので、day01 を読んだ読者には既知の絵である。

したがって、写経後の自己確認は各 Step の文章チェックリストで完結でき、画像は補助にとどまる。読者がそこで作業不能になる根拠は無い。
