# Gate3 初心者シミュレーション — Day 04 (2026-07-28)

- 対象: material/30days-curriculum/day04_ネットに公開.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 2 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

2 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行531 Vercel が出した公開 URL（ルート）をクリックして開いたが、出てくるのは Day 01 のトップページで、Day

**逐語引用**

```
- ページがエラーにならず表示される
- Day 01 から積み上げた見た目が見える
- Day 02 で作った自分用メッセージが見える
- GitHub 上の最新状態とズレていない
```

**初心者役が挙げた詰まり**

Vercel が出した公開 URL（ルート）をクリックして開いたが、出てくるのは Day 01 のトップページで、Day 02 で書いた自分用メッセージがどこにも見えない。確認ポイントを満たせているのか判断できず、デプロイ失敗を疑って止まる。

**初心者役が挙げた不足**

Day 01・Day 02 の手順では自分用メッセージは `http://localhost:3000/dashboard` に置かれ、ルートは「ダッシュボードへ入る」ボタンのあるトップページになっている。Day 04 は Step 7 で「表示された URL をクリックして、自分のアプリを開きましょう」としか書かず、公開 URL の末尾に `/dashboard` を付けて開く手順がどこにも無い。

**反証側が示した、成立しない根拠**

【引用の実在確認】day04_ネットに公開.md 528-531行に指摘の引用がそのまま存在する。逐語: 528「- ページがエラーにならず表示される」/ 529「- Day 01 から積み上げた見た目が見える」/ 530「- Day 02 で作った自分用メッセージが見える」/ 531「- GitHub 上の最新状態とズレていない」。また 518-519行「表示された URL をクリックして、／自分のアプリを開きましょう。」も実在し、ここに `/dashboard` を付ける明示指示が無いのも事実。

【前の日で埋まっている】読者は day01 → day02 の順で読んでおり、「ルートのボタンから /dashboard へ入る」動作は前の日で機械的に確立済み。
- day01 1198行 逐語: 「- `ダッシュボードへ入る` を押すと `http://localhost:3000/dashboard` が開く」
- day01 1199行 逐語: 「- `/dashboard` で `Hello Task-App` が見える」
- day01 971行 逐語: 「Day 02 では、ここから入れる `/dashboard` に自分だけのメッセージや情報を足していく。」
- day02 91-92行 逐語: 「先に `http://localhost:3000` を開いて、」「`ダッシュボードへ入る` ボタンから `/dashboard` に移動できることも見ておきましょう。」
つまり読者は Day 02 の時点で毎回「トップ → ボタン → /dashboard でメッセージ」という導線を自分の手で通している。公開 URL でも同じ画面構成である旨は day04 524行が逐語で明言している:「さっきまで `localhost` という自分のパソコンの中だけの画面だったものが、いまは公開された URL に置かれています。（中略）見た目は同じでも、届く範囲がまるで変わっています。」見た目が同じなら、ローカルで毎日踏んでいたボタン導線がそのまま使えると分かる。

【同ファイル内でも補強されている】day04 540行 逐語: 「上のキャプチャは予行演習（ローカル本番モード）で `/dashboard` を開いた例です。だからキャプションも「公開 URL の画面」ではなく「ローカル本番モードの画面」と書いてあります。」— 確認ポイント直下のスクリーンショット説明が、メッセージ画面は `/dashboard` であると明示している。さらに day04 158行 逐語: 「- `src/app/dashboard/page.tsx` が存在する」で、ダッシュボードが別パスのページであることを当日中にも再確認させている。

【結論】ルート直下にメッセージが無いこと、およびメッセージが `/dashboard` にあることは day01・day02 の実操作と day04 540行で読者に届いている。トップページのボタンは公開 URL でもそのまま表示されるため、読者は Day 02 と同じ操作でメッセージへ到達でき、「デプロイ失敗を疑って止まる」には至らない。指摘は成立しない。

### 消えた件 2. 行536 Step 6 で Deploy を押し終えた読者が、Step 7 で「デプロイの前に」と書かれた作業を指示されて、いま戻

**逐語引用**

```
デプロイの前に手元で `npm run build` を実行してから `npm start` で立ち上げると、ビルド済みのアプリを本番と同じモードで動かせます。
```

**初心者役が挙げた詰まり**

Step 6 で Deploy を押し終えた読者が、Step 7 で「デプロイの前に」と書かれた作業を指示されて、いま戻ってやるべきなのか、次回のための説明なのか判断できずに手が止まる。加えて `npm start` はここが初出で、実行してよいのか分からない。

**初心者役が挙げた不足**

この段落は Step 2（公開前のローカル確認）に属する内容だが、デプロイ完了後の Step 7 に置かれている。`npm start` は Day 01〜Day 03 でも Day 04 の Step 2 でも一度も出ておらず、`npm run dev` との違いは書かれているものの、いつ実行するのかが指示されていない。

**反証側が示した、成立しない根拠**

現物を開いて確認した結果、指摘は成立しない。

【引用の実在確認】
day04_ネットに公開.md:536 逐語:「デプロイの前に手元で `npm run build` を実行してから `npm start` で立ち上げると、ビルド済みのアプリを本番と同じモードで動かせます。`npm run dev` が開発中の確認用なのに対して、`npm start` は「ビルドが通るか、ビルド後も動くか」を先に確かめる予行演習です。ここで動けば、Vercel 上のビルドエラーの多くを事前に潰せます。」
引用は確かに536行に存在する。

【反証1: この段落は「作業指示」ではなく直前スクリーンショットのキャプション解説である】
533行 逐語:「【スクリーンショット】ローカル本番モードで開いたダッシュボード（デプロイ前の予行演習の例）」
534行 逐語:「![ローカル本番モードで開いたダッシュボード画面](./screenshots/day04/public-url.png)」
540行 逐語:「上のキャプチャは予行演習（ローカル本番モード）で `/dashboard` を開いた例です。だからキャプションも「公開 URL の画面」ではなく「ローカル本番モードの画面」と書いてあります。」
536行は533/534のキャプチャがなぜ「公開URLの画面」でないかを説明する文脈に挟まれており、540行がその説明を明示的に閉じている。文体も「〜と…動かせます」という叙述で、「実行してください」という命令形ではない。読者に今やらせる手順として置かれていない。

【反証2: 読者の次の一手が同じ位置で明示されている（手が止まらない）】
538行 逐語:「ただし、これで確認できるのはビルドが通ることまでです。Vercel 側の設定・環境変数・外から届くかどうかは、ローカルでは何も証明されません。今日のゴールは「ネットに公開」なので、完了の判定は必ず実際の公開 URL で行います。とくに、自分のパソコンと同じ Wi-Fi ではない回線（スマホのモバイル回線など）から URL を開けたら、本当に公開できた証拠になります。次の Step 8 でそれを確かめます。」
「次の Step 8 でそれを確かめます」と直後の行動が名指しされている。「いま戻ってやるべきか判断できずに手が止まる」という詰まりの前提が本文で潰れている。

【反証3: `npm run build` の実行はすでに Step 2 で済んでおり、536行が要求する「戻り作業」は存在しない】
163行 逐語:「## Step 2: 公開前にローカルで `npm run build` を通す」
173-175行 逐語（コードブロック）:「```bash / npm run build / ```」
38行（今日のチェックリスト）逐語:「- [ ] ローカルで `npm run build` を実行し、公開前の確認をする」
27行（今日のゴール）逐語:「- 公開前に `npm run build` でローカル確認する意味が分かる」
866行（最後のチェックリスト）逐語:「- [ ] `npm run build` が通った」
必須作業として要求されているのは `npm run build` だけで、それは Step 2 で完了済み。536行が新しい必須作業を追加しているわけではない。

【`npm start` 初出の点について】
grep（day01〜day04 全文、`npm start` 検索）の結果、`npm start` の出現は day04:536 の1件のみで、初出であることは事実。ただし同じ536行内で「`npm run dev` が開発中の確認用なのに対して、`npm start` は「ビルドが通るか、ビルド後も動くか」を先に確かめる予行演習です」と役割が説明されており、かつ 38行/866行のチェックリストのどちらにも `npm start` は入っていない。実行が必須項目として課されていない以上、「実行してよいのか分からず止まる」は成立しない（実行しなくてもチェックリストは全て埋まる）。
