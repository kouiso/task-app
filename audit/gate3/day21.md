# Gate3 初心者シミュレーション — Day 21 (2026-07-28)

- 対象: material/30days-curriculum/day21_統計カードを表示.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 3 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

3 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行1097 途中で分からなくなり「完成コード全体」で立て直そうとするが、Step 2 で直したサイドバー（`app-layout.t…

**逐語引用**

```text
今日は3つのファイルを触りました。断片を貼り重ねる作業が続いたので、途中でどこへ貼ったか分からなくなった場合は、以下のコードを上から順に貼り付けて、各ファイルを置き換えてください。
```

**初心者役が挙げた詰まり**

途中で分からなくなり「完成コード全体」で立て直そうとするが、Step 2 で直したサイドバー（`app-layout.tsx`）の全文が無く、`BarChart` とメニュー項目を正しく足せたか確かめられない。

**初心者役が挙げた不足**

今日編集するのは4ファイル。完成コードの表（1099〜1103行）は report.ts / root.ts / report/page.tsx の3つしか挙げておらず、Step 2（468〜494行）で編集させた `src/component/layout/app-layout.tsx` が抜けている。

**反証側が示した、成立しない根拠**

【引用の実在確認】

- 1097行（実物）: 「今日は3つのファイルを触りました。断片を貼り重ねる作業が続いたので、途中でどこへ貼ったか分からなくなった場合は、以下のコードを上から順に貼り付けて、各ファイルを置き換えてください。」→ 引用どおり存在。
- 完成コード全体の表（1099〜1103行）: 「| `src/server/api/routers/report.ts` | 集計済みの数値を返す `getOverview` | Step 0 |」「| `src/server/api/root.ts` | 手続きの一覧表への `report` の登録 | Step 0 |」「| `src/app/report/page.tsx` | 統計カードとプロジェクト統計の画面 | Step 2〜Step 8 |」の3行のみ。
- 見出し一覧（grep）でも完成コード配下は 1105行 `### \`src/server/api/routers/report.ts\``、1409行 `### \`src/server/api/root.ts\``、1450行 `### \`src/app/report/page.tsx\`` の3つだけで、`app-layout.tsx` の全文節は無い（grep 結果: 同ファイル内の app-layout 出現は 465 / 512 / 1460 行のみ）。よって「4ファイル触るのに完成コードは3ファイル」という事実指摘そのものは正しい。

【それでも読者は止まらない理由（現物の行）】

1. Step 2 の app-layout 編集は「全文を貼り直す」種類の作業ではなく、貼る場所が本文で一意に特定されている。
   - 465〜467行: 「`src/component/layout/app-layout.tsx` の / `lucide-react` の import に `BarChart` を追加してください。」直後 470〜478行に import 文7語の完全形（`BarChart, ClipboardList, FolderOpen, LayoutDashboard, ListTodo, LogOut, Search`）が貼られており、これは当該 import ブロックの**全文**である。
   - 480行: 「足したのは `BarChart` の1語だけです。……他の6つは Day 08 までに並べたものなので、消さずにそのまま残してください。」
   - 484〜486行: 「同じファイルの `menuItems` に、レポート用の項目を追加します。Day 20 までの項目を残し、配列の閉じかっこ直前へ入れます。」直後 488〜493行に追加要素4行の全文（`text: 'レポート'` / `icon: <BarChart className="h-5 w-5" />` / `path: '/report'`）。
   つまり app-layout に対する今日の変更は「import 1語」と「配列末尾に4行」の2箇所だけで、その両方が全文で示されている。1097行の想定する「どこへ貼ったか分からなくなる」状態が起きるのは断片を積み重ねた report.ts / page.tsx 側であり、app-layout の2箇所は貼り位置が本文で確定している。

2. 「正しく足せたか確かめられない」という詰まりは、Step 2 自身の確認手段で閉じている。
   - 497〜499行: 「**確認ポイント**: / - サイドバーに「レポート」が表示された / - 既存のメニュー項目が消えていない」
   全文コードを突き合わせなくても、画面を見れば BarChart アイコン付き「レポート」項目の追加成否と既存項目の破壊有無が両方判定できる。さらに 496行に「ここに書いた `/report` は、このあと作るファイルの場所と同じ綴りでなければいけません。ずれているとメニューは出るのに、押した先が404になります。」と失敗時の症状まで書かれており、誤りの切り分け手段がある。

結論: 表の記載漏れ（および「3つのファイル」という文言）は事実として不正確だが、読者が実際に手を止める箇所ではない。app-layout の変更は全文提示済み・貼り位置確定済み・画面での合否判定可能であり、詰まりは成立しない。

### 消えた件 2. 行39 「今日は page.tsx だけ触ればいい」と思って読み始めるが、Step 0 でいきなり `report.ts` と …

**逐語引用**

```text
| 対象ファイル | `src/app/report/page.tsx` |
| 今日作る範囲 | 統計カード4枚 + プロジェクト統計テーブル |
```

**初心者役が挙げた詰まり**

「今日は page.tsx だけ触ればいい」と思って読み始めるが、Step 0 でいきなり `report.ts` と `root.ts` の新規作成・編集が始まり、どこまでが今日の作業か分からなくなる。

**初心者役が挙げた不足**

スコープ表の「対象ファイル」が1つしか挙げていない。実際は Step 0 で `src/server/api/routers/report.ts`（新規）と `src/server/api/root.ts`、Step 2 で `src/component/layout/app-layout.tsx` も触る。

**反証側が示した、成立しない根拠**

対象: material/30days-curriculum/day21_統計カードを表示.md

【引用の実在確認】
行37-41（スコープ表）逐語:
| 区分 | 内容 |
|------|------|
| 対象ファイル | `src/app/report/page.tsx` |
| 今日作る範囲 | 統計カード4枚 + プロジェクト統計テーブル |
| 実コードとの違い | 実コードにはグラフ（Day 22）や週次リンク（Day 23）もあるが今日は扱わない |

→ 指摘の引用は行39に実在し、「対象ファイル」が page.tsx 1つだけなのも事実。

【同一ファイル内で埋まっている箇所（＝指摘が成立しない根拠）】

1) 行84-88「## 実装ステップ一覧」逐語:
| ステップ | 作業内容 | 所要時間 |
|---------|---------|---------|
| Step 0 | レポート集計 API（getOverview）を自分で書く | 14分 |
→ スコープ表の45行下、かつ一切のコードより前に、今日の1手目が「API を自分で書く」ことだと宣言されている。読者が「page.tsx だけ触ればいい」と思ったまま Step 0 に突入する経路がない。

2) 行107（Step 0 のゴール行）逐語:
**ゴール**: `src/server/api/routers/report.ts` を新規作成し、`getOverview` を写経して `api.report.getOverview` を自分で生やします。
行113 逐語: まず `src/server/api/routers/report.ts` を新規作成し、先頭に import を書きます。
→ report.ts の新規作成はステップ冒頭のゴール文で予告されており、「いきなり始まる」状態ではない。

3) 行373-375 逐語:

#### 0-6. root.ts に時系列順で登録する
ルーターを書いただけでは、まだ `api.report.getOverview` とは呼べません。`src/server/api/root.ts` に追加…
→ root.ts も見出し＋理由付きで予告されている。

4) 行1097-1103「## 完成コード全体」逐語:
今日は3つのファイルを触りました。…
| ファイル | 役割 | 対応する Step |
| `src/server/api/routers/report.ts` | 集計済みの数値を返す `getOverview` | Step 0 |
| `src/server/api/root.ts` | 手続きの一覧表への `report` の登録 | Step 0 |
| `src/app/report/page.tsx` | 統計カードとプロジェクト統計の画面 | Step 2〜Step 8 |
→ 触ったファイルとStepの対応表が本文末尾にある。

【結論】
スコープ表の「対象ファイル」が1つしか挙げていないのは事実だが、その直後の実装ステップ一覧（行88）と Step 0 のゴール文（行107）が、コードに入る前に「今日は集計APIも自分で書く」ことを明示している。読者がそこで作業範囲を見失って止まる経路が本文中に存在しないため、指摘は成立しない。

### 消えた件 3. 行19 今日の完成形を画像で確かめようとするが、本文が言う「プロジェクト統計テーブル」が画像に写っておらず、代わりに今日は作らな…

**逐語引用**

```text
完成イメージ: 4枚の統計カードとプロジェクト統計テーブルが並んだレポートページです。

![4枚の統計カードと2つの円グラフが並んだレポートページ](./screenshots/report.png)
```

**初心者役が挙げた詰まり**

今日の完成形を画像で確かめようとするが、本文が言う「プロジェクト統計テーブル」が画像に写っておらず、代わりに今日は作らない円グラフが写っている。自分の画面が正しいのか判断できない。

**初心者役が挙げた不足**

本文の説明と画像の中身が一致していない。Step 3（622〜626行）や Step 7（840〜842行）では「下の画像は今日の終わりの姿」「下の円グラフは Day 22 で作る」と断りがあるのに、最初の完成イメージには断りが無い。今日作る Step 8 のテーブルを写した画像はどこにも無い。

**反証側が示した、成立しない根拠**

確認したファイル: material/30days-curriculum/day21_統計カードを表示.md

1) 引用は実在する。19〜21行:
19: 「完成イメージ: 4枚の統計カードとプロジェクト統計テーブルが並んだレポートページです。」
20: (空行)
21: 「![4枚の統計カードと2つの円グラフが並んだレポートページ](./screenshots/report.png)」
本文（テーブル）と alt（円グラフ）が食い違っているのは事実。

2) ただし同じ「今日のゴール／なぜこれを作るのか」冒頭ブロック内、20行下の「### 今日のスコープ」表で断りが済んでいる。
35行: 「### 今日のスコープ」
39行: 「| 対象ファイル | `src/app/report/page.tsx` |」
40行: 「| 今日作る範囲 | 統計カード4枚 + プロジェクト統計テーブル |」
41行: 「| 実コードとの違い | 実コードにはグラフ（Day 22）や週次リンク（Day 23）もあるが今日は扱わない |」
→ 「画像に写っている円グラフは Day 22 のもので今日は扱わない」という、指摘が「無い」と言っている断りが、完成イメージ画像の直後の同じ導入セクションに明記されている。

3) 19〜21行は着手前の位置であり、この時点で読者はまだ1行も書いていない（実装の最初の手は Step 1 以降）。よって「自分の画面が正しいか判断できない」という詰まりは、比較対象の画面がまだ存在しないため発生しない。実際に自分の画面と突き合わせる確認地点には断りが入っている:
626行: 「![完成後のレポートページ。上に見出し、下にカードと円グラフが並ぶ](./screenshots/report.png)」および直前の「下の画像は今日の終わりの姿なので、いまは上の2行だけが見えていれば正しい状態です。」
842行: 「![4枚の統計カードが横に並んだ状態。下の円グラフは Day 22 で作る](./screenshots/report.png)」

4) screenshots/report.png は完成版アプリの共有画像で、day22（15/392/520/593行）、day23（19/473行）、appendix でも同一ファイルが使い回されている（grep 済み）。「今日作る Step 8 のテーブルだけを写した画像が無い」のは教材全体の作りであり、41行の「実コードとの違い」でその差分が読者に伝えられている。

以上より、指摘が言う不足は同一ファイルの41行で埋まっており、読者がそこで止まる根拠はない。
