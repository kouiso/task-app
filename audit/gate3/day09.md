# Gate3 初心者シミュレーション — Day 09 (2026-07-28)

- 対象: material/30days-curriculum/day09_プロジェクト一覧画面.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 3 件 / 反証を生き延びた件数: 1 件

## 生き延びた詰まり

### 1. 行611 「書き換え」と「前後に追加」が同じ場所で食い違い、消す範囲も書かれていない

**逐語引用**

```text
Step 6 で追加したグリッドの `<div>` 内を修正します。`{projects?.map(...)}` の部分を三項演算子に書き換えます。

Step 6 のmap部分を三項演算子で囲みます。`projects.map((project) => { ... })` の**前後**にコードを追加します。
```

**何ができないか**

すでに書いた Step 6 のコードのどこを消して、示されたブロックのどこから貼るのかを決められない。
示されたコードは既存の行とほぼ同じ内容を含み、しかも `name={project.name}` の途中で切れているので、
置き換えなのか追記なのか判断できずに手が止まる。

**教材のどこが足りないか**

行611は「書き換えます」、行613は「前後にコードを追加します」で、隣り合う2文の操作が逆になっている。
さらに提示ブロックには既に書いてある `const taskCount` から `<ProjectCard>` までが再掲されており、
追記すると行が二重になる。Step 6 のどこを削除するかの指示が Step 7 の範囲に無い。

**反証側が確かめた根拠**

- 行611、行613、行637を現物で確認した。
- Step 6 の既存コード（行535から559）と Step 7 の1つ目のブロック（行615から631）を照合し、
  map、taskCount、doneCount、return、ProjectCard が重複することを確認した。
- 行611から675の範囲に、既存行を削除せよという指示は無い。唯一の解きほぐしは行659だが、
  これは1つ目と2つ目のブロックを貼った後に出てくるので、手を動かす時点では判断材料にならない。
- 同シリーズには削除範囲を明示する書式が実在する（Day 05 の行460、Day 08 の行588）。
  Day 09 の Step 7 だけ、その明示が欠けている。
- 後続の行744は Step 9 の話で、Step 7 の確認ポイント（行677から679）を満たす前段では使えない。
- 配布済みの足場に関する記述も、この範囲には無い。

## 反証で消えた件

2 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行913 Step 1〜9 のあいだ 3000 番で開いていた画面があるのに、最後だけ 3001 で起動しろと言われる。既存のサー…

**逐語引用**

````text
```bash
# filepath: ターミナル
# 開発サーバーを起動
PORT=3001 npm run dev
```

**確認ポイント**:
- `http://localhost:3001` にアクセスできる
````

**初心者役が挙げた詰まり**

Step 1〜9 のあいだ 3000 番で開いていた画面があるのに、最後だけ 3001 で起動しろと言われる。既存のサーバーを止めるのか、2つ目を立てるのか書かれておらず、ポートが埋まっているというエラーの意味も分からない。

**初心者役が挙げた不足**

Day 07・Day 08 および本日の Step 1〜3 の確認はすべて `npm run dev`（3000番）で行っており、ここだけ `PORT=3001` に変わる理由も、それまでのサーバーを止める指示も無い。動作確認の期待値（`http://localhost:3001`）も他の日と食い違う。

**反証側が示した、成立しない根拠**

【引用の実在確認】day09_プロジェクト一覧画面.md:910-917 に確かに存在。910 "```bash" / 911 "# filepath: ターミナル" / 912 "# 開発サーバーを起動" / 913 "PORT=3001 npm run dev" / 914 "```" / 916 "**確認ポイント**:" / 917 "- `http://localhost:3001` にアクセスできる"。day09 内で `PORT=` の出現は 913 の1回のみ（grep 結果: 913:PORT=3001 npm run dev）で、理由の説明文は day09 内には無い（理由文は day11:1032「`PORT=3001` を付けるのは、3000 番を別の作業で開いたままでも確認を始められるようにするため」＝後日）。

【前の日で埋まっている点】day01_開発環境を整えて、初めてのアプリを動かそう.md:380 に「`Port 3000 is in use` と出たときは、3000 番の入口を別のプログラムが先に使っています。この場合 Next.js は自動で 3001 などの空いている番号に移って起動するので、そのまま進めて大丈夫です。」— 指摘が言う「ポートが埋まっているというエラーの意味も分からない」は day01 で明示的に解説済み。
同ファイル 400 に「`Local:` の番号が `http://localhost:3001` のように 3000 以外になることもあります。…ブラウザで開くのは、そのときターミナルに出ている `Local:` の URL です。この先の説明では `http://localhost:3000` と書きますが、番号が違っていたら自分の画面に出ている番号に読み替えてください。」— 3000 以外の番号で確認することが正常であると事前に宣言済みで、day09:917 の `http://localhost:3001` はその読み替え規約の範囲内。
同ファイル 402 に「このコマンドは『開発サーバー』を動かし続けるので…このターミナルは閉じずにそのままにしておきましょう。止めたいときは、このターミナルで `control + C` を押します。別のコマンドを打ちたくなったら、新しいターミナルをもう1つ開いて使います。」— 「既存のサーバーを止めるのか、2つ目を立てるのか」に必要な操作（control + C、新しいターミナルを開く）は day01 で両方与えられている。

【実行上ブロックされない】day09:913 の `PORT=3001 npm run dev` は、3000 番で既存サーバーが動いていても空いていても 3001 で起動する。したがって読者が既存サーバーを止めていなくてもエラーにならず、確認ポイント 917 の `http://localhost:3001` と実際の起動先が一致する。読者が手を止める地点が発生しない。

結論: 「なぜ 3001 か」の一文が day09 に無いのは事実だが、指摘が挙げた詰まり要素（ポート占有エラーの意味／番号が変わることの正常性／サーバーの停止・別ターミナルの立て方）はいずれも day01:380・400・402 で既出であり、コマンド自体も失敗しない。survives: false。

### 消えた件 2. 行59 `project-card.tsx` を自分で作るのかどうか判断できない。作る手順を探して本文を往復し、見つからないまま…

**逐語引用**

```text
├── component/
│   ├── project/
│   │   └── project-card.tsx  ← 今日の表示で使うカード
│   ├── layout/
│   │   └── app-layout.tsx    ← 既存（利用する）
```

**初心者役が挙げた詰まり**

`project-card.tsx` を自分で作るのかどうか判断できない。作る手順を探して本文を往復し、見つからないまま Step 4 の import を書いてモジュールが見つからないエラーが出たときに詰まる。

**初心者役が挙げた不足**

同じ図の中で `app-layout.tsx` と `loading-spinner.tsx` には「既存（利用する）」と明記されているのに、`project-card.tsx` だけ「今日の表示で使うカード」とだけ書かれ、既存なのか新規なのかのラベルが無い。Step 4 の「今日はこれを自分で書きません」も、いつ誰が用意したファイルなのかを述べていない（30日教材のどの日にも作成手順は無い）。

**反証側が示した、成立しない根拠**

【引用の実在】day09 の 58-64 行目に指摘どおりの図がある。60行目「│   │   └── project-card.tsx  ← 今日の表示で使うカード」、62行目「│   │   └── app-layout.tsx    ← 既存（利用する）」、64行目「│       └── loading-spinner.tsx ← 既存（利用する）」。ラベルの非対称は事実。

【同じファイル内で埋まっている】(1) 49行目のやること/やらないこと表に「| ProjectCard コンポーネントの表示 | カードのデザインをゼロから作る |」とあり、カードを作るのが今日の対象外だと図の直前で示している。(2) Step 4 の 466行目に「`ProjectCard` は、色帯・メンバー数・進捗バーまで作り込んであるカード部品です。今日はこれを自分で書きません。Step 0 の `getAll` が返してきたデータを流し込むことに集中します。」とあり、import を書く当のステップで「自分で書くのかどうか」に直接答えている。読者が import を書く瞬間に判断材料が同じ画面にある。

【前提となる「モジュールが見つからないエラー」は起きない】project-card.tsx は Day 01 の配布スクリプトで実配置される。scripts/scaffold-from-scratch.sh 517-534行目の copy_app_components():
「  # カリキュラムで「既存」と明示されている実装済みコンポーネントを配置する。」
「  # 学習者は自分では作らず、既存ファイルとして読み取り・利用する。」
「  # project コンポーネント（Day 09/10/11 で既存扱い）」
「    mkdir -p src/component/project」
「    cp "${comp_src}/project"/*.tsx src/component/project/」
実体も存在する（scripts/_app-components/project/project-card.tsx, 3.3K）。scaffold-from-scratch.sh 139行目のコピー対象リストにも `_app-components` が含まれる。

【この足場が読者へ渡ることが本文に書かれている】day01 22-23行目「- [ ] 配布 ZIP を展開して、`scripts` と `material` が見える場所を `task-app` の作業場所にする」「- [ ] `scripts/scaffold-from-scratch.sh` を実行して、土台を一発で作る」、244行目で同スクリプトの説明、267-268行目で実行、271行目「Day 10 以降にもう一度実行すると、`src/component/` に自分で書いたファイルが配布版で上書きされます。」＝ src/component 配下に配布版が入ることが本文に明記されている。したがって Day 09 の Step 4 で import を書いた時点でファイルは実在し、指摘が想定する「モジュールが見つからないエラー」は発生しない。

【残る差分は詰まりに至らない】図のラベルに「既存」の3文字が無いのは事実だが、(a) 49行目の対象外表、(b) 466行目の「今日はこれを自分で書きません」、(c) 実ファイルが存在してエラーが出ない、の3点により、読者がそこで停止する経路が成立しない。
