# Gate3 初心者シミュレーション — Day 05 (2026-07-28)

- 対象: material/30days-curriculum/day05_ログイン画面のUI.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 2 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

2 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行451 Step 6 で `Card` を貼る先を探すが、手元のファイルに `<div className="w-full ma

**逐語引用**

```
Step 1 で書いた外側の `<div>` はそのまま残し、その内側にあった `<div className="w-full max-w-sm">` を `Card` に入れ替えた形です。
```

**初心者役が挙げた詰まり**

Step 6 で `Card` を貼る先を探すが、手元のファイルに `<div className="w-full max-w-sm">` はもう存在しない。Step 4 で消させられているため、どこを消してどこに貼るのかが決まらず止まる。

**初心者役が挙げた不足**

Step 4 の 298 行で「その内側にある `<div className="w-full max-w-sm">` と `<h1>ログイン</h1>` を消して、代わりに次の `<form>` を丸ごと置きます」と既に削除させている。さらに、いま消すべき `<form>` の削除指示は 460 行「Step 4 と Step 5 で書いた `<form>` から `</form>` までは、先にすべて削除してください。」とコードブロックより後に出るため、貼り付け順も逆になっている。

**反証側が示した、成立しない根拠**

確認したのは /Users/kouiso/ghq/kouiso/task-app-worktrees/gate3/material/30days-curriculum/day05_ログイン画面のUI.md。

1) 引用の実在確認（成立）
451行: 「Step 1 で書いた外側の `<div>` はそのまま残し、その内側にあった `<div className="w-full max-w-sm">` を `Card` に入れ替えた形です。中央寄せの役目は外側の `<div>` が引き続き持ち、`Card` は枠線と影と余白だけを足します。」
298行（Step 4）: 「`LoginForm` の `return (` の中身を書き換えます。Step 1 で書いた外側の `<div>` は残し、その内側にある `<div className="w-full max-w-sm">` と `<h1>ログイン</h1>` を消して、代わりに次の `<form>` を丸ごと置きます。」
確かに Step 4 時点で当該 div は消えている。

2) しかし 451行は「貼る先を探せ」という作業指示ではない。作業指示は 423行「次に、LoginForm の return を書き換えます。」で、直後の 425-449行のコードブロックが 428行「return (」から始まり、429-430行「<div className="flex min-h-screen / items-center justify-center px-4">」、431行「<Card className="w-full max-w-sm">」まで丸ごと現物として与えられている。読者は差し込み先を探す必要がなく、return 直下をこのコードに置き換えるだけで済む。451行はコードブロックの**後ろ**に置かれた past-tense（「あった」）の設計解説であり、手を動かす対象ではない。

3) 削除順の指摘も、教材側が中間状態を明示して埋めている。
453行: 「このコードは `</CardHeader>` で終わっていて、`return (` に対応する閉じかっこがまだありません。この時点で保存すると、エディタは赤線を引き、`npm run dev` の画面にも構文エラーが出ます。壊れているのではなく、続きが未入力なだけです。閉じるのは Step 6 の最後のコードなので、そこまでは赤線が出たままで進めてください。」
458行（確認ポイント）: 「閉じかっこが未入力なので、この時点では構文エラーが出たままでよい」
460行: 「ここから4つのブロックで書き直します。**Step 4 と Step 5 で書いた `<form>` から `</form>` までは、先にすべて削除してください。** 残したまま足すと入力欄が二重になり、同じ欄が2つ並びます。」
460行の「ここから4つのブロックで」「先に」という語で、削除は 462行以降の4ブロック貼り付けの**前**に来るよう明示されており、順序は逆転していない。428-448行のブロックだけは form を残したまま書く設計だが、その中間状態（構文エラー・form が残っている状態）は 453行と 458行で「そのままでよい」と先に宣言されている。

よって読者が止まる箇所は存在しない。

### 消えた件 2. 行776 エラー表示を確かめようとするが、Day 05 までにアカウントを1つも作っていないため、どのメールアドレスで試せばよいか

**逐語引用**

```
- 間違ったパスワードでエラーメッセージが出る
- 同じメールアドレスで10回続けて失敗すると15分ログインできなくなるので、試すのは2〜3回にとどめる
```

**初心者役が挙げた詰まり**

エラー表示を確かめようとするが、Day 05 までにアカウントを1つも作っていないため、どのメールアドレスで試せばよいか分からない。出たエラーが「正しく動いている証拠」なのか「自分のコードの不具合」なのかも判別できず、数回試すうちに15分の締め出しに入り、その復旧方法も書かれていない。

**初心者役が挙げた不足**

登録画面は Day 06 で作るため、Day 05 時点で使えるアカウントが教材のどこにも示されていない（`admin@example.com` / `password123` が明かされるのは Day 06 の 714 行）。また「10回失敗で15分ロック」という制限はここが初出で、どこで実装されているのか、ロックされた場合にどう解除するのかの説明が無い。13 行では今日確かめられるのは3つだけと書いており、この確認ポイントとも食い違う。

**反証側が示した、成立しない根拠**

【引用の実在確認】material/30days-curriculum/day05_ログイン画面のUI.md L774-777（grep -n 実測）:
L774 「**確認ポイント**:」
L775 「- 間違ったパスワードでエラーメッセージが出る」
L776 「- 同じメールアドレスで10回続けて失敗すると15分ログインできなくなるので、試すのは2〜3回にとどめる」
L777 「- 送信中はボタンが「ログイン中...」に変わる」
（指摘は行776としているが、1点目の文言は実際は L775。引用自体は2行まとめて実在する）

【反証1: この確認に有効アカウントは不要】L775 が求めるのは「間違ったパスワード」を入れてエラー帯が出ることの確認。認証は「メールアドレスまたはパスワードが違います」を返す経路なので、存在しないメールでも同じエラーが出る。読者は「どのメールで試せばよいか分からない」状態にならず、任意の値を入れれば確認できる。指摘の前提（正しいアカウントが必要）が成立しない。

【反証2: 「正しく動いている証拠か自分の不具合か判別できない」も成立しない】同ファイル L744「`{error && ( ... )}` の形は、Step 6 でメール欄に書いた `{errors.email && ( ... )}` と同じ考え方です。…`error` を入れるのは Step 7 で書いた `onError` です」で、赤いエラー帯が onError 由来であることを説明済み。加えて L333-335 に「スクリーンショット: メールアドレス欄の下に赤字のエラーメッセージが出た状態の表示を確認してください。」＋ ./screenshots/login-error.png があり、期待される見え方が図で示されている。エラーが出ること自体が期待結果として明示されている以上、「出た＝正常」と判断できる。

【反証3: 15分ロックの復旧方法は同じ行に書かれている】L776 は「15分ログインできなくなる」と期間を明示し、さらに「試すのは2〜3回にとどめる」と踏み込ませない指示まで付けている。復旧＝15分待つ、は同一行から読み取れる。なお Day 07 L2419「| `ログイン試行回数が上限に達しました` | 同じメールで10回失敗したための一時ロック | 15分待つか、別のメールアドレスで試す。コードの問題ではない |」で表としても後日補強される。

【反証4: 認証情報は Day 06 まで一切出てこない、という主張は事実誤認】同じ day05 ファイル内 L926-927 および L999-1000 に
「demoFormData.set('email', 'admin@example.com');」
「demoFormData.set('password', 'password123');」
が実在する（Day 05 の Before/After 読み比べセクション内）。「admin@example.com / password123 が明かされるのは Day 06 の 714 行」という指摘の記述は、同一ファイル内の記載を見落としている。

【残る軽微な不整合（ただし読者は止まらない）】L13「自分の目で確かめられるのは「入力欄が反応するか」「未入力ならエラーが出るか」「通信中はボタンが押せなくなるか」の3つになります」と、L775 の4つ目の観測項目は文面上ずれている。ただし L775 の確認は上記のとおりアカウント無しで実行でき、期待結果も図示済みなので、読者がそこで作業を止める性質の欠落ではない。
