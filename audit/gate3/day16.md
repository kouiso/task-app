# Gate3 初心者シミュレーション — Day 16 (2026-07-28)

- 対象: material/30days-curriculum/day16_ステータス変更・時間記録.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 4 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

4 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行804 写経の指示に従って、この return をそのまま自分の task-card.tsx に書き写そうとする。既にあるカード…

**逐語引用**

```text
最後に、カードの一番外側に `TimeLogDialog` を置きます。

return (
  <>
    <Card>
      {/* ...カードの中身... */}
    </Card>
```

**初心者役が挙げた詰まり**

写経の指示に従って、この return をそのまま自分の task-card.tsx に書き写そうとする。既にあるカードの中身（タイトル・バッジ・担当者・期限）をどう扱えばよいかが書かれていないので、そのまま貼るとカードの中身が消えて画面が空になる。既存の return をどう書き換えるのか（<Card> と </Card> はそのまま残し、その外側だけを <> </> で包む）という手順が無い。

**初心者役が挙げた不足**

既存コードを置き換える手順なのに、置き換え前と置き換え後の対応が `{/* ...カードの中身... */}` という省略記号だけで示されている。省略した部分を「今あるものを残す」と明記していない。

**反証側が示した、成立しない根拠**

【引用の存在確認】対象ファイル material/30days-curriculum/day16_ステータス変更・時間記録.md を実際に開いた。指摘の引用は存在する。

- 799行: 「最後に、カードの一番外側に `TimeLogDialog` を置きます。」
- 801-819行:

```text
// filepath: src/component/task/task-card.tsx
// カードとダイアログをまとめて返す
return (
  <>
    <Card>
      {/* ...カードの中身... */}
    </Card>
    {canEdit && (
      <TimeLogDialog ... />
    )}
  </>
);
```

確かに 807行は `{/* ...カードの中身... */}` の省略記号だけである。

【同一ファイル内で埋まっている(3の経路)】

1. 1157行: `## 完成コード全体`
2. 1159行(逐語): 「今日は4つのファイルを触りました。断片を貼り重ねる作業が続いたので、どこへ貼ったか分からなくなった場合は、以下のコードと手元のファイルを見比べてください。`time-log-dialog.tsx` と `task-card.tsx` は今日の終了時点の全文です。`task.ts` と `page.tsx` は Day 13 から Day 15 で書いた中身がそのまま残るため、今日足した部分だけを載せます。」
   → 「どこへ貼ったか分からなくなった場合は見比べてください」という指示そのものが、まさに指摘の詰まり(「そのまま貼ると中身が消える」)に対する参照先を明示している。
3. その全文が実際に task-card.tsx の Card 中身を省略なしで載せている。確認した見出しと行番号:
   - 1555行付近 `// 完成版: カードの外枠` → `return ( <> <Card className={cn(...)} onClick={handleCardClick}>` と、`<Card>` が閉じずに続く形で提示。
   - 1597行付近 `// 完成版: タイトル` → `<CardHeader ...><CardTitle ...>{onClick ? (<button ...>{title}</button>) : (title)}</CardTitle>` の全文。
   - 続いて `// 完成版: 編集ボタン`（`<div className="flex gap-0">{canEdit && (<Button ... aria-label="タスクを編集">`）、`// 完成版: 削除ボタン`。
   - 1722行付近 `// 完成版: 合計作業時間と時間記録ボタン`。
   - 1751-1762行 `// 完成版: ダイアログの設置` → `{canEdit && (<TimeLogDialog ... />)} </> );  }` でファイル末尾まで閉じる。
   つまり 807行の `{/* ...カードの中身... */}` に相当する部分（タイトル・バッジ・担当者・期限を含むカード本体）は、同じファイルの 1555〜1762行に省略なしで全部載っている。読者はそこと突き合わせれば「Card の中身は今あるものを残す」ことを確認できる。

【本文の説明でも方向が示されている】

- 824-827行(逐語): 「`<>` と `</>` で囲むのは、カードとダイアログを1つの要素として返すためです。Reactは複数の要素を並べて返せないので、この空タグ（フラグメント）でまとめます。」→ Card を作り直すのではなく「囲む」操作であることを述べている。
- 1762行直後の逐語: 「小窓を `<Card>` の外に置いてあるのは、カードの枠や `overflow` の影響を受けずに画面の中央へ出すためです。」

【省略記号の記法は同日内で既に確立】

- 708行: `// ...既存のprops...`（「既存の」と明記）
- 866行: `// ...Day 15 で渡した props...`
同じ日の同じファイルで「`...` は今あるものを残す印」という使い方を2回先に見せてから、807行の `{/* ...カードの中身... */}` が出る順序になっている。

以上より、指摘が言う不足は同一ファイル内（1159行の案内 + 1555〜1762行の全文）で埋まっている。

### 消えた件 2. 行762 「カード内に」しか書かれていないので、task-card.tsx のどの行の下に貼るのかが決まらない。このファイルには …

**逐語引用**

```text
カード内に合計作業時間の表示と
「時間記録」ボタンを置きます。

<div className="space-y-2">
  <p className="text-sm text-muted-foreground">
    合計作業時間: {formatMinutes(timeSpentMinutes)}
```

**初心者役が挙げた詰まり**

「カード内に」しか書かれていないので、task-card.tsx のどの行の下に貼るのかが決まらない。このファイルには CardHeader・CardContent・担当者の div・期限の div と入れ子が多数あり、beginner はどれが「カード内」なのか選べない。

**初心者役が挙げた不足**

他のブロック（例: Step 2 の「`DialogHeader` の閉じタグの直後に書きます」）では貼り付け先を1点に特定しているのに、ここだけ位置指定が無い。正解の位置（期限の div を閉じたあと、CardContent の中）は、ファイル末尾の「完成コード全体」まで読まないと分からない。

**反証側が示した、成立しない根拠**

【現物確認】material/30days-curriculum/day16_ステータス変更・時間記録.md

1. 指摘箇所は実在する。L759-760 の本文「カード内に合計作業時間の表示と\n「時間記録」ボタンを置きます。」、L762 が ```typescript のフェンス開始、L763「// filepath: src/component/task/task-card.tsx」、L765「<div className="space-y-2">」、L767「合計作業時間: {formatMinutes(timeSpentMinutes)}」。確かに Step 3 のこのブロックだけ「カード内に」としか書かれておらず、貼り付け先を1行に特定していない。比較対象の L521「`DialogHeader` の閉じタグの直後に書きます。」、L133「`addTime` を、Day 15 で書いた `delete` の直後に足します。」、L799「最後に、カードの一番外側に `TimeLogDialog` を置きます。」は位置を特定しており、指摘の言う「ここだけ位置指定が無い」は事実。

2. しかし同じファイル内で埋まっている（照合手順3に該当）。

- L1157「## 完成コード全体」
- L1159 逐語:「今日は4つのファイルを触りました。断片を貼り重ねる作業が続いたので、どこへ貼ったか分からなくなった場合は、以下のコードと手元のファイルを見比べてください。`time-log-dialog.tsx` と `task-card.tsx` は今日の終了時点の全文です。」— まさにこの詰まり（どこへ貼ったか分からない）に対する明示の逃げ道が本文に書かれており、task-card.tsx は「今日の終了時点の全文」と宣言されている。
- L1161-1166 の対応表に「| `src/component/task/task-card.tsx` | 合計作業時間の表示と記録ボタン | Step 3 |」（L1165）があり、Step 3 とこの全文が紐付けられている。
- 正解位置は L1718「**合計作業時間と時間記録ボタン**:」〜 L1743 に、直前ブロック L1694「**期限**:」/ L1712「              )}」L1713「            </div>」と、直後 L1740「          </div>」L1741「        </CardContent>」L1742「      </Card>」まで含めて出ている。インデント（L1723「            <div className="space-y-2">」= 12スペース）も一致するので、貼り付け先は一意に確定できる。

3. 参照の負荷も「ファイル末尾まで読まないと分からない」ではなく、L1159 が「分からなくなったらここを見ろ」と誘導する設計になっている。加えて誤配置しても L833-836 の確認ポイント（合計作業時間が表示される／ボタンが表示される／押すとダイアログが開く）はほぼ満たされ、読者がそこで手が止まる性質のものではない。

よって「教材のどこにも埋まっていない」は成立しない。survives: false。

### 消えた件 3. 行246 言われたとおり中身を全部消してから「次のコード」を貼ろうとするが、直後のコードブロックはインポート7行だけ。全部消した状…

**逐語引用**

```text
**既存の `src/component/task/time-log-dialog.tsx` を丸ごと上書き**してください。
新しいファイルを作るのではなく、開いて中身を全部入れ替える、という操作です。
```

**初心者役が挙げた詰まり**

言われたとおり中身を全部消してから「次のコード」を貼ろうとするが、直後のコードブロックはインポート7行だけ。全部消した状態でそれを貼ると、画面がその場で壊れる。残りは解説文をはさんだ9個の断片に分かれていて、どこまでが「丸ごと」なのかが分からない。

**初心者役が挙げた不足**

「丸ごと上書き」という一括の操作を指示しながら、実際に渡されるコードは断片の連続になっている。「これから出てくる断片を上から順につないだものが全文です」という断りがこの位置に無い。

**反証側が示した、成立しない根拠**

【引用の実在確認】day16_ステータス変更・時間記録.md:243-249 に指摘の文言が実在する。逐語（243-248行）:
「1つ先に伝えておくことがあります。配布コードには最初から、/ 完成版の `time-log-dialog.tsx` が入っています。今日はこの機能が学習の主役なので、/ 完成版に頼らず自分の手で作り直します。次のコードは、/ **既存の `src/component/task/time-log-dialog.tsx` を丸ごと上書き**してください。/ 新しいファイルを作るのではなく、開いて中身を全部入れ替える、という操作です。」
直後の実装ブロック（253-264行）が `import` 7行だけである点も、指摘のとおり事実。

【不足がその日より前で埋まっているか → 埋まっている】

1) day02_ダッシュボードに自分だけのメッセージを追加しよう.md:158（逐語）:
「このあとコードを4つのブロックに分けて載せますが、**すべて上から順に1つの `src/app/dashboard/page.tsx` へ続けて書く、同じファイルの中身**です。ブロックの切れ目は読みやすさのための区切りで、別ファイルに分ける意味ではありません。」
指摘が「この位置に無い」と言う断り（＝これから出る断片を上から順につないだものが全文）は、day02 の時点で明示的に言語化されている。しかも同 154-155 行は「Day 01 と同じで、/ この段階は部分修正より丸ごと差し替えたほうが流れを掴みやすいです。」であり、「丸ごと差し替え＋断片で配る」という組み合わせ自体が day02 で導入済み。

2) day01_開発環境を整えて、初めてのアプリを動かそう.md:490（逐語）:
「では、上から順に貼っていきましょう。各ブロックのあとに「確認ポイント」を挟むので、そこまで貼れたら次のブロックへ進みます。」
＝断片を上から順に貼り足す運用は day01 で確立。

3) day16 に到達するまでに同じ運用を繰り返し明言している。逐語（各日の「完成コード全体」節冒頭）:

- day07:1495「今日は7つのファイルを触りました。断片を貼り重ねる作業が続いたので、…」
- day08:680「…断片を貼り重ねる作業が続いたので、…」
- day10:973 / day11:1316 / day14:1667 / day15:1176 も同一文型で「断片を貼り重ねる作業が続いた」と記述。
day15 は day16 の直前日であり、同じ「1つのダイアログ部品を断片で書き上げる」形式を既に通っている。

【同じファイル内で埋まっているか → 埋まっている】
day16:1157-1159（逐語）:
「## 完成コード全体 / / 今日は4つのファイルを触りました。断片を貼り重ねる作業が続いたので、どこへ貼ったか分からなくなった場合は、以下のコードと手元のファイルを見比べてください。`time-log-dialog.tsx` と `task-card.tsx` は今日の終了時点の全文です。」
＝「どこまでが丸ごとか分からない」という状態に対する復旧経路（全文の掲載）が同一ファイル内に用意されている。

【配布足場での補完も本文に明記】
day16:249（逐語）:「上書きする前の配布版は `scripts/_app-components/task/time-log-dialog.tsx` に残っています。見比べるときはこちらを開いてください。」
＝上書き前の版が消えないことも本文で読者に伝えられており、「その場で壊れて戻せない」状態にはならない。

以上より、指摘が求める断り書きは day01/day02 で確立し day07〜day15 で反復され、day16 内でも全文掲載と配布版の所在で二重に担保されている。読者が day16:246 で止まるとは言えない。

### 消えた件 4. 行24 今日のゴールとして示された画面を確認しようとするが、この日の手順にタスク詳細ダイアログを開く操作は一度も出てこない。しか…

**逐語引用**

```text
スクリーンショット: タスク詳細ダイアログの表示を確認してください。

![タスク詳細ダイアログの画面](./screenshots/task-detail-dialog.png)
```

**初心者役が挙げた詰まり**

今日のゴールとして示された画面を確認しようとするが、この日の手順にタスク詳細ダイアログを開く操作は一度も出てこない。しかも Step 1 では詳細ダイアログにステータス変更の操作は無いと書かれているので、この画像で何を確認すれば「今日のゴールに届いた」のかが判断できない。

**初心者役が挙げた不足**

今日作るもの（編集ダイアログでのステータス変更・時間記録ダイアログ・カードの合計作業時間）と、冒頭に置かれた確認用スクリーンショットの対象が一致していない。

**反証側が示した、成立しない根拠**

現物確認（material/30days-curriculum/day16_ステータス変更・時間記録.md）:

1) 引用は実在する。24-26行:
24:「スクリーンショット: タスク詳細ダイアログの表示を確認してください。」
26:「![タスク詳細ダイアログの画面](./screenshots/task-detail-dialog.png)」
画像実体も存在（material/30days-curriculum/screenshots/task-detail-dialog.png, 102.8K）。

2) 「今日のゴールに届いたかを判断できない」は、直後の28行が文章で明示的に定義しており、同一ファイル内で埋まっている:
28:「> **今日のゴールライン**: ステータスを変更すると一覧に反映され、時間を記録すると合計作業時間が増えます。この2つの流れが動けばOKです。」
読者はこの1行でゴール判定基準を得られる。判定を画像に依存していない。

3) 「詳細ダイアログを開く操作が出てこない」は前日までで埋まっている。詳細ダイアログはカードクリックで開く部品として Day13 で配置済み:
day13_タスク一覧画面.md:1086:「<TaskDetailDialog」
day13:1093:「`TaskDetailDialog` は `taskId` を受け取り、その1件を `api.task.getById` で取りに行きます。」
day13:1734:「<TaskDetailDialog open={detailOpen} taskId={selectedTask} onClose={handleDetailClose} />」
よって day16 の読者は既に開ける状態にあり、24行の「表示を確認」は実行可能。

4) 今日の成果物（編集ダイアログでのステータス変更・TimeLogDialog・カードの合計作業時間）に対応する確認用スクリーンショットは、当該箇所に別途置かれている:
632行「作業時間の記録ダイアログの表示を確認してください。時間と分の入力欄があります。」/ 634:「![作業時間の記録ダイアログ（時間と分の入力欄）](./screenshots/task-timer.png)」
902行「時間記録後の合計作業時間の表示を確認してください。」/ 904:「![時間記録後の合計作業時間の表示](./screenshots/task-list.png)」
890:「5. 合計作業時間が入力した分だけ増える」、899:「- 時間を記録すると合計作業時間が増える」

5) 222行「タスク詳細ダイアログ（`TaskDetailDialog`）にはステータスの表示だけがあり、」223行「そこから変更する操作はありません。変えたいときは編集ダイアログを開きます。」は、冒頭画像と矛盾するのではなく、変更操作の場所を先に切り分けている記述であり、24行は「表示の確認」しか求めていない。

結論: 冒頭画像の主題選択は最適とは言えないが、ゴール判定は28行、開き方は Day13、当日成果物の確認は634/904行で埋まっており、読者がそこで止まる根拠は無い。
