# Gate3 初心者シミュレーション — Day 11 (2026-07-28)

- 対象: material/30days-curriculum/day11_プロジェクト編集・削除.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 8 件 / 反証を生き延びた件数: 7 件

## 生き延びた詰まり

### 1. 行327 「`});` は増やしません」と宣言しているのに、貼るブロックが `});` を含む

**逐語引用**

```text
  unarchive: protectedProcedure
    .input(z.object({ id: z.string().cuid() }))
    .mutation(async ({ ctx, input }) => {
      return await setArchiveStatus(ctx.session.userId, input.id, false);
    }),
});
```

**何ができないか**

行155の「`});` は増やしません」に従って末尾の `});` の1行上へ貼ろうとするが、貼る側のコードの最後にも
`});` が入っている。増やさないために消すのか、そのまま貼るのかを決められず、
どちらを選んでも括弧の数が合わずに起動が止まる。

**教材のどこが足りないか**

行155で「増やしません」と宣言しているのに、行314から327のブロックだけが `});` を含んだ形で提示されている。
この1ブロックが例外であることも、既存の `});` を消して置き換える意味であることも書かれていない。

**反証側が確かめた根拠**

- 行155の宣言と行314から327のブロックを現物で照合し、最終行が `});` であることを確認した。
- 同じファイルの他のブロック（行231、行283）はいずれも `});` を含まずに終わっている。行327だけが例外。
- 直前の行312は「`delete` の下に、この関数を呼ぶ2つの手続きを追加します」で、ルーター内部への追記を指示している。
- 行330は完成状態の説明で、既存の `});` を消すとは書いていない。
- 教材全体を「増やしません」で検索すると Day 07、10、11、12、14、18、25 に同じ1文があるが、
  例外の説明はどこにも無い。
- Day 10 の単独 `});` 行（129/307/1006/1136/1212）を確認したが、ルーター末尾の `});` を含む
  続きブロックは Day 10 に無く、前日までにこの慣習は提示されていない。
- Day 11 内に `project.ts` の完成形全文は無く、配布物でもない（行87で自分で書く対象と明記）。

### 2. 行452 既存の仮実装がある `handleEdit` を「追加します」と指示している

**逐語引用**

```text
次に、Day 09 で定義した `handleCreate` の直下に `handleEdit` を追加します。実際のコードでは `handleCreate` → `handleEdit` の順番で並んでいます。
```

**何ができないか**

指示どおり書くと、Day 09 と Day 10 で置いた `const handleEdit = (projectId: string) => { void projectId; };` と
同名になり、再宣言のエラーで `npm run dev` が止まる。何を消せばよいのか本文に書かれていない。

**教材のどこが足りないか**

Day 09 の行516と Day 10 の行1546で、これらが「受け皿」の仮実装だと明記されている。Day 11 の行2131でも
それを認めている。にもかかわらず Step 1 と Step 2 は「追加します」としか書かず、古い仮実装を削除する手順が無い。
Step 9 だけは「仮定義を削除してから」と明記されているので、読者は削除が要る箇所と要らない箇所を見分けられない。

**反証側が確かめた根拠**

- 行452と行459のコメント、Day 09 の行500から501と行516、Day 10 の行1535から1536と行1546を現物で確認した。
- Day 11 全体を「重複」「すでに」「書き換え」「仮定義」「削除してから」で検索したが、Step 1 と Step 2 に
  対応する削除指示は無い。該当したのは import の重複注意（行358から359）と Step 9 の話（行937から956、行1089）だけ。
- 同じ Day 11 の Step 4（行699から706）では handleCreate について「更新します」と書き分けている。
- つまずきポイント表の行2226は Step 9 に限定されており、Step 1 と Step 2 の重複を扱っていない。
- 完成版の handleEdit は行1893以降にあり、行1316の迷子向け導線。Step 1 で詰まる読者は到達していない。

### 3. 行529 まだ定義していない `updateMutation` を貼り付けの目印にしている

**逐語引用**

```text
次に、削除用の mutation を定義します。実際のコードでは mutation の定義順は `createMutation` → `updateMutation` → `deleteMutation` です。`updateMutation` の直下に追加してください。
```

**何ができないか**

Step 2 の時点で `page.tsx` を開いても `updateMutation` がどこにも無いので、「直下」に当たる場所を
特定できず、貼り付け先を決められない。

**教材のどこが足りないか**

`updateMutation` を定義するのは次の Step 3（行575から585）で、Step 2 より後ろにある。
使う側の位置指定が、定義より先に出てくる順番の逆転になっている。代替の目印の注記も無い。

**反証側が確かめた根拠**

- 行529、行533のコメント、行544の確認ポイントを現物で確認した。いずれも行508の Step 2 の配下。
- 実定義は行573から579で、行566の Step 3 の配下。順序の逆転を確認した。
- Day 01 から Day 10 を横断検索した結果、`updateMutation` は1件も出現しない。読者の手元には存在しない。
- Day 11 内の `updateMutation` の全出現（529/533/544/573/578/591/608/628/1821/1942）を見たが、
  行529より前の出現は無い。行1821は完成コード節で、貼り付け時の参照にならない。
- `page.tsx` は Day 09 と Day 10 から読者が育てるファイルで、配布物ではない。

### 4. 行597 既存の `handleSubmit` があるのに「配置してください」としか書いていない

**逐語引用**

```text
> **配置の注意**: `handleSubmit` は `handleDelete` の直下に配置してください。コードの並び順は `handleCreate` → `handleEdit` → `handleDelete` → `handleSubmit` です。
```

**何ができないか**

言われたとおりに書き足すと、Day 10 Step 7 で書いた同名の `handleSubmit` が残ったままになり、
再宣言のエラーになる。どちらを残すのかが書かれていない。

**教材のどこが足りないか**

Day 11 の版は if と else に作り替えたものなので本来は置き換えだが、本文は「配置」「追加」としか言わず、
既存を消す指示が無い。Day 10 側も「編集処理は Day 11 で追加します」としか書いていない。

**反証側が確かめた根拠**

- 行595、行597、行603のコメント、行604を現物で確認した。指示語は一貫して「配置」「追加」。
- Day 10 の行748から772で同名の `handleSubmit` を同じファイルへ書かせていることを確認した。
- Day 11 全文を「置き換え」「書き換え」「差し替え」「削除して」「上書き」で検索したが、
  `handleSubmit` に関する置換指示は0件。該当したのは Day 12 用のプレースホルダー（行933、行938、行1878）だけ。
- 行1938の完成版は行1314の完成コード節にあり、行1316の説明どおり迷子になった時の救済で、
  手順どおり進む読者には届かない。
- `handleSubmit` は配布物ではなく Day 10 で読者が書いたコードなので、足場では埋まらない。

### 5. 行715 既に置いた `ProjectDialog` に対して「配置します」と書いている

**逐語引用**

```text
JSX 内のプロジェクトカード一覧グリッド（`<div className="grid gap-6 sm:grid-cols-2 ...">...</div>`）の閉じタグ直後に `ProjectDialog` を配置します。
```

**何ができないか**

言われた場所には Day 10 Step 7 で置いた `<ProjectDialog ... />` がすでにある。もう1つ足すのか、
書き換えるのかが分からない。足すとダイアログが二重に描かれる。

**教材のどこが足りないか**

今日の変更は `initialData={editingProject}` の1行を足すだけで、行2161にもそう書いてある。
しかし Step 4 の本文は「配置します」と新規配置の言い方をしており、既存タグへの属性追加だと読み取れない。

**反証側が確かめた根拠**

- 行693、行715、行717から726、行728を現物で確認した。既存タグへの追記だと示す語が無いことを確認した。
- Day 10 の行781、行786から790、完成コードの行1699から1704を確認し、同じ位置に同じタグが
  すでにあることを確認した。
- 解消する文言は行2161だが、これは行1314の完成コード節の中にあり、実装手順（行693から行1022）を
  進む読者は Step 4 の時点で到達していない。
- 同じ Step 4 の直前（行700から701）では既存コードの変更を「更新します」と書き分けており、
  読者は「配置」を新規追加として読む。

### 6. 行2124 完成コードが、その完成コードに定義の無い `handleProjectClick` を使っている

**逐語引用**

```text
                  onEdit={handleEdit}
                  onDelete={handleDelete}
                  onClick={
                    handleProjectClick}
```

**何ができないか**

行1316の「以下のコードを上から順に貼り付けて、各ファイルを置き換えてください」に従って完成コードで
`page.tsx` を丸ごと置き換えると、`handleProjectClick` が定義されていないためエラーになり、
そこから先へ進めない。

**教材のどこが足りないか**

Day 11 の完成コード全体に `handleProjectClick` の定義が1か所も含まれていない。Step 9 の仮定義ブロック
（行1868から1882）にも入っていない。断片を貼り重ねてきた読者は Day 10 の仮定義が残るので動くが、
完成コードで置き換えた読者だけが動かなくなる。

**反証側が確かめた根拠**

- 行1316の指示、行2106から2124の使用箇所を現物で確認した。
- 完成コード節（行1692から2199）で定義される state とハンドラーを全列挙し、
  `handleProjectClick` の定義が1件も無いことを確認した。Day 11 内の出現は行933と行2124の2件だけで、
  定義行は存在しない。定義は Day 09、Day 10、Day 12 側にある。
- Day 10 の行1541から1543に仮定義があるため、断片を貼り重ねた読者の手元には残る。
  置き換えた読者だけが `handleProjectClick is not defined` で止まる。
- Step 9 の仮定義ブロックは projectDetail、handleDetailClose、memberDialogOpen、handleRemoveMember の
  4つのみ。配布部品はハンドラーを提供しない。

### 7. 行678 参照先の Step 番号が実際と違う

**逐語引用**

```text
プロジェクト編集では `description ?? null` と `description || null` の違いに注意してください。Step 5 の更新ハンドラー（`src/app/project/page.tsx`）で `description || null` を使ったのは、
```

**何ができないか**

説明の根拠を確かめようと Step 5 を見に行くが、Step 5 は DeleteConfirmDialog の配置で
`description || null` はどこにも無い。探し回って時間を取られる。

**教材のどこが足りないか**

`description || null` を書いたのは Step 3（行611から612）で、Step 5 ではない。参照先の番号が食い違っている。

**反証側が確かめた根拠**

- 行678を現物で確認した。
- `description || null` を含む行はファイル全体で行612、行629、行678、行682のみで、Step 5 の範囲には無い。
- 行746の見出しと行562の説明が、Step 5 は削除確認ダイアログだと定義している。行678はこれと矛盾する。
- Step 見出しの実配置（351/508/566/693/746/802/848/894/925/1022）を確認し、
  Step 4 と Step 5 に更新ハンドラーが無いことを確認した。
- 番号の誤りなので、前の日の知識でも配布物でも埋まらない。行678を「Step 3」に直せば解消する。

## 反証で消えた件

1 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行153 「getAll の下」と「末尾の `});` の1行上」は、Day 10 まで書き終えたファイルでは別の場所（getAl…

**逐語引用**

```text
続けて `update` の手続き本体です。`getAll` の下に追加します。まず対象のプロジェクトを探し、無ければ止めます。

ここから先の「（続き）」のブロックは、`project.ts` の**末尾にある `});` の1行上**へ貼ります。
```

**初心者役が挙げた詰まり**

「getAll の下」と「末尾の `});` の1行上」は、Day 10 まで書き終えたファイルでは別の場所（getAll と create の間 / create の後ろ）を指す。どちらに貼るか決められないまま止まる。

**初心者役が挙げた不足**

直前の1文と直後の1文で貼り付け先が2通り提示されており、どちらが正しいかの説明が無い。同じ問題は292行の setArchiveStatus でも起きていて、そこでは「`export const projectRouter` の上」と書かれているのに、ブロックの見出しは「（続き）」＝ルーター内部を指している。

**反証側が示した、成立しない根拠**

【引用の実在確認】day11_プロジェクト編集・削除.md の 153行「続けて `update` の手続き本体です。`getAll` の下に追加します。まず対象のプロジェクトを探し、無ければ止めます。」、155行「ここから先の「（続き）」のブロックは、`project.ts` の**末尾にある `});` の1行上**へ貼ります。ファイルの一番下に足すとルーターの外に出てしまい、英語のエラーで止まります。`});` は増やしません。」、292行「…`project.ts` の `projectUpdateSchema` の下、`export const projectRouter` の上に追加します。」— 3箇所とも指摘どおり実在する。

【Day10 時点のファイル形状】day10_プロジェクト新規作成.md:195「- `projectCreateSchema` と `create` を追加し、`getAll` の直後に `}),` `});` まで閉じた」、同1773「Day 10 終了時点の状態は、完成版の `src/server/api/routers/project.ts` の `getAll` と `create` の部分と同じです。」→ Day11 開始時の並びは getAll → create → `});`。よって「getAll の下」と「末尾 `});` の1行上」は確かに別位置を指す（指摘の前提自体は正しい）。

【同一ファイル内で埋まっている】day11 の 1316行「今日は2つのファイルを触りました。断片を貼り重ねる作業が続いたので、途中でどこへ貼ったか分からなくなった場合は、以下のコードを上から順に貼り付けて、各ファイルを置き換えてください。」— まさに「どこへ貼るか決められない」読者向けの復旧経路が同日中に明示されている。1320行の表に `src/server/api/routers/project.ts`（Day 09/10 分を含む）が挙がり、1336行から完成版全文が並ぶ。1388行に `const setArchiveStatus = ...` がルーター外、1411行に `export const projectRouter = createTRPCRouter({` が続く形で、setArchiveStatus の正しい位置が現物のコードで確定している。さらに 1404行「…ルーターの外へ出してあるのは、`archive` と `unarchive` の両方から呼ぶためです。」で、292行のヘルパーがルーター内部ではないことが明文で説明されている。

【実害の程度】tRPC ルーターの手続きの並び順（getAll の下 vs create の下）は動作に影響せず、どちらへ貼っても `api.project.update` は動く。判断がつかない読者は 1316行の指示で完成版全文へ置き換えられる。よって読者がそこで止まり切るとは言えない。
