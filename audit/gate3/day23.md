# Gate3 初心者シミュレーション — Day 23 (2026-07-28)

- 対象: material/30days-curriculum/day23_週次レポート.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 5 件 / 反証を生き延びた件数: 1 件

## 生き延びた詰まり

### 1. 行280 追記ブロックが `});` を含み、既存の閉じ括弧と重なる

**逐語引用**

```text
      return {
        weeks: input.weeks,
        startDate: startDate.toISOString(),
        endDate: now.toISOString(),
        weeklyData,
        totalCompleted: tasks.length,
      };
    }),
});
```

**何ができないか**

Step 0 の指示どおり `getOverview` の直後に 0-1 から 0-6 を貼り付けると、この最後の `});` が
Day 21 で既に書いてあるルーターの閉じ `});` と重なる。保存した瞬間に構文エラーが出て、
どちらを消せばよいか分からず止まる。

**教材のどこが足りないか**

Step 0 の冒頭（行106）は「`getOverview` の直後に足します」としか言っておらず、既存の `});` の前に
差し込むことも、貼り付けた `});` が既存の閉じ括弧の代わりになることも書かれていない。
0-6 の見出しも「最後に返して閉じる」とだけで、何を閉じるのかを既存コードとの関係で説明していない。

**反証側が確かめた根拠**

- 行268から280を現物で確認し、追記ブロックの最終行が `});` であることを確認した。
- 行106、行126、行131、行283、行285から289を確認したが、既存の `});` を消す旨の記述は無い。
  確認ポイントにも閉じ括弧の項目が無い。
- Day 21 の行367から368で、`report.ts` は既に `});` で閉じ済みであることを確認した。
- 他の日の「直後に追加」ブロック（Day 15 の行363から374、Day 16 の行136、Day 20 の行456、
  Day 28 の行226と行261と行287）はいずれも末尾が `}),` で、`});` を含まない。
  逆に新規作成の Day 13 は行342で `});` を含め、行344と行348で明示している。
  Day 23 の行280はこの規則を破っている。
- 完成コード全体（行1210以降）は Step 0 の約930行後にあり、二重の `});` が出る予告も解消手順も無い。
- 付録のトラブルシューティングにも「二重」「余分」「閉じ括弧」の項目は無い。

## 反証で消えた件

4 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行855 Step 4 で `return (...)` と関数の閉じ `}` まで書き終えた状態なので、この `const ch…

**逐語引用**

````text
```typescript
// filepath: src/app/report/weekly/page.tsx
// グラフ用データの変換処理（完了数・優先度）
const chartData =
  reportData?.weeklyData.map((week) => ({
    name: week.week,
````

**初心者役が挙げた詰まり**

Step 4 で `return (...)` と関数の閉じ `}` まで書き終えた状態なので、この `const chartData` をどこへ書けばよいか分からない。ファイルの末尾に足すと `return` の後ろになり、グラフに渡す段階では存在しない。

**初心者役が挙げた不足**

Step 3 以前は「`if (isLoading)` の直後に、この `return` を書きます」のように場所が明示されていたのに、Step 6 の2つの変換処理（`chartData`／`statusData`）だけ挿入位置の指示が無い。完成コード（1705行目付近）では `return` より前に置かれているが、Step 6 本文にはその順序が書かれていない。

**反証側が示した、成立しない根拠**

【引用の現物確認】day23_週次レポート.md の 847行「### Step 6: グラフを表示する（5分）」直下、852-866行に指摘の引用が実在する。852行「// filepath: src/app/report/weekly/page.tsx」、853行「// グラフ用データの変換処理（完了数・優先度）」、854行「const chartData =」、855行「  reportData?.weeklyData.map((week) => ({」、856行「    name: week.week,」。Step 6 本文に「return の前に置く」という明示の一文が無いことも確認した（Step 6 = 847〜1030行を通読、位置指示の語は該当なし）。

【前日 day22 で埋まっている】読者は day22_グラフを表示.md を通過済み。同じ「レポート画面で Recharts へ渡す配列を `const xxxData = 〜?.map(...)` で作る」処理について、置き場所が2回明示されている。

- day22 264-265行: 「**実装**: Day 21 の `useQuery` の下、`return`\n文の前に追加してください。」→ 直後 269-277行が `const statusData = overview?.statusData.map((entry) => ({ ... }))`。
- day22 417-418行: 「> Step 3 の `statusData` と同じ場所\n> （`return` 文の前）に追加してください。」→ 直後 426-434行が `const priorityData = ...`。
- day22 752行で理由まで説明済み: 「`...oading)` の下へ移しても画面は動きますが、`return` を挟んだ先で `const` を書くことになり、値を用意する場所が読み込み判定の前後に散らば…」。
day23 の `chartData` / `statusData` は変数名（`statusData` は完全一致）・形（`?.weeklyData.map((week) => ({ ... }))`）・用途（Recharts へ渡す配列）が day22 と同型であり、「グラフ用の変換配列は useQuery の下・return の前」という規則は前日に確立済み。

【同ファイル内でも補強】day23 686-689行に「この Step の前半で書いた週数選択の `Select` は、…コメントの行と置き換えます。Step 5 と Step 6 で作る\nカードとグラフも、対応するコメントの行と置き換えていきます。」とあり、JSX プレースホルダ（682行 `{/* Step 5: サマリーカード3枚 */}`、683行 `{/* Step 6: グラフ3枚のグリッド */}`）へ置き換える対象は「カードとグラフ」＝JSX に限定されている。変換処理の `const` はその対象に含まれない。さらに完成コード側 1701-1703行「**グラフへ渡す2つの配列**:」→「// 完成版: グラフへ渡す2つの配列」が 1735-1737行「// 完成版: 見出しの行」「  return (」より前に置かれており、順序は同ファイル内でも読める。

以上より、前日 day22 の明示指示で埋まっているため survives: false。

### 消えた件 2. 行722 Day 22 で書いた `/report` の見出しは `<div>` の中に `<h1>` と `<p>` が縦に並ん…

**逐語引用**

````text
レポート見出しの右側へリンクを置きます。`Link` は Next.js が用意したページ移動用の部品で

```typescript
// filepath: src/app/report/page.tsx
<Link
  href="/report/weekly"
  className="inline-flex items-center gap-2
````

**初心者役が挙げた詰まり**

Day 22 で書いた `/report` の見出しは `<div>` の中に `<h1>` と `<p>` が縦に並んでいるだけなので、この `<Link>` を「見出しの右側」に置こうとしても置き場所が無い。素直に `<p>` の下へ貼ると説明文の真下に落ち、右側に来ない。

**初心者役が挙げた不足**

完成コード（2074〜2093行目）では、見出しを包む div に `flex ... sm:justify-between` を足し、`h1` と `p` をさらに内側の `<div>` で包み直すという構造変更をしている。Step 4 の本文はその作り替えに一切触れず、追加する `<Link>` だけを見せている。

**反証側が示した、成立しない根拠**

【引用の実在確認】material/30days-curriculum/day23_週次レポート.md の718行「レポート見出しの右側へリンクを置きます。`Link` は Next.js が用意したページ移動用の部品で、`<a>` タグと違って移動先の中身だけを差し替えます。」、720-731行に `<Link href="/report/weekly" className="inline-flex items-center gap-2 …>週次レポートを見る<ArrowRight className="h-4 w-4" /></Link>` のスニペット。引用は実在する。

【Day 22 側の前提の確認】material/30days-curriculum/day22_グラフを表示.md 762-771行「        <div>\n          <h1 className="text-3xl\n            font-bold tracking-tight">\n            レポート・統計\n          </h1>\n          <p className="text-muted-foreground">\n            プロジェクトの進捗とタスクの\n            状況を確認できます。\n          </p>\n        </div>」。指摘が言う通り、Day 22 時点の見出しは flex を持たない素の `<div>` で `<h1>` と `<p>` が縦に並ぶだけ。Step 4 の本文（718-735行）は確かに構造の作り替えに触れていない。

【同じファイル内で埋まっているか＝埋まっている】

1. 1209行「## 完成コード全体」→ 1963行「### `src/app/report/page.tsx`」で、その日のうちに読者へ渡される節。
2. 1965行に予告がある。逐語:「このファイルで今日書き換えたのは、先頭の import 2行と見出しの行だけです。ただし見出しの行はタグの入れ子が1段深くなるため、貼る場所を間違えると統計カードまで巻き込みます。以下は Day 22 の終わりの状態にリンクを足した全文で、ブロックの区切りは Day 22 の「完成コード全体」とそろえてあります。2つを並べると、どこが増えたかを行単位で見比べられます。」— 「タグの入れ子が1段深くなる」と明言し、Day 22 の完成コードと行単位で見比べる手順まで指示している。
3. 2066行「**見出しとリンクの行**:」→ 2074-2093行に作り替え後の現物がある。逐語（2074-2077, 2084-2085行）:「        <div className="flex flex-col gap-4\n          items-start sm:flex-row\n          sm:items-center sm:justify-between">\n          <div>」…「          </div>\n          <Link」。外側 div に flex/sm:justify-between を足し、h1 と p を内側 div で包み直した形がそのまま載っている。
4. 2096行が理由まで日本語で説明している。逐語:「今日の変更はここだけです。Day 22 では見出しと説明文を包む `div` が1つでしたが、リンクを右へ置くために外側の `div` を1つ増やし、見出しの組と `Link` を横に並べています。狭い画面では `flex-col` で縦積みになり、`sm` 以上で横並びに切り替わります。リンクを見出しの組の中へ入れてしまうと、説明文の下に潜って気づかれません。」— 指摘が「本文が一切触れていない」とする構造変更（外側 div を1つ増やす／h1・p を内側 div で包む／flex で横並び）を、指摘が想定した失敗（説明文の下に潜る）まで含めて明示している。

【止まらない根拠】Step 4 の確認ポイント（737-739行）は「- `/report` から週次レポートを開ける」「-「プロジェクト統計」テーブルは1つだけ表示される」であり、位置ではなく到達可否を問うている。仮に読者が `<p>` の下へ素直に貼っても確認ポイントは満たされて先へ進め、右側に来ない見た目の違和感は同じファイルの 1965 / 2074-2093 / 2096 行で回収される。

結論: 同じファイルの別の箇所（完成コード全体節、1965・2074-2093・2096行）で埋まっているため survives: false。

### 消えた件 3. 行387 「貼り付けないでください」と書かれた直後に、実ファイル名 `src/app/report/page.tsx` 付きの i…

**逐語引用**

````text
Day 21 で表示済みのテーブルを確認します。
以下のコードを2つ目のテーブルとして
貼り付けないでください。

```typescript
// filepath: src/app/report/page.tsx
// Table 関連のインポートを追加
import {
  Table, TableBody, TableCell,
````

**初心者役が挙げた詰まり**

「貼り付けないでください」と書かれた直後に、実ファイル名 `src/app/report/page.tsx` 付きの import ブロックが出て、さらに直後の確認ポイントで「6つのコンポーネントをインポートした」と求められる。書いてよいのか書いてはいけないのか判断できない。

**初心者役が挙げた不足**

以降の Table 本体のコードブロックには「読み比べ用サンプル（実ファイルには対応しません）」という断り書きが付いているが、この import ブロックだけ実ファイルの filepath になっている。Day 22 の完成コードには既に同じ import が入っているため、追記すると同じモジュールの二重 import になる。

**反証側が示した、成立しない根拠**

【引用の実在確認】day23_週次レポート.md:380-391 に指摘の引用は実在する。380「Day 21 で表示済みのテーブルを確認します。」381-382「以下のコードを2つ目のテーブルとして/貼り付けないでください。」384-391 が `// filepath: src/app/report/page.tsx` 付きの Table import ブロック、396 が「- Table 関連の6つのコンポーネントをインポートした」。

【同じファイルの前方で埋まっている（決定的）】同ファイル 292 行「### Step 1: プロジェクト統計の集計ロジック（5分）」の直後、298-301 に次の注記がある（逐語）:

```text
> Step 1・2 のコードは読み比べ用です。
> `overview`、`projectStats`、Table import、
> 「プロジェクト統計」カードを追加し直しません。
```

「Step 1・2 のコードは読み比べ用です」と Step 番号で範囲を明示し、さらに除外対象として `Table import` を名指ししている。指摘が問題視した 384-391 の import ブロックは、まさにこの「Table import」に該当する。読者は Step 1 冒頭でこの注記を読んでから Step 2 に入るため、384-391 を「貼らずに読むだけ」と判断できる。加えて 381-382 の「貼り付けないでください」が Step 2 内でも再掲されている。

【確認ポイントは Day 21 の作業で既に満たされている】day21_統計カードを表示.md:541-546（逐語）:

```text
// filepath: src/app/report/page.tsx
// テーブル部品（プロジェクト統計用）
import {
  Table, TableBody, TableCell,
  TableHead, TableHeader, TableRow,
} from '@/component/ui/table';
```

直後の day21:550「**確認ポイント**: - テーブル関連の部品をインポートした」。よって day23:396「Table 関連の6つのコンポーネントをインポートした」は Day 21 時点で達成済みの状態確認であり、Day 23 で新規に書けという指示ではない。

【二重 import が起きない裏取り】day22_グラフを表示.md:674-677 の完成版 import にも同じ 6 部品が既に含まれ、day23_週次レポート.md:1987-1990 の完成コードにも同一行がある。さらに day23:1993 は逐語で「今日足したのは `ArrowRight` と `Link` の2行です。」と述べており、Table import は Day 23 の追加分でないことが完成コード側でも明示されている。

【残る不整合は「詰まり」に至らない】384 行の filepath が実ファイル名のままで、410/442 行の「読み比べ用サンプル（実ファイルには対応しません）」と表記が揃っていないのは事実。ただし 298-301 の Step 単位の除外注記と 381-382 の禁止文、および day23:329「> 上記は Day 21 で追加済みのインポートです。まだ追加していない場合は追加してください。」が同じ趣旨を重ねて示しているため、読者が「書くのか書かないのか判断できず止まる」状態にはならない。表記統一は改善余地だが、詰まりとしては成立しない。

### 消えた件 4. 行473 「プロジェクト統計テーブルの表示を確認してください」と言われて画像を見るが、画像にはテーブルが写っていない。自分の画面の…

**逐語引用**

```text
スクリーンショット: プロジェクト統計テーブルの表示を確認してください。

![統計カードと円グラフ。プロジェクト統計テーブルはこの下に続く](./screenshots/report.png)
```

**初心者役が挙げた詰まり**

「プロジェクト統計テーブルの表示を確認してください」と言われて画像を見るが、画像にはテーブルが写っていない。自分の画面のテーブルが正しい形なのか確かめる手立てが無い。

**初心者役が挙げた不足**

確認せよと指示している対象（テーブル）が、貼られた画像の範囲外にあることを画像の説明文自身が認めている。テーブルが写った画像も、テーブル部分だけを見る手順も本文に無い。

**反証側が示した、成立しない根拠**

【引用の実在確認】
material/30days-curriculum/day23_週次レポート.md

- 471行: 「スクリーンショット: プロジェクト統計テーブルの表示を確認してください。」
- 473行: 「![統計カードと円グラフ。プロジェクト統計テーブルはこの下に続く](./screenshots/report.png)」
引用は逐語で実在する。

【画像の実物確認】
material/30days-curriculum/screenshots/report.png を実際に開いて描画した。写っているのは「レポート・統計」ヘッダ、統計カード4枚（タスク数9 / 完了率11% / 合計作業時間132.0h / 平均作業時間14.7h）、円グラフ2枚（ステータス別タスク / 優先度別タスク）まで。プロジェクト統計テーブルは画像下端で切れており写っていない。alt文の記述は事実と一致する。

【指摘が言う「不足」が同じファイルの直前で埋まっているか → 埋まっている】
テーブルの期待形は、471行の直前に本文テキストとして完全に提示されている。

- 420〜429行（Step 2 のコード）に見出し5列が逐語で載る:
  「<TableHead className="w-[200px]">プロジェクト</TableHead>」
  「<TableHead className="text-right">タスク数</TableHead>」
  「<TableHead className="text-right">完了</TableHead>」
  「<TableHead className="text-right">進捗</TableHead>」
  「<TableHead className="text-right">作業時間</TableHead>」

- 434〜436行:
  「**確認ポイント**:」
  「- `TableHeader` の中に `TableRow` と `TableHead` がある」
  「- ヘッダー5列を定義した」

- 462〜464行（471行の9行前）:
  「**確認ポイント**:」
  「- テーブルにプロジェクト名が並ぶ」
  「- 数値が `text-right` で右寄せ表示される」

- 447〜456行に各セルの中身（stat.name / stat.totalTasks / stat.completedTasks / stat.progress.toFixed(1)% / stat.totalTimeHours.toFixed(1)h）が載る。

よって読者は「列が5つ・見出しはプロジェクト/タスク数/完了/進捗/作業時間・数値は右寄せ・プロジェクト名が行に並ぶ」という照合基準を、画像を見ずに本文だけで持っている。自分の画面が正しい形かを確かめる手立ては存在する。

【alt文の役割】
473行のalt「プロジェクト統計テーブルはこの下に続く」は、画像がテーブルを写していない旨を隠さず告げ、テーブルは画面をさらに下へスクロールした先にあると読者へ伝えている。読者を誤誘導して止める文ではなく、位置を案内する文である。

結論: 照合基準（462〜464行の確認ポイントと420〜429行の列定義）が同一ファイルの直前で埋まっているため、読者はここで止まらない。
