# Gate3 初心者シミュレーション — Day 18 (2026-07-28)

- 対象: material/30days-curriculum/day18_コメント投稿.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 5 件 / 反証を生き延びた件数: 4 件

## 生き延びた詰まり

### 1. 行242 存在しない `});` を目印にし、直後のブロックがその `});` を増やす

**逐語引用**

```
ここから先の「（続き）」のブロックは、`comment.ts` の**末尾にある `});` の1行上**へ貼ります。ファイルの一番下に足すとルーターの外に出てしまい、英語のエラーで止まります。`});` は増やしません。
```

**何ができないか**

指示どおり `comment.ts` の末尾から `});` を探すが、直前に写経した `getByTaskId` のブロックは `}),` で
終わっていて `});` はまだ1つも無い。貼る目印が見つからないので手が止まる。さらに次に貼るブロック自体が
最終行に `});` を含んでいるため、「`});` は増やしません」とも食い違う。

**教材のどこが足りないか**

Step 0-3 の最初のコードブロックはルーターを閉じずに終わっているのに、次の指示が
「閉じ括弧が既に末尾にある」前提で書かれている。

**反証側が確かめた根拠**

- 行242と、直前のブロック（行212から230）を現物で確認した。最終行は行229の `    }),`。
- 行242より前で `});` に当たるのは行128だけで、これはスキーマ定義の閉じ。ここを目印にすると
  スキーマの内部へ手続きを差し込むことになる。
- 直後に貼らせるブロック（行244から263）の末尾が行262の `});` で、断り書きと食い違う。
- 行105と行112から、`comment.ts` は Day 18 でゼロから作る新規ファイルだと確認した。
  配布物のディレクトリにも comment 関連は無く、前日までに雛形が手元に無い。
- 完成形コード（行1051以降）は巻末で、Step 0 時点の読者を救わない。

### 2. 行645 「このあと閉じる」と書いた外側の箱が、Step の中で一度も開かれない

**逐語引用**

```
コメント欄を包む外側の箱はこのあと閉じるので、この時点ではまだ構文エラーが残ります。
```

**何ができないか**

「このあと閉じる」と書かれた外側の箱を、Step 4 と Step 5 を最後まで進めても開く指示も閉じる指示も出てこない。
構文エラーが残ったまま Step 7 の動作確認に到達し、画面がまったく表示されない。

**教材のどこが足りないか**

Step 3 が外側の箱を開くコードを一度も提示していないのに、閉じる約束だけを先に書いている。
実物は「完成コード全体」（行1421と行1468）まで読まないと現れない。

**反証側が確かめた根拠**

- 行645と行650を現物で確認した。先送りの約束が2回書かれている。
- Step 3 のコードブロック（行446から453、行473から483、行499から508、行520から538、
  行580から641）を全て確認したが、外側の箱を開く行は無い。
- 行1から475を `<div` と `</div>` で検索し、最初の出現が Step 3 の本文であることを確認した。
- Step 4 から Step 7（行666から1005）を通読したが、閉じる指示は無い。読者は行1004の `npm run dev` に
  構文エラーを抱えたまま到達する。
- 行438で配布ファイルにコメント欄が無いと明記されているので、足場でも前日でも埋まらない。

### 3. 行386 既存コードの説明と新規実装が同じ見出しで、貼ると二重宣言になる

**逐語引用**

```
Day 13 の Step 7 で配置した `TaskDetailDialog`
（`src/component/task/task-detail-dialog.tsx`）は、
内部で `api.task.getById` を呼んでいます。
```

**何ができないか**

「すでに呼んでいます」と書かれた直後に「実装」の見出しでコードブロックが出るので、書き足すのか
眺めるだけなのかを判断できない。書き足すと配布済みの同じ行と重なり、`taskDetail` が二重宣言になって
ビルドが止まる。

**教材のどこが足りないか**

既存コードの説明と、新しく書く実装とで同じ「実装」という見出しを使っている。
この Step は読むだけだという断りが無い。

**反証側が確かめた根拠**

- 行386から389、行391、行393から401を現物で確認した。
- 配布物 `scripts/_app-components/task/task-detail-dialog.tsx` の27から31行に同一の宣言があり、
  行12と行1049でこの足場が読者へ渡ることを確認した。貼ると二重宣言になる。
- 同ファイル内の他の「実装」見出し（行471、行696、行856）はいずれも書き足し指示なので、
  見出しは読むだけの合図にならない。行469の「確認しましょう」も直後が書き足しブロック。
- Step 2 内で「書き足す必要はありません」と書かれているのは行409だけで、対象は権限チェックに限定されている。
- 境界を明示する行438は Step 3 の冒頭にあり、Step 2 でのペーストを防げない。
- 確認ポイント（行411から414）にも、エラーが出た読者への説明が無い。

### 4. 行376 新規作成した手続きを「既存の」と呼んでいる

**逐語引用**

```
**確認ポイント**:
- 既存の `getByTaskId` を残して `create` を追加した
- 4 つのメソッドの名前と種別を把握した
```

**何ができないか**

Step 0 で `getByTaskId` と `create` を同時に新規作成したのに「既存の `getByTaskId` を残して」と言われ、
自分の作業が正しかったのか判断できない。

**教材のどこが足りないか**

配布済みファイルを有効化する構成だった頃の確認文が残っている。Step 0 の実態と食い違う。

**反証側が確かめた根拠**

- 行375から377と、Step 0 の行207、行209、行210、行314、行363を現物で照合した。
  教材自身が「今日ゼロから書く」と繰り返している。
- 教材全体を検索した結果、`getByTaskId` は Day 18 と Day 19 にしか出現せず、Day 01 から Day 17 には無い。
  読者の手元にも教材にも「既存」の根拠が無い。
- 本文中に comment.ts が配布されるという記述も無い。
- なお同じ確認ポイントの「4 つのメソッド」は、直前の表（行366から373）が4行で update と delete に
  「（Day 19）」と明記しているため、書き漏らしを疑わせる作りではない。この部分は成立しない。

## 反証で消えた件

1 件。消えた理由の型は判定の記録に残っていないため、ここには件数だけを書く。
