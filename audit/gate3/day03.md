# Gate3 初心者シミュレーション — Day 03 (2026-07-28)

- 対象: material/30days-curriculum/day03_GitHubに保存する.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 3 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

3 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行533 `git add` 群を実行して `git status --short` を見たとき、ここに書かれた5つと自分の画面が

**逐語引用**

```
いちばん下には、add しなかったものが `??` の行として残ります。配布 ZIP をそのまま使っていれば `?? .mise.toml` `?? .node-version` `?? doc/` `?? material/` `?? scripts/` が並びます。
```

**初心者役が挙げた詰まり**

`git add` 群を実行して `git status --short` を見たとき、ここに書かれた5つと自分の画面が一致するか確かめようとする。ところが同じ Step の中の出力例（471〜485行）には `?? material/` と `?? scripts/` の2つしか載っておらず、`.mise.toml` `.node-version` `doc/` は一度も出てこない。どちらが正しい期待値なのか分からず、add をやり直すべきか判断できない。

**初心者役が挙げた不足**

同じ Step 7 の中で、`git status --short` の出力例と「残るはずの `??` 5つ」の説明が食い違っている。`doc/` にいたっては Day 01 の配布物一覧（Day 01 の215〜222行）にも挙がっておらず、存在するかどうかも読者には確かめようがない。

**反証側が示した、成立しない根拠**

【引用の実在確認】day03_GitHubに保存する.md:533 に指摘どおりの文が実在する。逐語:
「いちばん下には、add しなかったものが `??` の行として残ります。配布 ZIP をそのまま使っていれば `?? .mise.toml` `?? .node-version` `?? doc/` `?? material/` `?? scripts/` が並びます。」
534: 「どれも add していないので正しい状態です。」
また 471〜484 の出力例には `?? material/` `?? scripts/` は在るが `.mise.toml` `.node-version` `doc/` は出てこない（477〜484: `?? biome.json` `?? docker-compose.yml` `?? prisma/` `?? src/app/dashboard/` `?? src/component/` `?? src/lib/` `?? material/` `?? scripts/`）。つまり「2つの一覧が食い違う」という事実関係そのものは正しい。

【しかし読者は止まらない — 同じファイルの前後で埋まっている】
(1) 487（1回目の出力例の直後、読者が最初に照合する地点）逐語:
「行数や並びが違っても心配いりません。`M` は履歴に入っているファイルを書き換えたという印、`??` はまだ一度も保存していないファイルという印です。」
→ 1回目の出力例は「自分の画面と一字一句そろえる対象ではない」と明示済み。読者はこの時点で照合を要求されていない。

(2) 516〜518（2回目の出力例の直前）逐語:
「行数が一気に増えて、数十行になります。`git add public src` が `src/` の下の
ファイルを1つずつ数えるためです。先頭のあたりは次のように見えます。」
→ 520〜525 の例は「先頭のあたり」の抜粋であると本文が宣言しており、全件一致を求めていない。

(3) 527〜531（533 の直前、この Step の合否基準そのもの）逐語:
「行が多くても、やりすぎではありません。数える必要もありません。
ここで確認したいのは次の2点だけです。

- `README.md` の `M` が左側（1文字目）に付いている
- どの行にも `.env` が出ていない」
→ 指摘は「どちらが正しい期待値か分からず、add をやり直すべきか判断できない」と言うが、やり直しの要否を決める判定条件は 528〜531 に2点だけと明記されている。`??` の並びは判定条件に入っていない。`.mise.toml` / `.node-version` / `doc/` が自分の画面に出ていても、どちらの2条件も壊れないので、読者は「正しい状態」と判断して 536 の次節へ進める。

(4) 512 逐語:「そのあと `git status --short` で、変更したファイルの行頭に `M` や `A` が付いた（ステージングされた）ことを確認します。今日の目的である `README.md` がステージングできていれば大丈夫です。」
→ ここでも合格条件は README.md の1点に絞られており、`??` 群の突合は要求されていない。

【`doc/` の存在について】
指摘は「`doc/` は Day 01 の配布物一覧にも挙がっておらず存在を確かめようがない」と言うが、day01_開発環境を整えて、初めてのアプリを動かそう.md:215 逐語:「見えていてほしい主なファイルとフォルダは以下の通りです。」——217〜221 は `README.md` / `material` / `scripts` / `scripts/scaffold-from-scratch.sh` / `.env.example` の5件で、明示的に「主な」と限定された部分列であって全量一覧ではない。よって「一覧に無い＝存在しない」とは読めない。加えてリポジトリ実体には `doc/` `.mise.toml` `.node-version` が実在する（ワークツリー直下の `ls -a` で確認）。読者が確かめる先は自分の端末の `git status --short` の実出力であり、533 の記述はそれと矛盾しない。

【結論】2つの出力例の粒度差は文章として整合していないが、その手前の 487・516〜518・527〜531 で「照合対象は2点のみ／数える必要なし／先頭のあたりの抜粋」と繰り返し明示されているため、読者が add のやり直し可否を判断できずに停止する経路は成立しない。

### 消えた件 2. 行574 コミット後の `git status -sb` に `?? .mise.toml` や `?? doc/` が出ていない

**逐語引用**

```
これらは GitHub へ送らないので、残っていて正常です。上に挙げた5つ以外の行が残っていたら、送るはずのファイルが送られていません。その行のファイルを `git add` してから、もう一度コミットしてください。
```

**初心者役が挙げた詰まり**

コミット後の `git status -sb` に `?? .mise.toml` や `?? doc/` が出ていない（＝5つに満たない）場合、あるいは6つ目が出た場合に、この判定基準ではどちらが「異常」なのか決められない。指示どおり不足分を `git add` すると、送らないと決めたはずの `material/` や `scripts/` まで送ってしまう。

**初心者役が挙げた不足**

完了判定を「上に挙げた5つ以外の行が残っていたら」という数と名前の一致に依存させているのに、その5つが直前の出力例（471〜485行）と一致していない。判定基準そのものが自己矛盾している。

**反証側が示した、成立しない根拠**

【引用の実在確認】
574行に指摘の引用は実在する（逐語）:
「これらは GitHub へ送らないので、残っていて正常です。上に挙げた5つ以外の行が残っていたら、送るはずのファイルが送られていません。その行のファイルを `git add` してから、もう一度コミットしてください。」

【1. 「上に挙げた5つ」は直前の573行が定義しており、471〜485行の例ではない】
573行（逐語）:
「`git status -sb` に残るのは、add しなかったものの `??` の行だけになります。配布 ZIP をそのまま使っていれば `?? .mise.toml` `?? .node-version` `?? doc/` `?? material/` `?? scripts/` が並びます。」
574行の「上に挙げた5つ」は、同じ段落の直前1行（573行）に列挙された5つを指す。指摘は判定基準の参照先を471〜485行と誤って結び付けている。判定基準は573→574で自己完結しており、自己矛盾していない。

【2. 533行にも同じ5つが同一表記で置かれており、教材内で一貫している】
533行（逐語）:
「いちばん下には、add しなかったものが `??` の行として残ります。配布 ZIP をそのまま使っていれば `?? .mise.toml` `?? .node-version` `?? doc/` `?? material/` `?? scripts/` が並びます。」
534行「どれも add していないので正しい状態です。」
add 直後（533行）と commit 直後（573行）で列挙が完全一致。読者は533行の時点で自分の画面と一度突き合わせ済みで、574行はその再掲にすぎない。

【3. 471〜485行は add 前の別時点の「たとえば」であり、網羅表ではないと本文が明示している】
468行（逐語）:「この時点では、たとえば次のような表示になります。」
487行（逐語）:「行数や並びが違っても心配いりません。」
つまり471〜485行のブロックは exhaustive な一覧として提示されていない。ここに `.mise.toml` `.node-version` `doc/` が出ていないことは、573行の列挙と矛盾する判定基準にはならない。

【4. 指摘が言う実害（material/ や scripts/ まで送ってしまう）は成立しない】
574行の指示対象は「上に挙げた5つ以外の行」。`material/` と `scripts/` は573行の5つに含まれているため、この指示に従っても add 対象にならない。指摘の実害シナリオは前提が誤っている。

【5. 「5つに満たない」場合に読者が止まるか】
574行の条件は「5つ以外の行が残っていたら」。4つしか出ない読者にとってこの条件は成立せず、追加操作は一切トリガーされない。次の575行「`git log` の1行目に、いま付けたメッセージが出ていれば成功です。」で完了判定できるため、読者は止まらない。

【6. 5つの実在確認】
作業ツリー直下（/Users/kouiso/ghq/kouiso/task-app-worktrees/gate3）の `ls -a` で `.mise.toml`(25B)、`.node-version`(3B)、`doc/`、`material/`、`scripts/` の5つすべての実在を確認。573行の列挙は現物と一致している。

### 消えた件 3. 行126 「ローカルの Git はもう始まっている」という前提を確かめようとするが、自分が Day 01 で作業したのは `scr

**逐語引用**

```
`scripts/scaffold-from-scratch.sh` は、空ディレクトリに公式の `create-next-app` を実行します。Day 01 の実行ログにも `Initialized a git repository.` と出ていました。
```

**初心者役が挙げた詰まり**

「ローカルの Git はもう始まっている」という前提を確かめようとするが、自分が Day 01 で作業したのは `scripts` と `material` が入った空でないフォルダだった。前提の説明が自分の状況と合わないため、`git status` が `not a git repository` になる側なのかどうかを判断できない。

**初心者役が挙げた不足**

Day 01 の Step 1（208行）が要求する作業場所は空ではないのに、Day 03 は「空ディレクトリに create-next-app を実行した」ことを Git 初期化済みの根拠として説明している。Day 01 側と同じ矛盾が引き継がれている。

**反証側が示した、成立しない根拠**

【引用の実在確認】day03_GitHubに保存する.md 126行は指摘どおり実在する。逐語:「`scripts/scaffold-from-scratch.sh` は、空ディレクトリに公式の `create-next-app` を実行します。Day 01 の実行ログにも `Initialized a git repository.` と出ていました。」

【却下理由1: 同一ファイルの前方で埋まっている】day03 の 64〜76行に Step 0 が置かれ、読者は「前提が自分に当てはまるか」を推論する必要がない。逐語:
- 64行「`create-next-app` で作ったプロジェクトは、最初から `git init` まで済んでいます。そのため、ほとんどの場合はこのまま次へ進んで問題ありません。」
- 66行「もしこのあと `git status` を実行して `not a git repository` と表示されたら、プロジェクトのルートで一度だけ次を実行します。」
- 69行「git init」
- 75〜76行「- `git init` が必要な場合だけ実行した / - すでに Git 管理されている場合は、このまま次へ進めると分かった」
つまり教材は「前提を理屈で判断せよ」ではなく「実際に `git status` を打って、出力で分岐せよ」と指示している。指摘が言う「`not a git repository` になる側なのかどうかを判断できない」は、実行前に判断する必要がそもそも無いため詰まりにならない。

【却下理由2: Step 1 でも再度回収されている】day03 148行 逐語:「もし `fatal: not a git repository (or any of the parent directories): .git` と出たら、Git 管理の外でコマンドを打っています。1行目の `pwd` の表示を見て、`task-app` のルートにいるか確かめてください。」同じ分岐が2箇所で拾われている。

【却下理由3: 前の日で読者の実測が一致している】day01_開発環境を整えて、初めてのアプリを動かそう.md 299行、Step 2「期待される出力」の中に逐語「Initialized a git repository.」がある。読者は Day 01 で自分の端末に同じ行が出たことを確認済みなので、day03 126行の「Day 01 の実行ログにも `Initialized a git repository.` と出ていました」は読者の実体験と一致する。

【残る瑕疵と、それが詰まりに至らない理由】day01 の 218行「- `scripts` と `material` が見える配布物ルートにいる」、251行「4. 空ディレクトリなら `create-next-app` を実行」より、作業場所が空でないのは事実で、day03 126行の「空ディレクトリに」は説明として不正確。ただしこれは背景説明の一文であり、読者に要求される操作（`git status` を打って分岐する）は上記 Step 0 で完結している。読者が手を止める箇所にはならない。
