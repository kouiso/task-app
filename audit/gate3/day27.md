# Gate3 初心者シミュレーション — Day 27 (2026-07-28)

- 対象: material/30days-curriculum/day27_プロジェクト詳細・アーカイブを実装しよう.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 4 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

4 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行33 冒頭で「書き写す必要はない」と読んだのに、Step 3 の見出しは「型と骨格を作る」、Step 4 は「2つのカードを配…

**逐語引用**

```text
この Day のコードブロックは**読んで見比べるためのもの**で、書き写す必要はありません。
> 「保存できない」「エラーが出る」といった記述も、書き写した場合の話として読んでください。
```

**初心者役が挙げた詰まり**

冒頭で「書き写す必要はない」と読んだのに、Step 3 の見出しは「型と骨格を作る」、Step 4 は「2つのカードを配置します」、実装ステップ一覧（100〜102行目）の「触るファイル」にも `project-detail-view.tsx` が入っている。手を動かすのか読むだけなのかが決まらず、Step 3 の最初のコードブロックで止まる。

**初心者役が挙げた不足**

同じ日の中で「書き写す必要はありません」（33行目）、「配布済みの `ProjectDetailView` に詳細表示を書き足す」（71行目）、Step 3・4 の作成指示が併存している。どれが正なのかを決める記述が無い。

**反証側が示した、成立しない根拠**

対象ファイル: material/30days-curriculum/day27_プロジェクト詳細・アーカイブを実装しよう.md

1) 引用の実在は確認できた。33行目「> この Day のコードブロックは**読んで見比べるためのもの**で、書き写す必要はありません。」、34行目「> 「保存できない」「エラーが出る」といった記述も、書き写した場合の話として読んでください。」

2) しかし「どれが正なのかを決める記述が無い」は成立しない。同じ冒頭ブロックの26〜30行目が、この Day の行動指針を明示している:
   26行目「> **この Day は Day 11・12 で作った機能の」
   27行目「> 統合確認です。** 以下のコードは完成状態との」
   28行目「> 照合用です。同じ名前の state、query、mutation、」
   29行目「> handler、Props を追加し直してはいけません。」
   30行目「> 不足がある場合だけ、該当箇所を補ってください。」
   すなわち「原則は照合（読む）、不足があるときだけ補う」という正が明記されている。71行目の「配布済みの `ProjectDetailView` に詳細表示を書き足す」は、この30行目の「不足がある場合だけ補う」と同じことを指しており、矛盾ではない。

3) 指摘が「止まる」と主張する Step 3 の最初のコードブロックの直前で、再度その場で行動が指定されている。
   289行目「### Step 3: `ProjectDetailView` の型と骨格を作る (8分)」
   293行目「Day 12 で作成済みのファイルを削除したり、」
   294行目「5 props の旧形式へ置き換えたりしないでください。」
   295行目「現在の8 props 契約を、次の完成形と照合します。」
   → 最初のコードブロック（299〜300行目「```ts」「// filepath: src/component/project/project-detail-view.tsx」）に入る直前に「照合します」と書かれており、読者は書き写すのではなく突き合わせると判断できる。

4) Step 4 も同様に、見出しの直後で行動が確定している。
   407行目「### Step 4: メンバー一覧とタスク一覧を表示する (10分)」
   411行目「Day 12 で実装済みなら、以下は読み比べだけ行います。」
   412行目「既存の権限制御やロール変更 UI を残してください。」

5) 同ファイル内の他箇所でも「写経しない」旨が個別に繰り返されている。725行目・768行目・792行目「**読み比べ用**: ここは写経しません。続けてコードを読み進めましょう。」、1696行目「このファイルは Day 01 の配布物に完成した形で入っています。Step 3 と Step 4 では説明のために一部を削った形を載せたので、ここで完成状…」

以上より、見出しの動詞（「作る」「配置します」）と実装ステップ一覧100〜102行目の「触るファイル」列だけを見ると一瞬紛らわしいが、各 Step の本文冒頭（295行目・411行目）と冒頭ブロック（26〜30行目）で「照合／読み比べ、不足時のみ補う」と明示されており、読者が止まる箇所ではない。

### 消えた件 2. 行387 「閉じタグが足りない状態」「ここまで書いて初めてファイルが保存できる状態になります」（397行目）と書いてあるので手を動…

**逐語引用**

```text
なお最後の `<div className="grid ...">` は開いたままです。閉じタグが足りない状態なので、この時点では保存してもエラーが出ます。続きを次のブロックで書きます。
```

**初心者役が挙げた詰まり**

「閉じタグが足りない状態」「ここまで書いて初めてファイルが保存できる状態になります」（397行目）と書いてあるので手を動かそうとするが、冒頭33行目では書き写す必要が無いと言われている。実際に貼ると配布済みの完成ファイルを壊すことになり、貼らないと本文の説明（保存できない→続きを書く）が成立しない。

**初心者役が挙げた不足**

Step 3 の本文が「読者が今このファイルを書いている」前提で書かれており、冒頭の「配布物に完成した形で入っている・書き写す必要はない」という前提と正面から食い違っている。

**反証側が示した、成立しない根拠**

対象ファイル: material/30days-curriculum/day27_プロジェクト詳細・アーカイブを実装しよう.md

1) 引用の実在確認（成立）

- 387行目（sed 330-430 の58行目）逐語: 「なお最後の `<div className="grid ...">` は開いたままです。閉じタグが足りない状態なので、この時点では保存してもエラーが出ます。続きを次のブロックで書きます。」
- 397行目（同68行目）逐語: 「…JSX は開いたタグが閉じていないとビルドで止まるため、ここまで書いて初めてファイルが保存できる状態になります。…」

2) 同じファイルの冒頭で既に埋まっている（＝指摘は成立しない）
冒頭の前提ブロックは32〜34行目で、指摘が言う「食い違い」をその場で明示的に解消している。

- 32行目: 「`project-detail-view.tsx` は Day 01 の配布物に完成した形で入っています。」
- 33行目: 「この Day のコードブロックは**読んで見比べるためのもの**で、書き写す必要はありません。」
- 34行目: 「「保存できない」「エラーが出る」といった記述も、書き写した場合の話として読んでください。」

34行目は、まさに387行目・397行目の「保存できない／エラーが出る」という表現を名指しして、「書き写した場合の話」という条件付きの記述であると読者に指示している。したがって読者が「手を動かして貼るべきか」で止まる余地はなく、配布済みファイルを壊す動機も生じない。

3) 補強（同ファイル内の他の箇所でも同趣旨が反復されている）

- 26〜30行目: 「**この Day は Day 11・12 で作った機能の統合確認です。** 以下のコードは完成状態との照合用です。同じ名前の state、query、mutation、handler、Props を追加し直してはいけません。不足がある場合だけ、該当箇所を補ってください。」
- 411行目（sed 330起点82行目）: 「Day 12 で実装済みなら、以下は読み比べだけ行います。」

以上より、指摘が主張する「Step 3 本文と冒頭前提の正面衝突」は、冒頭34行目の明示的な但し書きで既に埋まっている。

### 消えた件 3. 行332 Step 3 の指示どおり `?` を付けた形にすると、完成コード全体（1754〜1763行目）の「すべて必須」の形と一…

**逐語引用**

```text
末尾の3つに `?` が付いているのは、Day 11 の呼び出しがこの3つを渡さないためです。
必須にすると Day 11 で書いた記述が型エラーになります。
```

**初心者役が挙げた詰まり**

Step 3 の指示どおり `?` を付けた形にすると、完成コード全体（1754〜1763行目）の「すべて必須」の形と一致しない。逆に完成版に合わせて必須にすると、Step 3 が予告したとおり Day 11 の呼び出しが型エラーになる。どちらへ直せば正解なのか決められず、照合作業が止まる。

**初心者役が挙げた不足**

Step 3 の Props 定義（末尾3つがオプション）と、完成コード全体の Props 定義（8つとも必須・1766行目「完成版はすべて必須です」）が矛盾している。Day 11 の5引数の呼び出しをいつ8引数へ直すのかを指示する記述が無い。

**反証側が示した、成立しない根拠**

【引用の実在】day27_プロジェクト詳細・アーカイブを実装しよう.md 332-333行に逐語で存在:「末尾の3つに `?` が付いているのは、Day 11 の呼び出しがこの3つを渡さないためです。/ 必須にすると Day 11 で書いた記述が型エラーになります。」1755-1763行に完成版Props（8つとも `?` 無し）が実在。

【同ファイル内で埋まっている(1) — どちらが正解かの明示】1766行:「Step 3 では末尾3つに `?` を付けた形を載せましたが、完成版はすべて必須です。`?` は「渡さなくてもよい」という意味で、渡し忘れても型検査が通ってしまいます。権限に関わる値では、渡し忘れがそのまま「ボタンが出ない」という不具合になります。必須にしておけば、呼ぶ側が忘れた時点でエラーが出ます。」——Step 3 の形と完成版の食い違いを名指しで取り上げ、採用すべきは完成版（すべて必須）だと理由付きで宣言している。「どちらへ直せば正解なのか決められず」は成立しない。

【同ファイル内で埋まっている(2) — そもそも書き換えない指示】26-33行:「**この Day は Day 11・12 で作った機能の / 統合確認です。** 以下のコードは完成状態との / 照合用です。同じ名前の state、query、mutation、handler、Props を追加し直してはいけません。」「`project-detail-view.tsx` は Day 01 の配布物に完成した形で入っています。/ この Day のコードブロックは**読んで見比べるためのもの**で、書き写す必要はありません。/ 「保存できない」「エラーが出る」といった記述も、書き写した場合の話として読んでください。」
さらに Step 3 冒頭 293-295行:「Day 12 で作成済みのファイルを削除したり、/ 5 props の旧形式へ置き換えたりしないでください。/ 現在の8 props 契約を、次の完成形と照合します。」——「Step 3 の指示どおり `?` を付けた形に直す」という前提自体が本文で明示的に禁じられており、現行契約が8 props だと直前に書いてある。

【前の日で埋まっている — 5引数→8引数の移行時期】指摘は「Day 11 の5引数の呼び出しをいつ8引数へ直すのかを指示する記述が無い」と言うが、day12_メンバー追加.md 809-821行で8引数の呼び出しに置き換え済み:
「// Day 11 Step 9 の分岐内に配置する / <ProjectDetailView / projectDetail={projectDetail} / onBack={handleDetailClose} / onAddMemberClick={() => setMemberDialogOpen(true)} / onRemoveMember={handleRemoveMember} / onUpdateMemberRole={handleUpdateMemberRole} / onArchive={handleArchive} / canManageMembers={canManageMembers} / canArchive={canArchiveProject} />」
確認ポイント 824行:「- `ProjectDetailView` に8つのpropsを渡している」。day12 の 794行(該当節)でも `canManageMembers` / `canArchive` を props 表として定義済み（day12 の Step 2 表: 「| `onUpdateMemberRole` | ... |」「| `canManageMembers` | `boolean` | メンバー管理ボタンの表示可否 |」「| `canArchive` | `boolean` | アーカイブボタンの表示可否 |」）。
day11_プロジェクト編集・削除.md 983-993行の呼び出しは確かに5 props だが、同 950行に「この部品は Day 12 の機能まで含んだ形で配布されているので、渡す値が足りないと型エラーになり」と、Day 11 時点が暫定であることを明記している。

【残る瑕疵】day27 332-333行の「Day 11 の呼び出しがこの3つを渡さない」は Day 12 完了後の読者の実状（8 props）とズレた古い記述で、記述改善の余地はある。ただし読者は day27 で何も書き換えず（26-33行・293-295行）、正解は1766行で明示されているため、照合作業が止まることはない。

### 消えた件 4. 行344 Step 3 の指示に従って `project-detail-view.tsx` を書き進めると、`Button` と …

**逐語引用**

```text
<p>プロジェクトが見つかりません。</p>
      <Button variant="ghost" className="mt-4" onClick={onBack}>
        <ArrowLeft className="mr-2 h-4 w-4" />
```

**初心者役が挙げた詰まり**

Step 3 の指示に従って `project-detail-view.tsx` を書き進めると、`Button` と `ArrowLeft` が未定義でエラーになる。Step 4 でも `Card`・`Avatar`・`Badge`・`StatusBadge`・`PROJECT_MEMBER_ROLE_LABELS` などが同様に未定義になり、何を import すればよいか本文からは分からない。

**初心者役が挙げた不足**

Step 3 が示す import ブロック（300〜308行目）は `inferRouterOutputs`・`ProjectMemberRole`・`AppRouter` の型3つだけで、以降のコードが使う部品・アイコン・定数の import が含まれていない。部品側の import 一覧が出てくるのは、ずっと後ろの完成コード全体（1700行目以降）。

**反証側が示した、成立しない根拠**

【引用の実在確認】対象ファイル material/30days-curriculum/day27_プロジェクト詳細・アーカイブを実装しよう.md の 344-346行目は逐語で
344: `      <p>プロジェクトが見つかりません。</p>`
345: `      <Button variant="ghost" className="mt-4" onClick={onBack}>`
346: `        <ArrowLeft className="mr-2 h-4 w-4" />`
であり、引用は実在する。Step 3 の import ブロックが 299-309行目（301 `import type { inferRouterOutputs } from '@trpc/server';` / 302-304 `import type {` `  ProjectMemberRole,` `} from '@/lib/constant/roles';` / 305 `import type { AppRouter } from '@/server/api/root';`）で型3つのみ、という指摘の事実部分も正しい。

【それでも成立しない理由 — 同じファイルの前方（Step 3 より約270行前）で埋まっている】
同ファイル 26-34行目の「今日のゴールライン」ブロックが、Step 3 に入る前に読者へ次を明言している。
27: `> **この Day は Day 11・12 で作った機能の`
28: `> 統合確認です。** 以下のコードは完成状態との`
29: `> 照合用です。同じ名前の state、query、mutation、`
30: `> handler、Props を追加し直してはいけません。`
31: `> 不足がある場合だけ、該当箇所を補ってください。`
32: `> \`project-detail-view.tsx\` は Day 01 の配布物に完成した形で入っています。`
33: `> この Day のコードブロックは**読んで見比べるためのもの**で、書き写す必要はありません。`
34: `> 「保存できない」「エラーが出る」といった記述も、書き写した場合の話として読んでください。`

指摘の前提は「Step 3 の指示に従って `project-detail-view.tsx` を書き進めると」だが、32-33行目がその前提そのものを否定している。読者が触る対象ファイルではなく、Day 01 配布物として完成形（import 済み）が既に手元にある前提であり、Step 3〜5 のコードブロックは照合用の抜粋である。抜粋に import が無いのは、抜粋だからである。34行目は「エラーが出る」系の記述（387行目の「保存してもエラーが出ます」等）まで含めて「書き写した場合の話」と明示的に打ち消しており、読者がここで止まる導線が閉じられている。

配布物であることの本文記載も条件を満たす（足場の配布が本文 32行目に明記されている）。したがって指摘は成立しない。
