# Gate3 初心者シミュレーション — Day 20 (2026-07-28)

- 対象: material/30days-curriculum/day20_タスク検索機能.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 5 件 / 反証を生き延びた件数: 2 件

## 生き延びた詰まり

### 1. 行1984 触るファイルは5つなのに、完成コードが3つ分しかない

**逐語引用**

```
今日は3つのファイルを触りました。断片を貼り重ねる作業が続いたので、途中でどこへ貼ったか分からなくなった場合は、以下のコードを上から順に貼り付けて、各ファイルを置き換えてください。
```

**何ができないか**

途中で貼り先が分からなくなり「完成コード全体」で立て直そうとするが、Step 2 で直したサイドバーと
Step 8 で直したタスク一覧ページの全文がどこにも無く、直したはずの内容が正しいか確かめられない。

**教材のどこが足りないか**

今日実際に編集するのは5ファイル。`search.ts`、`loading.tsx`、`search/page.tsx` に加えて、
Step 2 の `src/component/layout/app-layout.tsx` と Step 8 の `src/app/task/page.tsx` を触る。
「今日作成・編集するファイル」表（行66から73）と完成コードの表（行1986から1990）が、そのうち3つしか挙げていない。

**反証側が確かめた根拠**

- 行1984と行1986から1990、行66から72を現物で確認し、いずれも3行だけであることを確認した。
- Step 2（行597以降）の行606から615と行623から629で `app-layout.tsx` を編集させている。
- Step 8（行1484以降）の行1526から1541、行1550から1558、行1568から1582、行1585から1589で
  `task/page.tsx` の既存行を置き換えさせている。まさに貼り先を見失う対象。
- 完成コード節（行1982から3426）の見出しを列挙し、`app-layout.tsx` と `task/page.tsx` の節が
  無いことを確認した。行2467の import 行は `search/page.tsx` の中身で、全文ではない。
- Day 13 は「今日書き換えた箇所だけを載せる」書式（行1209、行1212、行1425）を確立しているが、
  Day 20 はそれを踏襲していない。Day 13 の完成コードは Day 20 の変更を含まないので照合にも使えない。
- 本文に、これらのファイルが配布物として渡される記述は無い。

### 2. 行1001 Step 4 で、まだ定義していない `handleSearch` を使わせている

**逐語引用**

```
            {...form.register('keyword')}
            onKeyDown={(e) => {
              if (e.key === 'Enter')
                handleSearch();
            }} />
```

**何ができないか**

Step 4 の終わりで保存すると `handleSearch` が未定義でエラーになり、Step 4 の確認ポイント
「Enter キーで検索が実行される」を確かめられない。

**教材のどこが足りないか**

`handleSearch` の定義は Step 6。Step 5 の末尾（行1233）には「次の Step 6 で定義します」という断りがあるが、
先に使わせている Step 4 には断りが無く、確認ポイントだけが「Enter キーで検索が実行される」となっている。

**反証側が確かめた根拠**

- 行976、行1001から1004、行1064から1066を現物で確認した。
- 定義は行1249の Step 6 見出しの下、行1258。実装ステップ一覧（行94）でも Step 6 と確認した。
- 断りは Step 5 末尾の行1233から1234にしか無い。Step 4 の本文（行1009から1066）には同種の記述が無く、
  行1009から1010はむしろその場で動く前提の読み方を促している。
- Day 20 内の `handleSearch` の全出現（1003/1220/1233/1258/1396/2679/2777/2979/3180/3438/3440）を確認し、
  Step 4 より前に定義や注意書きを与えるものが無いことを確認した。行3440のトラブルシュート表も Step 4 より後ろ。

## 反証で消えた件

3 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行514 Step 0 の自己点検を終えた直後に、見出しの無い段落が始まり「同じ4行」と言われる。その4行がまだ出ていないので、何

**逐語引用**

```
完成版は同じ4行を dashboard・my-task・project・report・task の各フォルダにも置いています。今日は検索ページの1枚だけ作ります。ほかの画面にも同じ表示を出したくなったら、同じ内容のファイルをそのフォルダへ置いてください。
```

**初心者役が挙げた詰まり**

Step 0 の自己点検を終えた直後に、見出しの無い段落が始まり「同じ4行」と言われる。その4行がまだ出ていないので、何の話か分からないまま読み飛ばし、`loading.tsx` を作らずに Step 1 へ進む。

**初心者役が挙げた不足**

`loading.tsx` を新規作成させる手順に見出しが無く、実装ステップ一覧（86〜98行）にも項目が無い。さらに、まだ登場していないコードを「同じ4行」と先に指している。

**反証側が示した、成立しない根拠**

現物を開いて確認した結果、引用は実在するが「loading.tsx を作らずに Step 1 へ進む」という詰まりは成立しない。

【引用の実在確認】
- 514行: 「完成版は同じ4行を dashboard・my-task・project・report・task の各フォルダにも置いています。今日は検索ページの1枚だけ作ります。ほかの画面にも同じ表示を出したくなったら、同じ内容のファイルをそのフォルダへ置いてください。」— 逐語一致。
- 512行が `---`、513行が空行で、確かに 0-9 相当の見出しは無い（0-8 は 502行「#### 0-8. 最後に完成形を自己点検する」）。この点だけは指摘どおり。

【指摘が「不足」とする作成手順は、同じ画面内で埋まっている】
- 516行（引用のわずか2行下）: 「`src/app/search/loading.tsx` を新規作成します。ページと同じフォルダに `loading.tsx` を置くと、Next.js はそのページの読み込み中に自動でこれを表示します。」
- 518〜526行に該当のコードブロックが直後に出る:
  518 ```tsx
  519 // filepath: src/app/search/loading.tsx
  520 import { PageSkeleton } from '@/component/ui/page-skeleton';
  523 export default function Loading() {
  524   return <PageSkeleton />;
  525 }
  526 ```
  つまり「まだ出ていない4行」は 4行下で出る。読者が読み飛ばして未作成のまま Step 1（530行「### Step 1: 検索画面から使うAPIを確認する（3分）」）へ進むには、ファイル名を名指しした 516行とコードブロック全体を同時に飛ばす必要がある。
- 528行: 「中身は配布済みの `PageSkeleton` をそのまま返すだけです。この部品を使うのは今日がはじめてです。」— 依存部品の出所も明示済み。

【同じファイルの別箇所でも二重に埋まっている】
- 72行（冒頭「今日作成・編集するファイル」表）: 「| `src/app/search/loading.tsx` | ローディング画面（新規作成） |」— Step 一覧に項目が無くても、作成対象ファイルとして冒頭で宣言済み。
- 1989行（「完成コード全体」表）: 「| `src/app/search/loading.tsx` | 検索ページへ移動している間の仮表示 | Step 0 |」— Step 0 所属であることが明記されている。
- 2418行「### `src/app/search/loading.tsx`」〜2433行に全文が再掲され、2433行で「ファイル名が `loading.tsx` であることに意味があります。」と再度説明されている。

【結論】
残るのは「見出しが無い」「『同じ4行』が4行だけ先行参照になっている」という体裁上のぎこちなさのみで、作成指示・ファイルパス・コード・再掲のいずれも本文に揃っている。読者がそこで実際に止まる根拠は無い。

### 消えた件 2. 行1827 直前まで JSX を貼り足していたので、この `useState` と `useMutation` を JSX の中に書

**逐語引用**

```
タスク削除機能を追加します。削除確認ダイアログの state と mutation を定義します。
```

**初心者役が挙げた詰まり**

直前まで JSX を貼り足していたので、この `useState` と `useMutation` を JSX の中に書いてしまい、エラーになる。

**初心者役が挙げた不足**

貼り先が「`SearchPageContent` 内、return 文より前」と書かれていない。Step 6 では「`SearchPageContent` 内、return 文より前に追加します」（1253行）と明示していたのに、ここでは省かれている。直前の文脈が JSX なので、続きだと読める。

**反証側が示した、成立しない根拠**

【引用の実在確認】material/30days-curriculum/day20_タスク検索機能.md:1827 に「タスク削除機能を追加します。削除確認ダイアログの state と mutation を定義します。」が実在。直前 1805-1820 が JSX 断片（`{searchResults.totalCount === 0 && (` … `)}`）で、1827 の段落に「SearchPageContent 内、return 文より前」の明示が無いのも事実。同ファイル 1253「`SearchPageContent` 内、return 文より前に追加します。」との非対称も事実。

【しかし前の日で埋まっている】読者は day01 から順に読む前提。
- day08_サイドバーを完成させよう.md:336「状態と問い合わせを、関数の先頭でまとめて宣言しています。`useState` や `useQuery` のようなフックは、毎回同じ順番で呼ばれることを前提に作られています。`if` の中や `return` より後ろに置くと順番が変わり、React が値を取り違えてエラーになります。並べる場所には理由があります。」
- day08:338「`logoutMutation` を関数の先頭で用意しておくのは、`useMutation` もフックだからです。フックは毎回同じ順番で呼ばれる前提なので、`onClick` の中には書けません。」
- day08:884「関数の先頭が道具をそろえる場所です。…`if` の中や `return` の後ろへ動かすと React が値を取り違えます。」
つまり「useState / useMutation は関数の先頭、return より後ろ（＝JSX の中）に置くとエラー」は day08 で明文化済みで、指摘が言う「JSX の中に書いてエラーになる」ケースそのものが先に説明されている。

【反復強化も済んでいる】同じ「関数の先頭に置く」指示が day09:379「`ProjectPageContent` 関数の先頭（`return` の前）に以下を追加します。」/ day11:514「`ProjectPageContent` 関数の先頭にある state 一覧（`const [showArchived, ...]` の直下）に追加してください。」/ day12:1168「state を `ProjectPageContent` 関数の先頭に追加してください。」/ day13:521「次に `TaskPageContent` 関数の先頭（`return` の前）に以下を追加します。」と、day20 到達までに4日ぶん繰り返されている。

【同ファイル内の手がかり】1830-1831 のコードブロック先頭が「// filepath: src/app/search/page.tsx」「// 削除確認state」で、直前の JSX ブロック（1806-1807「// プロジェクトカード内容」等）とは別ユニットであることが示されている。さらに 1862「削除確認ダイアログのJSXです。検索結果の下に配置します。」で、JSX を貼るのはこの次のブロックだと直後に明示される。

以上より、貼り先の判断に必要な知識は day08 で明示的に、day09/11/12/13 で反復的に与えられており、読者がここで止まるとは言えない。

### 消えた件 3. 行1862 「検索結果の下」がどの閉じかっこの後なのか決められない。直前に書いた `)}`（1819行）の内側か外側かで手が止まる。

**逐語引用**

````
削除確認ダイアログのJSXです。検索結果の下に配置します。

```typescript
// filepath: src/app/search/page.tsx
// 削除確認ダイアログ
<DeleteConfirmDialog
  open={deleteTaskConfirm.open}
````

**初心者役が挙げた詰まり**

「検索結果の下」がどの閉じかっこの後なのか決められない。直前に書いた `)}`（1819行）の内側か外側かで手が止まる。

**初心者役が挙げた不足**

検索結果は `{isLoading ? ... : shouldSearch && searchResults ? ... : ...}` という三項演算子の中にあり、「下」に当たる場所が複数ある。完成コード側にも、このダイアログがどの階層に入るかを示す全文が離れているため、その場では確認できない。

**反証側が示した、成立しない根拠**

確認したファイル: /Users/kouiso/ghq/kouiso/task-app-worktrees/gate3/material/30days-curriculum/day20_タスク検索機能.md

1) 引用の実在確認（一致）
1862行: 「削除確認ダイアログのJSXです。検索結果の下に配置します。」
1864-1867行:
```typescript
// filepath: src/app/search/page.tsx
// 削除確認ダイアログ
<DeleteConfirmDialog
```
引用は実在する。

2) 同じStep内のインデントで階層は決まっている
直前ブロック（1805-1820行）の生インデントを確認した:
1808行: `    {searchResults.totalCount === 0 && (`（4スペース）
1813行: `  </div>`（2スペース）
1814行: `) : (`（0スペース）
1819行: `)}`（0スペース）
そしてダイアログ側の1867行は `<DeleteConfirmDialog`（0スペース）、1868行 `  open={deleteTaskConfirm.open}`（2スペース）。
つまり三項演算子を閉じる `)}` と `<DeleteConfirmDialog` が同じ0カラムに並んでおり、「`)}` の内側か外側か」はインデントで外側（兄弟要素）と読める。指摘が言う「内側か外側かで手が止まる」状態にはならない。

3) 同じファイル内の完成版に全文の階層がある（「離れている」＝別ファイルではない）
3374行: 「**JSX — 削除の確認画面と閉じタグ**:」
3369行: `        )}`（8スペース）
3379行: `        <DeleteConfirmDialog`（8スペース）
3395-3398行:
```
      </div>
    </AppLayout>
  );
}
```
完成版でも `)}` と `<DeleteConfirmDialog` が同じ8スペースで並び、その後 `</div>` → `</AppLayout>` と閉じる。階層は一意に確定する。

4) 散文でも閉じ順を明言している
3403行: 「閉じタグは `</div>`、`</AppLayout>`、`);`、`}` の順で、開いた順の逆になっています。」

以上より、Step本文のインデント・同一ファイル内完成版（3376-3399行）・3403行の散文の3か所で埋まっている。読者が配置を決められず止まる根拠は無い。
