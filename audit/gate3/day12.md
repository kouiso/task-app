# Gate3 初心者シミュレーション — Day 12 (2026-07-28)

- 対象: material/30days-curriculum/day12_メンバー追加.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 6 件 / 反証を生き延びた件数: 5 件

## 生き延びた詰まり

### 1. 行1229 目印どおりに貼ると、メンバー削除のダイアログが一覧側に入る

**逐語引用**

```
JSX 内の `</div>` 閉じタグの後、`</AppLayout>` の直前（`AppLayout` 内の一番最後）に `DeleteConfirmDialog` を配置します。
```

**何ができないか**

この説明に当てはまる場所は一覧画面側の return の末尾で、そこには Day 11 で置いたプロジェクト削除用の
`DeleteConfirmDialog` がすでにある。そこへ貼ると、メンバー削除の確認ダイアログは詳細画面に存在しないことになり、
Step 8 のシナリオ2「メンバーの削除ボタンをクリック、確認ダイアログが表示される」が何度やっても再現しない。

**教材のどこが足りないか**

完成版（行2739から2755）では、このダイアログは `if (projectIdParam && selectedProject)` の詳細画面分岐の中、
メンバー追加ダイアログの直後に置かれている。Step 6 の位置説明はその分岐に触れていない。

**反証側が確かめた根拠**

- 行1229と行1233のコメントを現物で確認した。
- 完成版の一覧分岐末尾（行2852から2873）を確認し、`</div>` の後で `</AppLayout>` の直前に当たるのは
  一覧側だけで、そこには Day 11 のダイアログが既にあることを確認した。
- 詳細分岐（行2626以降）では `</AppLayout>` の直前に来るのが `</Dialog>` であり、目印が一致しない。
- 行922は Step 4 の追加ダイアログについての記述で、Step 6 の削除ダイアログには触れていない。
- 分岐内である理由の説明（行2758）は完成コード節にあり、Step 8 の動作確認より後ろで、読者は未読。
- つまずきポイント表（行2907から2913）にもこの症状の項目は無い。

### 2. 行591 前日で追加済みの import を、もう一度追加させている

**逐語引用**

```
`ProjectDetailView` コンポーネントのインポートを追加します。

// filepath: src/app/project/page.tsx
// ProjectDetailViewのインポートを追加
import { ProjectDetailView } from
  '@/component/project/project-detail-view';
```

**何ができないか**

指示どおり import を追加すると、Day 11 Step 9 で書いた同じ import と重複し、同名の宣言があるという
エラーで起動しなくなる。

**教材のどこが足りないか**

Day 11 の行958から965でまったく同じ import 文を追加済みだが、Day 12 Step 1 には「Day 11 で追加済みなら不要」と
いった断りが無い。同じ日の Step 1 では handleDetailClose や projectDetail について
「Day 11 の仮定義を削除してから」と丁寧に書いているので、import だけ扱いが抜けていることに気付けない。

**反証側が確かめた根拠**

- 行588から596と、Day 11 の行958から972を現物で照合した。同一ファイルへの同名 named import の重複になる。
- 行600と行602は handler について重複回避を明示しているのに、直前の import には同種の断りが無い。
- Day 12 全文を「すでに」「既に」「重複」「再掲」で検索したが、import が Day 11 で追加済みである旨の
  記述は1件も無い。
- 行2242は「Step 1 で足したのが `ProjectDetailView`」と書いており、Day 11 の既存 import と食い違う。
- 対象は配布部品ではなく読者が書く `page.tsx` の import 行なので、足場では埋まらない。

### 3. 行908 `Label` の import が既にあるのに、新規追加として並べている

**逐語引用**

```
} from '@/component/ui/dialog';
import { Label } from '@/component/ui/label';
import {
  Select,
```

**何ができないか**

Step 4 の指示どおり `Label` を import すると、Day 09 以来 `page.tsx` にある同じ import と重複して
エラーになる。どちらを消すのか書かれていない。

**教材のどこが足りないか**

Day 11 の完成版（行1727）のとおり、`page.tsx` には `Label` の import がすでに存在する。
Step 4 は Dialog 系や Select 系と同じ扱いで `Label` も新規追加として並べており、既存分に触れていない。

**反証側が確かめた根拠**

- 行895から920を現物で確認した。確認ポイントも新規追加として書かれている。
- Day 09 の行695から705と行1184、Day 11 の行1727で、同じファイルに `Label` の import が
  既にあることを確認した。
- Day 12 内の `ui/label` の出現は行908と行2238の2箇所だけで、行2240は「Step 4 で足したのが
  `Dialog` 系と `Label` です」と書いており、誤りを補強している。
- 同じ Day 12 は state と handler の重複については行602、行655、行844、行864で明示している。
  `Label` の import にだけ同じ配慮が無い。

### 4. 行812 既にある `ProjectDetailView` タグを「配置する」と書いている

**逐語引用**

```
// Day 11 Step 9 の分岐内に配置する
<ProjectDetailView
  projectDetail={projectDetail}
  onBack={handleDetailClose}
```

**何ができないか**

Day 11 Step 9 の分岐にはすでに `<ProjectDetailView ... />` が置いてある。追加で貼るのか、
既存を書き換えるのかが分からず、そのまま貼ると詳細画面に同じビューが2つ並ぶ。

**教材のどこが足りないか**

今日の変更は既存タグへ `onUpdateMemberRole`、`canManageMembers`、`canArchive` の3つを足すことだが、
本文は「配置します」と新規配置の言い方をしている。Day 11 側の同じ箇所と見比べないと差分だと気付けない。

**反証側が確かめた根拠**

- 行804から812を現物で確認した。削除、置き換え、差し替えを指示する語が1つも無い。
- Day 11 の行980から993で、同じ場所に既に配置済みであることを確認した。
- Day 12 は他の Day 11 残骸（行600、行655、行842から844）については全て削除を明示しており、
  JSX タグだけがその扱いから漏れている。
- 確認ポイント（行826から830）は「8つのpropsを渡している」で、タグが1つであることを確認させない。
  タグが1つだと分かる完成版の説明（行2641）は章末で、Step 2 で詰まる時点の救いにならない。
- 配布部品の props 定義（`project-detail-view.tsx` の31から42行）で今日足す3つは optional のため、
  古いタグを残したまま貼り足しても型エラーが出ず、静かに二重描画になる。

### 5. 行2322 どの Step でも指示していない変更が、完成コードにだけ入っている

**逐語引用**

```
    // showArchived が true のとき isArchived フィルターを外して進行中・アーカイブ両方を取得する
    isArchived: showArchived ? undefined : false,
```

**何ができないか**

完成コードで `page.tsx` を置き換えたあと、自分が Day 09 から Day 11 で書いた `isArchived: showArchived` と
違うことに気付くが、どの Step で変えるよう言われたのかを探しても見つからない。
写し間違いかどうか判断できない。

**教材のどこが足りないか**

この書き換えを指示している Step が Day 09 から Day 12 のどこにも無い。行2798から2802のタスク集計
（CANCELLED を母数から外すループ）も、いずれの Step でも触れられていない変更が完成コードにだけ入っている。

**反証側が確かめた根拠**

- 行2316から2322と行2792から2802を現物で確認した。
- Day 09 から Day 11 を検索した結果、渡し方は一貫して `isArchived: showArchived,`（Day 09 の行391と行1206、
  Day 10 の行1510、Day 11 の行1797）で、三項演算子の形は0件。
- `CANCELLED` は Day 09 から Day 12 のうち Day 12 の行2801にしか出現しない。読者が書いた形は
  Day 09 の行1290から1293と Day 11 の行2091から2097で、Day 11 は「件数の数え方にも手を入れていません」と
  明記している。
- Day 12 の Step 範囲（行580から1328）に、この2つを書き換えさせる指示は無い。行640は
  「Day 09実装済み」として変更なし扱いにしている。
- 行1388の完成コード節の導入文は「自分が書いた断片の集合」として提示しており、
  Step で触れていない変更が混ざる旨の断りは無い。

## 反証で消えた件

1 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行1226 確認ポイントに従って「Step 2 で書いた console.log 入りの handleRemoveMember」を探

**逐語引用**

```
- Step 2 の仮実装（`console.log` の版）が削除されている
- 直接 `mutate` を呼ばず、まず確認ダイアログを開いている
```

**初心者役が挙げた詰まり**

確認ポイントに従って「Step 2 で書いた console.log 入りの handleRemoveMember」を探すが、Step 2 にはコードの追加が無く（706行に「コードの追加は不要です」と明記）、console.log を含む版もどこにも存在しない。何を消せばよいのか分からず止まる。

**初心者役が挙げた不足**

実際に消す対象は Day 11 Step 9 で置いた `const handleRemoveMember = (_userId: string) => {}`（空実装、944〜946行）で、console.log は使っていない。1185行の「Step 2 で追加した仮実装を削除」も出所の Step 番号が違う。

**反証側が示した、成立しない根拠**

【引用の実在確認】day12_メンバー追加.md:1226 に逐語で「- Step 2 の仮実装（`console.log` の版）が削除されている」、1185 に「まず **Step 2 で追加した仮実装を削除**してから、以下の本実装を書いてください。」、1216 のコードブロック直前コメントに「// Step 2の仮実装を削除してここに書き換える」が実在する。指摘どおり console.log 版はファイル内に存在せず（grep で day12 に console.log 一致ゼロ）、実際の削除対象は day11_プロジェクト編集・削除.md:944 の `const handleRemoveMember = (_userId: string) => {` （中身は「// Day 12 Step 6 で本実装に置き換え」のみの空実装）。よって「console.log の版」「Step 2 で追加」は文言として不正確。

【しかし読者は止まらない】同じ day12 の Step 2 内、706〜712 行で削除対象が名指しで説明済み。
- 706行: 「Day 11 Step 9 で `handleRemoveMember` の仮定義（何もしない空実装）を追加済みです。Step 6 で本実装に差し替えます。**ここでは仮定義がすでにあることを確認するだけで、コードの追加は不要です。**」
- 708行: 「> Day 11 の仮定義 `const handleRemoveMember = (_userId: string) => {}` は、**Step 6 で削除して本実装に**書き換えます。同名の `const` を2つ書くとエラーになるため、Step 6 では「Day 11 の仮実装を削除 → 本実装を書く」の順で進めてください。」
- 712行: 「- `handleRemoveMember` は仮実装であり、Step 6 で削除することを覚えておく」

さらに前日の day11:933 でも「Step 6 で `handleRemoveMember` を本実装したときに、それぞれこの仮定義を削除してください。」と予告済み。
ファイル全体で `handleRemoveMember` の定義は day11:944 の1個だけ（day12 の grep 一致は 706/708/712/818/1217/2633/2758 で、定義は 1217 の本実装のみ）なので、削除対象は一意に決まり、探し当てられずに止まる状況にならない。

判定: Step 番号と「console.log の版」という表現の誤りは事実だが、同一ファイル 706〜712 行および前日 day11:933 で削除対象が逐語で示されているため、読者が止まる指摘としては成立しない。
