# Gate3 初心者シミュレーション — Day 19 (2026-07-28)

- 対象: material/30days-curriculum/day19_コメント編集・削除.md
- やり方: 教材の本文だけを読む初心者役が通し、詰まりを行番号つきで挙げた。
  挙がった各件を、別の判定者が「成立しない」を既定の立場として現物の行で反証した。
- 挙がった件数: 4 件 / 反証を生き延びた件数: 0 件

## 生き延びた詰まり

なし

## 反証で消えた件

4 件。挙がった内容と、反証側が示した根拠を全件残す。

### 消えた件 1. 行396 Day 18 で作った task-detail-dialog.tsx を開き、この編集・削除ボタンの塊をどこへ貼るか探す

**逐語引用**

````
取得した `session?.user?.id` を使って、
各コメントの作者と一致するときだけ
編集・削除ボタンを表示します。

```typescript
// filepath: src/component/task/task-detail-dialog.tsx
// 本人チェックで編集・削除ボタンを表示
{comment.userId === session?.user?.id && (
````

**初心者役が挙げた詰まり**

Day 18 で作った task-detail-dialog.tsx を開き、この編集・削除ボタンの塊をどこへ貼るか探すが、貼り先が特定できず手が止まる。

**初心者役が挙げた不足**

貼り付け先の指定が一切ない。しかも完成コード（1418〜1426行）を見ると、日時の `<span>` とボタンを包む `<div className="flex items-center gap-2">` を新しく追加する必要があるのに、その追加はどの Step にも書かれていない。ステップだけを順に写経した人は、この囲みの無い状態になり、`justify-between` で日時とボタンが画面の両端へ離れる。

**反証側が示した、成立しない根拠**

現物: /Users/kouiso/ghq/kouiso/task-app-worktrees/gate3/material/30days-curriculum/day19_コメント編集・削除.md

1. 引用の実在確認（成立）
- 391-393行: 「取得した `session?.user?.id` を使って、\n各コメントの作者と一致するときだけ\n編集・削除ボタンを表示します。」
- 395-398行: 「```typescript」「// filepath: src/component/task/task-detail-dialog.tsx」「// 本人チェックで編集・削除ボタンを表示」「{comment.userId === session?.user?.id && (」
指摘の引用はそのとおり存在し、Step 3 の本文に「日時の span の直後へ」といった貼り先の指定は無い。ここまでは指摘の観測どおり。

2. しかし同ファイル別箇所で埋まっている（指摘は不成立）
(a) 貼り先が分からなくなった場合の回復手順が明記されている。
801行（「## 完成コード全体」799行の直下）逐語:
「今日は2つのファイルを触りました。Step 1 でサーバー側へ編集と削除の手続きを足し、Step 2 から Step 6 でタスク詳細ダイアログへ本人チェックと編集モードを組み込んでいます。断片を貼り重ねる作業が続いたので、途中でどこへ貼ったか分からなくなった場合は、以下のコードを上から順に貼り付けて、各ファイルを置き換えてください。1つのファイルが複数のブロックに分かれている場合は、そのファイルの見出しの下にあるブロックを、出てくる順につなげたものが全文です。」
= 「貼り先が特定できず手が止まる」という詰まりに対して、教材が明示的に出口を用意している。読者は 1031行「### `src/component/task/task-detail-dialog.tsx`」以下の全文で置き換えられる。

(b) 指摘が「どの Step にも書かれていない」と言う囲み `<div className="flex items-center gap-2">` は、完成コード側で追加され、かつ追加理由まで説明されている。
1414-1418行: 「<div className="flex items-center justify-between">」「<span className="font-medium">」…「<div className="flex items-center gap-2">」
1426行 逐語:
「Day 18 では日時が右端に1つ置かれていました。今日はその右へボタンが2つ並ぶため、日時とボタンをまとめる箱を1つ増やしています。`gap-2` で間隔を空けているので、日時とアイコンがくっついて読みにくくなることはありません。この箱が無いと、`justify-between` が日時とボタンを画面の両端へ引き離します。」
= 指摘が予測した「justify-between で日時とボタンが両端へ離れる」という症状そのものを、教材が名指しで先回りして説明している。

3. 結論
貼り先不明・囲み div 未記載という2点は、いずれも同一ファイル 801行と 1414-1426行で埋まっている。読者は完成コードで自ファイルを置き換えられ、かつ囲みの必要性と省いた場合の見た目の崩れも読める。よって survives: false。

### 消えた件 2. 行476 「表示します」としか書かれていないので、この三項演算子を既存の `<p>{comment.content}</p>` と

**逐語引用**

```
三項演算子で「編集中のコメントか」を判定し、
編集中はテキストエリアとボタン、通常時はテキストを
表示します。`? (` から `)}` までが 1 つの式です。
```

**初心者役が挙げた詰まり**

「表示します」としか書かれていないので、この三項演算子を既存の `<p>{comment.content}</p>` と入れ替えるのか、その下に足すのかが決められない。

**初心者役が挙げた不足**

Day 18 で書いた本文表示の `<p>` を置き換える指示が無い。追加してしまうと本文が二重に出る。Day 20 では「上の `<TaskCard ... />` を以下に**置き換えて**ください」と明示している箇所があるのに、ここには同じ明示が無い。

**反証側が示した、成立しない根拠**

【引用の実在確認】material/30days-curriculum/day19_コメント編集・削除.md の476-478行に指摘の逐語が実在する:「三項演算子で「編集中のコメントか」を判定し、/ 編集中はテキストエリアとボタン、通常時はテキストを / 表示します。`? (` から `)}` までが 1 つの式です。」。直後の480-506行が該当コードブロック。

【不足が埋まっている根拠1: コードブロック自身が置き換えを自明にしている】day19 の502-505行が
```
) : (
<p className="text-muted-foreground">
{comment.content}</p>
)}
```
であり、この else 側の `<p>` は Day 18 で書いた行と逐語で同一である（material/30days-curriculum/day18_コメント投稿.md:1464 `<p className="text-muted-foreground">{comment.content}</p>`）。つまり貼るコードの中に既存の `<p>` がそのまま含まれており、「下に足す」と読むと同じ `<p>` が2つ並ぶことがコードブロックの見た目だけで分かる。追加ではなく既存 `<p>` を三項演算子で包む差し替えであることが、コード自体から読み取れる。

【不足が埋まっている根拠2: 同ファイル内の完成版セクションが明示的に全文置き換えを指示している】day19:801「今日は2つのファイルを触りました。…断片を貼り重ねる作業が続いたので、途中でどこへ貼ったか分からなくなった場合は、以下のコードを上から順に貼り付けて、各ファイルを置き換えてください。…上から順に読めば、書いた断片が1つのファイルへどう収まったかを確かめられます。」
その完成版の該当箇所が day19:1475-1509 で、
1475: `{editingCommentId === comment.id ? (`
1507: `) : (`
1508: `<p className="text-muted-foreground">{comment.content}</p>`
1509: `)}`
となっており、`<p>` が三項演算子の else 側に1つだけ存在する最終形が示されている。1516行にも「`? (` から `)}` までが1つの式です。編集中なら上のテキストエリア、そうでなければ下の本文が描かれます。」と再説明がある。二重表示になる貼り方が最終形と食い違うことは、この完成版と突き合わせれば読者が自力で解消できる。

【Day 20 との比較について】指摘は Day 20 に「置き換えてください」の明示があると言うが、day19 側は「完成版の全文置き換え」という別の救済経路（801行）を同ファイル内に持っており、読者が止まったまま進めなくなる状態にはならない。

以上より、読者が本当にそこで止まるとは言えないため survives: false。

### 消えた件 3. 行493 Step 4 まで写経して保存すると、`handleSaveEdit` と `updateCommentMutation

**逐語引用**

```
<Button size="sm"
        onClick={() => handleSaveEdit(comment.id)}
        disabled={
          !editCommentForm.watch('content').trim()
          || updateCommentMutation.isPending}>
```

**初心者役が挙げた詰まり**

Step 4 まで写経して保存すると、`handleSaveEdit` と `updateCommentMutation` が未定義でエラーになり、画面が出ないまま次へ進めない。

**初心者役が挙げた不足**

どちらも Step 5 で定義するものを Step 4 で先に使わせている。Step 3 には「ここで使っている `handleStartEdit` と `handleDeleteComment` は、このあとの Step で定義します」（426行）という断り書きがあるのに、Step 4 には同じ断りが無い。

**反証側が示した、成立しない根拠**

【引用の実在確認】指摘の引用は実在する。493行「        onClick={() => handleSaveEdit(comment.id)}」、496行「          || updateCommentMutation.isPending}>」。定義位置も指摘どおり後方で、531行「const updateCommentMutation =」（Step 5、521行「### Step 5: 編集 API の呼び出しを確認する（5分）」）、568行「const handleSaveEdit =」（同じくStep 5内）。

【同一ファイル内で埋まっている】Step 3 の断り書き（426-427行）が Step 4 到達時点でまだ有効なまま残っている。
426行「ここで使っている `handleStartEdit` と `handleDeleteComment` は、」
427行「このあとの Step で定義します。定義するまでこの画面は表示できません。」
ここで名指しされた2つのうち `handleDeleteComment` の定義は 639行「const handleDeleteComment =」＝ Step 6（599行「### Step 6: 削除処理を確認する（5分）」）にある。つまり Step 4 を写経し終えた時点でも、Step 3 で宣言された「この画面は表示できません」という状態は一度も解除されていない。読者は Step 3 の時点で「Step 6 まで画面は出ない」と告げられており、Step 4 で画面が出ないことは教材が事前に予告した想定内の状態である。

補強: 417行「        handleDeleteComment(comment.id)}>」が Step 3 の JSX にあり、この未定義参照は Step 4 終了時点でも解消していない。よって「Step 4 で新たに詰まる」という主張は成立しない。実際の画面確認は 744行「### Step 7: 動作確認（3分）」で、全ハンドラー定義後に置かれている。

判定: 指摘が言う不足は同一ファイルの426-427行で埋まっているため survives: false。

### 消えた件 4. 行76 確認ダイアログの作り方を復習しようと Day 15 を開くが、そこに DeleteConfirmDialog の作成が見

**逐語引用**

```
| DeleteConfirmDialog（Day 15 の復習） | — | 削除確認ダイアログ | 「本当に消すか」の確認 |
```

**初心者役が挙げた詰まり**

確認ダイアログの作り方を復習しようと Day 15 を開くが、そこに DeleteConfirmDialog の作成が見つからず探し回る。

**初心者役が挙げた不足**

同じファイルの 272行「Day 11 で作ったものをそのまま読み込みます」、649行「`DeleteConfirmDialog` は Day 11 でタスク削除にも使った再利用コンポーネントです」と食い違う。概念表だけ Day 15 になっている。

**反証側が示した、成立しない根拠**

【引用の実在】day19_コメント編集・削除.md:76 に指摘どおりの行が実在する: 「| DeleteConfirmDialog（Day 15 の復習） | — | 削除確認ダイアログ | 「本当に消すか」の確認 |」。同ファイル272行「Day 11 で作ったものをそのまま読み込みます。」、649-650行「`DeleteConfirmDialog` は Day 11 で / タスク削除にも使った再利用コンポーネントです。」も実在する。

【Day 15 で埋まっている】76行は「Day 15 の復習」であって「Day 15 で作った」とは書いていない。読者が復習のため Day 15 を開くと、day15_タスク編集・削除.md:87 の「新しく学ぶ概念」表に「| DeleteConfirmDialog | デリート・コンファーム・ダイアログ | 削除確認ダイアログ | 「本当に捨てますか」の確認 |」が載っており、731-732行に「import { DeleteConfirmDialog } from / '@/component/ui/delete-confirm-dialog';」、786-796行に「続いて、JSXの閉じタグ付近に / `DeleteConfirmDialog` を配置します。」＋ open / onOpenChange / onConfirm を渡す実コードがある。さらに 2309行のチェックリスト「- [ ] `DeleteConfirmDialog` で確認ダイアログを表示できた」、2325行の用語表まである。つまり「確認ダイアログの作り方（使い方）を復習する」目的は Day 15 だけで完結して満たされる。

【「作成」はどの日にも存在しないので探し回りようがない】day11_プロジェクト編集・削除.md:83 は「│       └── delete-confirm-dialog.tsx  ← 削除確認ダイアログ（既存・変更なし）」、90行は「`delete-confirm-dialog.tsx` は配置のみ」で、Day 11 でも読者はこの部品を作らない。day19:1077 も「今日足したのは `DeleteConfirmDialog` の1行だけです。タスク削除で使ったものをそのまま読み込んでいます。」と再利用であることを明示。したがって「Day 15 に作成が見つからず探し回る」という詰まりは、Day 11 を見ても同じく作成が無いため、Day 15 表記が原因の詰まりではない。

【前の日で埋まっている】読者は day11 → day15 → day19 の順に読むため、DeleteConfirmDialog の import・props・配置位置は Day 11（382-383行の import、757行以降の配置、783行「#### DeleteConfirmDialog の props」、794行「shadcn/ui の `AlertDialog` を使った共通コンポーネント」）と Day 15（上記）で二度通過済み。Day 19 の76行は復習ポインタとして機能し、指し先に実体がある。

残るのは「Day 11 と Day 15 のどちらを指すか」の表記ゆれだけで、どちらを開いても必要な内容に到達するため、読者はそこで止まらない。
