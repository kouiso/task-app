# 品質監査: day25〜day30 + 目次・ロードマップ・付録4本

- 実施日: 2026-08-30
- 対象: `material/30days-curriculum/` の 12 本
  - `day25_プロフィール編集.md` (3346行) / `day26_エラーページを作って、バグを退治しよう.md` (910行) /
    `day27_プロジェクト詳細・アーカイブを実装しよう.md` (2190行) / `day28_タスク一括操作を実装しよう.md` (1799行) /
    `day29_ユーザー詳細・編集ページを作ろう.md` (3216行) / `day30_完成版を公開！.md` (1017行)
  - `00_カリキュラム目次.md` (359行) / `00-1_学びのロードマップ.md` (49行)
  - `appendix_トラブルシューティング.md` (142行) / `appendix_用語集.md` (268行) /
    `appendix_次のステップ.md` (165行) / `appendix_参考資料.md` (102行)
- 検証環境: リポジトリ `/home/user/task-app` 実測。`package.json` / `biome.json` /
  `scripts/scaffold-from-scratch.sh` / `src/` / `src/command/seed.ts` を実物で確認。
  外部リンクは 34 本すべて HTTP 実測。
- **画像の中身は見ていない**。この環境は `screenshots/` の読み取りが権限で拒否される。
  画像に関する指摘は「同一ファイルが複数箇所で使われている（md5 一致）」という
  バイト単位の事実と、キャプション・本文の記述だけを根拠にしている。
  「画面に何が写っているか」を根拠にした指摘は1件も書いていない。

---

## 0. 件数サマリ

| 項目 | 件数 |
|---|---|
| (a) 事実の誤り・矛盾 | **16件**（重大 5 / 中 7 / 軽 4） |
| (b) 図が要る節 | **11節** |
| (c) 理解チェック3問の起案 | 6日 × 3問 = **18問**（答え付き） |
| (d) つまずきポイント（clean-run 監査由来） | **5件**（うち day25〜30 該当 3件） |
| (e) その日の終わりの状態 | 6日すべて記載。**無予告の破損 0件**、ただし day27 に構造的な問題1件 |
| (f) 手元データ累積 | 6日ぶんの表を作成。**day28 に不足リスク1件** |
| (g) スクショの問題 | **撮り直し必須 5枚 / 新規推奨 2枚 / 削除推奨 10枚** |
| (h) 目次の所要時間の整合 | 範囲外 **2日**（day16, day19）+ 記載なし **4日**（day01〜04） |
| (i) 付録の妥当性 | **7件**（用語集の欠落が最大） |
| (j) day間参照の壊れ | **3件**（すべて「初出の日」の誤り） |

---

## (a) 事実の誤り・矛盾

### 【最重要 A-1】lint の完了条件が day05 の断り書きと矛盾する

**現物の3箇所（全文）**

`day05_ログイン画面のUI.md:356`
> ここで先に断っておきます。**この教材のコードは、行の幅を狭く保つ都合で、Biome の整形前の形で載せています。** たとえば `<h1>` を3行に分けて書いている箇所がありますが、Biome は1行にまとめるのが正しいと判断します。そのため写経したあとに `npm run lint` を実行すると、いくつかのファイルで整形の差分が報告されます。**写経を間違えたわけではありません。** `npm run fix` を実行すれば Biome の形にそろい、差分は消えます。改行位置が変わるだけで、動きは変わりません。

`day26_エラーページを作って、バグを退治しよう.md:757`
> 1. `npm run lint`が0エラーで通過した

`day28_タスク一括操作を実装しよう.md:1112`
> - `npm run lint` でエラーが出ない

**なぜ矛盾か（実測）**

- `scripts/scaffold-from-scratch.sh:316` が読者の `package.json` に
  `"lint": "biome check src prisma.config.ts next.config.ts package.json tsconfig.json"` を書き込む。
- `biome check` は linter だけでなく **formatter と assist（import 並べ替え）も走る**。
  `biome.json:62` / `:89` に formatter 設定があり、`biome.json:8-13` で `organizeImports: "on"`。
- したがって day05:356 が予告したとおり、写経直後の `npm run lint` は**必ず整形差分を報告する**。
  `npm run fix` を先に走らせない限り「0エラー」にはならない。
- day05:356 は正しい。誤っているのは day26 と day28 の完了条件のほう。

**巻き添えで直す必要がある同型の箇所**

| 箇所 | 現在の文言 |
|---|---|
| `day26:32` | `- `npm run lint` を実行して、修正後に警告が残っていないか確認できる`（前提） |
| `day26:110` | `\| Step 7 \| Biome lintで全体チェック \| 4分 \| ターミナル \| lint警告ゼロ \|`（成功状態） |
| `day26:737` | `- lintチェックが完了した`（ここだけ表現が正しい。基準にできる） |
| `day26:886` | `- [ ] `npm run lint` で `src` 以下と設定ファイルをチェックした`（ここも正しい） |

**参考になる先例**: `day09_プロジェクト一覧画面.md:734` は
「**今日は `npm run lint` に `dialogOpen` の指摘が出ます**（略）写経の間違いではないので、そのまま進めてください」
と、指摘が出る前提で完了条件を書いている。この書き方に day26/28 をそろえるのが最小の修正。

#### 書き換え案（そのまま差し替え可）

**① `day26:757`**

```markdown
**確認ポイント**:
1. `npm run lint` を実行し、`noConsole` の指摘が0件になった
   （整形の差分は Day 05 で断ったとおり残ります。`npm run fix` を実行すると消えます）
2. Biomeの自動修正が使えることを理解した
```

**② `day28:1109-1112`**

```markdown
エラーがなければ完成です。ただし整形の差分は別です。Day 05 で断ったとおり、
教材のコードは Biome の整形前の形で載せているので、写経直後は `format` の差分が出ます。
`npm run fix` を実行してから、もう一度 `npm run lint` を走らせてください。

**確認ポイント**:
- 上記のテスト項目がすべてパスする
- `npm run fix` のあとの `npm run lint` でエラーが出ない
- `npm run dev` でブラウザにエラーが出ない
```

**③ `day26:110`（成功状態の列）**

```markdown
| Step 7 | Biome lintで全体チェック | 4分 | ターミナル | `npm run fix` のあと lint 指摘ゼロ |
```

**④ `day26:32`（前提）**

```markdown
- `npm run lint` と `npm run fix` を実行して、修正後に指摘が残っていないか確認できる
```

---

### 【最重要 A-2】day29 の実装ステップ一覧が、11ステップ中6ステップで別のファイルを指している

`day29:81-93` の「触るファイル」列と、各 Step 内の `// filepath:` マーカーを機械的に突き合わせた結果。

| Step | 表の記載 (`day29:81-93`) | 実際に書くファイル（Step 内の filepath） | 判定 |
|---|---|---|---|
| Step 3 (`:86`) | `src/app/user/[id]/page.tsx` | `src/app/user/[id]/user-detail-client.tsx` | **不一致** |
| Step 4 (`:87`) | `src/app/user/[id]/page.tsx` | `src/app/user/[id]/user-detail-client.tsx` | **不一致** |
| Step 5 (`:88`) | `src/app/user/[id]/page.tsx` | `src/app/user/[id]/user-detail-client.tsx` | **不一致** |
| Step 6 (`:89`) | `src/app/user/[id]/page.tsx` | `src/app/user/[id]/user-detail-client.tsx` | **不一致** |
| Step 8 (`:91`) | `src/app/user/[id]/edit/page.tsx` | `src/app/user/[id]/edit/user-edit-client.tsx` | **不一致** |
| Step 9 (`:92`) | `src/app/user/[id]/edit/page.tsx` | `src/app/user/[id]/edit/user-edit-client.tsx` | **不一致** |
| Step 10 (`:93`) | `src/app/user/[id]/edit/page.tsx` | 5ファイルにまたがる（`user-edit-client.tsx` が本体） | 不正確 |

`day29:961`（Step 6 の実コード）が `// filepath: src/app/user/[id]/user-detail-client.tsx` である一方、
表の Step 6 は `page.tsx`。Step 2（`:85`）で作った `page.tsx` は server wrapper で、
`day29:1005-1010` の説明どおり `notFound()` を呼ぶだけの 40 行のファイル。
そこへ Step 3〜6 の詳細 UI を書き込むと、`'use client'` の無いファイルで `useQuery` を呼ぶことになり
必ず壊れる。表だけを見て進む読者を確実に詰まらせる。

**修正**: 表の「触るファイル」列を上表の右側に置き換える。あわせて Step 2 の成功状態
（`:85` `ファイルが存在する`）を「`page.tsx` と `user-detail-client.tsx` の2ファイルが存在する」に直す。

---

### 【最重要 A-3】day28 が既存関数 `isTaskStatus` を「書くコード」として出している

`day28:1048-1057`

```typescript
// filepath: src/lib/constant/status.ts
// isTaskStatus 型ガード関数
export function isTaskStatus(
  value: unknown
): value is TaskStatus {
  return typeof value === 'string'
    && value in TASK_STATUS;
}
```

- この関数は**配布物に既にある**。`src/lib/constant/status.ts:28` に
  `export function isTaskStatus(value: unknown): value is TaskStatus {` が実在する。
- この教材では `// filepath:` マーカーは一貫して「このファイルにこれを書く」意味で使われている
  （`day25:702`、`day28:486`、`day29:961` ほか全編）。
- 直前の `day28:977-980` は import について
  「同じ `@/lib/constant/status` の import 文に `isTaskStatus` と `type TaskStatus` が
  無い場合だけ加えてください。別の import 文を重複させません」と**明示的に注意している**のに、
  関数本体のブロックには同じ断りが無い。
- 素直に貼ると `status.ts` に `export function isTaskStatus` が2つ並び、
  TS2393（Duplicate function implementation）で `npm run build` が止まる。
- さらに `day28:111` の実装ステップ一覧は Step 8 の触るファイルを
  `src/app/task/page.tsx` **のみ**としており、`status.ts` に触ると読めない。

**修正**: このブロックを「読むだけ」に落とすか、`day28:977-980` と同じ断りを付ける。
day27 が使っている `**読み比べ用**: ここは写経しません。` （`day27:725` ほか）の書式が使える。

---

### 【最重要 A-4】day27 は「実装の日」なのか「照合の日」なのかが本文の中で割れている

同じファイルの中に、互いに打ち消し合う指示が並んでいる。

| 行 | 文言 | 立場 |
|---|---|---|
| `:1` | `# Day 27: プロジェクト詳細・アーカイブを実装しよう` | 実装 |
| `:11` | `一覧表示から詳細表示へ切り替わる UI を実装します` | 実装 |
| `:26-30` | `**この Day は Day 11・12 で作った機能の統合確認です。** 以下のコードは完成状態との照合用です。同じ名前の state、query、mutation、handler、Props を追加し直してはいけません。` | 照合 |
| `:32-34` | `` `project-detail-view.tsx` は Day 01 の配布物に完成した形で入っています。この Day のコードブロックは**読んで見比べるためのもの**で、書き写す必要はありません。`` | 照合 |
| `:71`（やること表） | `配布済みの `ProjectDetailView` に詳細表示を書き足す` | **実装**（`:32` の「完成した形」と直接矛盾） |
| `:96-104`（ステップ表） | `作る` `実装する`、触るファイル・成功状態つき | 実装 |
| `:387` | `なお最後の `<div className="grid ...">` は開いたままです。閉じタグが足りない状態なので、この時点では保存してもエラーが出ます。` | **実装**（写さない人には起きない） |
| `:397` | `ここまで書いて初めてファイルが保存できる状態になります` | 実装 |
| `:409` | `ここで作るのは完成版を少し削った形です` | 実装 |
| `:478` | `今日はまず一覧を出すところまでで、この出し分けは Day 12 で書いた既存のコードにそのまま残しておいて問題ありません` | 照合 |

`:409` と `:478` は同じ Step 4 の中にあり、**「完成版を削った形を作れ」と「Day 12 のコードを残せ」を
同時に言っている**。素直に `:409` に従うと、Day 12 で作ったロール変更 `Select` と
`canManageMembers` による読み取り専用切り替えを自分で消すことになる。つまり
指示どおりに手を動かすと機能が**後退する**。

**判断が要る**: この日を「照合日」に統一するなら、タイトル・`:11`・`:71`・`:96-104` を
確認系の語に直し、`:387` `:397` `:409` を削る。「実装日」に統一するなら `:26-34` を削る。
ただし後者は `:32` の「Day 01 の配布物に完成した形で入っています」と衝突する。

---

### 【最重要 A-5】配布物に完成版 `src/` が入るのか入らないのかが、同一ファイル内で逆になっている

| 行 | 文言 |
|---|---|
| `day27:32` | `` `project-detail-view.tsx` は Day 01 の配布物に完成した形で入っています。`` |
| `day27:2179` | `（販売用 ZIP に完成版の `src/` は入っていません。教材内のコードと確認ポイントが正本です）` |
| `day27:71` | `配布済みの `ProjectDetailView` に詳細表示を書き足す` |
| `00_カリキュラム目次.md:12` | `共通UIや設定などのスターターは配布ZIPに同梱しているので、外部のリポジトリを見に行く必要はありません` |

`day27:32` と `day27:2179` は同じファイルの中で正反対のことを言っている。
買い手が最初に確かめるのは「何が同梱されているのか」なので、ここが割れているのは
返金要求に直結する種類の矛盾。
`day27:684-693` の「`project-detail-dialog.tsx` というファイル自体は残っていても」も、
ファイルが手元にある前提で書かれており `:2179` と整合しない。

---

### 【中 A-6】day26 のバグ3件を「直す」のか「学ぶだけ」なのかが割れている

| 行 | 文言 |
|---|---|
| `day26:19` | `3つとも実際に直します。2つは教材内の演習コードで、1つはアプリのファイルです。` |
| `day26:86`（やること表） | `3つのバグパターンを学び、1つは実際に修正する` |
| `day26:883`（まとめ） | `- [ ] バグB: useEffect依存配列ミスの無限ループを**理解した**` |
| `day26:882`（まとめ） | `- [ ] バグA: Optional Chainingなしのクラッシュを**修正できた**` |

実体は `day26:384` `:479` が示すとおり、バグA・Bは
`// filepath: 教材内の演習コード（実行不要）` で、手元のファイルには何も起きない。
`:19` の「3つとも実際に直します」が過大。`:86` の書き方が正しい。

---

### 【中 A-7】day28 Step 1 の「`useState` だけを使います」が直後に破られる

`day28:356`
> 実際のコードを確認しましょう。`useState` だけを使います（`useCallback` は不要です）。

しかし同じ Step 1 の `day28:401` と `:410` で `useMemo` を2回使う。
さらにここで呼んでいる `canEditProject`（`:404`）は
`day13_タスク一覧画面.md:879` で `const canEditProject = useCallback(` として定義されたものなので、
「`useCallback` は不要」の一文は読者を混乱させる。

**修正案**: `useState と useMemo を使います。この Step で新しく useCallback を足す必要はありません。`

---

### 【中 A-8】day26 の実装ステップ一覧が Step 2 の新規ファイル作成を落としている

`day26:107` は Step 2 の触るファイルを `dashboard/page.tsx` としているが、
Step 2 は冒頭（`day26:219`）で
> まず `src/app/not-found.tsx` を新規作成します。

と、**新しいファイルを1つ作らせる**。`day26:785`（完成コードの表）は
`| `src/app/not-found.tsx` | 存在しない URL を開いたときに出る画面 | Step 2 |`
と正しく Step 2 に紐づけており、ステップ一覧のほうが落ちている。

---

### 【中 A-9】day30 の Vercel DB 作成手順が同一ファイル内で2通りある

| 行 | 文言 | 状態 |
|---|---|---|
| `day30:53-55` | `本番では Neon や Supabase（略）などのマネージド DB を、**Vercel の Marketplace 連携から追加します**` | 現行 |
| `day30:103-106` | `Vercel なら、管理画面で対象プロジェクトを開きます。**Storage タブから Postgres データベースを作成**すると、接続文字列が発行されます` | 旧UI |

Vercel の自社 Postgres は Marketplace（Neon）へ移行済みで、
`Storage タブから Postgres データベースを作成` は現在の画面に存在しない。
Step 4 は10分と最長のステップで、ここで詰まると 30 日が完走できない。
`:103-106` を `:53-55` に合わせて書き直す。

---

### 【中 A-10】day30 のページ数の基準と実測がずれている

| 行 | 文言 |
|---|---|
| `day30:599` | `- ページ数が 12 以上あれば充実したアプリ` |
| `day30:613` | `> 30日間で、教材スターターから機能を段階的に足して、17ページ以上のアプリを構築しました。` |

`find src/app -name "page.tsx" | wc -l` の実測は **17**（`src/app/page.tsx` から
`src/app/user/[id]/edit/page.tsx` まで、全17本が day01〜day29 のどこかで作られる）。
`:599` の「12以上」は、4〜5ページ落としている読者を合格にしてしまう。
`day30:592` はまさにこの `find` コマンドを読者に叩かせる確認ポイントなので、
基準値は 17 にそろえるべき。

---

### 【中 A-11】30日の区切りが3ファイルで3通りある（詳細は (h) 参照）

| 出典 | 区切り |
|---|---|
| `00_カリキュラム目次.md:75-308` | **8 Phase**。Phase 4 = Day 13-17 / Phase 5 = 18-20 / Phase 6 = 21-23 / Phase 7 = 24-25 / Phase 8 = 26-30 |
| `00-1_学びのロードマップ.md`「7つの局面」 | **7 局面**。局面4 = Day 13-16 / 局面5 = 17-20 / 局面6 = 21-25 / 局面7 = 26-30 |
| `day30:603-610`「週ごとの学習内容」 | **6 週**。第4週 = 13-16 / 第5週 = 17-22 / 第6週 = 23-30 |
| `day30:975-990`「卒業チェックリスト」 | さらに別の括り（11-16 / 17-20 / 21-23 / 24-25 / 26 / 27-28 / 29-30） |

Day 17 は目次では Phase 4、ロードマップでは局面5。Day 21-22 は目次では Phase 6、
day30 では第5週。`appendix_用語集.md:9-16` は目次の Phase 番号を引用しているので、
**ロードマップと day30 だけが浮いている**。

---

### 【中 A-12】未使用引数の書き方 — 何が規約違反で何が意図的か

依頼の切り分け結果。**規約違反は1件も無い**。混在は「教え方の一貫性」の問題であって、
Biome も TypeScript も止めない（`biome.json:39-41` の `correctness.noUnusedVariables: "error"` は
`_` 前置きも `void` 参照もどちらも黙らせる）。

| 分類 | 箇所 | 実体 |
|---|---|---|
| **意図的な仮実装（消して置き換える前提・明記あり）** | `day11:948` `const handleRemoveMember = (_userId: string) => {` | `day12:713` が「Day 11 の仮定義（略）は、**Step 6 で削除して本実装に**書き換えます。同名の `const` を2つ書くとエラーになるため、Step 6 では『Day 11 の仮実装を削除 → 本実装を書く』の順で」と明記 |
| 同上 | `day17:819-820` `const handleEdit = (_taskId: string) => {};` / `handleDelete` | `day17:958` `Step 8 の `const handleEdit = (_taskId: string) => {}` を**削除して**、以下で**置き換えて**ください。` / `day17:1025` 同文 |
| **意図的な受け皿（そのまま残す前提・明記あり）** | `day09:501,504,507` `void projectId;` / `void id;` | `day09:518` `` > `void projectId` は「この引数を意図的に使わない」ことをTypeScriptに伝える書き方です。`_` プレフィックスの代わりに使えるテクニックです。`` と教材が明示的に教えている。`day11:2151` が「Day 09 では `void projectId` と書いた受け皿だったので、押しても何も起きませんでした」と回収 |
| 同上 | `day09:1220,1223,1226`（完成コード再掲） | 同上 |
| 同上 | `day11:1942` `void id;` | Day 11 時点でまだ本実装が無い受け皿 |
| 同上 | `day13:809,813,817` `void taskId;` | Day 15 で本実装に差し替わる受け皿 |

**残る問題（軽微だが読者を迷わせる）**: `day09:518` が
「`_` プレフィックスの代わりに使えるテクニック」と教えたあとで、
`day11:948` と `day17:819` が予告なく `_` 前置きに戻る。どちらでもよいと1度言ってあるので
誤りではないが、初心者向け教材としては片方に寄せるほうがよい。
**推奨は `_` 前置きへの統一**（day11/day17 の書き方）。理由は `void x;` が
「値を捨てる」以外の意味を持たない式で、Day 09 の読者にはノイズになるため。
統一する場合の変更箇所は上表の `void` 行 8箇所のみ。

---

### 【軽 A-13】JSXバッククォート漏れ — day25〜30 では**0件**

`tsx` / `typescript` / `ts` / `jsx` のコードブロックだけを抜き出し、
テンプレートリテラルの構文（`${`、`` =` ``、`` (` ``、`` `; ``）を持たない
バッククォート行を機械的に走査した。day25〜day30 で該当行は無い。
（clean-run 監査が day21〜24 で指摘した JSX 内の日本語途中改行も、day25〜30 では検出されない。）

---

### 【軽 A-14】day26 の「Pro パターン」がコードを1行も示さない

`day26:764-776`「Pro パターンで書こう（Error Boundary の表示分岐は early return で整理する）」は、
`| 状態 | 表示 |` の3行の表と、`覚えておきたいこと` の1文だけで終わる。
day27（`:713-816`）と day30（`:733-`）の同名セクションは Before / After のコードを載せているので、
day26 だけ中身が無い。有料教材の節としては空。

---

### 【軽 A-15】day25 の実装ステップ一覧だけ列が2本少ない

| ファイル | ステップ表の列 |
|---|---|
| `day24:100-108` / `day25:86-102` / `day30:68-78` | ステップ / 作業内容 / 所要時間（**3列**） |
| `day26:104-110` / `day27:96-104` / `day28:101-112` / `day29:81-93` | ステップ / 作業内容 / 所要時間 / 触るファイル / 成功状態（**5列**） |

day25 は 15 ステップ・3画面という本編最多の日なのに、どのファイルを触るかが表から読めない。
（皮肉なことに、5列を持つ day29 は列の中身が A-2 のとおり間違っている。）

---

### 【軽 A-16】day30 の「やること / やらないこと」に Step 2.5 が無い

`day30:44-49` の「やること」は環境変数・Docker・デプロイ・動作確認の4行。
`day30:267-317` の Step 2.5「セキュリティヘッダーを設定する」（6分、`next.config.ts` を編集する
唯一のコード作業）が載っていない。ステップ番号が `2.5` である点も、後から差し込まれたことを示す。

---

## (b) 図が要る節

判定は1問だけ。**この節を読んだ人が紙に描いて確かめたくなるか。**
描く3類型（①目に見えんものの関係／②順番と分岐／③前後の変化）に当たるものだけを挙げる。
「表で足りる」「装飾になる」ものは挙げていない。

| # | 場所 | 類型 | なぜ図が要るか |
|---|---|---|---|
| B-1 | `day28:399-420`（Step 1、`selectableTasks` / `selectedTasks` / `selectedTaskList`） | ① 関係 | 似た名前の3つが8行のうちに出そろい、`selectedTasks` は Set、他2つは配列、`selectedTaskList` は「Set に居て、かつ今の一覧にも居る」交わり。`:420` が「フィルターを切り替えると画面から消えるタスクがあるが Set の中の id は消えない」と説明する内容は、重なりの図でしか伝わらない。**この日で最も混同が起きる箇所。** ベン図（全タスク ⊃ selectableTasks、selectedTasks ∩ 一覧 = selectedTaskList）。 |
| B-2 | `day28:678`（Step 4、分母が `tasks` ではなく `selectableTasks` である理由） | ① 関係 | B-1 と同じ集合図の上に、3状態チェックボックスの分母がどの円かを重ねる。B-1 と1枚に統合してよい。 |
| B-3 | `day26:199-208`（Step 1、`error.tsx` が拾うもの／拾わないもの） | ① 関係 | 「描画中に投げた例外は拾う」「`onClick` やタイマーの続きは拾わない」「`useEffect` の中で投げたものは拾う」「`fetch` は書いた場所で分かれる」「存在しない URL は `not-found.tsx` の担当」——5つの境界が10行の散文で並ぶ。どれが `error.tsx` の内側でどれが外側かの領域図。 |
| B-4 | `day26:176-183` + `:300-310`（サーバー側で落ちたときの届き方） | ② 順番 | 「最初の HTML は 500 で中身が無い → JS が読まれる → はじめて `ErrorPage` が描かれる」ので「Network に 500 の赤い行が残ったまま画面にはやり直しボタンが出る」。時間軸の帯（サーバー / ネットワーク / ブラウザの3レーン）。 |
| B-5 | `day26:547-555`（Step 4、無限ループの流れ） | ② 順番（環） | 既に `\| ステップ \| 動作 \|` の表があるが、表は最終行が `∞ 無限ループ` で止まり、**4→1 に戻る環**が描けていない。環になっていることが症状の本体。 |
| B-6 | `day27:164-167`（Step 1、権限は `project` ではなく `projectMember` を引く） | ① 関係 | `User` — `ProjectMember`（役割が載る行）— `Project` の3テーブルの並びと、`findUnique` が引くのは真ん中である、という位置関係。`:167` の「`prisma.project` を引いても、そのユーザーがそのプロジェクトの何なのかは分かりません」は、3つの箱と2本の線を描けば1秒で伝わる。 |
| B-7 | `day27:219-255`（Step 2、URL を唯一の起点にする） | ③ 前後の変化 | `:255` の「state も一緒に書き換えると更新の経路が2本になる／ブラウザの戻るボタンで URL だけ `/project` に戻って詳細の表示が残る」。**壊れる経路（2本）と正しい経路（1本）を並べた Before / After の2枚**。冒頭の mermaid（`:50-62`）は正常系しか描いていない。 |
| B-8 | `day29:1258-1394`（Step 8、`useQuery` → `useEffect` → `form.reset` → `register`） | ② 順番 + ③ 変化 | `day29:3072` のつまずきポイントが「フォームの初期値が空になる」を挙げており、実際に詰まる箇所。データが「まだ無い」状態で1回描画され、届いてから `form.reset` で入る、という**2回描画される**ことが図でしか見えない。`:3101-3123` に sequence diagram はあるが Step 10 の後ろにあり、詰まる時点では読まれない。**同じ図を Step 8 にも置くのが最小の直し方。** |
| B-9 | `day29:1394-1514`（Step 9、`role` / `isActive` を誰が送れるか） | ② 分岐 | `day29:3075` の `FORBIDDEN` エラーの原因がここ。「管理者 × 他人」「管理者 × 自分」「本人 × 自分」「一般 × 他人」の4象限と、送ってよい項目の対応。`:985-990` に4行の表はあるが、それは**ボタンの表示**の話で、**送信する項目**の分岐は別。 |
| B-10 | `day30:366-516`（Step 4、Vercel 設定 → push → ビルド → `prisma db push`） | ② 順番 | 10分と最長のステップで、順番を1つ間違えるとビルドが落ちる（環境変数が無い状態で push すると失敗する）。やり直しが効きにくい一発勝負なので、順番の図の価値がいちばん高い。**day30 で図が1枚も無いのは Step 4 が最大の穴。** |
| B-11 | `day25:1637-1786`（Step 12、編集ページのデータ取得と初期化） | ③ 前後の変化 | `day25:3314` のつまずきポイント「編集が反映されない / useEffectの依存配列」に対応。B-8 と同じ型なので、**day25 と day29 で同じ図を使い回せる**（同じ形を2度なぞらせる教材の方針にも合う）。 |

---

## (c) 理解チェック3問の起案

各設問は、その日の本文だけで答えられることを確認済み（根拠行を併記）。

### Day 25（プロフィール編集）

| # | 問 | 答え | 根拠 |
|---|---|---|---|
| 1 | プロフィールページの「ユーザー管理」ボタンは管理者にしか出ません。では一般ユーザーが `/user` を URL に直接打ったらどうなりますか。理由も書いてください。 | 開けない。ボタンを隠しているのは見た目の配慮にすぎず、実際の線引きは2か所ある。`/user` のページ自身が管理者かを見て「アクセス権限がありません」と出し、`user.getAll` がサーバー側で管理者だけを通す。 | `day25:740` |
| 2 | アバターURLを空のまま「更新」を押しても、サーバーがエラーを返さないのはなぜですか。 | クライアントは空文字を許すが、サーバーは URL 形式だけを許すため条件がずれている。`normalizeAvatarValue` が空文字や未入力を `null` に変換してから送るので、サーバーが受け取れる。 | `day25:1744-1748`, `:3317` |
| 3 | Step 9 でパスワードを変更したあと、控えを忘れてログインできなくなりました。`npm run db:seed` で戻せますか。戻せない場合の手順は。 | 戻せない。初期データを入れ直す処理は、すでにいるユーザーの中身を書き換えない作りだから。`npx prisma studio` で `User` テーブルを開き、`admin@example.com` の `password` 欄へ `user1@example.com` の `password` 欄の値をコピーして保存する（`password123` と直接打っても戻らない。中身は暗号化された文字列のため）。 | `day25:1446-1452` |

### Day 26（エラーページとデバッグ）

| # | 問 | 答え | 根拠 |
|---|---|---|---|
| 1 | 「画面は出ているのにボタンを押しても何も起きない」とき、`error.tsx` の画面に切り替わりますか。理由も書いてください。 | 切り替わらない。`error.tsx` が拾えるのは画面を描いている最中に投げられた例外だけで、`onClick` の中は描画が終わったあとに起きるため。Console に赤い行が出るだけで画面はそのまま残る。 | `day26:203-208` |
| 2 | 開発中に「文言がブラウザの Console に出たから、これはブラウザ側で起きたエラーだ」と判断できますか。 | できない。開発モードはサーバー側で起きたエラーも文言をそのまま送り届けるため。伏せられるのは本番ビルドのときだけで、そのときはサーバー側の文言が消えて `error.digest` という短い符号だけがブラウザへ渡る。 | `day26:305-310` |
| 3 | Network タブに同じリクエストが何百回も並んでいます。ステータスは全部 200 です。まずどちら側を疑いますか。 | ブラウザ側。サーバーは聞かれたことに正しく答え続けているだけで、おかしいのは同じことを繰り返し聞き続けているブラウザのほう。原因は `useEffect` の依存配列に毎回新しく作られるオブジェクトが入っていること。（赤い行が並んでいたらまずサーバー側を疑うが、赤い行もサーバーの不具合を証明はしない。） | `day26:511-521` |

### Day 27（プロジェクト詳細・アーカイブ）

| # | 問 | 答え | 根拠 |
|---|---|---|---|
| 1 | カードをクリックしたとき、`setSelectedProject` を直接呼ばずに `router.push` で URL だけを書き換えるのはなぜですか。 | 画面の状態を決める大元を URL 1か所にそろえるため。state も一緒に書き換えると更新の経路が2本になり、ブラウザの戻るボタンで URL だけ `/project` に戻って詳細の表示が残る、といった食い違いが起きる。URL を唯一の起点にすれば `useEffect` が state を追従させてくれる。 | `day27:64`, `:255` |
| 2 | `archive` と `unarchive` を1つにまとめて「現在値を反転させる」作りにしない理由は。 | 反転にすると画面が送ってきた「今の状態」を信じることになり、同じプロジェクトを2人が開いているとお互いの結果がずれる。呼ぶ名前で結果を決めておけば、サーバーが受け取るのは「こうしたい」という最終状態だけになる。`archive` を2回呼んでも `isArchived` は `true` のまま。 | `day27:186` |
| 3 | メンバー一覧の削除ボタンは、相手がオーナーなら一律で押せません。サーバー側の `removeMember` は同じ厳しさですか。 | 画面のほうが厳しい。サーバーは2段構えで、オーナー以外がオーナーを外そうとしたら `FORBIDDEN`、オーナーが1人しか残っていなければ `BAD_REQUEST` で拒む。つまりオーナー同士なら2人目以降を外せるが、画面からは外せない。 | `day27:474` |

### Day 28（タスク一括操作）

| # | 問 | 答え | 根拠 |
|---|---|---|---|
| 1 | 権限の確認に `every` を使い、`some` を使わないのはなぜですか。 | 権限のないタスクが1件でも混ざったら操作そのものを止めたいから。選択は複数のプロジェクトをまたげるので「編集はできるが削除はできない」タスクが紛れる場面が実際に起きる。`some` にすると権限のあるタスクが1件でもあればボタンが出て、押した先でサーバーに断られる。 | `day28:439` |
| 2 | 全選択チェックボックスの分母を `tasks` ではなく `selectableTasks` にしているのはなぜですか。 | 閲覧専用のタスクまで分母に入れると、選べるものを全部選んでも数が足りず、いつまでも部分選択のままになる。読者からは「全部選んだのに全チェックにならない」という不可解な動きに見える。 | `day28:678` |
| 3 | 一括完了とステータス変更には確認ダイアログが無く、一括削除にだけあるのはなぜですか。 | 完了とステータス変更は元に戻せる（ステータスを変えれば戻る）が、削除は DB から消えて元に戻せないため。ボタンの見た目も、押した瞬間に消える赤い塗りつぶしと、確認をはさむボタンで区別してある。 | `day28:947-951`, `:919` |

### Day 29（ユーザー詳細・編集）

| # | 問 | 答え | 根拠 |
|---|---|---|---|
| 1 | `page.tsx` が Prisma で存在を確かめ、そのあと `getById` がもう一度サーバーへ行きます。2回問い合わせるのはなぜで、どんな副作用がありますか。 | `page.tsx` が確かめるのは「その ID のユーザーが居るか」だけで、居なければ `notFound()` で画面が1度も描かれない。居たときだけ `getById` が本物のデータを取りに行き、そこで見る権限を確かめる。副作用として、居ないIDは404・居るIDは権限エラーと返り方が分かれるので、**外から「そのIDのユーザーが実在するか」を言い当てられる**。 | `day29:55` |
| 2 | 一般ユーザーが他人の `/user/他人のid` を開くと、編集ボタンが消えるだけですか。 | 違う。`getById` の権限チェックで `FORBIDDEN` になり、データそのものが取得できない。「ボタンが非表示」ではなく「ページ自体が表示されない」という設計。 | `day29:990-992` |
| 3 | 期限の列を `format(new Date(task.dueDate), ...)` ではなく `formatDateOnly(task.dueDate)` で書くのはなぜですか。 | User 詳細だけ違う日付処理にすると、Day 17 と Day 20 で学んだ「date-only は helper に寄せる」という方針と矛盾するため。 | `day29:530-534` |

### Day 30（完成版を公開）

| # | 問 | 答え | 根拠 |
|---|---|---|---|
| 1 | `open https://your-app.vercel.app` でアプリが開けました。これで「インターネット越しに届いている」と言えますか。言えないなら、いちばん早い確かめ方は。 | 言えない。`open` は自分のパソコンのブラウザを開くだけで、手元では開発サーバーも動いており見分けが付かない。Wi-Fi を切ってモバイル回線にしたスマートフォンで同じ URL を開き、自分の家の回線を1つも通らない端末で画面が出れば確実。 | `day30:536` |
| 2 | `npm run db:push` を本番 DB に対して実行してよいのはどんなときですか。 | 本番では `prisma migrate deploy` を使うのが一般的。この教材は migration 履歴を作る手順を扱っていないため、**Step 4 で新しく作った教材用の本番 DB に限り** `prisma db push` を1回だけ実行する。既存データがある実務の DB では、この手順をそのまま使ってはいけない。 | `day30:257-263` |
| 3 | セキュリティヘッダーを6つ入れる一方で、`Content-Security-Policy` は入れていません。なぜですか。 | 効き目は大きいぶん、書き方を誤ると自分のアプリの画像やスクリプトまで止まるため。まず6つを入れて動く状態を保ったまま公開し、そのあとの課題として足す。 | `day30:317`, `:932` |

---

## (d) つまずきポイント（`audit/clean-run-2026-07-29-day21-30.md` 由来）

監査ログの記載をそのまま引く。**同ログ自身が「29件はまだ全件を現物で確かめていない」と断っている**ので、
成立が確認済みのものと未確認のものを分けて扱う。

### 監査ログが「成立（実測）」としているもの

| # | 内容 | day25〜30 への影響 |
|---|---|---|
| D-1 | 目印（`// filepath:` の行）を貼っても画面に文字が出ない。204行を貼って14ページで0件、構文エラーも0件 | **問題なし**。day29 は `user.ts` に11個、`[id]/page.tsx` に2個、`user-detail-client.tsx` に18個、`edit/page.tsx` に2個、`edit/user-edit-client.tsx` に21個を貼って、`/user/[id]` と `/user/[id]/edit` の両方で0件。この日の目印は安全と実測済み |
| D-2 | JSX の子要素の位置で日本語の文が途中改行され、画面に半角スペースが入る。教材全体で **5箇所** | **day25〜30 は0箇所**。該当は `day12:944` / `day21:605` / `day21:1523` / `day22:766` / `day24:485`。ただし day25〜30 の担当者が同じ折り返し幅で新しい文を足すと再発する型 |

### 監査ログが取り下げた記録（day25〜30 関連・再指摘しないこと）

`day25` のアバター空文字（`day25:1626` に説明あり）、`day25` のパスワード復旧（`day25:1448` に手順あり）、
`day27` が確認だけの日だと知らされない（`day27:115` ほか各 Step 冒頭に記載あり）、
`day29` の `getById` 権限（`day29:165` / `:1903` に記載あり）、404 の確認に CUID 形式が要る（サーバーは形式を問わない）
——以上5件は監査側の誤りとして取り下げ済み。

**ただし D-3 として1件を再提起する。**
取り下げ理由「`day27:115` ほか各 Step の冒頭が『コードを追加せず』と書いている」は、
**冒頭だけを見た判断**である。上記 (a) A-4 のとおり、同じ日の `:387` `:397` `:409` は
書き写す前提の文になっており、`:409` と `:478` は Step 4 の中で矛盾する。
取り下げは Step 冒頭に限れば正しいが、Day 27 全体としては未解決。

### 監査ログの手法上の限界（引き継ぎ事項）

- 監査ログ末尾: 「**スクリーンショットは残していない。** 本文が `d29-detail.png` などを根拠として
  挙げているが、実物はリポジトリに入れていないので、第三者は追試できない。」
  → day29 の指摘は現時点で追試不能。
- 残り28件は未確認のまま。Day 01〜10 の回で8件中3件が成立しなかった実績があるので、
  件数として扱わないこと。

---

## (e) その日の終わりの状態

| Day | 終了時に動くか | 途中で壊れる窓 | 予告 |
|---|---|---|---|
| **25** | **動く**。`/profile`・`/profile/change-password`・`/profile/edit` の3画面が完成（`day25:3295`）。ただし**パスワードを変更した状態で終わる**ので、控えを取らせる必要がある（`day25:1444`） | Step 4 の閉じタグ（`:753`）、Step 7〜8 の `</form>` 前（`:1389`）、Step 13 の閉じタグ（`:3003`） | **あり**。3箇所とも「この `}` が無いと（略）エラーが出ます」と明記 |
| **26** | **動く**。`error.tsx` と `not-found.tsx` の2ファイルが残る（`day26:780-785`） | Step 2 で `dashboard/page.tsx` に `throw` を入れる／Step 5 で `console.log` を2行入れる | **あり**。`:322`「この削除を飛ばすと、ダッシュボードはこの先ずっとエラー画面のままになります」、`:665`「**追加した`console.log`を必ず削除した**」、`:780`「Step 2 でダッシュボードに入れた `throw` の行も削除済みです」。**残る問題**: `PORT=3001 npm run dev` で起動した開発サーバーを止める指示が無い（`:222`, `:681` で起動、停止の記述なし） |
| **27** | **前日と同じ**。この日は照合日で、`:2179` が「Day 27 全 Step 完了後の状態は、完成版の `src/app/project/page.tsx` と同じです」と書くとおり、指示に従えば新しいものは何も残らない | Step 3 の `<div>` 開きっぱなし（`:387`） | **あり**。ただし A-4 のとおり、そもそも書き写すのか否かが本文で割れているので、この予告が誰に向けたものかが不明 |
| **28** | **動く**。Step 9（`:1080-1112`）で5項目の動作確認を通す | 無し（Step 5 の `<>...</>` プレースホルダーは空でも壊れない、`:741`） | 該当なし |
| **29** | **動く**。ただし**Step 8 の途中から Step 10 の最後まで、約19分ぶん画面が出ない**（`:1417` `:1541` `:1588` の3箇所で「`</form>` はこのあと書くので、この時点では閉じタグが足りないというエラーが残ります」）。解消は `:3064`「この6行でそれが消えます」 | 上記のほか Step 2 直後（`:424` `Module not found: Can't resolve './user-detail-client'`）、Step 5 途中（`:834`） | **あり**。`:434`「この時点ではブラウザで開かず、次の Step へ進む」まで書いてある。**予告としては本編最良**。ただし19分は長く、`:1512` で「この時点ではまだ描画されません」と断りながら完成形の画像を出しているのは、モチベーション設計としては逆効果 |
| **30** | **公開済み**。本番 URL でチェックリスト8項目（`:548-557`） | 無し | 該当なし |

**総括**: 無予告の破損は **0件**。予告の質は day29 が最も高い。
構造的な問題は day27 ただ1件で、「実装の日と銘打った日に、指示どおりだと何も残らない」こと。

---

## (f) その日までに読者の手元にあるデータ（スクショ用日別シード）

### ベースライン（`src/command/seed.ts` の実測）

| 対象 | 件数 | 内訳 |
|---|---|---|
| User | **4** | 開発者本人（`.env` の値）/ `user1@example.com`（田中太郎）/ `user2@example.com`（山田花子）/ `empty@example.com`（新人 太郎） |
| Project | **2** | 「Webサイトリニューアル」「モバイルアプリ開発」 |
| Task | **5** | `IN_PROGRESS` 1 / `DONE` 1 / `TODO` 3 |
| Comment | **2** | — |
| アーカイブ済み Project | **0** | seed に `isArchived` の指定なし＝全件 `false` |

### 教材が読者に追加させる分（明示的な指示があるものだけ）

| Day | 追加指示 | 出典 |
|---|---|---|
| Day 10 | プロジェクト新規作成を1件 | `day10` Step |
| Day 14 | タスク新規作成 | `day14` Step |
| Day 21 | （例え話として「タスクを10個登録したとき」。**指示ではない**） | `day21:30` |
| Day 23 | 「`/task` で自分を担当者にしたタスクを**2〜3件作り**、Day 16 の手順で「完了」にしてから開く」 | `day23:28` |
| Day 25 | データ追加なし。ただし**自分のパスワードが変わる**（Day 26〜30 で使う、`day25:1444`） | `day25:1444` |
| Day 28 | 「一括削除を試すため、**消えてもよい練習用タスク**を用意している」（**件数の指定なし**） | `day28:31` |

### 日別の推奨シード（スクリーンショット撮影用）

| Day | User | Project | Task | Comment | 特記 |
|---|---|---|---|---|---|
| 25 | 4 | 3（seed 2 + Day 10 で1） | 8〜9（seed 5 + Day 14 で1 + Day 23 で2〜3、うち完了3） | 2〜3 | アバターURLが入った状態と空の状態の両方を撮れるようにする |
| 26 | 4 | 3 | 8〜9 | 2〜3 | ダッシュボードが正常に描ける状態（`throw` を入れる前後の2枚を撮るため） |
| 27 | 4 | 3 | 8〜9 | 2〜3 | **アーカイブ済み 1件が必要**。`archived-project-list.png` が既にあるのに未使用（後述 G-4） |
| 28 | 4 | 3 | **最低 8**（うち同一画面に同時表示できるもの ≥ 5） | 2〜3 | **下記の警告参照** |
| 29 | 4 | 3 | 8〜9 | 2〜3 | プロジェクトとタスクを両方持つユーザー1人＋どちらも持たないユーザー1人（`empty@example.com` が使える、`day29:950` の「ありません」メッセージ用） |
| 30 | 4 | 3 | 8〜9 | 2〜3 | 本番 DB は `prisma db push` 直後で**空**（`day30:262`）。Step 5 のチェックリスト（`:550-557`）は `/register` から作り直す前提。本番のスクショはローカルと件数が違って当然 |

### 【警告 F-1】day28 Step 9 のテスト表が要求する件数を、前提が保証していない

`day28:1096-1099` の動作確認表は、
「3件選択して完了」→「2件選択して削除キャンセル」→「2件選択して削除（**2件消える**）」→「5件選択してステータス変更」
の順で実行させる。最後の「5件選択」に到達する時点で**2件が消えている**ので、
開始時に**最低7件**のタスクが同一画面に必要。

しかし前提（`day28:31`）は「消えてもよい練習用タスクを用意している」としか書いておらず、件数がない。
seed の5件＋Day 14 の1件＝6件で始めた読者は、最後の「5件選択」ができない（残り4件）。

**修正案**: `day28:31` を
`- 一括削除を試すため、消えてもよい練習用タスクを 7 件以上用意している（うち 2 件は削除します）`
に差し替える。

---

## (g) スクショの問題

### 使い回し（md5 で同一ファイルであることを確認済み）

| # | Day | ファイル | 使用箇所 | 判定 |
|---|---|---|---|---|
| G-1 | 25 | `profile.png` | `:17`（今日のゴール） / `:691`（Step 3 末尾） / `:762`（Step 4 末尾） | **3回**。`:17` と `:762` は完成形なので妥当。**`:691` が問題**。同一ファイルである以上、Step 3 の時点で Step 4 の3つのナビゲーションボタンが写った画像を見せている。Step 3 の確認ポイント（`:684-687`）はアバター・名前・バッジ・メール・日付だけなので、画像がそれより先を見せている |
| G-2 | 26 | `error-page.png` | `:25`（今日のゴール） / `:281`（Step 2 の 404 確認） | **2回**。かつ**キャプションが食い違う**。`:25` は「404とページが見つかりませんが中央に表示された画面」、`:281` は「404とページが見つかりませんが中央に並び、**ダッシュボードに戻るボタンが出た**画面」。同一ファイルなのでどちらかが不正確。さらにファイル名 `error-page.png` に対し中身は `not-found.tsx` の画面（`:281` の直後 `:283` が「いま書いた `not-found.tsx` のとおりの画面です」と明記）で、名前が誤解を招く |
| G-3 | 27 | `project-detail-archive-action.png` | `:15`（今日のゴール） / `:667`（Step 6 末尾） | **2回**。`:667` は「補助ダイアログと完成形を整える」Step の末尾で、直前の確認ポイント（`:663-665`）は「メンバー削除は確認ダイアログを挟んで実行される」。ダイアログが写っていない完成形の画像では、その確認ポイントを確かめられない |
| G-4 | 27 | `archived-project-list.png` | **どこからも参照されていない**（全 day ファイルを grep して0件） | Step 5 の成功状態「成功後は `/project` に戻って一覧が更新される」（`:604`）に対応する画像が既に存在するのに使われていない。撮影不要、貼るだけ |
| G-5 | 30 | `login.png` | `day05` ×3 / `day07` ×2 / `day30:544` | **同一ファイルを 6 箇所で使用**。`day30:544` のキャプションは「**本番環境の**ログイン画面」だが、day05 で使っているのと同じファイル。ローカルの画像を本番と称している |
| G-6 | 30 | `dashboard.png` | `appendix_トラブルシューティング:7` / `appendix_参考資料:5` / `appendix_次のステップ:5` / `day30:578` | **4箇所**。`day30:578` のキャプションは「**完成版の**ダッシュボード画面」 |
| G-7 | 28 | `bulk-operations-complete.png`（`:21` ゴール）と `bulk-task-operations.png`（`:1086` Step 9） | 別ファイル（md5 相違）だが、ファイルサイズが 122775 / 122671 バイトで 104 バイトしか違わず、同解像度（1280×720） | **同一場面の重複の疑いが強い**。どちらも「完成した一括操作機能」を示すもので、`:19` と `:1084` の文言もほぼ同じ。※画像を開けないため断定はしない |
| G-8 | 付録 | `dashboard.png` / `project-list.png` / `report.png` / `login.png` / `task-list.png` | `appendix_トラブルシューティング:5,7,9` / `appendix_参考資料:5,7,9` / `appendix_次のステップ:5,7,9` | **9枚が純粋な装飾**。トラブルシューティングの冒頭に「正常起動時の」画面を3枚並べても、詰まっている読者の役には立たない。参考資料（リンク集）と次のステップ（読み物）に至っては本文と無関係 |

### まだ作っていない UI・データが写る恐れ

**画像を開けないため、写っているものを直接確認できない。** 以下は「同一ファイルである」ことだけを
根拠にした論理的な指摘に限る。

| # | 箇所 | 論拠 |
|---|---|---|
| G-9 | `day25:691`（Step 3） | `:17`（完成形のゴール画像）と md5 が同一。よって Step 4 で作るナビゲーションボタン3つが必ず写っている |
| G-10 | `day27:667`（Step 6） | `:15`（ゴール画像）と md5 が同一。`:15` のキャプションが「右上にアーカイブボタンがある」なので、Step 5 で作るアーカイブボタンを含む完成形 |
| G-11 | `day29:1512-1514`（Step 9） | 本文が「**この時点ではまだ描画されません**」と自ら断ったうえで完成形の `user-edit-form.png` を貼っている。**断りがあるので誤りではない**が、Step 8〜10 は画面が出ない19分間（(e) 参照）なので、出ない画面の完成図を先に見せる設計になっている |

### 「ここをクリック」に赤枠が無い箇所

day25〜30 の21枚のキャプションを全数確認した結果、
**「ここをクリック」「この場所を押す」の類の指示をキャプションに持つ画像は0枚**。
したがって「赤枠が無い」という指摘は成立しない。

代わりに別の問題がある。21枚中 **8枚のキャプションが「〜の表示を確認してください。」という
指示文**で、画像の説明になっていない（`day25:17`, `:691`, `:762` / `day28:21`, `:480`, `:1086` /
`day29:17`, `:945`）。しかもそのうち6箇所は、直前の本文が同じ文を繰り返している
（例: `day28:19` 「スクリーンショット: タスク一括操作の完成画面の表示を確認してください。」→
`:21` `![タスク一括操作の完成画面の表示を確認してください。](...)`）。
残り13枚（`day25:1309`, `:2020`, `day26:25`, `day27:13`, `:15`, `day28:592`, `:722`,
`day29:574`, `:1514`, `day30:544`, `:578` ほか）は状態を説明する良いキャプション。**書き方が2種類混在している。**

### 撮り直し枚数

| 区分 | 枚数 | 内訳 |
|---|---|---|
| **撮り直し・新規撮影 必須** | **5枚** | ① day25 Step 3 用（ナビゲーションボタンが**まだ無い**プロフィール画面） ② day26 用（`error.tsx` のフォールバック UI。「エラーが発生しました」＋「もう一度試す」ボタン。Step 2 の確認ポイント2・3に対応する画像が**1枚も無い**） ③ day27 Step 6 用（メンバー削除の確認ダイアログが開いた状態） ④⑤ day30 用（本番 URL がアドレスバーに見えるログイン画面とダッシュボード。現行の `login.png` / `dashboard.png` はローカルの使い回し） |
| **新規撮影 推奨** | **2枚** | ⑥⑦ day30 Step 4（10分・最長ステップ）の Vercel 環境変数登録画面とデプロイ成功画面。day30 は Step 1〜4 に画像が1枚も無い |
| **撮影不要・貼るだけ** | **1枚** | `archived-project-list.png` を day27 Step 5 の末尾へ（G-4） |
| **リネームのみ** | **1枚** | `error-page.png` → `not-found.png`（G-2）。参照は `day26:25` と `:281` の2箇所 |
| **削除推奨** | **10枚** | 付録の装飾9枚（G-8）＋ day28 の重複1枚（G-7、要確認） |

---

## (h) 目次の所要時間の整合

`00_カリキュラム目次.md:322` が宣言する範囲：

> - **内訳**: 各Dayの手順表の合計（**30分〜1時間20分**）はコードを読んで理解する目安で、
>   写して打ち込む時間はそれとは別に、同じくらいかそれ以上かかります

宣言範囲 = **30分〜80分**。各日の `**合計時間**` を全数実測して突き合わせた結果。

### 範囲外の日（全件）

| Day | 実測 | 記載箇所 | 判定 |
|---|---|---|---|
| **Day 16** | **26分** | `day16_ステータス変更・時間記録.md:101` `**合計時間**: 約26分です。` | **下限を4分下回る** |
| **Day 19** | **29分** | `day19_コメント編集・削除.md:90` `**合計時間**: 約29分です。` | **下限を1分下回る** |

範囲外は上記2日のみ。上限（80分）を超える日は無い（最長は Day 15 の79分、`day15:109`）。

### 記載が一切無い日

| Day | 状況 |
|---|---|
| **Day 01** | `day01_開発環境を整えて、初めてのアプリを動かそう.md`（1282行）に `合計時間` の文字列なし |
| **Day 02** | `day02_ダッシュボードに自分だけのメッセージを追加しよう.md`（1238行）同上 |
| **Day 03** | `day03_GitHubに保存する.md`（832行）同上 |
| **Day 04** | `day04_ネットに公開.md`（948行）同上 |

**買い手が最初に触る4日ぶんに、所要時間の記載がない。**
目次 `:321` が「1日あたりの学習時間: 2〜3時間を見ておくと安心です」と全体では言っているのに、
Day 01 を開いた読者は「今日は何分で終わるのか」を知る手段がない。
day05 以降は全26日に記載があるので、この4日だけが欠けている。

### 範囲内だが端に寄っている日（参考）

下限側: Day 26 = 37分（`day26:112`）、Day 08 = 38分（`day08:83`）、Day 22 = 38分（`day22:121`）、Day 27 = 41分（`day27:105`）、Day 18 = 42分（`day18:94`）
上限側: Day 15 = 79分（`day15:109`）、Day 29 = 76分（`day29:95`）、Day 25 = 73分（`day25:104`）、Day 28 = 73分（`day28:114`）

### 修正案

1. 範囲を `25分〜80分` に広げる（実測に合わせる。最小の変更）。
2. または Day 16 と Day 19 の内訳を見直して 30 分に届かせる。ただし数字を合わせるためだけに
   時間を水増しするのは避ける。
3. **Day 01〜04 に `**合計時間**: 約N分です。` を追加する（必須）**。書式は
   `day05:87` の `**合計時間**: 約47分です。` にそろえる。
   Day 01 は各種インストールを含むので、実測しないと出せない。

### 【付随】区切りの不整合（A-11 の詳細）

| Day | `00_カリキュラム目次.md` | `00-1_学びのロードマップ.md` | `day30:603-610` |
|---|---|---|---|
| 13-16 | Phase 4（13-**17**） | 局面4（13-**16**） | 第4週（13-16） |
| 17 | Phase 4 | **局面5** | **第5週** |
| 18-20 | Phase 5 | 局面5 | 第5週 |
| 21-22 | Phase 6（21-23） | 局面6（21-**25**） | **第5週**（17-22） |
| 23 | Phase 6 | 局面6 | **第6週**（23-30） |
| 24-25 | **Phase 7** | 局面6 | 第6週 |
| 26-30 | Phase 8 | 局面7 | 第6週 |

- 目次は **8 Phase**、ロードマップは **7 局面**、day30 は **6 週**。数も境界も違う。
- `appendix_用語集.md:9-16` の「主に登場するPhase」列は目次の Phase 番号を使っており
  （`Phase 2 (Day 05-08)`、`Phase 3-5 (Day 09-20)`、`Phase 6 (Day 21-23)`）、**目次と整合する**。
- したがって**目次を正**とし、ロードマップの「7つの局面」と day30 の「週ごとの学習内容」を
  そちらへ寄せるのが変更量が最も小さい。
- ロードマップは `00_カリキュラム目次.md:8` から「最初に一度目を通してから Day 01 を始めると」と
  リンクされている。買い手が2番目に読むファイルなので、ここが目次と食い違うのは印象が悪い。

---

## (i) 付録の妥当性

### `appendix_トラブルシューティング.md`（142行）

#### 【I-1・最大】Day 26-30 の節が実質空

`:67-72` の「Day 26-30: 仕上げ・公開」は**2行しかない**。

| エラー | 原因 | 解決方法 |
|---|---|---|
| 型エラー（`tsc --noEmit`） | TypeScriptの型定義が不正 | エラーメッセージの行番号を確認して型を修正 |
| Biomeのエラー | コードスタイルの違反 | `npm run lint:fix`で自動修正 |

どちらも Day 26-30 に固有ではない。一方、各日は自前のつまずきポイント表を持っており、
そこにある項目が**1件も付録に反映されていない**。

| 出典 | 拾われていない項目 |
|---|---|
| `day26:890-895` | `Cannot read properties of null` / Network でリクエストが止まらない / error.tsx が表示されない（開発モードのオーバーレイが優先） |
| `day27:2146-2152` | 詳細が開かない / API が毎回エラー（`enabled` 未指定） / 一覧に戻れない / アーカイブ後に画面が古い / 詳細 UI が教材画像と違う |
| `day28:1746-1754` | `updateMany` で型エラー ほか |
| `day29:3068-3075` | `[id]` フォルダ名が全角 / `await params` の `id` が `undefined` / フォームの初期値が空 / 権限チェックが効かない / `FORBIDDEN` |
| `day30:947-952` | ビルド失敗（環境変数未設定）/ DB 接続エラー / JWT エラー / ページが真っ白 |

**5日ぶん・約20項目が欠けている。** Day 26-30 の節を、上記から統合して埋める。

#### 【I-2】lint の整形差分の項目が無い

(a) A-1 で扱った「写経直後の `npm run lint` は必ず整形差分を出す」は、
**全30日を通じていちばん起きる指摘**であり、`day05:356` にしか説明が無い。
Day 20 あたりで初めて lint を叩いた読者は day05 まで戻れない。
付録に1行入れるのが最も費用対効果が高い。

**追記案**（`:32` の「Day 05-08」表へ）:

| エラー | 原因 | 解決方法 |
|---|---|---|
| `npm run lint` に `format` の差分がたくさん出る | 教材のコードは行幅を狭く保つため Biome の整形前の形で載せている（Day 05 参照） | 写経ミスではありません。`npm run fix` を実行すると Biome の形にそろい、差分は消えます |

#### 【I-3】Day 21-23 の節が無い

`:59` は「Day 17-25: 応用機能」で3行。Recharts（Day 22）・週次レポート（Day 23）由来の
つまずきが1件も入っていない。`day23:2355` の「0件で割り算」など、実際に踏むものがある。

#### 【I-4】冒頭の3枚の画像が役に立たない

`:5,7,9` の「正常起動時の」ログイン・ダッシュボード・タスク一覧。
壊れている読者が開くページなので、正常な画面を3枚並べても診断の助けにならない。削除推奨（G-8）。

### `appendix_用語集.md`（268行）

#### 【I-5・重大】tRPC のフックが1語も載っていない

見出し（`###`）を全数列挙して、教材本編が「新しく学ぶ概念」「今日学んだ用語」で
定義している語と突き合わせた。以下は**用語集に項目が存在しない**（grep 実測、いずれも0件）。

| 分類 | 用語集に無い語 | 教材での初出 |
|---|---|---|
| **tRPC（本編の骨格）** | `useQuery` / `useMutation` / `invalidate` | `00-1_学びのロードマップ.md` が「tRPC useQuery（データ取得）」を Day 08-09、「tRPC useMutation」を Day 05 の**必修の型**として挙げている。Day 09 以降ほぼ全日で使う |
| React | `useEffect`（見出しなし。`### Hook（フック）` の例として名前が2回出るだけ）/ `useState`（同）/ `Suspense` | Day 09 ほか |
| Day 25 | `changePassword` / `updateProfile` / `toast` / `Separator` / `isPending` / `refine` / `normalizeAvatarValue` / `PasswordInput` | `day25:73-80`, `:3321-3329` |
| Day 26 | Error Boundary / Optional Chaining / Nullish Coalescing / useEffectの依存配列 / DevTools | `day26:94-98` |
| Day 27 | `useSearchParams` / `router.push()` / `inferRouterOutputs` / アーカイブ / `enabled` | `day27:77-82`, `:2160-2167` |
| Day 28 | `Set<string>` / `indeterminate` / `updateMany` / `isTaskStatus` / `completedAt` | `day28:89-95` |
| Day 29 | 動的ルーティング `[id]` / `notFound()` / server wrapper / `await params` / `form.reset` | `day29:69-77`, `:3087-3097` |
| Day 30 | CI/CD / Production / マネージド DB | `day30:59-64`, `:956-963` |

用語集の収録は約45語で、React / Next.js / Tailwind / tRPC / Prisma といった**技術名の定義**に偏り、
**日々のコードに出てくる関数・フック名がほぼ入っていない**。
「言葉の意味: [用語集]」は day25〜30 全ファイルの末尾（`day25:3346` など）からリンクされているので、
Day 28 で `indeterminate` が分からず用語集を開いた読者は空振りする。

**最優先で足すべきは `useQuery` / `useMutation` / `invalidate` の3語**（Day 09 以降の全日で使う）。
次が Day 26-30 の新語（上表の下半分）。

#### 【I-6】カテゴリ早見表が Day 26-30 を扱っていない

`:9-16` の早見表は 8 行あるが、最も新しい記載が `レポート・可視化 | Recharts | Phase 6 (Day 21-23)`。
Phase 7（Day 24-25）と Phase 8（Day 26-30）に対応する行が無い。
（なお早見表の Phase 番号は `00_カリキュラム目次.md` と整合している。(h) 参照。）

### `appendix_参考資料.md`（102行）

#### 【I-7】リンク切れは **0件**（実測）

掲載されている外部 URL **34本すべてに HTTP リクエストを実行**した結果：

- 32本が `200`。
- `https://github.com/panva/jose` と `https://github.com/dcodeIO/bcrypt.js` は `403` だが、
  これは当環境のプロキシが GitHub を弾いているため。**リンク切れではない**。
- `https://vitest.dev/` は接続タイムアウト（`000`）だったが、同一ドメインの
  `https://vitest.dev/guide/` は `200` を返した。一時的なもの。

#### 情報の鮮度

| 箇所 | 内容 | 判定 |
|---|---|---|
| `:37` | Zod について「このカリキュラムは **v3 系**を使うので、公式が v4 の書き方を出している場合は v3 のドキュメントに切り替えて読んでください」 | **正しい**。`package.json` の zod は 3.x 系。バージョン差を先回りして書いてあるのは良い |
| `:5,7,9` | 装飾スクリーンショット3枚 | 削除推奨（G-8）。リンク集に画面写真は不要 |
| `:88` | 「ブラウザの開発者ツール（Networkタブ）\| Chrome / Edge / Firefox に標準搭載 \| このアプリの通信は tRPC を通るので、専用ツールより画面を操作して確認するほうが早い」 | 良い記述 |

**欠けているもの**: Day 26 で本編の1つの Step を使う **DevTools の公式ドキュメント**（Chrome DevTools）が
「便利なツール」表（`:85-91`）に無い。`React Developer Tools` はあるのに DevTools 本体が無い。

### `appendix_次のステップ.md`（165行）

| 箇所 | 内容 | 判定 |
|---|---|---|
| `:58` | `npx create-next-app@15.5.21 my-new-app --typescript --tailwind --app --src-dir --import-alias "@/*" --yes` | **パッチ版まで固定**（`@15.5.21`）。読者が半年後に叩くと古いバージョンが降りてくる。`@15` か `@latest` が妥当。なお `--yes` の位置と組み合わせは有効 |
| `:61-63` | `npm install @trpc/server @trpc/client @trpc/react-query` ほか | バージョン指定なし。本教材の tRPC は 11.x だが、`@latest` が入ると 12 以降で API が変わる可能性。せめて `@trpc/server@11` と書くほうが安全 |
| `:72-76` | 4手順の順番の理由を散文で説明 | **良い**。他の付録に無い、本編と同じ「なぜ」の書き方 |
| `:5,7,9` | 装飾スクリーンショット3枚 | 削除推奨（G-8） |
| `:20-28` | 拡張機能の表（8件） | 妥当。ただし Day 27 で作ったアーカイブ、Day 28 の一括操作の自然な続き（例: アーカイブ専用ページ＝`day27:72` が「やらないこと」に挙げたもの）が入っていない。本編の「やらないこと」を拾うと接続が良くなる |

---

## (j) day間参照の壊れ

day25〜30 に現れる `Day NN` の言及を全数抽出し（機械抽出＋目視）、参照先の実在を確認した。
**存在しない Day を指す参照は0件**。ファイルへのリンク（`./dayNN_*.md`）も全て実ファイルに解決する。

誤っているのは「その技術を最初に習う日」の指定。3件。

| # | 箇所 | 現在の記載 | 実際 | 根拠 |
|---|---|---|---|---|
| **J-1** | `day25:79` | `\| useForm + zod（復習） \| — \| フォーム管理とバリデーション（**Day 14 参照**） \| 記入用紙のルール自動チェック \|` | **Day 05** | `00-1_学びのロードマップ.md`「react-hook-form + zod（フォーム）\| 最初に習う日 **Day 05** \| 応用・復習する日 Day 06・10・14・18・25・29」。`00_カリキュラム目次.md:111` も Day 05 を「react-hook-form + zodでバリデーション付きフォームを作成」としている。`useForm` を含む最初の日は `day05` |
| **J-2** | `day29:76` | `\| useForm + zod（復習） \| — \| フォーム管理＋バリデーション（**Day 14 参照**） \| 記入用紙のルール自動チェック \|` | **Day 05** | J-1 と同じ。day25 と day29 で同じ誤りがコピーされている |
| **J-3** | `day25:80` | `\| refine（**Day 16 の復習**） \| リファイン \| zod のカスタムバリデーション \| 複数フィールドを横断して検証するルール \|` | **Day 06** | `00_カリキュラム目次.md:118` が Day 06 を「`.refine()`でパスワード確認チェック（**複数フィールド間バリデーション**）」としている。day25 の説明「複数フィールドを横断して検証するルール」はまさに Day 06 の用法。`.refine(` を含むのは day05 / day06 / day16 / day25 / day28 / day29 で、初出は day05〜06 |

### 検証済みで問題なかった参照（再指摘しないこと）

| 箇所 | 記載 | 確認 |
|---|---|---|
| `day25:112` | `Day 24 で作った `src/server/api/routers/user.ts`` | ✅ `day24:117` が「`src/server/api/routers/user.ts` を**新規作成**し」と明記 |
| `day25:2204` | `Day 08 で作った長いファイル`（`app-layout.tsx`） | ✅ `app-layout.tsx` を含む最初の日は day08 |
| `day28:142` | `Day 13 で書いた `import { Prisma } from '@prisma/client';`` | ✅ day13 が初出 |
| `day28:223` | `Day 16 で書いた `addTime` の直後に足します` | ✅ `addTime` は day16 のみ |
| `day28:1162` | `Day 15 で書いた1件用のヘルパー`（`findTaskWithPermission`） | ✅ day15 が初出 |
| `day29:533` | `Day 17 と Day 20 で学んだ「date-only は helper に寄せる」` | ✅ day17 に `dateOnlyFromValue` / `localDateOnly`、day20 にも `dateOnly` 系が14箇所 |
| `day29:5` | `Day 24 で作ったユーザー一覧ページの続き` | ✅ |
| `day26:899` | `Day 27 では、Day 11・12 で作ったプロジェクト詳細のインライン表示とアーカイブ機能を、完成形と照合して仕上げます` | ✅ 内容は正しい。ただし A-4 のとおり day27 側の立場が割れている |

### 参考（軽微・修正は任意）

`day29:1529` の `Day 25 で学んだ `useMutation` パターンを使い` は、ロードマップが
`useMutation` の初出を Day 05 としているため厳密には不正確。ただし「Day 25 で使ったのと同じ形」
という意味なら通る。J-1〜J-3 のような明確な誤りではない。

---

## 直す順番の提案

買い手が「品質に不満を持って次を買わない」確率が高い順。

| 順 | 項目 | 理由 |
|---|---|---|
| 1 | **A-2**（day29 のステップ表が6ステップで別ファイルを指す） | 表どおりに書くと必ず壊れる。修正は表の1列を書き換えるだけ |
| 2 | **A-3**（day28 が既存関数を書かせて重複定義になる） | 素直に貼るとビルドが止まる。修正は断り書き1行 |
| 3 | **A-1**（lint の完了条件が day05 と矛盾） | 「教材どおりにやったのに0エラーにならない」は最も不信感を生む型。修正案は本文に記載済み |
| 4 | **A-4 / A-5**（day27 の立場割れ・配布物の説明が逆） | 「何が同梱されているか」が割れているのは返金要求に直結 |
| 5 | **I-5**（用語集に `useQuery` / `useMutation` / `invalidate` が無い） | 全日リンクされている参照先が骨格の用語で空振りする |
| 6 | **(h) Day 01〜04 の所要時間欠落** | 買い手が最初に開く4日 |
| 7 | **A-11 / (h) 区切りの3通り** | 目次とロードマップは購入直後に読み比べられる |
| 8 | **G-5, G-6**（ローカルの画像を「本番環境の」と称している） | 事実と違う説明 |
| 9 | **I-1**（付録の Day 26-30 が実質空） | 統合元が各日に揃っているので作業は転記 |
| 10 | 残り | — |
