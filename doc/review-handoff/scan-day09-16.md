# Day 09〜16 品質監査（実測スキャン）

- 対象: `material/30days-curriculum/day09_*.md` 〜 `day16_*.md` の8本（計 17,789 行）
- 実施日: 2026-08-30
- 方法: 8本を全文通読 + `src/`・`prisma/`・`scripts/`・`biome.json`・`tsconfig.json` の現物照合 + Biome 実行 + スクリーンショット実画像の目視
- 参照: `audit/clean-run-2026-07-29.md`（Day 01〜10）、`audit/clean-run-2026-07-29-day11-20.md`（Day 11〜20）

## 0. 全体サマリ

| 項目 | 件数 |
|---|---|
| (a) 事実の誤り・矛盾 | 24件（うち BLOCKER 2 / 重大 6 / 中 9 / 軽 7） |
| (b) 図が要る節 | 19節 |
| (c) 理解チェック起案 | 24問（8日 × 3問） |
| (d) つまずきポイント | 18件（うち実測由来 7件） |
| (e) 型/ビルドエラーが残る日 | Day 11 のみ（無予告の追加エラーあり） |
| (g) 撮り直しが必要なスクリーンショット | **22ファイル全部**（参照 36箇所） |

### 最重大の5件

| # | 場所 | 内容 |
|---|---|---|
| 1 | `day11:987-995` / `day11:2045-2051` | `ProjectDetailView` は必須 props が8個なのに5個しか渡していない。教材自身が `day11:954` で「渡す値が足りないと型エラーになり、`npm run dev` が通りません」と書いておきながら、そのすぐ下のコードが3個足りない。Day 11 の終わりで `npm run build` が落ちる主因 |
| 2 | `day15:633`（`handleSubmit`） | Day 14 の `handleSubmit`（`day14:1367`）を消す指示が**無い**。同じ日の `handleEdit`(520) / `handleDelete`(800) / `handleCreate`(860) には全部「置き換え」注記があるのに、`handleSubmit` だけ抜けている。順に貼ると `Cannot redeclare block-scoped variable 'handleSubmit'` で止まる |
| 3 | `day13:773` | 「初期データでは参加プロジェクトが1つなので件数は変わりません」が事実と違う。読者は `admin@example.com`（`day06:724` の指示）でログインしており、シードでは2プロジェクト両方の OWNER。プロジェクトを絞ると 5件 → 3件/2件に**変わる** |
| 4 | 全22スクリーンショット | 全画像がサイドバー7項目（レポート/検索/ユーザー管理を含む）＋上部検索バーで撮られている。Day 09 時点の読者のサイドバーは3項目、Day 13 以降でも4項目。Day 20〜24 で作る UI が Day 09 の完成イメージに映っている |
| 5 | `day11:1057-1080` | 削除フローで「Day 10 で作ったプロジェクトを消せ」と指示した直後に、アーカイブフローで「プロジェクト詳細でアーカイブせよ」と言う。消した後に残るのはシードの2件だけなので、必ずシードをアーカイブすることになる。**解除する手順がどこにも無い** |

---

# Day 09（プロジェクト一覧画面 / 1,436行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| 9-a1 | 611, 748 | 重大 | 同じ `ProjectCard` の props ブロックを **Step 6 → Step 7 → Step 9 で3回**書かせる。611行は「Step 6 で書いた `{projects?.map(...)}` から `})}` までは、先にすべて削除してください」、748行は「`return` 全体を以下に置き換えます」。1日のうちに同じ12行を2回捨てる設計で、写経コストが実質3倍になる。教材自身が削除を明記しているぶん誤りではないが、買い手の不満に直結する構成 |
| 9-a2 | 915, 919 | 中 | `PORT=3001 npm run dev` が**説明なしで初登場**する。Day 01〜08 は一貫して `http://localhost:3000`。なぜ 3001 に変えるのかを書いているのは `day11:1036` が最初で、2日ぶん遅い |
| 9-a3 | 275, 344, 377, 400, 438, 470, 513, 582, 711, 730, 896 | 中 | 確認ポイントに「`npm run dev` で型エラーが出ていない」が11回出る。`next dev` は `tsc` を通さないので、この確認では型エラーを検出できない。`day04:179` で「`build` が見てくれるのは型まで」と教えているのと噛み合わない |
| 9-a4 | 1403 | 軽 | 「完成版には `src/app/project/page.tsx` がありますが」と書きつつ同じ段落で「販売用 ZIP に完成版の `src/` は入っていません」と続く。読者が参照できない物を基準に説明している |
| 9-a5 | 246 | 軽 | 「件数だけが欲しい場面では、Prisma の `_count` を使うとDB側で数えてから数字だけを返せます」と書くが、`_count` はこの教材で一度も使わない。用語集にも無い |

### 検証済み・**成立しない**とわかった既知指摘

- `audit/clean-run-2026-07-29.md` の「`day09:1379` の表と完成コードが矛盾する」は**現在は解消済み**。`day09:1381-1382` の2行は「`ProjectPage` の外側で `AppLayout` を囲むな」「`if (projectsLoading)` のスピナーは囲め」と書いており、完成コード（1232-1238 は囲む / 1354 の `Suspense` fallback は囲まない）と一致する。
- 同 audit の「`npm run fix` で `dialogOpen` が消えて Day 10 で手戻り」も**現在は解消済み**。Biome 2.4.15 で実測したところ、`biome check --write`（= `npm run fix`）は unsafe fix を適用せず `dialogOpen` はそのまま残る。`day09:738` の記述は正しい。

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| Step 0-5 の `include` 3階層（237-246） | ①目に見えん関係 | Project →(include members)→ ProjectMember →(include user)→ User、横に tasks。「なぜ入れ子が3段になるか」は表では出ない |
| Step 6 → Step 7 → Step 9 のコード変遷（528-891） | ③前後の変化 | 同じ `<div className="grid">` の中身が「map だけ」→「三項演算子」→「`return` 全体を組み直し」と3回変わる。読者が「今どの形か」を見失う唯一の場所 |
| 読み込み中の表示が2か所ある話（341, 445, 1729） | ①目に見えん関係 | `Suspense` の `fallback`（部品の読み込み待ち）と `if (projectsLoading)`（データ待ち）が別物であることを1枚で |

既存 mermaid: 25行（データ取得の sequenceDiagram）1本のみ。上記3つとは重複しない。

## (c) 理解チェック3問

1. **何をしとるか**: `getAll` の `where.members = { some: { userId: ctx.session.userId } }` は、どのプロジェクトを選ぶ条件か。
   - 答: ログイン中のユーザーがメンバーに含まれるプロジェクトだけを選ぶ条件（191-206行）。
2. **こう変えたらどうなるか**: `Step 2` の `data: projects` を `data: projects = []` に変えると、画面はどう変わるか。
   - 答: 読み込み中も `projects` が空配列になるため、`isLoading` の分岐より前では「まだ来ていない」と「0件」が同じ見た目になる（397行、1014行）。
3. **なぜこの書き方か**: 空状態の `<div>` に `col-span-full` を付けるのはなぜか。
   - 答: この `<div>` がグリッドの子なので、付けないと1列分の幅に押し込まれてメッセージが左端に寄る（683行、1345行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| 1 | `npm run lint` が**必ず落ちる**。(a) `dialogOpen` の `lint/correctness/noUnusedVariables`（Biome で実測・error 設定）(b) 教材のコードが Biome 整形前の形（`<h1 className="...`改行）なので formatter 差分も出る。前者は `day09:734` が、後者は `day05:356` が予告済み。ただし**「lint コマンド自体が exit 1 で終わる」とはどちらにも書いていない** | 実測（audit + 本監査で Biome 実行） |
| 2 | Step 7 で Step 6 のコードを削除し損ねると、カードが二重に出る | 教材が 611行で明記 |
| 3 | `PORT=3001` に切り替えた結果、3000 のタブが古いまま残り「変更が反映されない」と誤解する | 本監査（915行の無説明切替） |
| 4 | アーカイブ表示スイッチを ON にすると一覧が空になる（追加表示ではなく絞り込み） | 教材が 944-946行で明記 |

## (e) その日の終わりの状態

- アプリ: **動く**。`/project` にシードの2プロジェクトがカード表示される。
- 型エラー: **無し**。`dialogOpen` は Day 01 の `create-next-app` 由来 tsconfig（`noUnusedLocals` 無効）では型エラーにならない。
- ビルド: **通る**。
- lint: **落ちる**（上記 (d)1）。予告は `day09:734` と `day05:356` にあり、**無予告ではない**。

## (f) その日までに読者の手元にあるデータ

| 種別 | 件数 | 内訳 |
|---|---|---|
| ユーザー | 5 | シード4（`admin@example.com`「管理者」ADMIN / `user1@example.com`「田中太郎」/ `user2@example.com`「山田花子」/ `empty@example.com`「新人 太郎」）＋ Day 06 で読者が自分で登録した1件 |
| プロジェクト | 2 | 「Webサイトリニューアル」（メンバー3: 管理者=OWNER, 田中太郎=MEMBER, 山田花子=MEMBER）／「モバイルアプリ開発」（メンバー2: 管理者=OWNER, 田中太郎=ADMIN） |
| タスク | 5 | P1に3件（デザインモックアップ作成 IN_PROGRESS/HIGH/720分, データベース設計 DONE/HIGH/1200分, API仕様書作成 TODO/MEDIUM）、P2に2件（プロトタイプ開発 TODO/HIGH, ユーザーテスト実施 TODO/MEDIUM） |
| コメント | 2 | task1 と task2 に1件ずつ |

ログイン中のアカウント: `admin@example.com`（`day06:724` の指示）。**2プロジェクト両方の OWNER**。

**スクショ撮影用シード指定**: プロジェクト2件・カードの進捗は「1/3 タスク」「0/2 タスク」。サイドバーは **3項目（ダッシュボード/プロジェクト/マイタスク）**、上部検索バーなし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 15, 902 | `project-list.png` | ①同一日で2回使用。②実画像を確認: サイドバーに **タスク・レポート・検索・ユーザー管理**（Day 13/22/20/24 で作る）が写る、上部に**検索バー**（Day 20）が写る、シードに存在しない「**社内データ分析基盤**」が3枚目のカードとして映る、タスク件数が「1/3」「0/3」「1/4」でシード（1/3・0/2）と合わない、メンバー数が全部「3 メンバー」でモバイルアプリ開発の2人と合わない |
| 350 | `day09-step1.png` | 未検証だが同一環境で撮られているため同じサイドバー問題を持つ見込み |
| 443 | `day09-loading.png` | 同上 |
| 589 | `day09-cards.png` | **実画像を確認**。「ヘッダー実装前」という状態そのものは正しい（新規プロジェクトボタンとスイッチが無い）。ただしサイドバー7項目＋検索バー＋幻のプロジェクトは同じ |
| 681 | `day09-empty.png` | 未検証 |
| 936 | `day09-responsive.png` | 未検証。「レスポンシブ表示」と書きながら1枚しかない（幅違いの2枚が要る） |

- ③「ここをクリック」に赤枠等の視覚指示: **0件**。Step 10 の確認表（923-931）は「新規プロジェクトボタン」「カードの色帯・メンバー数・進捗」を見よと言うが、画像に一切の指示線が無い。
- **Day 09 の撮り直し必要枚数: 6ファイル（参照7箇所）**

---

# Day 10（プロジェクト新規作成 / 1,792行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| 10-a1 | 1748 | 重大 | つまずきポイントの行「作成ボタンを押しても何も起きない \| `currentUser` が null \| ログイン状態を確認」。**`currentUser` は Day 10 のコードに1つも存在しない**（初出は `day11:443`）。Day 10 の `handleSubmit`（757-773）にログイン確認は無い。読者が探しても見つからない変数名を原因として提示している |
| 10-a2 | 432, 469 | 重大 | Step 4 の `const handleClose = ...` と `return (` のコードブロックが**インデント0・貼り先の指示なし**（`// filepath:` だけ）。ファイル末尾に貼ると `reset` / `onClose` が未定義になる。同じ Day の他ブロック（724行「ProjectPageContent内に追加」等）は貼り先を明記しているのに、この2つだけ抜けている |
| 10-a3 | 195 | 中 | 確認ポイント「`getAll` の直後に `}),` `});` まで閉じた」が 0-2（142行）の「末尾にある `});` の1行上へ貼る。`});` は増やしません」と食い違って読める。`create` は `getAll` の `}),` の後ろに来るので「getAll の直後」は正しいが、「`});` まで閉じた」は既にある `});` を指すのか新たに書くのかが曖昧 |
| 10-a4 | 595 | 中 | 「選んだ色は、あとで Day 29 のユーザー詳細ページでバッジの背景になります」と Day 29 を前提に暗い色を選ばせる。Day 10 の読者には検証不能な理由で入力を制約している |
| 10-a5 | 744 | 軽 | 「この教材では、失敗を知らせる仕組みを Day 15 で `toast.error` として足します。それまでの Day 10 から Day 14 は…`onError` を書きません」。実際 Day 15 で `toast` を足すのは `updateMutation` だけで、`createMutation`（`day15:1968-1974`）と `deleteMutation`（`day15:2000-2005`）には最後まで `onError` が付かない |
| 10-a6 | 1777 | 中 | 「必須マークの付け方や列の分け方は違います」という**スクリーンショットとコードの不一致の断りが、ファイル末尾（1777行）にだけ**ある。画像を貼っている 15/642/839行の近くには無い |

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| Step 3 の `defaultValues` と `useEffect(reset)`（387-400, 417） | ②順番と分岐 | 「開く→defaultValues（初回だけ）→入力→閉じる→別プロジェクトで開き直す→reset」の時間軸。`defaultValues` が2回目に効かない理由は文章だと伝わらない |
| Step 7 の「保存は成功したのに一覧が古い」（796） | ③前後の変化 | `create` 成功時点のキャッシュ（作成前の配列）と DB（作成後）のズレ → `invalidate` → 取り直し、の3コマ |

既存 mermaid: 25行（作成フローの flowchart）1本。上記2つとは重複しない（既存図は「押してから表示まで」で、キャッシュの中身の変化は描いていない）。

## (c) 理解チェック3問

1. **何をしとるか**: Step 0 の `create` で `members: { create: { userId: ctx.session.userId, role: OWNER } }` は何をしているか。
   - 答: プロジェクト本体を作るのと同時に、作成者を OWNER としてメンバー行に1件登録している（161-163行）。
2. **こう変えたらどうなるか**: その `members.create` を消すとどうなるか。
   - 答: プロジェクトは作られるが作成者がメンバーでないため、`getAll`（自分がメンバーのものだけ返す）の一覧に出てこない（163行）。
3. **なぜこの書き方か**: キャンセルボタンに `type="button"` を書くのはなぜか。
   - 答: `<form>` の中のボタンは指定しないと送信ボタンになるため。付けないと押した瞬間に送信が走り、名前が空なら zod のエラーが出る（672行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| 1 | Step 4 の `handleClose` / `return (` をコンポーネントの外に貼って `reset is not defined` | 本監査（10-a2） |
| 2 | 作成しても一覧に出ない → `onSuccess` の `invalidate()` 忘れ | 教材が 796行で明記 |
| 3 | つまずき表の `currentUser` を探して見つからない | 本監査（10-a1） |
| 4 | 画面の入力欄の並び（カラーが1行、開始日/終了日が2列）がスクショと違う | 実画像で確認。教材コードは `grid-cols-3` で3列 |

## (e) その日の終わりの状態

- アプリ: **動く**。新規プロジェクトが作成でき、一覧に自動反映される。
- 型エラー: **無し**。ビルド: **通る**。
- lint: Day 09 の `dialogOpen` は Day 10 で読み出されるようになるため解消。整形差分は残る（`day05:356` で予告済み）。
- 予告: あり。

## (f) その日までに読者の手元にあるデータ

Day 09 の状態 ＋ Step 8 の動作確認で **プロジェクト1件を新規作成**（作成者=管理者が OWNER、メンバー1人）。

| 種別 | 件数 |
|---|---|
| ユーザー | 5（変化なし） |
| プロジェクト | **3**（シード2 ＋ 読者作成1） |
| タスク | 5（変化なし） |
| コメント | 2（変化なし） |

**スクショ撮影用シード指定**: プロジェクト3枚。3枚目は読者が作った「進捗 0/0 タスク・1 メンバー」のカード。サイドバー3項目、検索バーなし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 15 | `project-create-dialog.png` | 完成イメージとしては用途に合う。ただし②サイドバー7項目＋検索バー（Day 20-24 の UI）、③必須マーク `*` が写るが教材のコードには無い、④**列の分け方が教材と違う**（画像: カラー1行 + 開始日/終了日2列 ／ 教材: `grid-cols-3` で3列） |
| 642 | 同上（2回目） | ①使い回し。②キャプション「**フォーム入力中**のダイアログ」だが、実画像はプレースホルダーが見えている**完全に空のフォーム**。キャプションが事実と違う |
| 807 | `project-list-after-create.png` | 未検証 |
| 839 | 同上（3回目） | ①使い回し。②キャプション「完成した作成フロー」。貼り位置は Step 8 の動作確認表（「一覧を確認→新しいプロジェクトが追加されている」「カードの色帯を確認」）の直後なのに、実画像は**ダイアログが開いたまま・一覧も見えない**。確認したい対象が1つも写っていない |

- ③視覚指示: 0件
- **Day 10 の撮り直し必要枚数: 2ファイル（参照4箇所）**

---

# Day 11（プロジェクト編集・削除 / 2,277行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| **11-a1** | **987-995, 2045-2051** | **BLOCKER** | `src/component/project/project-detail-view.tsx` の `ProjectDetailViewProps` は **8個すべて必須**（`projectDetail` / `onBack` / `onAddMemberClick` / `onRemoveMember` / `onUpdateMemberRole` / `onArchive` / `canManageMembers` / `canArchive`、`?` は1つも無い）。教材の Step 9 と完成コードはどちらも**5個しか渡していない**（`onUpdateMemberRole` / `canManageMembers` / `canArchive` が欠落）。しかも `day11:954` が「この部品は Day 12 の機能まで含んだ形で配布されているので、**渡す値が足りないと型エラーになり、`npm run dev` が通りません**」と自分で警告している。予告されているのは `getById` の1件だけ |
| 11-a2 | 972 | 重大 | **設問の確認結果**: 「この時点ではエディタに `getById` が無いという型エラーの出ることがあります。**Day 12 Step 0 で `getById` を足すと消えます**」——**その日のうちには解消しない**。Day 11 は型エラーを残したまま終わる設計であることが明記されている |
| 11-a3 | 347, 923, 959 | 重大 | 一方で確認ポイントは「`npm run dev` で型エラーが出ていない」（347）「`npm run dev` でTypeScript エラーが出ていない」（959）と書く。959行は `ProjectDetailView` の import（964行）の直前なので厳密には矛盾しないが、読者は「型エラーが出ていない」を3回言われた直後に「型エラーが出ても進め」（977）と言われる |
| 11-a4 | 1057-1080 | 重大 | 削除フロー（1057-1065）で「Day 10 で自分が作ったプロジェクトを消せ」と指示 → その直後のアーカイブフロー（1071-1076）で「プロジェクト詳細画面でアーカイブせよ」。**消した後に残るのはシードの2件だけ**なので、読者は必ずシードプロジェクトをアーカイブする。**アーカイブを解除する手順が最後まで書かれていない**（Day 27 で「進行中とアーカイブ済みを1画面に並べる」と 1084行にあるが、それは11日後） |
| 11-a5 | 155 vs 292 | 中 | 155行が「ここから先の『（続き）』のブロックは、末尾にある `});` の1行上へ貼ります」と全称で宣言。しかし 295行の `setArchiveStatus` も「（続き）」なのに、貼り先は 292行で「`projectUpdateSchema` の下、`export const projectRouter` の上」と例外指定。さらに 312行では「このブロックだけは最後の `});` を含みます。ファイルの一番下にある `});` の1行を先に消してから」と第2の例外。1つの規則に例外が2つある |
| 11-a6 | 858 | 中 | 「実際のコードでは `deleteMutation` → `addMemberMutation` の間にいくつか mutation がありますが」——読者が持っていない完成版（`day11:1095` が「販売用 ZIP に完成版の `src/` は入っていません」の趣旨を明記）を根拠にした説明 |
| 11-a7 | 452, 549 | 軽 | 「その2行下の `};` までの3行を消してください」。Day 09 の受け皿は `const handleEdit = (projectId: string) => {` / `void projectId;` / `};` の3行なので正しいが、`npm run fix` を通した読者のファイルでは1行になっている可能性がある（Biome が短い関数を畳む）。行数で位置を指定している唯一の箇所 |
| 11-a8 | 2247-2250 | 軽 | 「表の3行目と4行目は、サーバーが `この操作を実行する権限がありません` を返しています」。表の3行目（削除ボタンを押しても消えない）は `delete` の独自チェック（272-277行）が返す文言で一致するが、4行目（アーカイブ）は `assertMemberPermission` 経由で、こちらも同じ文言。記述は正しいが「3行目と4行目」という行番号参照は表が編集されると壊れる |

### 検証済み・**成立しない**とわかった既知指摘

- audit の「`day11:347` と `day11:969` が矛盾する」は**部分的にしか成立しない**。347行は Step 0（サーバー側のみ）の確認ポイントで、`ProjectDetailView` を import する前。矛盾しているのは 347 ではなく、**959行の確認ポイントと 972行の予告**の距離の近さ、および (a)11-a1 の未予告エラー。

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| Step 3 の `null` と `undefined`（663-676） | ③前後の変化 | DB の1行を四角で描き、`null` を送ったとき（列が空になる）と `undefined` を送ったとき（列が変わらない）の前後2コマ。表（671-674）は「意味」しか書いておらず、行がどうなるかは絵でないと入らない |
| Step 9 の URL と state のズレ（1001） | ②順番と分岐 | `/project?projectId=x` を直接開いた1回目の描画で `selectedProject` がまだ `null`、という1描画ぶんの遅れ。2本の帯（URL / state）を時間軸で並べる |
| Step 0 の3つの権限チェックの使い分け（332-342） | ②順番と分岐 | update / delete / archive の3経路で「誰が通るか」の分岐。表があるが、`canDelete` が ADMIN にも付いているから `delete` だけ直接比較にした、という**なぜ**は分岐図でないと見えない |

既存 mermaid: 29行（編集・削除の2本の道）、827行（アーカイブの分岐）。上記3つとは重複しない。

## (c) 理解チェック3問

1. **何をしとるか**: `update` の `updateData` を空オブジェクトから始めて `if (data.name !== undefined)` で1つずつ足しているのは何をしているか。
   - 答: 送られてきた項目だけを更新対象にしている。送られなかった項目は `updateData` に入らず、Prisma が触らない（212行）。
2. **こう変えたらどうなるか**: `delete` の権限チェックを `assertMemberPermission(project.members, 'canDelete')` に書き換えるとどうなるか。
   - 答: `canDelete` はタスク削除用で ADMIN にも与えられているため、ADMIN がプロジェクトごと削除できてしまう（286-288行）。
3. **なぜこの書き方か**: `handleDelete` が `deleteMutation.mutate` を直接呼ばないのはなぜか。
   - 答: 削除は取り消せないため、`deleteTargetId` に控えて確認ダイアログを開き、実行の合図は `onConfirm` に預けるから（564行、49-51行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| **1** | **Day 11 完了時点で `npm run build` が `Failed to compile.` で落ちる。実測5件**（`getById` 1件＋そこから派生した `project-detail-view.tsx` の implicit any 4件）。教材が予告しているのは `getById` の1件だけ。`npm run dev` は型検査をしないので画面は動き、`day04:179` で「公開前に必ず `npm run build`」を習慣づけた読者が原因を追えなくなる | **実測**（`audit/clean-run-2026-07-29-day11-20.md`） |
| 2 | 上記に加えて、本監査で **`ProjectDetailView` の必須 props 3個欠落**を確認。これも同じ build 失敗の一因（11-a1） | 本監査（`src/component/project/project-detail-view.tsx:31-40` と照合） |
| 3 | Step 9 の仮定義を貼る位置を間違えて同名 `const` が2つになる | 教材が 2244行のつまずき表で明記 |
| 4 | 削除ボタンを押しても消えない（OWNER 以外）。画面には何も出ない | 教材が 2247-2250行で明記 |
| 5 | アーカイブしたシードプロジェクトが一覧から消えたまま Day 13 に進む | 本監査（11-a4） |

## (e) その日の終わりの状態

- アプリ: **動く**（`npm run dev` は起動し、編集・削除・アーカイブは操作できる）。
- 型/ビルドエラー: **残る**。`npm run build` は落ちる。
- 予告: **一部だけ有り＝実質「無予告」**。972行が `getById` 1件を予告するのみで、実測5件のうち4件と、本監査で見つけた必須 props 欠落は予告されていない。**「型エラーが1件残る」と読んだ読者が「5件出る」を見て自分の写経ミスを疑う**構図。

## (f) その日までに読者の手元にあるデータ

Day 10 の状態から、Step 10 の動作確認で以下が起きる。

| 種別 | 件数 | 備考 |
|---|---|---|
| ユーザー | 5 | 変化なし |
| プロジェクト | **2** | Day 10 で作った1件を削除（1057-1065行の指示）。**残り2件のうち1件が `isArchived = true` のまま**（1071-1076行、解除手順なし） |
| タスク | 5 | 変化なし（アーカイブはタスクを消さない。削除したのは読者作成のタスク0件のプロジェクト） |
| コメント | 2 | 変化なし |
| プロジェクト名の変更 | 1件 | 編集フロー（1044-1049）で名前を変更している |

**スクショ撮影用シード指定**: 編集ダイアログは「Webサイトリニューアル」の既存値入り。削除確認は読者作成のプロジェクト。アーカイブ表示 ON の一覧はカード1枚。サイドバー3項目、検索バーなし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 17, 1053 | `project-detail-dialog.png` | ①同一日で2回使用（17は冒頭の完成イメージ、1053は Step 10 の編集フロー確認）。ファイル名が `project-**detail**-dialog` なのにキャプションは「プロジェクト**編集**ダイアログ」。`src/component/project/project-detail-dialog.tsx` は別部品として実在するため、画像がどちらの UI か本文から判別できない |
| 691 | `project-list.png` | ②**Day 09 と同じ画像**。キャプションは「編集後に一覧が更新された画面」だが、Day 09 の一覧と1ピクセルも変わらない画像なので「編集後」の証拠にならない。③サイドバー7項目・幻の「社内データ分析基盤」も同じ |
| 802, 1069 | `project-delete-confirm.png` | ①同一日で2回使用 |
| 1022 | `project-detail-archive-action.png` | 未検証。Day 11 時点の詳細画面は `projectDetail = undefined`（仮定義）なので、実機では「プロジェクトが見つかりません。」しか出ない（`project-detail-view.tsx:52-59`）。**アーカイブボタンは Day 11 の読者の画面には出ない**。画像は Day 12 以降の状態 |
| 1080 | `archived-project-list.png` | 未検証 |

- ③視覚指示: 0件。1022行は「詳細画面のアーカイブボタンの表示を確認してください」と言いながら、どこがそのボタンかの指示が無い
- **Day 11 の撮り直し必要枚数: 5ファイル（参照7箇所）**

---

# Day 12（メンバー追加 / 2,949行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| 12-a1 | 111 vs 687 | 中 | 実装ステップ一覧は「Step 2 \| ProjectDetailViewのpropsを**確認する**」、実際の見出しは「Step 2: ProjectDetailViewのpropsを**作る**」。目次と本文の齟齬 |
| 12-a2 | 691 | 重大 | Step 2 本文「`ProjectDetailView` は独立したコンポーネントとして**作ります**。まず props の型定義を決めて」。しかし 73行の作業ファイル一覧と 80行は「`project-detail-view.tsx` にはメンバー一覧の見た目が**用意されています**」と書いており、読者はこのファイルを1行も書かない。Step 2 で実際にやるのは表(695-704)を読むことだけ |
| 12-a3 | 1225 | 中 | コメント「// **Step 2** の仮実装を削除してここに書き換える」。しかし仮実装を置いたのは **Day 11 Step 9** で、Day 12 Step 2 は 711行で「ここでは仮定義がすでにあることを確認するだけで、コードの追加は不要です」と明言している。同じ Step 6 の本文（1194行）は正しく「Day 11 Step 9 で書いた仮定義」と書いており、コードコメントだけが間違っている |
| 12-a4 | 1379-1381 | 中 | キャプション「メンバー一覧に3人が並び、それぞれにロールが付いている画面」に対して貼っている画像は `project-detail-**tasks**.png`。683行で「左にメンバー、右にタスクが並ぶ」として使った画像と同一。1377行の前置きも「メンバーを追加する**前**の一覧です」と、1379行のキャプション（3人が並ぶ）と噛み合わない |
| 12-a5 | 1277-1283 | 軽 | メンバー削除の確認ダイアログに `project-delete-confirm.png`（プロジェクト削除の画像）を使い、1279-1283行で「この画像で見出しの文字を照らし合わせても意味がありません」と5行かけて弁解している。読者に「合っていない画像を見せられている」と明示している状態 |
| 12-a6 | 1375 | 軽 | テストシナリオ2手順3「OWNERの削除ボタンを確認 \| ボタンが `disabled` で押せない」。`project-detail-view.tsx:200` の `disabled={member.role === PROJECT_MEMBER_ROLE.OWNER}` と一致するので**正しい**（照合済み）。ただし読者は自分が OWNER なので、自分の行のボタンが押せない、という体験になる説明が無い |
| 12-a7 | 103 | 軽 | 「`roles.ts` には `canEdit` という権限定義がありますが、`project.ts` の `update` API は `canManageMembers` を見ています」。この注記は正しいが、Day 13（`day13:882`）で `canEdit` を使ってタスク編集ボタンの出し分けをするので、「`canEdit` は使わない権限」と誤読される |

### 検証済み・**成立しない**とわかった既知指摘（audit の 4件が現在は解消済み）

| audit の指摘 | 現在の状態 |
|---|---|
| 「（続き）」ブロックの貼り先が複数ある（`day12:253` の `projectMemberSchema`） | **解消済み**。250行に「次のブロックだけは『（続き）』と書いてありますが、ルーターの**外**、`projectUpdateSchema` の下へ貼ります。`});` の1行上へ入れるとルーターの中に入ってしまい、英語のエラーで止まります」と明記されている |
| `day12:604` の `handleProjectClick` が二重宣言になる | **解消済み**。604行が「あわせて、Day 09 で置いた受け皿の `handleProjectClick` も **削除して** 本実装に書き換えてください」、606行にも重ねて注意がある |
| `day12:1193` が「Step 2 の `console.log` 版を削除」と書くが該当コードが無い | **概ね解消済み**。1194行は「Day 11 Step 9 で書いた仮定義（何もしない空実装）を削除」に直っている。ただし 1225行のコードコメントに「Step 2 の仮実装」という言い回しが残る（12-a3） |
| `day12:1373` の「候補は1人だけ」が実際は3件 | **解消済み**。1381行は「追加の候補には『新人 太郎』と、Day 06 で自分で登録したアカウントが並びます」に直っている |

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| 0-3/0-4/0-5 の OWNER 保護3か所（280-298, 391-405, 502-517） | ②順番と分岐 | `addMember`（OWNER を作らせない）/ `removeMember`（OWNER を外させない）/ `updateMemberRole`（昇格も降格も止める）の3経路を1枚の分岐図に。3か所に散っているので、読者は「同じ守りを3回書いた」ことに気づけない |
| 0-5 の `$transaction` で `count` する理由（543） | ②順番 | 2本のリクエストの時間軸。「両方が『OWNER は2人』と数える → 両方が降格させる → 0人になる」を2レーンで |
| `getAvailableUsers` の `none`（239） | ①目に見えん関係 | User 全体 ⊃ このプロジェクトのメンバー、その差集合が候補。`some`（Day 09）との対比をベン図で |

既存 mermaid: 27行（page.tsx ↔ ProjectDetailView の props/callback）、1303行（MEMBER が弾かれる sequence）。上記3つとは重複しない。

## (c) 理解チェック3問

1. **何をしとるか**: `getAvailableUsers` の `projects: { none: { projectId: input.projectId } }` はどんなユーザーを選ぶか。
   - 答: そのプロジェクトのメンバー行を1件も持たないユーザー、つまりまだ参加していない人（239行）。
2. **こう変えたらどうなるか**: `addMember` の重複チェック（`if (existing)`）を消すとどうなるか。
   - 答: `userId_projectId` の一意制約に当たり、Prisma の例外がそのまま画面に出る。日本語の説明が付かない DB エラーが利用者に見える（321行）。
3. **なぜこの書き方か**: `canManageMembers` を持つ ADMIN でも OWNER を追加できないようにしているのはなぜか。
   - 答: ADMIN が自分の別アカウントを OWNER として追加し、元の OWNER を外してプロジェクトを乗っ取れるため（294-298行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| 1 | Day 11 の仮定義4つ（`projectDetail` / `handleDetailClose` / `memberDialogOpen` / `handleRemoveMember`）と Day 09 の `handleProjectClick` を消し忘れて同名 `const` が2つになる | 教材が 606/659/851/1194行で個別に明記（**5か所とも指示あり**を確認済み） |
| 2 | 「このユーザーは既にプロジェクトのメンバーです」（CONFLICT） | **実測**（audit が実 API で確認） |
| 3 | 「プロジェクト唯一のオーナーは削除できません」（BAD_REQUEST） | **実測**（audit が実 API で確認） |
| 4 | メンバー追加ボタンが出ない → MEMBER でログインしている | 教材が 1355行で明記 |
| 5 | Step 4 のダイアログを貼っても `handleAddMember` が未定義でエラー（Step 5 まで定義されない） | 教材が 1088-1089行で明記 |

## (e) その日の終わりの状態

- アプリ: **動く**。詳細ページ・メンバー追加・削除・ロール変更が通る。
- 型/ビルドエラー: **無し**。Step 0 で `getById` が入り、Step 2 で `ProjectDetailView` に8 props を渡すので、Day 11 の残エラーが**この日に全部解消する**。
- 予告: `day11:1099` が「`getById` / `addMember` / `removeMember` は Day 12 で追加する」と書いており、解消は予告どおり。

## (f) その日までに読者の手元にあるデータ

Day 11 の状態から、Step 8 のテストシナリオ1で **メンバー1人を追加**、シナリオ2で **メンバー1人を削除**。

| 種別 | 件数 | 備考 |
|---|---|---|
| ユーザー | 5 | 変化なし |
| プロジェクト | 2 | うち1件は Day 11 でアーカイブしたまま |
| ProjectMember | 5 → 6 → 5 | 「Webサイトリニューアル」が3人→4人→（削除して）3人。追加候補は「新人 太郎」と Day 06 登録の自分のアカウント |
| タスク | 5 | 変化なし |
| コメント | 2 | 変化なし |

**スクショ撮影用シード指定**: 詳細ページのメンバー欄は「メンバー (3)」。追加ダイアログの候補は2件（新人 太郎 / 読者のアカウント）。右のタスク欄は3件。サイドバー3項目、検索バーなし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 17 | `project-detail-members.png` | 未検証 |
| 683, 1379 | `project-detail-tasks.png` | ①同一日で2回使用。②1379行のキャプション「メンバー一覧に3人が並び、それぞれにロールが付いている画面」がファイル名（tasks）と一致しない。③1377行の前置き「メンバーを追加する**前**の一覧です」とキャプションが噛み合わない |
| 1099 | `project-add-member.png` | 未検証。候補リストの中身がシード（新人 太郎＋読者アカウント）と合うかは要確認 |
| 1277 | `project-delete-confirm.png` | ②**プロジェクト削除の画像をメンバー削除の説明に流用**。教材自身が 1279-1283行で「この画像で見出しの文字を照らし合わせても意味がありません」と弁解 |

- ③視覚指示: 0件
- **Day 12 の撮り直し必要枚数: 4ファイル（参照5箇所）**

---

# Day 13（タスク一覧画面 / 1,812行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| **13-a1** | **773** | **重大** | 確認ポイント「プロジェクトを選択すると表示が絞り込まれる（**初期データでは参加プロジェクトが1つなので件数は変わりません**）」。**事実と違う**。`src/command/seed.ts` を確認したところ、`admin@example.com`（`day06:724` が Day 07 以降のログイン先として指定したアカウント）は「Webサイトリニューアル」と「モバイルアプリ開発」**両方の OWNER**。したがって `/task` には5件出て、プロジェクトで絞ると 3件／2件に**変わる**。読者は「件数が変わってしまった＝自分の実装が間違い」と誤解する |
| 13-a2 | 1064 | 重大 | 「第2引数ごと省くと、描画のたびに中身が動きます。**第2引数ごと省くと描画のたびに走ります。** ダイアログを閉じても値がすぐ戻るので、閉じられない画面になります。**ただし、あとで中身を書き換えたときに、描画のたびに走る処理として残ります。**」——同じ文が2回続き、その後の2文も接続が壊れている。編集ミスがそのまま残っている段落 |
| 13-a3 | 1069 | 軽 | 「Step 6 の `handleTaskClick` の**仮実装（空の関数）**を以下の本実装に差し替えます」。Step 6（807-810行）の実装は空関数ではなく `void taskId;` を書いている |
| 13-a4 | 15 | 中 | 冒頭の完成イメージ `task-list.png` に、Day 16 で作る「合計作業時間: 0m」と「時間記録」ボタンが全カードに写っている。同じ Day の 825行が「`timeSpentMinutes`（合計作業時間）という作業時間まわりの prop は、いまの `TaskCard` にはまだありません」と明記しているのと真っ向から食い違う |
| 13-a5 | 547-551 | 軽 | `api.project.getAll.useQuery()` を引数なしで呼ぶ。Day 09 のページは `{ isArchived: showArchived }` を渡すので、こちらは**アーカイブ済みも含む全件**が返る。Day 11 でシードを1件アーカイブしている読者は、プロジェクトフィルターにアーカイブ済みが並ぶ。説明が無い |
| 13-a6 | 927 | 軽 | 「else 側をいったん `<div />` にしているのは、0件のときの表示をこの節の最後で差し替えるからです」。同じ Step 6 の中で2回書かせる構成で、Day 09 の Step 6→7 と同じ手戻りパターン |

### 検証済み・**成立しない**とわかった既知指摘（audit の 2件が現在は解消済み）

| audit の指摘 | 現在の状態 |
|---|---|
| `day13:826/1009` の手順どおりだと `react` の import が3本になる | **解消済み**。832行に「// react は Step 1 で書いた行に足します。新しい行は増やしません。」、1016行に「// react はここでも既にある行に足します。」がある |
| `day13:802` の仮ハンドラーが未使用引数を放置 | **解消済み**。807-818行は3つとも `void taskId;` を書いており、Day 09 と揃っている |

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| 0-3 の2段の絞り込み（200-217） | ①目に見えん関係 ＋ ②順番 | (1) `where.projectId = { in: projectIds }` で「自分の全プロジェクト」に限定 →(2) `input.projectId` があればその1件に**上書き**、ただし範囲外なら FORBIDDEN。「範囲を先に決めてから絞る」順序が絵でないと入らない |
| Step 6 の `myRoleByProject` Map（852-867） | ①目に見えん関係 | `projects[]` × `session.user.id` → `Map<projectId, role>` → `canEditProject(task.projectId)` → カードのボタン表示、という4段の対応関係 |

既存 mermaid: 25行（一覧の構成）、46行（フィルタリングのデータフロー）。上記2つとは重複しない（既存2本はどちらも「画面の部品構成」と「state→再取得」で、権限の対応表は描いていない）。

## (c) 理解チェック3問

1. **何をしとるか**: `getUserProjectIds(ctx.session.userId)` の結果を `where.projectId = { in: projectIds }` に入れているのは何のためか。
   - 答: ログイン中のユーザーがメンバーであるプロジェクトのタスクだけに、検索の範囲をあらかじめ限定するため（217行）。
2. **こう変えたらどうなるか**: Step 4 のステータス選択で `isTaskStatus(value)` を外して `setFilterStatus(value as TaskStatus)` にするとどうなるか。
   - 答: 中身を確かめないまま型だけ合わせることになり、想定外の文字列がそのままサーバーへ飛ぶ（712行）。
3. **なぜこの書き方か**: `canEdit` / `canDelete` を `TaskCard` に必ず渡すのはなぜか。
   - 答: 渡さないと `TaskCard` 側の既定値 `true` が使われ、閲覧者にも編集・削除ボタンが見えるから（956行、1730行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| **1** | **プロジェクトで絞ると件数が変わるのに「変わりません」と書いてある**（13-a1）。読者が最初に「教材が間違っている」と気づく地点になり得る | 本監査（`src/command/seed.ts` と照合） |
| 2 | `react` の import が3回に分かれて重複宣言になる | **実測**（audit で成立）。ただし現在は 832/1016行の注記で解消済み |
| 3 | サイドバーの `menuItems` に `/task` を足し忘れて 404 | 教材が 432行で明記 |
| 4 | 1064行の壊れた段落で `useEffect` の依存配列の説明が読み取れない | 本監査（13-a2） |
| 5 | Step 6 の `<div />` を差し替え忘れ、0件のときに何も出ない | 教材が 962行で明記 |

## (e) その日の終わりの状態

- アプリ: **動く**。`/task` にタスク5件がカード表示され、フィルターと詳細ダイアログが動く。
- 型/ビルドエラー: **無し**。Step 0 で `getById` を先に書くので、`TaskDetailDialog` の型も Day 13 中に揃う（286行に「Day 13 の終了時点で型チェックと詳細表示の両方が成立するように」と明記）。
- 予告: 不要（残らない）。

## (f) その日までに読者の手元にあるデータ

Day 12 の状態と同じ（Day 13 はデータを作らない）。

| 種別 | 件数 | 備考 |
|---|---|---|
| ユーザー | 5 | |
| プロジェクト | 2 | うち1件は Day 11 でアーカイブしたまま。**タスク一覧のプロジェクトフィルターには2件とも並ぶ**（13-a5） |
| タスク | 5 | Webサイトリニューアル3件（IN_PROGRESS/DONE/TODO）、モバイルアプリ開発2件（TODO/TODO） |
| コメント | 2 | |

**スクショ撮影用シード指定**: タスクカード5枚（グリッド）。カードに**合計作業時間と時間記録ボタンは無い**（Day 16 で追加）。ヘッダーは h1 + フィルター2つだけ（新規タスクボタンは Day 14）。担当者フィルターは無い（Day 20）。サイドバー**4項目**、検索バーなし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 15 | `task-list.png` | ②**実画像を確認**。(1) サイドバー7項目（レポート/検索/ユーザー管理＝Day 20-24）(2) 上部検索バー（Day 20）(3) **全カードに「合計作業時間: 0m」と「時間記録」ボタン**（Day 16）(4) タスクが8枚以上写り、うち「初めての…」「プッシュ…」「売上デー…」「ダッシュ…」「BIツール…」はシードに存在しない。読者の画面（5枚・作業時間表示なし）と全く違う |
| 778 | `task-list-filtered.png` | 未検証。キャプション「ステータスを完了に絞った状態」に対し、読者のシードで DONE は「データベース設計」1件のみ。1枚だけの画面になるはずで、画像がそうなっているか要確認 |
| 1109 | `task-detail-dialog.png` | 未検証。Day 16 でも同じ画像を使う（`day16:26`） |

- ③視覚指示: 0件
- **Day 13 の撮り直し必要枚数: 3ファイル（参照3箇所）**

---

# Day 14（タスク新規作成 / 3,300行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| 14-a1 | 3050-3053 | 重大 | 完成コードの `handleEdit` / `handleDelete` が `(taskId: string) => {}`（**空の本体・引数未使用**）。Day 13 Step 6（`day13:811-818`）は同じ関数を `void taskId;` 付きで書かせており、Day 09（`day09:501`）も同じ流儀。Biome で実測すると `lint/correctness/noUnusedFunctionParameters` が出る。完成コード全体は 975行相当の前置きで「上から順に貼り付けて各ファイルを置き換えてください」と誘導しているため、貼った読者は Day 13 で正しかった書き方を失う |
| 14-a2 | 1396-1399 | 中 | 「作ったタスクは、一覧の**いちばん下**に足されます」。`orderBy: [{ position: 'asc' }, { createdAt: 'desc' }]` は全プロジェクト横断なので、シードの position は P1=1,2,3 / P2=1,2。**モバイルアプリ開発に作ると position 3** になり、同じ position 3 の「API仕様書作成」と並んで `createdAt` 降順で**先に**来る。「いちばん下」が成り立つのは Webサイトリニューアルに作った場合だけ |
| 14-a3 | 766-774 vs 680 | 軽 | 680行「`useForm` から受け取った**7つ**は、すべてこのあとの入力欄で使います」。表（766-774）には5つ（`register` / `handleSubmit` / `control` / `reset` / `errors`）しか載っておらず、`watch` と `setValue` が抜けている。しかもこの2つは「入力欄」ではなく Step 3 の `useEffect` で使う |
| 14-a4 | 151-153 | 中 | `getNextTaskPosition` が `tx.$queryRaw` で生 SQL（`SELECT "id" FROM "projects" WHERE "id" = ${projectId} FOR UPDATE`）を書く。教材はここまで一貫して Prisma のメソッドだけで書かせており、テーブル名 `"projects"`（`@@map("projects")` 由来）が突然出てくる。なぜ Prisma で書けないのかの説明が無い |
| 14-a5 | 20, 934, 1199 | 重大 | 同一の `task-create-dialog.png` を**3回**貼っている。934行は Step 4（タイトルと説明だけを作った直後）、1199行は Step 6（プロジェクトと担当者を作った直後）。しかし実画像には**ステータス・優先度・プロジェクト・担当者・期限・見積時間が全部そろっている**（＝Step 7 完了後の姿）。Step 4 の読者が自分の画面と見比べると「6つ足りない」と見える |
| 14-a6 | 1338 | 軽 | 「`getSession` のほうは、送信の直前にログインが切れていないかを確かめるために使います」。実際の `handleSubmit`（1369行）は `session?.user?.id` を見るだけで、`getSession` を送信直前に呼び直してはいない。Day 13 で1回取ったキャッシュを見ている |
| 14-a7 | 452 | 軽 | 「`src/component/task/task-dialog.tsx` は **Day 01 の scaffold が配布済み**です。今日はこの部品を作ることが学習の主役なので、開いて中身をすべて書き換えます」。Day 10 の `project-dialog.tsx`（`day10:70`）も同じ扱いだが、そちらは「配布済み」の断りが冒頭のファイル構成図にもある。Day 14 は 69行の「配布済みの TaskDialog を書き直す」だけで、構成図が無い |

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| 0-2 の `FOR UPDATE` ロック（150-170） | ②順番 | 2本のリクエストの時間軸。ロック無しだと両方が「最大値 3」を読んで両方 4 を付ける／ロック有りだと後発が待たされて 5 を得る。この教材で唯一の並行性の話 |
| Step 3 の3つの `useEffect`（704-748） | ②順番 | 「`projectsRef` を最新に保つ」「開いた瞬間に `reset`」「候補が遅れて届いたら `projectId` だけ `setValue`」がそれぞれいつ走るか。3つが別々の理由で存在することは文章では追えない |
| Step 6 のプロジェクト切替の連鎖（1098-1114, 684-698） | ②順番と分岐 | プロジェクトを選び直す →`setValue('assigneeId','')`→ `watch('projectId')` が変わる → `getMembersByProject` が取り直す → 担当者の候補が入れ替わる |

既存 mermaid: 46行（作成フローの graph）1本。上記3つとは重複しない。

## (c) 理解チェック3問

1. **何をしとるか**: `assertTaskAssigneeBelongsToProject(input.projectId, input.assigneeId)` は何を確かめているか。
   - 答: 指定された担当者が、そのプロジェクトの `ProjectMember` に存在するか。いなければ `BAD_REQUEST` で弾く（176-199行）。
2. **こう変えたらどうなるか**: `Select` を `register('status')` で登録するとどうなるか。
   - 答: `Select` の引き金は `<button>` で `value` を持たず `change` も出さないため、値がフォームに入らない（968行）。
3. **なぜこの書き方か**: 担当者の未選択を空文字ではなく `'unassigned'` という文字列で持つのはなぜか。
   - 答: shadcn/ui の `Select` は空文字を選択済みとして扱えず、placeholder が出たままになるから。送信時に空文字へ戻す（1167行、1191行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| 1 | Step 4/Step 6 のスクショと自分の画面が違う（項目が6つ足りない／多い） | 本監査（14-a5、実画像を確認） |
| 2 | 作ったタスクが「いちばん下」に見つからない（モバイルアプリ開発に作った場合） | 本監査（14-a2、`seed.ts` の position と `orderBy` から算出） |
| 3 | `session` を2回宣言してページ全体が止まる | 教材が 1334行で明記 |
| 4 | 担当者一覧が空 → プロジェクト未選択 | 教材が 3270行のつまずき表で明記 |
| 5 | `TaskDialog` をグリッドの中に貼ってカードの並びに巻き込まれる | 教材が 1427行で明記 |

## (e) その日の終わりの状態

- アプリ: **動く**。タスクを作成でき、一覧に自動反映される。
- 型/ビルドエラー: **無し**。
- lint: 完成コードをそのまま貼った場合のみ `noUnusedFunctionParameters` の警告が2件出る（14-a1）。Biome では warning 扱いなので `npm run lint` は落ちない。予告は無い。

## (f) その日までに読者の手元にあるデータ

Day 13 の状態 ＋ Step 9 の動作確認で **タスクを1件作成**。

| 種別 | 件数 | 備考 |
|---|---|---|
| ユーザー | 5 | |
| プロジェクト | 2 | うち1件アーカイブ済み |
| タスク | **6** | シード5 ＋ 読者作成1（担当者・期限・見積時間は任意） |
| コメント | 2 | |

**スクショ撮影用シード指定**: 作成ダイアログのプロジェクト選択肢は2件。担当者選択肢は「未割当」＋選択中プロジェクトのメンバー（Webサイトリニューアルなら3人）。一覧は6枚。サイドバー4項目、検索バーなし、担当者フィルターなし、合計作業時間なし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 20, 934, 1199 | `task-create-dialog.png` | ①**同一日で3回使用**。②**実画像を確認**: (1) サイドバー7項目＋上部検索バー（Day 20-24）(2) 背景の一覧に「**すべての担当者**」フィルター（Day 20）(3) 背景のカードに「**合計作業時間: 100h 0m**」と「**時間記録**」ボタン（Day 16）(4) 背景のカードに「**新規タ…**」「**repro3…**」というシードに無いタスク (5) 入力欄に**必須マーク `*`** が付いているが教材のコードには無い。③934行（Step 4）と1199行（Step 6）は途中経過のはずなのに完成形の画像 |
| 1403 | `task-list-after-create.png` | 未検証 |

- ③視覚指示: 0件
- **Day 14 の撮り直し必要枚数: 2ファイル（参照4箇所）**

---

# Day 15（タスク編集・削除 / 2,373行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| **15-a1** | **631-633** | **BLOCKER** | Step 4 が `const handleSubmit = (data: TaskFormData) => {` を新規に書かせるが、**Day 14 で書いた同名の関数（`day14:1367`）を消す指示が無い**。同じ Day 15 の他の3つのハンドラーには全部「置き換え」注記がある——`handleEdit`（520行「この中身へ**丸ごと置き換え**ます。2つ並べると同じ名前を2回宣言することになり、ページ全体が止まります」）、`handleDelete`（800行「Day 13 で置いた仮の `handleDelete` を**置き換え**ます」）、`handleCreate`（860行「Day 14 で書いた `handleCreate` を**置き換え**ます」）。`handleSubmit` だけ抜けており、順に貼ると `Cannot redeclare block-scoped variable 'handleSubmit'` で止まる |
| 15-a2 | 229 | 重大 | `const targetProjectId = isProjectChanging ? (data.projectId **as string**) : existingTask.projectId;` ——型アサーション `as` を使っている。同じ教材の `day13:712` は「そのまま `setFilterStatus(value)` と書くと型が合わず、`as TaskStatus` で黙らせたくなります。ただ、それは中身を確かめずに正しいと言い張る書き方なので」と `as` を明確に否定し、`day13:715` の確認ポイントも「`as` キャストではなく `isTaskStatus()` 型ガードで安全に判定している」。教材が自分の禁じ手を使っている（`isProjectChanging` が真なら `data.projectId` は `undefined` でないので、`if` の中で扱えば `as` は不要） |
| 15-a3 | 26, 23-24 | 重大 | 冒頭のスクショに `task-create-dialog.png`（**作成**ダイアログ）を貼り、23-24行で「下の画像は新規作成のときの姿で、編集で開くと見出しが『タスク編集』、ボタンが『更新』に変わり、各欄には今の値が入ります」と3行かけて弁解している。Day 15 の主題そのもの（編集モードの見た目）を示す画像が1枚も無い |
| 15-a4 | 846-849 | 中 | 削除確認に `project-delete-confirm.png`（**プロジェクト**削除の画像）を流用し、848-849行で「画像はプロジェクトを消すときのものなので見出しが違います」と弁解。Day 12 の 1277行と同じパターンで、2日連続で同じ弁解をしている |
| 15-a5 | 849 | 軽 | 「見出しは `本当に削除しますか` の形になります」。実際の既定値は `src/component/ui/delete-confirm-dialog.tsx:27` で `'本当に削除しますか？'`（末尾に `？`）。文字が1つ違う |
| 15-a6 | 764-778 | 中 | Step 6 の `deleteDialogOpen` / `deleteTargetId` / `deleteMutation` を書かせるコードブロックに**貼り先の指示が無い**（`// 削除用のstateとmutation` だけ）。Day 11 の同じ場面（`day11:516`「`const [showArchived, ...]` の直下に追加してください」）は位置を明記している |
| 15-a7 | 96, 109 | 軽 | Step 0 が30分、全体79分。8日中で最長だが、Step 0 だけで11の小節（0-1〜0-11）に分かれる。他の日の Step 0 は最大でも 0-8（Day 13）。「30分」が現実的かの根拠が無い |

### 検証済み・**成立しない**とわかった既知指摘

- audit の「`day15` の完成コードに、`day14` が『追加しない』と明記した `api.search.getProjectMembers` が入っている」は**解消済み**。Day 15 全文を `grep` した結果 `getProjectMembers` は0件。

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| 0-9 の楽観ロック（297-331） | ②順番 | 2人（または「別の人」と「自分の時間記録」）の時間軸。A が開く → B が開く → A が保存（`updatedAt` が動く）→ B が保存 → `where` が一致せず P2025 →CONFLICT。**この教材で唯一の並行更新の話で、文章だけでは絶対に伝わらない** |
| 0-8 のプロジェクト移動時の担当者処理（269-295） | ②順番と分岐 | 「担当者が送られてきた／来ていない」×「プロジェクトが変わる／変わらない」の4象限。`else if` の中身がなぜ必要かが分岐図でしか見えない |

既存 mermaid: 48行（編集・削除の2本の道）1本。上記2つとは重複しない。

## (c) 理解チェック3問

1. **何をしとるか**: `update` の `where` に `updatedAt: new Date(expectedUpdatedAt)` を足すと何が起きるか。
   - 答: 「編集を始めた時点から更新時刻が変わっていないとき**だけ**書き換える」条件になる。変わっていれば対象が見つからず失敗する（331行）。
2. **こう変えたらどうなるか**: `description: data.description || null` を `data.description ?? null` に変えるとどうなるか。
   - 答: 空文字は `??` を素通りするため、説明を空にしたつもりが「長さ0の説明がある」状態で保存される（653行、`day11:684-687`）。
3. **なぜこの書き方か**: 更新では日付が空のとき `null`、新規作成では `undefined` を送るのはなぜか。
   - 答: `null` は「既存の日付を消す」指示、`undefined` は「この項目には何もしない」指示で、Prisma はこの2つを区別するから（677-688行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| **1** | **`handleSubmit` の二重宣言でページが止まる**（15-a1）。同じ日に「同名 `const` を2つ書くと止まる」を3回警告しておきながら、4つ目だけ警告が無い | 本監査 |
| 2 | Step 6 の state / mutation をどこへ貼るか分からない | 本監査（15-a6） |
| 3 | `toast` の import 忘れで、失敗した瞬間だけ `toast is not defined` | 教材が 565行で明記 |
| 4 | `timeSpentMinutes` / `onTimeLogSuccess` を今日渡すと型エラー | 教材が 921-925行で明記 |
| 5 | 編集ダイアログの日付が空のまま開く → `taskToFormData` の変換を疑う | 教材が 547-549行で明記 |

## (e) その日の終わりの状態

- アプリ: **動く**。編集・削除が通り、更新失敗時は toast が出る（`Toaster` は `src/app/providers.tsx:10` にマウント済みで、読者は追加不要）。
- 型/ビルドエラー: **無し**（15-a1 を回避できた場合）。**15-a1 に素直に従うと `Cannot redeclare` で止まる**。
- 予告: 15-a1 について予告は無い。

## (f) その日までに読者の手元にあるデータ

Day 14 の状態から、Step 11 の動作確認で **タスク1件を編集**（優先度など）、**タスク1件を削除**。

| 種別 | 件数 | 備考 |
|---|---|---|
| ユーザー | 5 | |
| プロジェクト | 2 | うち1件アーカイブ済み |
| タスク | **5** | Day 14 で6件 → 1件削除。編集で1件の優先度が変わっている |
| コメント | 2 | ただし削除したタスクにコメントが付いていた場合は連動して消える（`onDelete: Cascade`）。動作確認で消すのは Day 14 で自分が作ったタスクにするのが安全（教材に指定が無い） |

**スクショ撮影用シード指定**: 編集ダイアログは既存値入り・見出し「タスク編集」・ボタン「更新」。削除確認は見出し「本当に削除しますか？」。一覧は5枚。サイドバー4項目、検索バーなし、合計作業時間なし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 26 | `task-create-dialog.png` | ②Day 14 と共有。**編集モードの画像が1枚も無い**（見出し「タスク作成」・ボタン「作成」の画像で、Day 15 の主題を説明している）。教材自身が 23-24行で弁解 |
| 846 | `project-delete-confirm.png` | ②**プロジェクト**削除の画像をタスク削除に流用。Day 11・Day 12 と合わせて3日連続で同じ画像 |
| 965 | `task-list-after-edit.png` | 未検証。キャプション「先頭のタスクの優先度が高に変わっている」は、読者が何を編集したかに依存する（教材は「タイトルや優先度を変更」としか言っていない） |

- ③視覚指示: 0件。965行は「先頭のタスクの優先度が高に変わっている」と言うが、どのカードのどのバッジかの指示が無い
- **Day 15 の撮り直し必要枚数: 3ファイル（参照3箇所）**

---

# Day 16（ステータス変更・時間記録 / 1,850行）

## (a) 事実の誤り・矛盾

| # | 行 | 重大度 | 内容 |
|---|---|---|---|
| 16-a1 | 1788-1803 | 重大 | 完成コードの `<TaskCard>` に **`key={task.id}` が無い**。Day 13（1712行）・Day 14（3172行）・Day 15（894行）の同じ `<TaskCard>` にはすべて `key` がある。完成コードをそのまま貼った読者は React の key 警告を受ける。しかも `day09:1325` と `day13:932` で「`key` を消すとコンソールに警告が残る」と2回教えている |
| 16-a2 | 704-724, 806-820 | 重大 | Step 3 の `task-card.tsx` の編集指示が**省略記号だらけの疑似コード**。`interface TaskCardProps { // ...既存のpropsのうち onEdit より前... }`（708行）、`<Card>{/* ...カードの中身... */}</Card>`（809行）。読者はこのファイルの中身を一度も見ていない（配布物）。さらに 721-724行は「`TaskCard` 関数の引数（分割代入）では `timeSpentMinutes = 0` と既定値 0 を付けてください」と文章だけで指示し、対応するコードブロックが無い。Day 09〜15 が一貫して「全文を示す／削除範囲を明示する」方式なのに、ここだけ流儀が違う（完成コード全体の 1418-1768行に全文はあるが、Step の作業中には無い） |
| 16-a3 | 906 | 重大 | Step 4 のスクショ `task-list.png` にキャプション「**時間記録後の合計作業時間の表示**」。実画像を確認したところ、8枚のカードのうち7枚が「合計作業時間: **0m**」で、記録後の状態を1つも示していない。しかも Day 13 の冒頭（`day13:15`）と同じ画像 |
| 16-a4 | 26 | 中 | 冒頭の完成イメージが `task-detail-dialog.png`（タスク詳細ダイアログ）。しかし Day 16 は詳細ダイアログを一切変更せず、222-223行で「タスク詳細ダイアログ（`TaskDetailDialog`）にはステータスの表示だけがあり、そこから変更する操作はありません」と明言している。今日作るもの（時間記録ダイアログ＝`task-timer.png`）が冒頭に無い |
| 16-a5 | 496 | 軽 | `<Dialog open={open} onOpenChange={handleClose}>`。Day 10（470行）と Day 14（859行）は `onOpenChange={(isOpen) => !isOpen && handleClose()}` で書かせており、流儀が違う。動作は変わらないが、3つのダイアログで2通りの書き方が混在する |
| 16-a6 | 600-604 | 軽 | `<Button variant="outline" onClick={handleClose}>キャンセル</Button>` に `type="button"` が無い。`<form>` の外なので実害は無いが、Day 10（672行）と Day 14（1260行）で「この1語を落とすと押した瞬間に送信が走る」と強調した直後の日に、その1語が無いコードを書かせる |
| 16-a7 | 248 | 中 | 「上書きする前の配布版は `scripts/_app-components/task/time-log-dialog.tsx` に残っています」。`scripts/` は Day 01 の scaffold 用ディレクトリで、教材は Day 01 以降このパスに触れていない。販売用 ZIP にこのパスが含まれるかの記載も無い |
| 16-a8 | 101 | 軽 | 「合計時間: 約26分」。Step 3 が5分だが、実際には配布ファイルの構造を読み取って4か所（Props / 分割代入の既定値 / JSX 2か所 / `return` の再構成）を編集する作業。他の日の同規模 Step（Day 14 Step 8 = 7分）と釣り合わない |

## (b) 図が要る節

| 節 | 類型 | 何を描くか |
|---|---|---|
| 分の数字が往復する道筋（431-435, 465-478, 684-699） | ③前後の変化 ＋ ①目に見えん関係 | 入力「1時間30分」→ `hours*60+minutes = 90` → `increment` → DB `720 → 810` → `getAll` → `formatMinutes(810)` → 画面「13h 30m」。入力側の掛け算と表示側の割り算が逆向きの対で、**この教材で数字が一周する唯一の場所**。既存の mermaid（53行）はステータス遷移図で、この流れは描いていない |
| `stopPropagation` の有無（743-758） | ①目に見えん関係 | ボタン → カード → ページ とイベントが外側へ伝わる様子と、`stopPropagation` がどこで止めるか。「止めないと小窓と詳細が同時に立ち上がる」を絵で |

既存 mermaid: 53行（stateDiagram-v2 のステータス遷移）1本。上記2つとは重複しない。

## (c) 理解チェック3問

1. **何をしとるか**: `addTime` の `timeSpentMinutes: { increment: input.minutesToAdd }` は何をしているか。
   - 答: 現在の値を読み出さず、DB に「この分だけ増やして」と直接頼んでいる。ほぼ同時に2回記録しても両方が足される（153行）。
2. **こう変えたらどうなるか**: `minutesToAdd` に `data.minutes` だけを渡すとどうなるか。
   - 答: 1時間30分と入れても30分しか足されない。エラーは1つも出ないまま合計だけが足りない状態で残る（473-478行）。
3. **なぜこの書き方か**: `refine` に `path: ['minutes']` を書くのはなぜか。
   - 答: 書かないとそのエラーがどの欄にも結び付かず、`errors.hours` にも `errors.minutes` にも入らないため画面に何も出ない。両方0で押した人にはボタンが効かない画面に見える（585-591行）。

## (d) つまずきポイント

| 順位 | 内容 | 出典 |
|---|---|---|
| **1** | **`task-card.tsx` のどこに何を足すのか分からない**（16-a2）。省略記号で示された4か所を、中身を見たことのないファイルに対して当てる作業になる | 本監査 |
| 2 | 合計作業時間が増えない → `onTimeLogSuccess` を `TaskCard` に渡し忘れ | 教材が 860行で明記 |
| 3 | 数値が文字列扱いになる → `valueAsNumber` 未指定 | 教材が 543-547行 / 1820行で明記 |
| 4 | 「時間記録」ボタンを押すとカードの詳細も同時に開く → `stopPropagation` 忘れ | 教材が 755-758行で明記 |
| 5 | 2回記録しても合計が上書きされる（＝`increment` でなく代入になっている）ことの見分け方 | 教材が 921-924行で明記（30分→45分→`1h 15m`）。**実測でも 12h 0m → 1時間30分追加 → 13h 30m を確認済み** |

## (e) その日の終わりの状態

- アプリ: **動く**。ステータス変更と時間記録が通り、カードの合計作業時間が更新される。
- 型/ビルドエラー: **無し**。
- lint: 完成コードの `<TaskCard>` から `key` が抜けているが、これは lint ではなく React のランタイム警告（開発者ツールのコンソール）。予告なし。
- 予告: 不要（残らない）。

## (f) その日までに読者の手元にあるデータ

Day 15 の状態 ＋ Step 4 の動作確認で **ステータス1件変更**、**作業時間を2回記録**（921-924行の指示: 30分 → 45分）。

| 種別 | 件数 | 備考 |
|---|---|---|
| ユーザー | 5 | |
| プロジェクト | 2 | うち1件アーカイブ済み |
| タスク | 5 | うち1件のステータスが変更済み、1件の `timeSpentMinutes` が +75分 |
| コメント | 2 | |
| `timeSpentMinutes` の初期値 | デザインモックアップ作成 = 720（=12h 0m）／データベース設計 = 1200（=20h 0m）／他3件 = 0 | `src/command/seed.ts` |

**スクショ撮影用シード指定**: カードに「合計作業時間: 12h 0m」→ 1時間30分追加 → 「13h 30m」。時間記録ダイアログは時間/分の2欄。サイドバー4項目、検索バーなし、担当者フィルターなし。

## (g) スクショの問題

| 行 | ファイル | 問題 |
|---|---|---|
| 26 | `task-detail-dialog.png` | ②今日は一切変更しない部品の画像を冒頭の完成イメージにしている（16-a4）。Day 13 の 1109行と共有 |
| 634 | `task-timer.png` | 未検証。今日の主役の画像だが、冒頭ではなく Step 2 の末尾にしか無い |
| 906 | `task-list.png` | ②**Day 13 の 15行と同じ画像**。キャプション「時間記録後の合計作業時間の表示」に対し、実画像は7枚が「0m」。記録後の状態を示していない。③サイドバー7項目・検索バー・シードに無いタスク8枚も同じ |

- ③視覚指示: 0件
- **Day 16 の撮り直し必要枚数: 3ファイル（参照3箇所）**

---

# 付録A: (g) スクリーンショット全体集計

## 全画像に共通する問題（実画像で確認）

Day 09〜16 で使われる**全22ファイルが、完成した Day 30 のアプリで撮られている**。読者の画面には Day 09〜16 のどの時点でも存在しないものが写る。

| 写っているもの | 読者が作るのは |
|---|---|
| サイドバー「レポート」 | Day 22 |
| サイドバー「検索」 | Day 20 |
| サイドバー「ユーザー管理」 | Day 24 |
| サイドバー「タスク」 | Day 13（Day 09〜12 の画像には出てはいけない） |
| 上部の検索バー「タスク・プロジェクトを検索...」 | Day 20 |
| 一覧の「すべての担当者」フィルター | Day 20 |
| カードの「合計作業時間」「時間記録」ボタン | Day 16（Day 13〜15 の画像には出てはいけない） |
| 「新規タスク」ボタン | Day 14（Day 13 の画像には出てはいけない） |
| ダイアログの必須マーク `*` | 教材のコードに存在しない |
| プロジェクト「社内データ分析基盤」 | シードにも読者の操作にも存在しない |
| タスク「初めての…」「プッシュ…」「売上デー…」「ダッシュ…」「BIツール…」「repro3…」 | シードにも読者の操作にも存在しない |

## 集計

| 区分 | 件数 |
|---|---|
| 画像参照の総数（Day 09〜16） | **36箇所** |
| ユニークなファイル数 | **22ファイル** |
| 同一日での使い回し | **6箇所 / 計14参照**（day09 `project-list`×2、day10 `project-create-dialog`×3、day11 `project-detail-dialog`×2・`project-delete-confirm`×2、day12 `project-detail-tasks`×2、day14 `task-create-dialog`×3） |
| 日をまたいだ流用 | 5組（`project-list` day09↔day11、`project-delete-confirm` day11↔day12↔day15、`task-create-dialog` day14↔day15、`task-list` day13↔day16、`task-detail-dialog` day13↔day16） |
| キャプションと中身が一致しない | **8箇所**（day10:642、day10:839、day11:691、day12:1379、day14:934、day14:1199、day15:26、day16:906） |
| 教材が本文で弁解している画像 | **4箇所**（day10:1777、day12:1279-1283、day15:23-24、day15:848-849） |
| 「ここをクリック」に赤枠等の視覚指示 | **0件 / 36箇所** |
| **撮り直しが必要な枚数** | **22ファイル全部**（＝参照36箇所すべて） |

### 撮り直し優先順位

1. `task-create-dialog.png`（4参照・day14/day15、途中経過として3回使われている）
2. `task-list.png`（2参照・day13/day16、Day 16 の UI が Day 13 の完成イメージに写る）
3. `project-list.png`（3参照・day09×2/day11、幻のプロジェクトとサイドバー7項目）
4. `project-create-dialog.png`（3参照・day10、列レイアウトが教材のコードと違う）
5. `project-delete-confirm.png`（4参照・day11×2/day12/day15、3日連続で別対象に流用）
6. 残り17ファイル

---

# 付録B: 8日を貫く構造的な問題

| # | 問題 | 該当 |
|---|---|---|
| B-1 | **確認ポイントの「`npm run dev` で型エラーが出ていない」が機能していない**。`next dev` は `tsc` を通さないので型エラーを検出できない。8日で合計40回以上出る | 全日 |
| B-2 | **同じコードを1日のうちに2〜3回書き直させる**。Day 09（Step 6→7→9 で `ProjectCard` を3回）、Day 13（Step 6 内で `<TaskCard>` と `<div />` を2回）。教材は削除範囲を明記しているが、写経の総量が増える | Day 09、Day 13 |
| B-3 | **貼り先の指示が抜けているコードブロックがある**。Day 10 Step 4（2箇所）、Day 15 Step 6。他の全ブロックは位置を明記しているため、抜けた箇所だけ読者が止まる | Day 10、Day 15 |
| B-4 | **「置き換え」注記の網羅漏れ**。同名 `const` の二重宣言を防ぐ注記は Day 11・12・13・15 で計10回書かれているが、`day15:633` の `handleSubmit` だけ抜けている | Day 15 |
| B-5 | **完成コードと Step のコードが食い違う**。Day 14（`void taskId` の有無）、Day 16（`key` の有無）。完成コードは「上から順に貼り付けて置き換えてください」と誘導しているため、貼ると Step の正しい形が失われる | Day 14、Day 16 |
| B-6 | **読者が持っていない「完成版」を根拠にした説明**が残る。`day11:858`、`day09:1403`、`day10:1773/1777/1781`。各所に「販売用 ZIP に完成版の `src/` は入っていません」という但し書きはあるが、説明そのものは完成版を前提にしている | Day 09〜11 |
| B-7 | **`as` 禁止を教えながら教材自身が `as` を使う**。`day13:712/715` で明確に否定 → `day15:229` で `as string` | Day 13 ↔ Day 15 |
| B-8 | **エラーを利用者に知らせる仕組みが Day 15 まで無い**。Day 10〜14 の失敗はすべて「押しても何も起きない」。`day10:744`・`day11:2247-2250`・`day12:1334-1336` が3回同じ断りを書いており、断りの反復自体が読者の不安になる | Day 10〜14 |

---

# 付録C: 監査で使った照合先

| 主張 | 照合先 |
|---|---|
| シードデータの中身 | `src/command/seed.ts`（users 4 / projects 2 / tasks 5 / comments 2） |
| `ProjectDetailView` の必須 props 8個 | `src/component/project/project-detail-view.tsx:31-40` |
| OWNER 行の削除ボタンが `disabled` | `src/component/project/project-detail-view.tsx:200` |
| `TaskCard` の `canEdit` / `canDelete` 既定値が `true` | `scripts/_app-components/task/task-card.tsx:48-49` |
| 配布版 `task-card.tsx` に `timeSpentMinutes` が無い | `scripts/_app-components/task/task-card.tsx`（grep 0件） |
| `DeleteConfirmDialog` の既定タイトル | `src/component/ui/delete-confirm-dialog.tsx:27`（`'本当に削除しますか？'`） |
| `assertMemberPermission` の第2引数が optional | `src/server/api/routers/_helpers/permission.ts:14-17` |
| `Toaster` がマウント済み | `src/app/providers.tsx:10` |
| `react-hot-toast` が依存に入っている | `package.json:78` |
| `@@map("projects")` | `prisma/schema.prisma:92` |
| Biome の `noUnusedVariables` が error / unsafe fix は `npm run fix` で適用されない | `biome.json` ＋ Biome 2.4.15 を実行して確認 |
| Biome の `noUnusedFunctionParameters` が発火する | 同上（実行して確認） |
| 教材のコードが Biome formatter を通らない | 同上（`<h1 className="...` の改行を実行して確認） |
| 読者の `tsconfig.json` は `create-next-app@15.5.21` の既定（`noUnusedLocals` 等が無効） | `scripts/scaffold-from-scratch.sh:154, 328-349` |
| `day11:502` の「`exactOptionalPropertyTypes` を有効にしていません」は**正しい** | 同上（リポジトリ本体の `tsconfig.json` とは別物） |
| スクリーンショットの中身 | `screenshots/{task-create-dialog, task-list, project-list, project-create-dialog, day09-cards}.png` を実際に開いて確認 |
