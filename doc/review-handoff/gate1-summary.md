# Gate 1 — 36本を読んだ結果の集計

Wave 1 の走査4体が全36本を読了。**ワイが裏取りした分は「検証済」と明記する。**

## 総量

| 項目 | day01-08 | day09-16 | day17-24 | day25-30+付録 | 合計 |
|---|---:|---:|---:|---:|---:|
| 事実の誤り・矛盾 | 31 | 24 | 41 | 16 | **112件** |
| 図が要る節 | 19 | 19 | 17 | 11 | **66箇所** |
| 理解チェック（起案済） | 24 | 24 | 24 | 18 | **90問** |
| 撮り直しが要るスクショ | 16 | **22（全部）** | 20 | 5 | **63枚** |
| 新規に撮るスクショ | 5 | 0 | 0 | 2 | **7枚** |

うち BLOCKER / 致命: day01-08 が3件、day09-16 が2件、day17-24 が1件、day25-30 が5件 = **11件**。

## ワイが自分で裏取りした分

### ✅ 検証済 — day04 の冒頭画像が本文と正反対

`day04_ネットに公開.md:18` の `day04-prep-panel.png` を実際に開いて見た。画像に
**「まだ本番公開はしません」「今日はやらない: 本番公開」**と書いてある。同ファイル `:42-44` の
目標は「Production Deploy を実行する」「発行された URL をブラウザで開き、表示を確認する」。真逆。

さらにこの1枚に4つの欠陥が同居しとる:
- アプリの実画面やのうてモックの絵
- 左にサイドバーが見切れて重なり、描画が崩れとる
- 「User Admin 管理者」が写る。day04 時点でログインは未実装（day05〜07 で作る）
- 下60%が空白。PDF で1ページ丸ごと無駄になる

### ✅ 検証済 — lint 完了条件の矛盾

`day05:356` が「教材のコードは Biome 整形前の形。写経後に lint すると差分が出る。**写経を間違えたわけではない**」と
明言しとるのに、`day26:757` は「`npm run lint` が0エラーで通過した」を完了条件にし、
`day28:1112` も「`npm run lint` でエラーが出ない」と書いとる。

根拠: `scripts/scaffold-from-scratch.sh:316` が読者の lint を `biome check src …` と定義。
`biome check` は formatter と organizeImports も走らせるので、`npm run fix` 前は必ず差分が出る。
**day05 が正しく、day26/28 が誤り。**

### ✅ 検証済 — 30日再構成ビルドの NG は大半がツールの限界

`--all --verify` は 1/30 しか通らんかったが、day02/day03 のエラーは
`src/app/dashboard/page.tsx(1,25): error TS2323: Cannot redeclare exported variable 'default'`。
day01 が書いたファイルに day02 が**書き直し版**を出しとるのに、連結が両方を繋げただけ。
**教材の欠陥やない。**連結ロジックを置換対応に直させとる。

## 特に重いもの（子分の報告。ワイの裏取りはこれから）

| # | 中身 |
|---|---|
| 1 | `screenshots/dashboard.png` が **Day20-21 の完成ダッシュボード**（統計カード5枚・グローバル検索・7項目サイドバー）。それを `day07:1393` と `day08:636` が使っとる。読者の実際の `/dashboard` は day02 の静的カード |
| 2 | **day09-16 のスクショ22枚が全部、完成版アプリで撮影**。サイドバー7項目＋上部検索バー（Day20〜24 の UI）、シードに無いプロジェクト、day13 の画像に day16 の機能が写る |
| 3 | `day29:81-93` のステップ表が11中6ステップで**別ファイルを指す**。表どおりに書くと `'use client'` の無い server wrapper に `useQuery` を書くことになり必ず壊れる |
| 4 | `day28:1048-1057` が**既存関数を書かせる**（`isTaskStatus` は配布物の `src/lib/constant/status.ts:28` に実在）。貼ると重複定義でビルド停止 |
| 5 | `day11:1073-1078` のアーカイブ手順が対象を指定してへん → admin に残る1件をアーカイブしてしまい、**Day21〜23 の統計・グラフ・レポートが3日連続で全部0**になる |
| 6 | `day23:851-1016` Step6 にグリッドの `div` が無く、完成コードと食い違う。**エラーが出んので機械検査では検出不可** |
| 7 | `day11` は型エラー5件を残して終わるが、本文の予告は1件分だけ |
| 8 | `day15:633` が Day14 の `handleSubmit` を消す指示を落としとる → `Cannot redeclare` で止まる |
| 9 | 目次が day02 に約束する「const と let の違い / number・boolean / 型エラーを体験」が**本文に一つも無い** |
| 10 | 目次 `:135`「モバイル対応を確認」 vs `day08:61`「モバイル対応は扱いません」 |

## 副産物 — 監査ログの訂正

`audit/clean-run-2026-07-29-day11-20.md` の「成立」判定のうち **5件は現在の教材では既に解消済み**やった。
古い監査を根拠に直すと、直っとるものをまた触ることになる。

## 撮影の前提（不満#10 対策）

各 scan ファイルの (f) に、日ごとの累積データ数（ユーザー/プロジェクト/タスク/コメント）と
**「その日の画面に出てはいけない UI」**の表が入っとる。これを日別シードに落とす。

day01-08 は特殊で、**画面に DB 由来の値が出るのは day08 サイドバーの2項目だけ**。
それ以前はデータ状態を作らんでも撮れる。
