# 走査で挙がった指摘（未裏取り・候補リスト）

2026-08-30。落ちた10本のワークフローの journal から、実ファイル名つきの指摘だけを
抜き出して重複を落としたもの。**この一覧はまだワイが1件も現物で裏取りしとらん。**
過去に、走査の指摘を鵜呑みで直して正しい教材を壊しかけた事故がある
（`seed.ts:126-165` の件。`progress.md` 参照）。1件ずつ現物を開いて確かめてから直すこと。

## 走査の出どころと網羅率

| runId | 観点 | 状態 | agent数 | 指摘 |
|---|---|---|---:|---:|
| `wf_4c874d04-af7` | 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコード/画面状態の食い違い） | completed | 10 | 40 |
| `wf_bf0398f9-bf2` | task-app 30日カリキュラム教材の整合性チェック（本文とチェックリスト/表の食い違い） | partial | 19 | 3 |
| `wf_7520f0e9-2ab` | task-app 30日カリキュラムの品質チェック（複数観点、詳細不明） | partial | 33 | 0 |
| `wf_7f126890-01a` | 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証 | partial | 18 | 16 |
| `wf_5f4faded-512` | 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序) | partial | 38 | 23 |
| `wf_3cda121b-411` | 写経版(day30スナップショット)と完成版コードの整合性チェック | partial | 72 | 13 |

**partial は「問題が無い」やのうて「見切れてへん」。**セッション上限で子分が途中で落ちた。
完了したんは1本目だけで、残り5本は走査の途中で止まっとる。

---

## リリースブロッカー候補（55件）

読者がその手順で止まる／画面に到達できん


### `material/30days-curriculum/day01_開発環境を整えて、初めてのアプリを動かそう.md`

- **:1275** チェックリストが『Day 01 Ready』というバッジ文言を期待させるが、実際のコード（867/892/1149/1203行）では `作りかけ` としか表示されない
  - 該当: ``Day 01 Ready` の小さなバッジが見える`
  - 観点: task-app 30日カリキュラム教材の整合性チェック（本文とチェックリスト/表の食い違い）
- **:1345（理解チェック。本文657-660・page.tsx実装と食い違い）** --radiusを変えてもカードの角丸(rounded-[28px]等)は変わらず、入力欄はDay01に存在しないのに答えが「変わる」と断定しており、読者が答え合わせで誤解する。
  - 該当: `画面じゅうの角丸がまとめて大きくなります。…カード・ボタン・入力欄は、どれもこの1行の下にぶら下がっています。`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:1319（つまずきポイント）** 手順どおりchmod +x済みbash実行のため提示された原因は成立せず、実際の原因（書き込み権限のない場所での実行）に読者が辿り着けない。
  - 該当: `Step 2 が `Permission denied` で止まる | 実行権限の付いていないファイルを `./scripts/...` の形で直接呼んでいる | `bash scripts/scaffold-from-scratch.sh` の形で実行する`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証

### `material/30days-curriculum/day02_ダッシュボードに自分だけのメッセージを追加しよう.md`

- **:517** 読者のファイルに `getGreetingByHour(currentHour)` という行は存在せず（実際は `getGreetingByHour(hour)`）、指示された文字列で検索しても書き換え箇所が見つからない。
  - 該当: `試しに `getGreetingByHour(currentHour)` を `getGreetingByHour('9')` に書き換えて保存してください。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:526** 指示通り `currentHour` に戻すとスコープ外参照で `Cannot find name 'currentHour'.` エラーが発生し、「赤い線が消える」という期待結果に到達できない。
  - 該当: `確かめたら `getGreetingByHour(currentHour)` に戻してください。赤い線が消えて…`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1276** チェックポイントが指す文言「Day 01 Ready」がDay 01本文にもpage.tsx(867行の実際の表示は「動作中」)にも存在せず、読者は写経ミスと誤認して止まる。
  - 該当: `- `Day 01 Ready` の小さなバッジが見える`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:691** アロー関数`=>`がDay01/Day02の本文・用語集で一度も説明されないまま初出し、直後の解説もmapの働きのみでアロー関数自体を説明していない。
  - 該当: `{focusCards.map((card) => (`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:523** 「引数」という用語がDay01に一度も登場せず用語集にも項目が無いまま使われ、読者がどの値を指すか判断できない。
  - 該当: `「`string` の値を `number` の引数には渡せません」という意味です。`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:795** 本文294行で自分の名前に書き換えることを推奨しているのに、Step4の合格条件は依然`Taro`固定表示になっており、書き換えた読者は失敗と誤認する。5行違いの790行の記述とも矛盾。
  - 該当: `- `Taro` の名前が画面に出る`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day03_GitHubに保存する.md`

- **:872（つまずきポイント）** 本文(:582-583,:622)では.node-versionはStep7でadd済みのため未追跡は4件と説明されており、5件という記述と食い違い、正常な状態を誤りと誤認させる。
  - 該当: `配布 ZIP をそのまま使った場合は `.mise.toml` `.node-version` `doc/` `material/` `scripts/` の5件が残っている`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:874（つまずきポイント）** 実際は「何も起きない」のではなく`The following paths are ignored by one of your .gitignore files`のエラーで止まる（本文:557,:900と矛盾）。
  - 該当: ``git add .env.example` が何も起きない | `.gitignore` の `.env*` がこのファイルも除外している`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:875（つまずきポイント）** 値を付けない形は読み取り専用コマンドで登録されず、本来必要な値付きコマンド(:591-592)と別物のため解決策として効かない。
  - 該当: ``git config --global user.name` と `git config --global user.email` を1回だけ実行する`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:876（つまずきポイント）** この日は常に未追跡ファイルが残るため実際には`no changes added to commit`等が出て、記載の文言は発生せず読者が該当箇所を見つけられない（本文:613も同じ誤り）。
  - 該当: ``git commit` が `nothing to commit, working tree clean` と言う | ステージングが空`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:801** .envを誤ってpushした際の復旧手順4番目が`git filter-repo`(未インストール・導入方法の説明なし)を要求する行き止まりになっている。
  - 該当: `**4. そのうえで、過去のコミットから値を消す。** `git filter-repo` などで履歴を書き換える作業が要ります。`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:561** `git add`が同一行に存在しないファイル名を含むとその行全体がfatalで失敗し暗黙にprisma/やsrc/ごと抜け落ちうるのに、確認基準がREADME.mdの`M`表示のみで、欠落が翌日Day04のビルド失敗まで発覚しない。
  - 該当: `そのあと `git status --short` で、変更したファイルの行頭に `M` や `A` が付いた（ステージングされた）ことを確認します。今日の目的である `README.md` がステージングできていれば大丈夫です。`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day04_ネットに公開.md`

- **:569** 直前の手順では公開URLのトップページしか開いておらず、ダッシュボードへ移動する指示が本文にないため、この画像の画面には到達できない。
  - 該当: `![Day 02 で作ったダッシュボード…](./screenshots/day02/dashboard-message.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:984（つまずきポイント、Vercelデプロイ）** Vercelは.node-versionを読まず(公式ドキュメントで対応手段はProject SettingsまたはpackageのenginesのみでNode-versionは非対応)、配布物のpackage.jsonにengines指定も無いため、指示通りaddしてもNodeバージョン差は解消しない。
  - 該当: `公開先だけビルドの結果が手元と違う | Node のバージョンが手元と公開先でそろっていない | Day 03 の Step 7 で `.node-version` を add したか確認する`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:586** Step7で開くURLはトップページ(/)であり、Day02の内容は/dashboardにあるため、末尾に/dashboardを付ける指示が無いまま「メッセージが見える」という確認項目が成立しない。
  - 該当: `- Day 02 で作った自分用メッセージが見える`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:191** Day03自身がブランチ名がmain以外でもよいと明言しているのに、Day04ではmain固定を合格条件にしており矛盾する。202行・814行にも同種の記述。
  - 該当: `- `git branch --show-current` が `main` になっている`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:982** デプロイ後に環境変数を追加する画面遷移(Settings→Environment Variables)とRedeployボタンの操作がDay04全体で一度も説明されておらず、読者が復旧できない行き止まり。
  - 該当: `まず Step 2 の `npm run build` が手元で通るか確かめる。通るなら、Environment Variables に `DATABASE_URL` と `JWT_SECRET` が入っているかを見る`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day05_ログイン画面のUI.md`

- **:1389（つまずきポイント）** 本文の実装はLoginPage自身ではなくLoginPageの中でLoginFormをSuspenseで包む設計であり、指示通りLoginPageを包んでもuseSearchParamsを呼ぶLoginFormが境界外のままでエラーが解決しない。
  - 該当: ``useSearchParams` エラー | Suspense が不足 | LoginPage を `<Suspense>` でラップする`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:315** 仮のonSubmit(console.log)の動作確認手段となる送信ボタンがStep9まで存在しないのに、Step3の確認ポイントが「動作確認後に削除」を要求しており実行不能。
  - 該当: `- `console.log` は動作確認後に残さず、Step 7で必ず削除する`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:140** 実際のtsconfig(noUnusedLocals/noUnusedParameters)では未使用importは警告ではなくTS6133のエラーになる(実機確認済み)。直後の確認ポイントは「赤いエラーが出ていない」を合格基準としており自己矛盾。
  - 該当: `いまはどれも使っていないので、エディタが「未使用」の警告を出しますが、Step 10までに全部出番が来ます。`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day06_ユーザー登録画面.md`

- **:393** 送信ボタンが追加されるのはStep 9（677行）でこの時点ではボタンが存在せず、確認ポイントの操作自体ができない。
  - 該当: `- 空のまま送信するとエラーメッセージが出る`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:436** 送信ボタンがまだ無く（Step 9で追加）、この確認ポイントの操作を読者は実行できない。
  - 該当: `- 7文字以下で送信するとエラーが出る`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:479** 登録ボタンがまだ存在しないため（Step 9で追加）、`.refine()` の動作をこの時点で確認できない。
  - 該当: `- 異なるパスワードで送信すると「パスワードが一致しません」エラーが出る`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1205（つまずきポイント）** ページ遷移を担うのはrouter.push('/dashboard')でありrouter.refresh()ではない(本文:533,:1177で明言)ため、提示の解決策を実行しても遷移しない問題は直らない。
  - 該当: `登録後にページが変わらない | `router.refresh()` の呼び忘れ | `onSuccess` で `router.refresh()` を呼ぶ`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:463** 「先に削除」の指示が、削除を前提とした直前のコードブロック(L428〜452)より後ろに置かれており、どのタイミングで既存の`<form>`を処理すべきか読者が判断できない。
  - 該当: `ここから3つのブロックで書き直します。**Step 4 と Step 5 で書いた `<form>` から `</form>` までは、先にすべて削除してください。**`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:787** Day01でDocker上に立てたPostgreSQLの起動を促す記述がDay02〜Day06の「始める前の前提」に一度もなく、DB停止時は別のエラーメッセージが表示されるため読者が誤ってチェックを通過し翌日以降に詰まる。
  - 該当: `- 間違ったパスワードでエラーメッセージが出る`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day07_ログイン体験を改善しよう.md`

- **:879** この時点ではStep 1の指示で開発サーバーが止められており、auth.tsも未完成（root.ts/route.tsの接続はStep4、middlewareはStep5）なので、この画面自体を読者は表示できない。
  - 該当: `![ログイン画面の上に「エラー メールアドレスまたはパスワードが正しくありません」…](./screenshots/day07/login-failed.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day08_サイドバーを完成させよう.md`

- **:1354（つまずきポイント）** リダイレクトを起こすのはrouter.push('/login')でありrefresh()ではない(本文:342で明言、refresh忘れの症状は「古い表示が残る」)。原因説明が本文と逆転しており本当の原因に辿り着けない。
  - 該当: `ログアウトしてもリダイレクトされない | `router.refresh()` の呼び忘れ | `onSuccess` 内に `router.push('/login'); router.refresh();``
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:1375（理解チェック）** early returnが保証するのはsession.userの存在のみでnameはPrisma上String?のため未設定になり得る。実コード(:469,:484)は?.と||'U'でnameの欠落に対処しており、答えを信じてこれを外すと表示が壊れる（本文:948にも同じ誤り）。
  - 該当: `だからサイドバーでは `session.user.name` の有無を確かめずに書けます。`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証

### `material/30days-curriculum/day09_プロジェクト一覧画面.md`

- **:690** seed.tsではadminが参加中プロジェクトが必ず1件あり、この日の本文にはempty@example.comへのログイン切り替え指示が一切ないため、0件画面に到達する手段がない。
  - 該当: `プロジェクトが0件のときメッセージが表示される（screenshots/day09/empty.png）`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day10_プロジェクト新規作成.md`

- **:644** Step 5終了時点ではJSXも関数も閉じられておらず（閉じタグはStep 6）、この時点のコードは構文エラーでダイアログを描画できない。
  - 該当: `スクリーンショット: フォーム入力中のダイアログの表示を確認してください。（create-form-filled.png）`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day11_プロジェクト編集・削除.md`

- **:700** `initialData={editingProject}` を渡すのはStep 4で、Step 3時点では編集ボタンを押してもupdateMutationではなくcreateMutation側に落ち、更新は起きない。
  - 該当: `スクリーンショット: 編集後に更新された一覧の表示を確認してください。（project-list.png）`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1067** `handleProjectClick` はDay11終了時点も `void id;` のまま（詳細画面はDay12で作成）で、カードをクリックしても詳細画面へ到達する経路が存在しない。
  - 該当: `スクリーンショット: プロジェクト詳細画面のアーカイブボタンの表示を確認してください。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1111** カードクリックで詳細画面に遷移する実装がまだ無く（Day12で実装）、この手順自体を読者は実行できずアーカイブフロー全体が進められない。
  - 該当: `1. プロジェクト一覧から Day 10 で自分が作ったカードを開き、詳細画面のアーカイブボタンをクリック`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1151** adminがメンバーのプロジェクトはseedデータでは1件のみのため、手順通り進めても一覧は永久に1件のままで「2件」という確認ポイントに到達できない。
  - 該当: `- 最後に解除まで済ませ、一覧にプロジェクトが2件そろっている`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day13_タスク一覧画面.md`

- **:792** タスクカードのグリッドはStep 6で初めて追加され、Step 5時点ではカードが1枚も表示されないと教材自身が明言しているのに、キャプションはカード1枚が絞り込まれた状態を示している。
  - 該当: `![ステータスを「完了」に絞った一覧。「データベース設計」1枚だけが残っている](./screenshots/day13/task-list-filtered.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:787** タスクカードのグリッドがまだJSXに無いStep 5時点で、絞り込み結果を確認する手段が存在しない。
  - 該当: `- プロジェクトを選択すると表示が絞り込まれる…`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day14_タスク新規作成.md`

- **:1424** 「新規タスク」ボタンと`<TaskDialog>`のJSXはこの画像の位置より後で追加されるため、この時点でタスクを1件も作成できず画面を再現できない。
  - 該当: `![タスク一覧の最後に「トップページの文言を見直す」のカードが増えた画面](./screenshots/day14/task-list-after-create.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1413** 「新規タスク」ボタンと`<TaskDialog>`の描画コードはこの確認ポイントより後ろにあり、ボタン自体が画面に存在しないため確認できない。
  - 該当: `- 「新規タスク」ボタンでダイアログが開く`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:953** TaskDialogを画面に描画するJSXはStep 8まで追加されないため、Step 4〜7時点ではダイアログ自体が表示されず、断り書きも確認ポイントも実行不能。
  - 該当: `いまの自分の画面には、この2欄しか並んでいません。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day16_ステータス変更・時間記録.md`

- **:627** ダイアログを開く「時間記録」ボタンとTimeLogDialogの組み込みはStep 3で初めて追加されるため、Step 2時点では4項目中3項目が実行不能。
  - 該当: `確認ポイント: ダイアログが開閉できる／時間と分の入力欄がある／「1時間30分」を入力して追加できる／両方0のまま送信するとエラーが出る`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:634** ダイアログを開くボタンはStep 3でTaskCardに組み込まれるため、Step 2時点でこの開いた状態のダイアログを表示させる手段がない。
  - 該当: `スクリーンショット: 作業時間の記録ダイアログの表示を確認してください。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day17_自分のタスクページ.md`

- **:974** Step 8で実際に読者が書いたコードは `const handleEdit = (taskId: string) => { void taskId; };` であり、引用された文字列はファイル中に存在しないため検索・置換ができない。
  - 該当: `Step 8 の `const handleEdit = (_taskId: string) => {}` を削除して、以下で置き換えてください。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day19_コメント編集・削除.md`

- **:644** JSXが参照する `handleDeleteComment` はStep 6まで未定義で、Step 5時点でこのダイアログを開くとエラー画面になり、編集モードの画面自体を確認できない。
  - 該当: `Step 4 で書いた編集モードが、ここでようやく動きます。（comment-edit-actions.png）`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:572** 実際は `handleDeleteComment` がStep 6まで未定義のため、Step 5終了後も画面は描画されず、指示通り確認すると必ず失敗する。
  - 該当: `編集モードの画面を目で見るのも Step 5 のあとになります。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day20_タスク検索機能.md`

- **:637** 直前の本文で「今押すと404になる」と自ら明言しているのに、確認ポイントは`/search`が開けることを合格条件にしており、実際には404になる。
  - 該当: `確認ポイント: サイドバーの「検索」から `/search` を開ける`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day25_プロフィール編集.md`

- **:1311** Step 7終了時点では`</form>`等の閉じタグが無くビルドエラーになり、教材自身も直前で「画面での確認は後の動作確認で行う」と認めているのに、完成画面の画像が置かれている。
  - 該当: `![パスワード変更ページ…](./screenshots/day25/change-password.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1227** パスワード変更の動作確認は実際にはStep 9で行うのに、4か所で誤ってStep 14と案内しており、指示通り進めると確認手順に永久に出会えない。
  - 該当: `- 画面での確認は、`</form>` を書き終える Step 14 の動作確認で行う`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day28_タスク一括操作を実装しよう.md`

- **:743** Step 5時点のJSXは空のプレースホルダーのみで「完了にする」「削除」「ステータス変更」ボタンは1つも存在せず、本文自身も「今はこのまま進めてください」と書いているのに矛盾する画像が置かれている。
  - 該当: `![一括操作ボタンがページヘッダーの右側に表示されている状態](./screenshots/bulk-operation-header.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `src/app/task/page.tsx`

- **(行不明)** タスク一覧の個別削除ボタンを押しても確認ダイアログが出ず、タスクが1件も削除できない（一括削除のダイアログだけは出る）。読者が写経すると削除機能が壊れたアプリになる。
  - 該当: `個別削除の確認ダイアログ（DeleteConfirmDialog）がJSXに無い。deleteDialogOpen・deleteTargetId・deleteMutationを繋ぐ経路が抜けている`
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック
- **(行不明)** 優先度フィルタ・担当者フィルタ・URLとフィルタのクエリ同期・taskIdとedit=trueによる編集リンク起動・編集権限のあるプロジェクトのみへの新規タスク選択肢絞り込みが読者版に無い。絞り込み結果がリロードや共有リンクで復元されず、権限のないプロジェクトも新規タスクの選択肢に出てしまう。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `src/component/layout/app-layout.tsx`

- **(行不明)** サイドバーはhidden md:flexでスマホでは非表示になる設計なのに、代替のモバイルナビが存在しない。読者がスマホ幅でアプリを開くとどの画面にも移動できない行き止まりになる。
  - 該当: `モバイル用ハンバーガー＋Sheetドロワー、上部ヘッダー、QuickSearch、アバターのドロップダウン、プロフィール導線、UserRoleBadge、max-w-7xl中央寄せ、100dvh対応が全部欠けている`
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

## 品質候補（38件）

読者は進めるが商品として気になる


### `material/30days-curriculum/day02_ダッシュボードに自分だけのメッセージを追加しよう.md`

- **:185** Step4の実写経量は328行(実測)あり25分は非現実的、実際は60〜90分かかる。
  - 該当: `| Step 4 | 自分専用の最初のページを作る | 25分 |`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:1116** CTAという略語が本文・用語集ともに未定義のまま唯一使われ、初心者が画面上の役割を理解できない。
  - 該当: `- `bg-primary` と `text-primary-foreground` で CTA`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:326** 戻り値の型注釈`: string`が引数の型注釈と混同されやすいのに、この日どこにも説明が無い。
  - 該当: `function getGreetingByHour(hour: number): string {`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:145** 約200行の写経・4つの新概念が入るStep3の見積もり12分は実態の数倍で、かつ本文中の所要時間の説明(187-188行)が自己矛盾している。
  - 該当: `| Step 3 | メッセージの横に、ダッシュボードらしい情報を添える | 12分 |`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day03_GitHubに保存する.md`

- **:872** 同じDayのStep 7本文は`.node-version`を含まない4件と明言・反復しており、つまずきポイント表だけが5件と食い違う。
  - 該当: `配布 ZIP をそのまま使った場合は `.mise.toml` `.node-version` `doc/` `material/` `scripts/` の5件が残っている`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:511** Day01で全面置換した`src/app/globals.css`が読者の`git status`には必ず`M`として出るのに例には含まれておらず、逆に触っていない`layout.tsx`が載っている。
  - 該当: ` M README.md\n M package.json\n M package-lock.json\n M src/app/layout.tsx\n M src/app/page.tsx\n M tsconfig.json`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:872** つまずきポイント表は『.mise.toml .node-version doc/ material/ scripts/ の5件』が残ると書くが、本文582行・622行は`.node-version`を含めない『4件』と2回明記しており矛盾する
  - 該当: `配布 ZIP をそのまま使った場合は `.mise.toml` `.node-version` `doc/` `material/` `scripts/` の5件が残っている`
  - 観点: task-app 30日カリキュラム教材の整合性チェック（本文とチェックリスト/表の食い違い）
- **:286** チェック項目は`## ローカル起動`セクションが2行と書くが、直前のREADMEテンプレート（246-251行）は`npm install`・`cp .env.example .env`・`npm run dev`の3行構成
  - 該当: ``## ローカル起動` の下に `npm install` と `npm run dev` の2行が入っている`
  - 観点: task-app 30日カリキュラム教材の整合性チェック（本文とチェックリスト/表の食い違い）
- **:112** macユーザーの多くがHomebrew未導入(Day01はpkgインストール)で、brew install ghの初回はXcode Command Line Toolsのダウンロードで15〜30分かかるのに7分と見積もられている。
  - 該当: `| Step 5 | GitHub CLI（ターミナルから GitHub を操作する道具）で認証する | 7分 |`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:66** Day01・Day02本文にはgit add/commitが一度も出てこず、190行で教材自身が「まだ保存されていません」と認めているのに、66行だけ「Day01〜Day03までの変更履歴」と矛盾した記述をしている。
  - 該当: `今日の終わりには、ブラウザで `https://github.com/<自分のユーザー名>/task-app` のような URL を開いて、Day 01 から Day 03 までの変更履歴を確認できるようになります。`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day04_ネットに公開.md`

- **:979（つまずきポイント、ビルドエラー）** Next.jsの実際の出力順序は逆で、ファイル名:行:列が先に出てその後にType error:とコードフレームが続くため、記載順で探すとファイル名を見つけられない。
  - 該当: ``Type error:` の後ろに理由、その下にファイル名と行番号が出る`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:369** Neonアカウント登録・DB作成・Connection string取得までを含めて12分は非現実的（Day03のGitHub登録相当の作業量なのに時間配分が揃っていない）。
  - 該当: `### Step 5: Project 設定画面で見るべきポイントを絞る（12分）`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **:593** 「デプロイの前に」という指示がデプロイ完了後のStep7に配置されており順序が逆転している。また`npm run dev`を止めずに`npm start`するとポート競合する注意も欠落。
  - 該当: `デプロイの前に手元で `npm run build` を実行してから `npm start` で立ち上げると、ビルド済みのアプリを本番と同じモードで動かせます。`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)

### `material/30days-curriculum/day05_ログイン画面のUI.md`

- **:335** この画像はStep 4の中にあるが、キャプションが指すパスワードエラー表示はStep 5でしか出せず、Step 4時点で読者が画面と突き合わせられる要素がない。
  - 該当: `![完成後のログイン画面。赤枠の中に「パスワードを入力してください」という赤い文字が出ている](./screenshots/day05/login-error.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:1386（つまずきポイント）** @hookform/resolversはDay01のscaffoldで既にインストール済み(package.json:46)のため、この原因説明は手順通り進めた読者には当てはまらない。
  - 該当: ``zodResolver is not a function` | `@hookform/resolvers` が未インストール | `npm i @hookform/resolvers` を実行`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:196** 正規表現の記法(`/.../`及び`^`の「以外」の意味)がDay01〜Day05のどこでも説明されておらず、用語集にも項目がない。読者が`^A-Za-z0-9`の意味を誤読しうる。
  - 該当: `.regex(/[A-Z]/,`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **(行不明)** 背景の光のぼかし装飾（bg-primary/10 blur-3xl）、Cardのbg-card/95 backdrop-blur-sm shadow-2xl border-0、テーマ色トークン化（bg-primary等）が読者版に無く、day05当時の青から藍のグラデ直書きのまま残る。ログイン画面が完成版と見た目で別物になる。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `material/30days-curriculum/day06_ユーザー登録画面.md`

- **:395** 実際にはパスワード欄はStep 5、パスワード(確認)欄はStep 6で追加されるのに、「2つともStep 6」と誤って案内している。
  - 該当: `いまの自分の画面には、その下のパスワードとパスワード(確認)の欄がまだ出ていません。2つとも Step 6 で足します。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:481** Step 6の見出しは「パスワード確認欄を作る」で追加されるのは1欄のみ（もう1欄はStep 5で追加済み）なのに、「この Stepで足した2つの欄」と誤って書かれている。
  - 該当: `赤枠の中が、この Step で足した2つの欄です。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:612** 同じStep7内で「エラーが出ていないこと」を合格条件とする記述が4箇所あるのに、この注記だけ「エラーが出る場合がある」と例外を認めており、想定内のエラーと写経ミスを判別する材料がない。
  - 該当: ``useSearchParams` を使うコンポーネントには `Suspense` ラッパーが必要です。Step 9で追加するので、このステップではエラーが出る場合があります。`
  - 観点: 30日カリキュラム 初心者通し読み検証(所要時間・用語未定義・チェックポイント整合性・手順順序)
- **(行不明)** 登録成功後の遷移先が完成版と逆（読者版はダッシュボードへ、完成版はログインへ）。未ログイン状態でダッシュボードに飛ぼうとするため結局ログインへ跳ね返される回り道になる。またサーバーが複数の検証エラーを配列で返した場合、読者版はJSON文字列をそのままエラー欄に表示してしまう（完成版は箇条書き表示）。
  - 該当: `完成版はrouter.push('/login')のみ、読者版はrouter.push('/dashboard')とrouter.refresh()`
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック
- **(行不明)** 登録画面も同様に背景装飾・テーマ色トークンが読者版に無く、青系グラデ直書きのまま。加えて必須マークの赤いアスタリスクが読者版に無く、どの欄が必須か画面から分からない。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `material/30days-curriculum/day07_ログイン体験を改善しよう.md`

- **:2508（理解チェック）** encryptのsetExpirationTime('7d')がexpを署名前に上書きするため、/1000を消してもトークンの実効期限は7日のまま変わらず、答えが誤り（本文:338にも同じ誤り）。
  - 該当: `その結果、有効期限が1000倍先になります。実質的に期限切れしないトークンができあがります。`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証
- **:2477（つまずきポイント）** schema.prismaのUserモデルは@@map("users")で実テーブル名がusersのため、実際に出るエラー文言は`User`という綴りではなく、記載の文字列で検索しても該当箇所に辿り着けない。
  - 該当: ``relation "User" does not exist` | DB にテーブルがない | `npm run db:push && npm run db:seed` を実行`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証

### `material/30days-curriculum/day08_サイドバーを完成させよう.md`

- **:1352（つまずきポイント）** Day08のStep2は読むだけの手順でProviderは配布時から組み込み済み(:159)であり、そもそもProvider欠落時はDay05時点のtRPC呼び出しでログイン画面自体が先に落ちるため「ログイン後に白い画面」という症状は発生しない。
  - 該当: `ログイン後に白い画面 | `providers.tsx` が `layout.tsx` に組み込まれていない | Step 2 を確認`
  - 観点: 30日カリキュラムの「つまずきポイント」表・「理解チェック」の答えが本文/実機動作と食い違っていないかの検証

### `material/30days-curriculum/day09_プロジェクト一覧画面.md`

- **:443** 同じDayのつまずきポイント表はサイドバーが消える状態を不具合として扱っているのに、キャプションはその不具合状態を正解のように見せており、正しく実装した読者を混乱させる。
  - 該当: `![読み込み中のプロジェクト一覧。サイドバーも消え、…](./screenshots/day09/project-loading.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day10_プロジェクト新規作成.md`

- **:819** 本文には「ポートフォリオサイト」という名前を入力する指示が一切なく、読者は自分で決めた別名のカードを見ることになるため画像と一致しない。
  - 該当: `![作成した「ポートフォリオサイト」のカードが一覧に増えた画面](./screenshots/day10/project-list-after-create.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **:849** 導入文は「完成した作成フロー」と案内しているのに、実際の画像はバリデーションエラー画面であり、どちらが正しい結果か読者が判断できない。
  - 該当: `スクリーンショット: 完成した作成フローの表示を確認してください。（create-validation-error.png）`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day13_タスク一覧画面.md`

- **:792** 本文の指示にはステータスを「完了」に切り替える操作が書かれておらず、指示通り進めた読者の画面はキャプションの状態と一致しない。
  - 該当: `![ステータスを「完了」に絞った一覧。「データベース設計」1枚だけが残っている](./screenshots/day13/task-list-filtered.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day15_タスク編集・削除.md`

- **:26** 直前の本文は同じ写真を「新規作成のときの姿」と説明しているのに、キャプションは正反対の「編集」モードとして説明しており矛盾する。
  - 該当: `![見出しが「タスク編集」、ボタンが「更新」のタスクダイアログ。…](./screenshots/day15/task-edit-dialog.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day16_ステータス変更・時間記録.md`

- **:26** 「今日のゴール」節の画像なのに、キャプションはその日に実装する成果（ステータス変更・作業時間記録）をどちらも否定する内容になっている。
  - 該当: `![タスク詳細ダイアログ。ステータスは表示だけで、ここから変える操作はない](./screenshots/day16/task-detail-dialog.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー
- **(行不明)** 時間記録ダイアログ（TimeLogDialog）の実装が完成版と別解になっている。読者版はreact-hook-form+zodで入力を弾いてエラー文言を出すが、完成版はuseStateベースで打鍵フィルタ・placeholder・入力欄下の補足テキスト・通信中の全欄無効化を実装しており、UI上の手応え（弾かれ方・表示文言の有無）が異なる。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `material/30days-curriculum/day17_自分のタスクページ.md`

- **:1177** 本文はどのタブを押すか指定しておらず、初期データでは「進行中」以外を押すと画面が空になり、キャプションが指す状態を再現できない読者が出る。
  - 該当: `![ステータスタブで「進行中」を選び、その状態のタスクだけに絞り込んだマイタスクページ](./screenshots/day17/my-task-in-progress.png)`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day21_統計カードを表示.md`

- **:1002** Day14で読者が作成したタスクが残っていれば正しい実装でも件数は3件より増えるのに、本文は「多いときは実装ミス」と断定しており、正しく実装した読者を誤診させる。
  - 該当: `逆に多いときは、Step 0-3 の `activeTasksFilter` をどこかの `count` で使い忘れています。`
  - 観点: 30日カリキュラムの整合性チェック（本文の指示・確認ポイント・スクリーンショットキャプションと、読者が実際に写経したコー

### `material/30days-curriculum/day23_週次レポート.md`

- **(行不明)** レポートを出力リンクと、期間選択（4/8/12週）のURLクエリ同期（normalizeReportWeeksParam等）が読者版に無い。期間を変えてもURLに残らずリロードや共有で必ず4週間に戻る。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `material/30days-curriculum/day27_プロジェクト詳細・アーカイブを実装しよう.md`

- **(行不明)** 個別タスクの時間記録ボタン・ダイアログにcanEditガードが無い完成版に対し、読者版は追加している。閲覧専用ロールでも完成版はボタン操作でき、保存時に初めてエラーで弾かれる（読者版の方が安全側だが双方向差異として記録）。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `src/app/page.tsx`

- **(行不明)** 完成版のルートページはセッション判定してdashboardまたはloginへ即リダイレクトする12行のページだが、読者版はDay1のヒーロー紹介画面（154行、はじめての画面等の文言）がそのまま残っている。ログイン済みでルートを開いても自動でダッシュボードに飛ばず、1日目の紹介文言が完成品に混在して見える。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `src/app/search/page.tsx`

- **(行不明)** 読者版は検索ボタン＋Enterで確定する型、完成版はボタンを廃止し300msデバウンス＋URL自動同期（router.replace）に変更されている。読者版はrouter.pushのため検索のたびブラウザ履歴が積もり、フィルタを変えても放置するとURLに残らない。プロジェクト検索結果カードのタスク件数・メンバー数表示も欠けている。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

## 細かいもの（2件）

### `material/30days-curriculum/day25_プロフィール編集.md`

- **(行不明)** 見出しの名前フォールバック（currentUser.name || currentUser.email）やmin-w-0とbreak-wordsによるレイアウト崩れ防止が読者版に無く、名前未設定アカウントや長い名前で見出しが空欄・はみ出しになりうる（アプリ経由の通常利用ではname必須のため発火しにくい）。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

### `material/30days-curriculum/day29_ユーザー詳細・編集ページを作ろう.md`

- **(行不明)** 名前必須エラーの文言が完成版は「名前を入力してください」、読者版（教材2637行目）は「名前は必須です」で表記ゆれ。機能上は同じ位置で弾かれるが商品として文言が統一されていない。
  - 観点: 写経版(day30スナップショット)と完成版コードの整合性チェック

