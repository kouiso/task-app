version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn fix
    build:
      commands:
        - yarn build
  artifacts:
    files:
      - '.next/**/*'
  cache:
    paths:
      - node_modules/**/*
