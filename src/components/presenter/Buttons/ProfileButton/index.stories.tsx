import { Meta, StoryObj } from '@storybook/react'
import { ProfileButton } from '.'

export default { component: ProfileButton } as Meta<typeof ProfileButton>

export const Parts: StoryObj<typeof ProfileButton> = {
  args: {},
}
