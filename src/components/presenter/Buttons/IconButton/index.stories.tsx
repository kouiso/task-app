import { Meta, StoryObj } from '@storybook/react'
import { IconButton } from '.'

export default { component: IconButton } as Meta<typeof IconButton>

export const Parts: StoryObj<typeof IconButton> = {
  args: {},
}
