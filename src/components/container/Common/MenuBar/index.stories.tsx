import { Meta, StoryObj } from '@storybook/react'
import { MenuBar } from '.'

export default { component: MenuBar } as Meta<typeof MenuBar>

export const Parts: StoryObj<typeof MenuBar> = {
  args: {},
}
