import { Meta, StoryObj } from '@storybook/react'
import { TopBar } from '.'

export default { component: TopBar } as Meta<typeof TopBar>

export const Parts: StoryObj<typeof TopBar> = {
  args: {},
}
